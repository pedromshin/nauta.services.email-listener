# Continuation Brief — 2026-06-13 (after live UAT of the review surface)

## Where things stand

- **Phases 1–8 complete.** Milestone v1.0 audit = `tech_debt`
  (.planning/v1.0-MILESTONE-AUDIT.md): 13/13 reqs, 7/7 integration seams, E2E flow
  complete. Lifecycle (complete-milestone/cleanup) is PAUSED pending the remaining
  human actions (deploy + migrations) and ongoing UAT.
- **Phase 9 formalized** (entity/field region-relationship model + canvas surface) —
  see ROADMAP + .planning/phases/09-entity-field-region-relationships-canvas/09-CONTEXT.md
  (user requirements captured verbatim). NOT started. **Next session:** run
  `/gsd:discuss-phase 9` → `/gsd:plan-phase 9` → execute. It's a large core-surface
  redesign — do it with full context.

## Live-UAT bugs fixed this session (all real, all from the fake-repo gap)

All committed, gates green (Python 90.8%, web+api-client typecheck/build):
- `.env.local` missing EMAIL_LISTENER_URL/API_KEY → all web write-proxy mutations threw.
- 739ea1d entity_type_fields mapping (field_type + config.is_identifier) → autofill 500.
- facd71d Titan V1 (1536) embeddings vs halfvec(1536) → confirm 500 + retrieval arm.
- 7c3b017 + b6cbb18 reprocess bulk-supersedes pending regions (was stacking dupes; live had 984).
- b1d83f1 parse_embedding — pgvector string form; confirmed regions were unloadable (404).
- 80770bd emails.detail dedupe (one row per component) → React duplicate-key crash.
- 8627747 overlay/draw z-index above react-pdf textLayer → click/hover/draw now work.
- 73696b6 Classify Page; d1ca722 Classify Document (multi-page attachment as ONE entity,
  verified live: 4-page BL → B/L Number + Vessel autofilled across pages).
- eea4e80 preview auto-opens (first PDF attachment) — no click needed.

## HUMAN ACTIONS (carried in STATE.md "HUMAN ACTIONS REQUIRED")

1. **Restart the FastAPI email-listener service** — the running instance serves a STALE
   snapshot (classify-document route returned route-not-found). A restart makes ALL the
   backend fixes above live for the browser. (Stale instance holds port 8000.)
2. Push (main ~95 commits ahead, unpushed) + deploy; apply migrations 0010/0011/0012 to
   staging/prod; IAM allow bedrock:InvokeModel for amazon.titan-embed-text-v1; deployed
   web needs EMAIL_LISTENER_URL/API_KEY server-side.
3. To clear the 984-duplicate region pile on the test email: click Reprocess once
   (now bulk + scale-safe).

## Dispatch pattern (worked well)

gsd subagents: gsd-pattern-mapper / gsd-planner(opus) / gsd-plan-checker /
gsd-executor(sonnet, sequential main tree) / gsd-verifier / gsd-code-reviewer /
gsd-code-fixer. npm workspaces (NOT pnpm); Python via uv from apps/email-listener.
Client components import @nauta/api-client/geometry subpath only. To reproduce a live
backend bug, run a throwaway script via `uv run python` using create_container() — it
tests current on-disk code against the real local Supabase + Bedrock.

## GUARDRAILS
No push/deploy/staging-prod migrations; never commit .env; direct bulk DB mutation is
blocked by the safety classifier (use the product's Reprocess path instead).

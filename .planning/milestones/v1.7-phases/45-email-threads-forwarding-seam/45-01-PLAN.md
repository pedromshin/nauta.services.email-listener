---
phase: 45-email-threads-forwarding-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/threads.ts
  - packages/db/src/schema/forwarding-addresses.ts
  - packages/db/src/schema/emails.ts
  - packages/db/src/schema/index.ts
  - packages/db/src/ownership.ts
  - packages/db/src/ownership.test.ts
  - packages/db/migrations/0035_threads_forwarding.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/migrations/meta/0035_snapshot.json
autonomous: true
requirements: [THRD-01, THRD-04]

must_haves:
  truths:
    - "A threads table exists, importer-anchored (tenant-scoped via importers.user_id join)"
    - "emails carry a nullable thread_id FK to threads"
    - "A forwarding_addresses table exists mapping a unique secret token to a user_id"
    - "The migration is applied to the local DB and both tables + emails.thread_id are live"
    - "New tables carry RLS defense-in-depth policies (deny-anon + owner-authenticated) like every Phase 44 user-owned table"
  artifacts:
    - path: "packages/db/src/schema/threads.ts"
      provides: "threads table (id, importer_id FK, subject, created_at, updated_at)"
      contains: "export const Threads"
    - path: "packages/db/src/schema/forwarding-addresses.ts"
      provides: "forwarding_addresses table (id, user_id FK auth.users, token unique, created_at)"
      contains: "export const ForwardingAddresses"
    - path: "packages/db/migrations/0035_threads_forwarding.sql"
      provides: "CREATE TABLE threads/forwarding_addresses + emails.thread_id + RLS policies"
      contains: "create table"
    - path: "packages/db/src/ownership.ts"
      provides: "assertThreadOwnership helper (thread -> importer.user_id)"
      contains: "assertThreadOwnership"
  key_links:
    - from: "packages/db/src/schema/emails.ts"
      to: "threads.id"
      via: "thread_id FK column"
      pattern: "thread_id|threadId"
    - from: "packages/db/migrations/0035_threads_forwarding.sql"
      to: "importers.user_id"
      via: "RLS owner-authenticated policy on threads"
      pattern: "importer_id IN \\(SELECT id FROM importers WHERE user_id = auth.uid\\(\\)\\)"
---

<objective>
Add the persistence foundation for Phase 45: a real `threads` table (importer-anchored, per the CONTEXT decision to model threads as a table over an `emails.thread_id`-only design), a nullable `emails.thread_id` FK, and a `forwarding_addresses` table mapping a unique secret token to a user. Generate and apply the migration locally, with Phase-44-style RLS defense-in-depth policies on both new tables.

Purpose: Everything downstream in this phase (thread grouping/resolution, inbox grouping, the forwarding seam) reads or writes these tables. They must exist and be tenant-scoped before any consumer plan runs.
Output: Two new Drizzle tables, the `emails.thread_id` FK, one applied migration (0035), RLS policies, and the `assertThreadOwnership` ownership helper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-email-threads-forwarding-seam/45-CONTEXT.md
@.planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-01-SUMMARY.md

<interfaces>
<!-- Existing schema patterns the executor must mirror. Extracted from codebase; no exploration needed. -->

Tenant anchor — packages/db/src/schema/importers.ts:
  export const Importers = pgTable("importers", { id: uuid().primaryKey().defaultRandom(), slug, name, config, userId: uuid("user_id").notNull().references(() => AuthUsers.id, { onDelete: "cascade" }), createdAt });

Cross-schema auth stub — packages/db/src/schema/_auth.ts exports AuthUsers = pgSchema("auth").table("users", { id: uuid().primaryKey() }). forwarding_addresses.user_id references AuthUsers.id (same idiom as importers.userId / chat_conversations.userId).

emails table — packages/db/src/schema/emails.ts: importerId FK importers ON DELETE cascade; append-only (D-03). Add thread_id here.

Ownership helper idiom — packages/db/src/ownership.ts: assertEmailOwnership joins Emails -> Importers on importerId and checks Importers.userId === userId, else throws OwnershipError. assertThreadOwnership must mirror this (Threads -> Importers).

RLS policy idiom — packages/db/migrations/0034_rls_user_scoping.sql: per user-owned table, DROP the anon+authenticated deny-all then CREATE PERMISSIVE `<table>_owner_authenticated` FOR ALL TO authenticated USING/WITH CHECK. Importer-descendant form: `importer_id IN (SELECT id FROM importers WHERE user_id = auth.uid())`. Direct-user_id form: `user_id = auth.uid()`. anon stays denied.

Migration tooling gotchas (44-01-SUMMARY, MUST heed):
  - Run from packages/db: `npm run migration:generate` (auto-name) or `npm run migration:generate:custom -- --name=threads_forwarding`; then `npm run migrate:local`.
  - Journal `when` timestamps are fake-future-dated. Current max is 1784054400000 (idx 34, 0034_rls_user_scoping). Any NEW journal entry's `when` MUST exceed that; follow the round-day (+86400000ms) increment pattern → next value 1784140800000. If `when` <= the current max, migrate:local silently no-ops the new migration.
  - Never let a generated migration emit `CREATE TABLE "auth"."users"` — the migrating role lacks CREATE on schema auth. FK references to auth.users apply without it. _auth.ts already has a snapshot (0031), so a fresh generate should NOT re-emit it; if it does, strip that statement by hand.
  - drizzle-kit generate does NOT emit RLS policy SQL — append the policy statements to the generated 0035 file by hand (mirroring 0034), each separated by `--> statement-breakpoint`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: threads table + emails.thread_id FK + assertThreadOwnership helper</name>
  <files>packages/db/src/schema/threads.ts, packages/db/src/schema/emails.ts, packages/db/src/schema/index.ts, packages/db/src/ownership.ts, packages/db/src/ownership.test.ts</files>
  <read_first>
    - packages/db/src/schema/emails.ts (append-only emails table; add thread_id)
    - packages/db/src/schema/importers.ts (FK + index idiom to mirror)
    - packages/db/src/schema/index.ts (export order = dependency order)
    - packages/db/src/ownership.ts (assertEmailOwnership is the exact pattern to mirror)
    - packages/db/src/ownership.test.ts (test idiom for ownership helpers)
  </read_first>
  <action>
    Create `Threads` in threads.ts (per THRD-01): columns `id` uuid PK defaultRandom; `importerId` uuid NOT NULL references Importers.id onDelete cascade (tenant anchor — TENA-01: threads scope to a user via their importer, matching emails); `subject` text (the derived/normalized thread subject, nullable); `createdAt` timestamp notNull defaultNow; `updatedAt` timestamp notNull defaultNow. Add a btree index on `importerId`. Export `ThreadRow`/`InsertThread` inferred types like emails.ts.
    In emails.ts, add `threadId` uuid("thread_id") nullable, references(() => Threads.id, { onDelete: "set null" }) — nullable + SET NULL because emails are append-only (D-03) and must survive thread deletion/merge. Add a btree index on `threadId`. Import Threads (declare Threads' export order BEFORE emails is fine since emails already imports Importers; place `export * from "./threads"` immediately after `./importers` and before `./emails` in index.ts to preserve dependency order).
    In ownership.ts, add `assertThreadOwnership(db, threadId, userId)` mirroring assertEmailOwnership exactly: select Importers.userId from Threads innerJoin Importers on Importers.id === Threads.importerId where Threads.id === threadId limit 1; throw `new OwnershipError("thread", threadId)` when the row is missing or userId mismatches. Import Threads from ./schema/threads.
    In ownership.test.ts, add a test proving assertThreadOwnership resolves for an owned thread and throws OwnershipError for a foreign/missing thread (follow the existing assertEmailOwnership test setup).
  </action>
  <verify>
    <automated>cd packages/db && npx tsc --noEmit && npm run test -- ownership</automated>
  </verify>
  <acceptance_criteria>
    - `Threads` exported from threads.ts with importerId FK (onDelete cascade) + importerId index.
    - emails.ts declares `threadId` nullable FK to Threads.id with onDelete "set null" + threadId index.
    - index.ts exports ./threads between ./importers and ./emails.
    - ownership.ts exports `assertThreadOwnership`; ownership.test.ts covers owned + foreign/missing cases.
    - `npx tsc --noEmit` clean in packages/db; ownership tests green.
  </acceptance_criteria>
  <done>threads table + emails.thread_id + assertThreadOwnership compile and pass the ownership unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: forwarding_addresses table (token -> user) + ownership helper</name>
  <files>packages/db/src/schema/forwarding-addresses.ts, packages/db/src/schema/index.ts, packages/db/src/ownership.ts, packages/db/src/ownership.test.ts</files>
  <read_first>
    - packages/db/src/schema/chat-conversations.ts (direct user_id FK to AuthUsers — the exact idiom for a direct-user_id table)
    - packages/db/src/schema/_auth.ts (AuthUsers stub)
    - packages/db/src/ownership.ts (assertConversationOwnership = the direct-user_id ownership idiom to mirror)
  </read_first>
  <action>
    Create `ForwardingAddresses` in forwarding-addresses.ts (per THRD-04, seam only): columns `id` uuid PK defaultRandom; `userId` uuid("user_id") notNull references(() => AuthUsers.id, { onDelete: "cascade" }) — a direct-user_id table like chat_conversations (VISION guardrail #1: new table tenant-scoped); `token` text notNull; `createdAt` timestamp notNull defaultNow. Add a UNIQUE index on `token` (the resolution key — must be globally unique) and a UNIQUE index on `userId` (seam scope: exactly one forwarding address per user, so get-or-create is deterministic). Export `ForwardingAddressRow`/`InsertForwardingAddress`. Add a schema comment documenting the address contract: the routable address is `u-{token}@{FORWARDING_EMAIL_DOMAIN}` — the `u-` local-part prefix + token are the seam contract shared with the FastAPI resolver (Plan 45-05) and the web surfacing (Plan 45-06); token itself is an opaque high-entropy secret.
    Add `export * from "./forwarding-addresses"` to index.ts after `./chat-widget-interactions` (it depends only on _auth, already exported early).
    In ownership.ts add `assertForwardingAddressOwnership(db, addressId, userId)` mirroring assertConversationOwnership (direct user_id, no join). Add an ownership.test.ts case for it.
  </action>
  <verify>
    <automated>cd packages/db && npx tsc --noEmit && npm run test -- ownership</automated>
  </verify>
  <acceptance_criteria>
    - `ForwardingAddresses` exported with userId FK (onDelete cascade), UNIQUE token, UNIQUE userId.
    - Schema comment states the `u-{token}@{FORWARDING_EMAIL_DOMAIN}` contract.
    - `assertForwardingAddressOwnership` present + tested.
    - `npx tsc --noEmit` clean; ownership tests green.
  </acceptance_criteria>
  <done>forwarding_addresses table + ownership helper compile and pass unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Generate + hand-finish + apply migration 0035 (tables, thread_id, RLS)</name>
  <files>packages/db/migrations/0035_threads_forwarding.sql, packages/db/migrations/meta/_journal.json, packages/db/migrations/meta/0035_snapshot.json</files>
  <read_first>
    - packages/db/migrations/0034_rls_user_scoping.sql (the exact RLS policy idiom + statement-breakpoint format to mirror)
    - packages/db/migrations/meta/_journal.json (current max `when` = 1784054400000 at idx 34)
    - .planning/phases/44-tenancy-user-id-scoping-enforced-isolation/44-01-SUMMARY.md (Deviations: journal timestamp + auth.users CREATE-TABLE hazards)
  </read_first>
  <action>
    [BLOCKING] Schema migration — this task MUST run AFTER Tasks 1-2 and BEFORE any verification. From packages/db run `npm run migration:generate:custom -- --name=threads_forwarding` (or the plain generate) to emit 0035 with CREATE TABLE threads, CREATE TABLE forwarding_addresses, ALTER TABLE emails ADD COLUMN thread_id + FK + indexes. Inspect the generated SQL: if it contains any `CREATE TABLE "auth"."users"` statement, delete that statement entirely (44-01 deviation 3 — the migrating role lacks CREATE on schema auth; the FK applies without it).
    Hand-append RLS defense-in-depth to 0035 (drizzle-kit does not emit policies), each statement separated by `--> statement-breakpoint`, mirroring 0034 exactly: enable RLS on both tables (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`); a RESTRICTIVE `deny_all_threads_anon` / `deny_all_forwarding_addresses_anon` policy (TO anon, USING false); and a PERMISSIVE owner-authenticated policy. threads is importer-anchored → `threads_owner_authenticated` USING/WITH CHECK `importer_id IN (SELECT id FROM importers WHERE user_id = auth.uid())`. forwarding_addresses is direct-user_id → `forwarding_addresses_owner_authenticated` USING/WITH CHECK `user_id = auth.uid()`. (These are defense-in-depth only per PROJECT.md TENA-04 — the app boundary is primary.)
    Fix the new journal entry: set its `when` to 1784140800000 (current max 1784054400000 + 86400000), strictly greater than the current max, or migrate:local will silently no-op it (44-01 deviation 2).
    Apply with `npm run migrate:local`. Verify live against the local DB (127.0.0.1:54322) via information_schema / pg_policies that both tables exist, emails.thread_id exists and is nullable, the FK constraints are present, and the four RLS policies exist. Also run `npm run check` (drizzle-kit) — it must report no drift.
  </action>
  <verify>
    <automated>cd packages/db && npm run migrate:local && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - 0035 migration file contains CREATE TABLE threads + forwarding_addresses, ALTER emails ADD thread_id, and the 6 RLS statements (2 enable, 2 anon-deny, 2 owner-authenticated).
    - No `CREATE TABLE "auth"."users"` in 0035.
    - New _journal.json entry `when` = 1784140800000 (> 1784054400000).
    - `npm run migrate:local` applies cleanly; `information_schema.columns` shows emails.thread_id is_nullable=YES; `pg_policies` lists threads_owner_authenticated + forwarding_addresses_owner_authenticated.
    - `npm run check` reports no drift.
  </acceptance_criteria>
  <done>Migration 0035 applied locally; threads, forwarding_addresses, and emails.thread_id are live with RLS policies; drizzle-kit check is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated session → Postgres (future PostgREST/anon path) | RLS is the defense-in-depth wall for any non-superuser path; app boundary is primary |
| new tables → tenant isolation | threads/forwarding_addresses must not leak across users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Information Disclosure | threads table cross-tenant read | mitigate | RLS `threads_owner_authenticated` scopes via importers.user_id join; ownership.assertThreadOwnership for app-boundary reads |
| T-45-01-02 | Information Disclosure | forwarding_addresses token leak | mitigate | RLS `user_id = auth.uid()`; token column readable only to its owner; anon fully denied |
| T-45-01-03 | Tampering | emails.thread_id reassignment deleting emails | mitigate | FK onDelete SET NULL (not cascade) preserves append-only emails (D-03) |
| T-45-01-SC | Tampering | npm/drizzle install | accept | No new packages added in this phase; migration uses existing drizzle-kit toolchain only |
</threat_model>

<verification>
- `npm run migrate:local` applies 0035 with zero errors; re-running is a no-op (idempotent migrator).
- `npx tsc --noEmit` clean in packages/db.
- `pg_policies` confirms deny-anon + owner-authenticated on both new tables.
- `npm run check` reports no schema drift.
</verification>

<success_criteria>
- threads (importer-anchored) + forwarding_addresses (direct user_id) exist locally with RLS.
- emails.thread_id nullable FK live.
- assertThreadOwnership + assertForwardingAddressOwnership exported and tested.
</success_criteria>

<output>
Create `.planning/phases/45-email-threads-forwarding-seam/45-01-SUMMARY.md` when done.
</output>

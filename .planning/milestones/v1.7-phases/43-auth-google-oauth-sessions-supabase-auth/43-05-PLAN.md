---
phase: 43-auth-google-oauth-sessions-supabase-auth
plan: 05
type: execute
wave: 3
depends_on: [43-02]
files_modified:
  - .planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md
  - supabase/config.toml
  - .env.example
  - apps/web/e2e/auth-redirect.spec.ts
autonomous: true
requirements: [AUTH-05]
user_setup:
  - service: google-cloud + supabase
    why: "Google OAuth client creation + provider enablement are external actions the user must perform (autonomous execution never creates external OAuth clients)"
    env_vars:
      - name: SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID"
      - name: SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → same OAuth client → client secret"
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project URL (local: http://127.0.0.1:54321; staging/prod: project ref URL)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project anon/publishable key (npm run sb:status locally; dashboard for hosted)"
    dashboard_config:
      - task: "Enable Google provider + paste client id/secret per environment"
        location: "Supabase Dashboard → Authentication → Providers → Google (staging + prod projects, each separately)"
      - task: "Register authorized redirect URIs on the Google OAuth client"
        location: "Google Cloud Console → OAuth client → Authorized redirect URIs"

must_haves:
  truths:
    - "A runbook exists (with the phase artifacts) covering Google Cloud OAuth client creation, per-environment authorized redirect URIs, Supabase provider config, and the env-var list per environment"
    - "Local supabase config declares the Google provider reading its secret from env (no literal secret committed)"
    - ".env.example documents the new auth env vars as placeholders"
    - "A Playwright signed-out → /login redirect smoke spec is authored (for future enablement)"
  artifacts:
    - path: ".planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md"
      provides: "user runbook for Google Cloud + Supabase OAuth configuration"
      min_lines: 40
    - path: "supabase/config.toml"
      provides: "[auth.external.google] block reading client id/secret from env()"
      contains: "auth.external.google"
    - path: ".env.example"
      provides: "documented auth env-var placeholders"
      contains: "NEXT_PUBLIC_SUPABASE_ANON_KEY"
  key_links:
    - from: "supabase/config.toml [auth.external.google]"
      to: "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET)"
      via: "env() reference, never a literal secret"
      pattern: "env\\(SUPABASE_AUTH_EXTERNAL_GOOGLE"
    - from: "GOOGLE-OAUTH-RUNBOOK.md"
      to: "apps/web/src/app/auth/callback/route.ts"
      via: "documents the /auth/v1/callback + app callback redirect URIs"
      pattern: "callback"
---

<objective>
Deliver the AUTH-05 user runbook + the checked-in config that the runbook drives: the local `supabase/config.toml` Google provider block (reading secrets from `env()`), the `.env.example` documentation of the new auth vars, and an authored Playwright signed-out → `/login` redirect smoke spec for future enablement. Autonomous execution documents external OAuth setup; it never creates Google OAuth clients or changes dashboards.

Purpose: AUTH-05 runbook half + the "code only reads config" wiring. The env fail-fast half of AUTH-05 ships in Plan 01.
Output: `GOOGLE-OAUTH-RUNBOOK.md`, `supabase/config.toml` provider block, `.env.example` additions, `e2e/auth-redirect.spec.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-auth-google-oauth-sessions-supabase-auth/43-CONTEXT.md
@.planning/research/v1.7-polytoken-foundation/STACK.md
@.planning/research/v1.7-polytoken-foundation/PITFALLS.md

<interfaces>
<!-- Facts the runbook + config must encode: -->
- Supabase Auth already enabled in `supabase/config.toml` (`[auth] enabled = true`, `site_url = "http://127.0.0.1:3000"`). Add an `[auth.external.google]` sub-block; do NOT restructure the file.
- Google OAuth fixed callback (registered in Google Cloud Console) = `https://<project-ref>.supabase.co/auth/v1/callback` for hosted; local uses the Supabase local callback. The APP's own callback (`/auth/callback`, from Plan 02) is where Supabase redirects afterward.
- Two live Supabase projects: staging `fyfwkjvbcrmjqjysdyqw`, prod `dazyccjijdahxyciptkp` (Pitfall 10 — each needs its own Google provider config + redirect URI; document doing it TWICE).
- New env vars: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (public), `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID`, `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET` (secret). Existing: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`.
- Sign-up policy: open Google sign-in in v1.7 (no allowlist code — single-user reality). Runbook notes the Supabase dashboard restriction option (Pitfall 8 allowlist).
- JWT signing-key mode (research flag): this phase deliberately AVOIDS local JWT verification by using `getUser()` round-trips + `X-User-Id` forwarding, so the signing-key mode is NOT blocking for Phase 43. The runbook records "determine signing-key mode (legacy HS256 vs asymmetric ES256) before Phase 44 FastAPI JWT verification" as a Phase-44 prerequisite.
- CONSTRAINT: NEVER print or commit real secret values. config.toml uses `env(...)` references only; .env.example uses placeholders only.
- Playwright is NOT installable this phase (the one-new-dep budget is spent on @supabase/ssr). Mirror the existing authored-but-unrun precedent at `apps/web/playwright.config.ts` + `apps/web/e2e/code-island-isolation.spec.ts`. The automatable redirect gate already ships as `resolveAuthRedirect`'s unit test in Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Google OAuth user runbook</name>
  <files>.planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/43-auth-google-oauth-sessions-supabase-auth/43-CONTEXT.md ("Specific Ideas" — runbook must cover console client creation, redirect URIs, provider config, env vars per env)
    - .planning/research/v1.7-polytoken-foundation/STACK.md ("Google Cloud setup" steps + fixed /auth/v1/callback)
    - .planning/research/v1.7-polytoken-foundation/PITFALLS.md (Pitfall 10 redirect-URI drift; Pitfall 8 allowlist option)
  </read_first>
  <action>
    Write `GOOGLE-OAUTH-RUNBOOK.md` with these sections: (1) Google Cloud Console — create an OAuth consent screen (scopes openid, userinfo.email, userinfo.profile) and a Web-application OAuth 2.0 client; (2) Authorized redirect URIs — the exact `https://<project-ref>.supabase.co/auth/v1/callback` value for BOTH staging (`fyfwkjvbcrmjqjysdyqw`) and prod (`dazyccjijdahxyciptkp`), plus the local Supabase callback, explicitly stating this must be done per environment (Pitfall 10); (3) Supabase provider config — enable Google + paste client id/secret in each hosted project's dashboard, and the local `supabase/config.toml` block (cross-reference Task 2); (4) Env-var list per environment — a table of `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID`, `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET` with where each value comes from (local / staging / prod); (5) Sign-up policy note — Google sign-in is open in v1.7 (single-user); to restrict, use the Supabase dashboard allowlist option; (6) Phase-44 prerequisite note — determine the project's JWT signing-key mode (legacy HS256 vs asymmetric ES256) before FastAPI-side JWT verification is added in Phase 44. Use placeholder tokens (e.g. `<paste-client-id>`) — NEVER real secret values. State clearly that these are user actions (autonomous execution does not create OAuth clients or change dashboards).
  </action>
  <verify>
    <automated>grep -qi "auth/v1/callback" ".planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md" && grep -qi "fyfwkjvbcrmjqjysdyqw" ".planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md" && grep -qi "SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET" ".planning/phases/43-auth-google-oauth-sessions-supabase-auth/GOOGLE-OAUTH-RUNBOOK.md"</automated>
  </verify>
  <acceptance_criteria>
    - Runbook contains all six sections; covers console client creation, per-environment redirect URIs (both project refs named), Supabase provider config, the env-var table, the open-signup/allowlist note, and the Phase-44 signing-key-mode prerequisite.
    - No real secret values appear — only placeholders / env-var names (grep for obvious secret patterns finds none).
    - The grep gate passes (callback URI + staging ref + secret var name all present).
  </acceptance_criteria>
  <done>A user runbook exists with everything needed to configure Google OAuth per environment, with no committed secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Add the local Google provider config block + document env vars in .env.example</name>
  <files>supabase/config.toml, .env.example</files>
  <read_first>
    - supabase/config.toml (existing `[auth]` block — append the provider sub-block; do not restructure)
    - .env.example (existing SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY section + its NEXT_PUBLIC warning comment)
    - .planning/research/v1.7-polytoken-foundation/STACK.md (config.toml [auth.external.google] field shape)
  </read_first>
  <action>
    In `supabase/config.toml`, append an `[auth.external.google]` block with `enabled = true`, `client_id = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID)"`, and `secret = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET)"` — secrets referenced via `env(...)` ONLY, never inline literals. Also add `"http://127.0.0.1:3000/auth/callback"` to the existing `additional_redirect_urls` allow-list so the app callback is permitted locally (keep the existing entry). Do not modify any other section.
    In `.env.example`, add (near the existing Supabase section) documented PLACEHOLDER lines for: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID`, `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET`, with a comment for each pointing to the runbook and noting which are public (NEXT_PUBLIC_*) vs secret (never NEXT_PUBLIC_). Use commented/empty placeholders consistent with the file's existing style — no real values.
  </action>
  <verify>
    <automated>grep -q "auth.external.google" supabase/config.toml && grep -q "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET)" supabase/config.toml && grep -q "NEXT_PUBLIC_SUPABASE_ANON_KEY" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml` has an `[auth.external.google]` block with `enabled = true` and `client_id`/`secret` set to `env(...)` references (no literal secret strings).
    - `additional_redirect_urls` includes the local `/auth/callback` URL alongside the pre-existing entries.
    - `.env.example` documents all four new auth vars as placeholders with public-vs-secret comments; no real values committed.
    - `git diff supabase/config.toml` shows only the appended provider block + the redirect-url addition (no other section changed).
  </acceptance_criteria>
  <done>Local Supabase reads the Google provider from env; the new auth vars are documented for every environment with zero committed secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Author the signed-out → /login Playwright redirect smoke spec (future-enablement)</name>
  <files>apps/web/e2e/auth-redirect.spec.ts</files>
  <read_first>
    - apps/web/e2e/code-island-isolation.spec.ts (existing authored-but-unrun Playwright precedent + style)
    - apps/web/playwright.config.ts (testDir ./e2e, testMatch *.spec.ts; Playwright not installed note)
    - apps/web/middleware.ts (Plan 02 — the behavior under test: unauthenticated request to a protected surface 302s to /login)
  </read_first>
  <action>
    Create `apps/web/e2e/auth-redirect.spec.ts` mirroring the existing e2e spec's structure and header comment: a `test` asserting that a signed-out `page.goto("/chat")` ends on a URL matching `/login` and carries a `redirectTo` query param equal to `%2Fchat` (or `/chat`). Add a top-of-file comment matching the code-island precedent: Playwright is not installed in the autonomous run (the phase's single new-dep budget is spent on `@supabase/ssr`); enable with `npm i -D @playwright/test && npx playwright install chromium` and run against a dev server. Note that the automatable redirect gate already ships as the `resolveAuthRedirect` unit test in 43-02 — this spec is the browser-level UAT counterpart for future enablement.
  </action>
  <verify>
    <automated>MISSING — Playwright is not installed (installing @playwright/test would exceed the one-new-dep budget). The redirect logic is gated automatically by 43-02's `resolveAuthRedirect` unit test. This spec is authored-not-run per the existing e2e precedent; verify it exists and matches the e2e testMatch pattern: test -f apps/web/e2e/auth-redirect.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/auth-redirect.spec.ts` exists, matches the `*.spec.ts` testMatch, and asserts a signed-out visit to a protected surface lands on `/login` with a `redirectTo` param.
    - The file's header comment documents the not-installed status + the enablement command, mirroring `code-island-isolation.spec.ts`.
    - The file lives under `apps/web/e2e/` (excluded from tsconfig + vitest — will not affect the typecheck/test gates).
  </acceptance_criteria>
  <done>A browser-level signed-out → /login redirect spec is authored for future Playwright enablement; the automatable gate remains 43-02's unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external OAuth provider config → app | Redirect-URI / provider settings across two live Supabase projects are easy to desync |
| repo → committed config | Secrets must never enter config.toml, .env.example, or the runbook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-P5-01 | Spoofing (redirect-URI drift / mismatch) | Google/Supabase per-env config | mitigate | Runbook enumerates exact redirect URIs per environment (both project refs + local), flags doing it twice (Pitfall 10) |
| T-43-P5-02 | Information Disclosure (secret leakage) | config.toml, .env.example, runbook | mitigate | `env(...)` references + placeholders only; grep/acceptance confirm no literal secrets committed |
| T-43-P5-03 | Elevation (open sign-up) | Google provider enable_signup | accept | Open Google sign-in in v1.7 (single-user reality); runbook documents the Supabase dashboard allowlist option to restrict later |
| T-43-P5-04 | Tampering (config drift local vs hosted) | supabase/config.toml vs dashboard | mitigate | config.toml is the local source of truth; runbook makes hosted dashboard config an explicit per-project checklist |
</threat_model>

<verification>
- Task 1 + Task 2 grep gates pass (callback URI, project ref, secret var name, config block, env.example var).
- No literal secret values in any committed file (config.toml/.env.example use `env(...)`/placeholders).
- Playwright spec authored under `apps/web/e2e/`; automatable redirect gate remains 43-02's unit test.
</verification>

<success_criteria>
- Google Cloud OAuth client runbook exists (AUTH-05 runbook half).
- Local config + .env.example wire the provider from env with zero committed secrets.
- Browser-level redirect smoke authored for future enablement; unit-tested redirect logic is the live gate.
</success_criteria>

<output>
Create `.planning/phases/43-auth-google-oauth-sessions-supabase-auth/43-05-SUMMARY.md` when done.
</output>

---
phase: 32-knowledge-canvas-tiered-graph-exploration
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/src/app/knowledge/_components/tier-edge-style.ts
  - apps/web/src/app/knowledge/_components/tier-edge-style.test.ts
  - apps/web/src/app/knowledge/_components/tier-filter.ts
  - apps/web/src/app/knowledge/_components/tier-filter.test.ts
  - apps/web/src/app/knowledge/_components/tier-filter-control.tsx
  - apps/web/src/app/knowledge/_components/knowledge-graph.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-03]

must_haves:
  truths:
    - "`/knowledge` renders EXTRACTED kne- edges solid, INFERRED kne- edges dashed, and AMBIGUOUS kne- edges faint"
    - "The 8 structural FK edge types (has_field, instance_of, ...) are visually untouched by tier styling and by the filter"
    - "A segmented tier-filter control narrows the view to EXTRACTED-only or widens to include INFERRED/AMBIGUOUS"
    - "A legend maps the three line encodings to plain reviewer-facing labels (Confirmed / Suggested / Uncertain)"
  artifacts:
    - path: "apps/web/src/app/knowledge/_components/tier-edge-style.ts"
      provides: "Pure tier->style map (dashed/faint) applied only to kne- edges"
      contains: "tierEdgeStyle"
    - path: "apps/web/src/app/knowledge/_components/tier-filter-control.tsx"
      provides: "3-segment cumulative radiogroup tier filter"
      contains: "TierFilterControl"
    - path: "apps/web/src/app/knowledge/_components/tier-filter.ts"
      provides: "Pure tier-visibility predicate for edge filtering"
      contains: "tierAllowsEdge"
  key_links:
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "tierEdgeStyle"
      via: "toFlowEdges applies style when id startsWith kne-"
      pattern: "tierEdgeStyle"
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "tierAllowsEdge"
      via: "edge-level filter in filteredEdges"
      pattern: "tierAllowsEdge"
---

<objective>
Deliver GRAPH-01 (tier visual encoding) and GRAPH-03 (tier filter): kne- edges render with a
tier-dependent line style, a compact legend explains the encoding, and a segmented control lets the
reviewer narrow to confirmed-only or widen to include suggestions.

Purpose: tier becomes a first-class *visual* concept on the canvas. Everything is token-only (no raw
hex, no new color) and reuses the existing both-endpoints-visible edge filter so orphaned nodes
degrade exactly as today.
Output: pure tier-style + tier-filter helpers (unit-tested), a `TierFilterControl` component, a legend
panel, and the wiring in `toFlowEdges` / the edge filter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-CONTEXT.md
@.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-UI-SPEC.md

<interfaces>
From apps/web/.../knowledge-graph.tsx:
- toFlowEdges(graphEdges) ~127-147: maps GraphEdge → FlowEdge; taxonomy edges (TAXONOMY_RELATION_TYPES = {"has_field"}) get no markerEnd; all others get MarkerType.ArrowClosed
- both-endpoints-visible filter ~254-266 (initialNodes) and ~260-267 (initialEdges): drops edges whose source/target aren't both visible
- local GraphEdge mirror now carries `tier?: string` (added in 32-01)
- GraphToolbar is rendered ~418 above the ResizablePanelGroup; the tier control lives in this toolbar row (NOT inside FilterRail)

UI-SPEC LOCKED tier encoding (kne- edges only):
- EXTRACTED: React Flow default stroke, solid (no override)
- INFERRED: stroke hsl(var(--muted-foreground)), strokeDasharray "5 3"
- AMBIGUOUS: stroke hsl(var(--muted-foreground)), opacity 0.45, labelStyle opacity 0.6
UI-SPEC LOCKED filter: 3 cumulative single-select segments "Confirmed only" / "+ Inferred" / "+ Ambiguous"; default = widest (all shown)
UI-SPEC LOCKED legend labels: "Confirmed" / "Suggested" / "Uncertain"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier edge styling (GRAPH-01) + legend</name>
  <files>apps/web/src/app/knowledge/_components/tier-edge-style.ts, apps/web/src/app/knowledge/_components/tier-edge-style.test.ts, apps/web/src/app/knowledge/_components/knowledge-graph.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/_components/knowledge-graph.tsx (toFlowEdges ~127-147, TAXONOMY_RELATION_TYPES ~106, ReactFlow children ~464-476 for the legend Panel placement)
    - .planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-UI-SPEC.md (Color → GRAPH-01 table + tierEdgeStyle shape; Legend section — plain labels, bottom-left Panel)
  </read_first>
  <behavior>
    - tierEdgeStyle("INFERRED"): returns a style with strokeDasharray "5 3" and stroke hsl(var(--muted-foreground)), no opacity override
    - tierEdgeStyle("AMBIGUOUS"): returns style with stroke hsl(var(--muted-foreground)) and opacity 0.45, and a labelStyle with opacity 0.6
    - tierEdgeStyle("EXTRACTED") and tierEdgeStyle(undefined): return an empty object (React Flow default — no override)
  </behavior>
  <action>
    Create `tier-edge-style.ts` exporting a pure `tierEdgeStyle(tier: string | undefined)` returning
    `{ style?, labelStyle? }` exactly per the UI-SPEC Color table (INFERRED dashed muted; AMBIGUOUS muted
    at opacity 0.45 + label opacity 0.6; EXTRACTED/undefined empty). Tokens only — `hsl(var(--muted-foreground))`,
    never raw hex. In knowledge-graph.tsx `toFlowEdges`, spread `tierEdgeStyle(ge.tier)` into the returned
    edge object ONLY when `ge.id.startsWith("kne-")`; structural edges keep their current output untouched
    (defensive guard even though structural rows carry no tier). Preserve the existing arrowhead convention
    (taxonomy edges no markerEnd; kne- edges keep ArrowClosed). Add a legend as a React Flow `Panel`
    position="bottom-left" (a child of ReactFlowJSX, alongside Background/Controls/MiniMap) rendering three
    small swatches with the plain labels "Confirmed" (solid) / "Suggested" (dashed) / "Uncertain" (faint) —
    each swatch's line reuses the same stroke/dash/opacity values as `tierEdgeStyle` (import the helper or
    cross-reference with a comment; single source of truth). Never render the raw enum names in the UI.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/knowledge/_components/tier-edge-style.test.ts && npm run build --workspace=@nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `tier-edge-style.test.ts` asserts INFERRED → strokeDasharray "5 3" + muted stroke; AMBIGUOUS → opacity 0.45 + label opacity 0.6; EXTRACTED and undefined → empty object
    - toFlowEdges applies tierEdgeStyle only to ids starting with "kne-" (assertion: a structural edge id like "field-..." receives no style override)
    - Legend renders the plain strings "Confirmed" / "Suggested" / "Uncertain" (grep the component), never "EXTRACTED"/"INFERRED"/"AMBIGUOUS"
    - No raw hex introduced (grep the new/changed lines for `#` hex — none in edge styles)
    - `npm run build --workspace=@nauta/web` succeeds
  </acceptance_criteria>
  <done>kne- edges render solid/dashed/faint by tier, structural edges are unchanged, and a plain-label legend explains the encoding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier filter control (GRAPH-03) + edge-level filter</name>
  <files>apps/web/src/app/knowledge/_components/tier-filter.ts, apps/web/src/app/knowledge/_components/tier-filter.test.ts, apps/web/src/app/knowledge/_components/tier-filter-control.tsx, apps/web/src/app/knowledge/_components/knowledge-graph.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/_components/knowledge-graph.tsx (both-endpoints-visible filter ~254-266, GraphToolbar render ~418, useState patterns ~173-184)
    - apps/web/src/app/knowledge/_components/filter-rail.tsx (segmented/active-inactive token pair: border-primary bg-primary text-primary-foreground vs border-border bg-background text-muted-foreground; aria-label discipline)
    - .planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-UI-SPEC.md (Interaction Contracts → GRAPH-03 + Accessibility → tier filter radiogroup; Copywriting Contract segment labels)
  </read_first>
  <behavior>
    - tierAllowsEdge(edge, filterState): a structural (non-kne-) edge is ALWAYS allowed regardless of filter
    - filterState "confirmed" allows only EXTRACTED kne- edges; "inferred" allows EXTRACTED+INFERRED; "ambiguous" allows all three tiers
    - a kne- edge with an unknown/undefined tier is treated conservatively (only shown at the widest "ambiguous" state)
  </behavior>
  <action>
    Create `tier-filter.ts` exporting a `TierFilterState` union ("confirmed" | "inferred" | "ambiguous")
    and a pure `tierAllowsEdge(edge, state)` predicate: structural edges (id NOT startsWith "kne-") always
    pass; kne- edges pass by cumulative tier membership per the UI-SPEC. Create `tier-filter-control.tsx`
    exporting `TierFilterControl` — a single-select segmented control implemented as a
    `role="radiogroup"` (aria-label "Filter by trust tier") of three `role="radio"` Buttons using the
    exact segment labels "Confirmed only" / "+ Inferred" / "+ Ambiguous", reusing filter-rail's active/
    inactive token pair (no new color); arrow-key navigation per the radiogroup pattern. In knowledge-graph.tsx,
    add `tierFilter` local `useState` defaulting to "ambiguous" (widest — required so the confirm→see-
    suggestions demo path works, per UI-SPEC), render `TierFilterControl` in the GraphToolbar row (NOT inside
    FilterRail), and extend the existing edge filter so `filteredEdges` also drops edges failing
    `tierAllowsEdge(edge, tierFilter)` — layered on top of the current both-endpoints-visible check so
    orphaned nodes degrade exactly as today. Filter state is session-only `useState` (no localStorage).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/knowledge/_components/tier-filter.test.ts && npm run build --workspace=@nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `tier-filter.test.ts` asserts "confirmed" narrows to EXTRACTED-only kne- edges (INFERRED/AMBIGUOUS dropped) and "ambiguous" allows all three
    - `tier-filter.test.ts` asserts a structural (non-kne-) edge passes in EVERY filter state
    - `tier-filter.test.ts` asserts a kne- edge with undefined tier only passes at "ambiguous"
    - TierFilterControl uses role="radiogroup" + role="radio" and the exact labels "Confirmed only" / "+ Inferred" / "+ Ambiguous"
    - Default filter state is "ambiguous" (all shown)
    - `npm run build --workspace=@nauta/web` succeeds
  </acceptance_criteria>
  <done>The reviewer can narrow the graph to EXTRACTED-only or widen to include suggestions; structural edges are unaffected; state is session-local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-origin strings → DOM | relationType/labels rendered on edges and legend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering (XSS) | edge labels / legend | mitigate | all DB-origin strings render as plain escaped React text; no dangerouslySetInnerHTML introduced (T-11-05 discipline preserved) |
| T-32-05 | Information Disclosure | tier filter (client-only) | accept | the filter is a purely presentational client narrowing over data the server already returned scoped by importer; it grants no new data access (auto-injection gate remains server-side, Phase 30) |
| T-32-SC | Tampering | npm installs | accept | zero new dependencies (all primitives — Button, Panel — already installed); nothing to vet |
</threat_model>

<verification>
- `npx vitest run` green for tier-edge-style.test.ts and tier-filter.test.ts
- `npm run build --workspace=@nauta/web` green
- Manual smoke (optional): switching to "Confirmed only" hides dashed/faint kne- edges while structural edges remain
</verification>

<success_criteria>
GRAPH-01 satisfied: EXTRACTED solid / INFERRED dashed / AMBIGUOUS faint via existing edge-style
conventions, token-only. GRAPH-03 satisfied: a tier filter control narrows to EXTRACTED-only or widens
to include INFERRED/AMBIGUOUS.
</success_criteria>

<output>
Create `.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-02-SUMMARY.md` when done.
</output>

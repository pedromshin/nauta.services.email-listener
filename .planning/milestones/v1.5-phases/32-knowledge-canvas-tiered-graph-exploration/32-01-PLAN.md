---
phase: 32-knowledge-canvas-tiered-graph-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-client/src/router/knowledge/expand.ts
  - packages/api-client/src/router/knowledge/expand.test.ts
  - packages/api-client/src/router/knowledge/index.ts
  - apps/web/src/app/knowledge/_components/graph-merge.ts
  - apps/web/src/app/knowledge/_components/graph-merge.test.ts
  - apps/web/src/app/knowledge/_components/knowledge-graph.tsx
autonomous: true
requirements: [GRAPH-02]

must_haves:
  truths:
    - "Clicking a knowledge-graph node fetches its neighbours via a bounded server-side query and merges them onto the canvas"
    - "The server walk is depth-clamped to <=2 hops and truncates at a hard node/edge budget (~50) — it can never walk the whole graph"
    - "Expansion results are tenant-scoped to the seed node's importer"
    - "Re-clicking an already-expanded node is idempotent (dedupe by id — no duplicate nodes/edges)"
  artifacts:
    - path: "packages/api-client/src/router/knowledge/expand.ts"
      provides: "expandNode read-only tRPC procedure + pure clampDepth/capBudget helpers"
      contains: "expandNode"
    - path: "apps/web/src/app/knowledge/_components/graph-merge.ts"
      provides: "Pure dedupe-by-id merge of new nodes/edges into the current canvas set"
      contains: "mergeGraph"
  key_links:
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "api.knowledge.expandNode"
      via: "useMutation fired from onNodeClick"
      pattern: "expandNode"
    - from: "packages/api-client/src/router/knowledge/expand.ts"
      to: "KnowledgeNodeEdges"
      via: "BFS select over active edges, importer join"
      pattern: "KnowledgeNodeEdges"
---

<objective>
Deliver GRAPH-02: a reviewer clicks a node on `/knowledge` and its neighbours expand onto the canvas
via a bounded (<=2-hop) server-side graph walk, then merge in without duplicating existing nodes.

Purpose: tier becomes an *interaction* concept, not just a rendered column — the reviewer navigates
the confidence graph rather than reading a static snapshot. The BFS is written in a reusable shape so
a future stage-3 retrieval path could adopt it, but it is NOT wired anywhere near prompts here.
Output: a new read-only `knowledge.expandNode` tRPC procedure, a pure client-side merge helper, and
the click-to-expand wiring in the canvas.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-CONTEXT.md
@.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-UI-SPEC.md

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

From packages/api-client/src/router/knowledge/graph.ts:
- interface GraphNode { readonly id: string; readonly type: NodeType; readonly label: string; readonly [key: string]: unknown }
- interface GraphEdge { readonly id: string; readonly source: string; readonly target: string; readonly relationType: string; readonly tier?: string }
- interface GraphResponse { readonly nodes: ReadonlyArray<GraphNode>; readonly edges: ReadonlyArray<GraphEdge> }
- function shapeExplicitEdgeRow(row: ExplicitEdgeRow): GraphEdge | null  // excludes isActive!==true and null targetRefId; ids are prefixed `kne-${row.id}`
- publicProcedure, and the router-compose idiom in index.ts (spread `...knowledgeGraphProcedures`)

From packages/db schema (@nauta/db/schema):
- KnowledgeNodeEdges { id, sourceNodeId, targetRefId, targetRefType, relationType, confidence, source, tier, provenance, isActive, promotion }
- KnowledgeNodes { id, importerId, isActive, title, scope, scopeRefId, scopeRefType, source, confidence }
- tier enum values: "EXTRACTED" | "INFERRED" | "AMBIGUOUS"

From apps/web/.../knowledge-graph.tsx (client GraphEdge mirror, ~lines 95-100): local interface currently lacks `tier` — add it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: expandNode read-only tRPC procedure (bounded BFS)</name>
  <files>packages/api-client/src/router/knowledge/expand.ts, packages/api-client/src/router/knowledge/expand.test.ts, packages/api-client/src/router/knowledge/index.ts</files>
  <read_first>
    - packages/api-client/src/router/knowledge/graph.ts (GraphNode/GraphEdge/GraphResponse shapes, shapeExplicitEdgeRow, importer-scope query idiom, publicProcedure import)
    - packages/api-client/src/router/knowledge/graph.test.ts (DB-free pure-helper + Zod test conventions)
    - packages/api-client/src/router/knowledge/index.ts (router compose spread)
  </read_first>
  <behavior>
    - clampDepth(input): returns 1 when depth is undefined; clamps any value to the closed range [1,2]; a depth of 5 clamps to 2, a depth of 0 or negative clamps to 1
    - capBudget(nodes, edges, cap): truncates the combined result to at most `cap` nodes (default ~50) and returns a flag indicating whether truncation occurred
    - expandInputSchema: rejects a non-uuid nodeId; accepts an optional integer depth; parses with depth defaulting to 1
    - a seed with no active outgoing/incoming edges returns just the seed context (no throw)
  </behavior>
  <action>
    Create `expand.ts` exporting `knowledgeExpandProcedures` with a single read-only `expandNode`
    procedure built on `publicProcedure`. Input `expandInputSchema = z.object({ nodeId: z.string().uuid(),
    depth: z.number().int().optional() })` — export the schema for DB-free tests. Inside the query:
    resolve the seed node's owning `importerId` via a `KnowledgeNodes` lookup on `nodeId` (same importer
    join idiom used by graph.ts); if the seed is not found or not active, return an empty GraphResponse
    (fail-closed, no throw that leaks existence). Run a breadth-first walk over `KnowledgeNodeEdges`
    filtered to `isActive = true`, following `sourceNodeId`/`targetRefId` up to `clampDepth(input.depth)`
    hops, but ONLY across edges whose endpoints belong to the same `importerId` as the seed (tenant scope
    via the KnowledgeNodes importer join — never trust a client-supplied importer). Shape edges through
    the existing `shapeExplicitEdgeRow` so ids stay `kne-`-prefixed and inactive rows are dropped. Apply
    `capBudget` with a hard cap constant (~50) and surface a `truncated` boolean on the response. Extract
    `clampDepth` and `capBudget` as exported pure functions (no DB) for tests. Write the BFS as a
    standalone traversal function that takes an "edges-for-node" fetch callback so the graph-walk shape
    is reusable server-side later (CONTEXT "one implementation serves both") — do NOT import or reference
    any prompt/autofill module. Register the procedure by spreading `...knowledgeExpandProcedures` into
    `createTRPCRouter` in index.ts, after the existing spreads.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run src/router/knowledge/expand.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `expand.test.ts` asserts clampDepth(undefined)===1, clampDepth(5)===2, clampDepth(0)===1
    - `expand.test.ts` asserts capBudget truncates a >50-item set and reports truncated=true, and does NOT truncate a <=50 set
    - `expand.test.ts` asserts expandInputSchema rejects a non-uuid nodeId and defaults depth to 1
    - `knowledge.expandNode` is reachable from the composed knowledgeRouter (index.ts spread present)
    - `npx tsc --noEmit` clean in packages/api-client
  </acceptance_criteria>
  <done>expandNode exists, is registered, depth is clamped to <=2, results are budget-capped and importer-scoped, and pure helpers pass DB-free tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client click-to-expand wiring + dedupe merge</name>
  <files>apps/web/src/app/knowledge/_components/graph-merge.ts, apps/web/src/app/knowledge/_components/graph-merge.test.ts, apps/web/src/app/knowledge/_components/knowledge-graph.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/_components/knowledge-graph.tsx (handleNodeClick ~317-322, displayedNodes ~397-400, layoutGraph usage ~257, local GraphEdge mirror ~95-100, useNodesState/useEdgesState ~269-270)
    - apps/web/src/app/knowledge/_components/graph-layout.ts (layoutGraph dagre signature)
    - .planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-UI-SPEC.md (Interaction Contracts → GRAPH-02: pulse-ring loading, merge semantics, budget-exceeded toast copy)
  </read_first>
  <behavior>
    - mergeGraph(existingNodes, existingEdges, newNodes, newEdges): returns NEW arrays; a node/edge whose id already exists is NOT duplicated; new-only items are appended; original array references are never mutated
    - merging the same expansion result twice yields the identical set as merging once (idempotent)
  </behavior>
  <action>
    Add `tier?: string` to the local `GraphEdge` interface mirror in knowledge-graph.tsx so merged edges
    keep their tier. Create `graph-merge.ts` exporting a pure `mergeGraph(existingNodes, existingEdges,
    newNodes, newEdges)` that dedupes by `id` (Set of existing ids) and returns new immutable arrays —
    no mutation of inputs. In knowledge-graph.tsx: keep the existing `handleNodeClick` selection behavior
    (still sets `selectedNodeId`) and ADD an `api.knowledge.expandNode.useMutation` call fired on the same
    click with `{ nodeId: node.id, depth: 1 }`. On success, feed the current `nodes`/`edges` plus the
    returned nodes/edges through `mergeGraph`, then re-run the existing `layoutGraph` (dagre) helper over
    the merged union and push via `setNodes`/`setEdges` functional updates. While the mutation is in
    flight, add `animate-pulse motion-reduce:animate-none` to the clicked node's rendered wrapper (extend
    the existing `displayedNodes` mapping keyed on the pending node id — reuse the selection-ring surface,
    do NOT add a canvas spinner). If the response `truncated` flag is true, show the sonner toast with the
    EXACT copy from the UI-SPEC Copywriting Contract: "Showing the first 50 related items — narrow the tier
    filter to see more." Do not add any success toast (the appearing nodes ARE the confirmation).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/knowledge/_components/graph-merge.test.ts && npm run build --workspace=@nauta/web</automated>
  </verify>
  <acceptance_criteria>
    - `graph-merge.test.ts` asserts a duplicate-id node/edge is not added twice and inputs are not mutated (original array length unchanged)
    - `graph-merge.test.ts` asserts merging the same result twice equals merging once (idempotent)
    - knowledge-graph.tsx calls `api.knowledge.expandNode.useMutation` from the node-click path and both select + expand fire on one click
    - The budget-exceeded toast string matches the UI-SPEC verbatim: "Showing the first 50 related items — narrow the tier filter to see more."
    - Pulse class includes `motion-reduce:animate-none`
    - `npm run build --workspace=@nauta/web` succeeds
  </acceptance_criteria>
  <done>Clicking a node expands its neighbours onto the canvas, merges without duplicates, shows a pulse while loading, and surfaces the budget toast on truncation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC (expandNode) | client-supplied nodeId/depth crosses into a server graph walk |
| tRPC → Postgres | BFS query must stay tenant-scoped and bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Denial of Service | expandNode BFS | mitigate | depth clamped server-side to <=2 via clampDepth; hard node/edge budget cap (~50) via capBudget; a client cannot request an unbounded walk |
| T-32-02 | Information Disclosure | expandNode tenant scope | mitigate | traverse only edges whose endpoints share the seed node's `importerId` (KnowledgeNodes importer join), mirroring graph.ts D-12; never read importer from a client claim |
| T-32-03 | Tampering | expandNode input | mitigate | Zod `nodeId: z.string().uuid()` + integer depth at the boundary; unknown seed returns empty GraphResponse (fail-closed, no existence leak) |
| T-32-SC | Tampering | npm installs | accept | zero new dependencies added this plan (v1.4 hard discipline); nothing to vet |
</threat_model>

<verification>
- `npx vitest run` green for expand.test.ts (api-client) and graph-merge.test.ts (web)
- `npx tsc --noEmit` clean in packages/api-client
- `npm run build --workspace=@nauta/web` green
- Manual smoke (optional): clicking a knowledge node with materialized edges adds neighbours; re-click adds nothing new
</verification>

<success_criteria>
GRAPH-02 satisfied: clicking a node expands and displays its neighbours via a bounded (<=2-hop)
server-side query — not an unbounded or client-only walk. Merge is idempotent and tenant-scoped.
</success_criteria>

<output>
Create `.planning/phases/32-knowledge-canvas-tiered-graph-exploration/32-01-SUMMARY.md` when done.
</output>

---
phase: 29-tier-ladder-edge-materialization
plan: 04
type: execute
wave: 4
depends_on: ["29-02", "29-03"]
files_modified:
  - apps/email-listener/app/application/use_cases/confirm_region.py
  - apps/email-listener/app/container.py
  - apps/email-listener/tests/test_confirm_region.py
autonomous: true
requirements: [SYNTH-01]
must_haves:
  truths:
    - "ConfirmRegionUseCase accepts an optional KnowledgeSynthesizer and invokes it AFTER the embedding persists"
    - "The synthesis call is best-effort: a synthesizer failure is logged and NEVER fails the confirm"
    - "The DI container wires SupabaseKnowledgeGraphRepository + KnowledgeSynthesizerService into ConfirmRegionUseCase"
    - "Confirming a region end-to-end materializes a knowledge_node and EXTRACTED-tier edges (empty table becomes queryable)"
  artifacts:
    - path: "apps/email-listener/app/application/use_cases/confirm_region.py"
      provides: "wired best-effort synthesis call replacing the D-13 no-op comment"
      contains: "synthesize_from_confirmation"
    - path: "apps/email-listener/app/container.py"
      provides: "DI factory building ConfirmRegionUseCase with the synthesizer"
      contains: "KnowledgeSynthesizerService"
  key_links:
    - from: "apps/email-listener/app/application/use_cases/confirm_region.py"
      to: "apps/email-listener/app/domain/ports/knowledge_synthesizer.py"
      via: "self._knowledge_synthesizer.synthesize_from_confirmation(...)"
      pattern: "synthesize_from_confirmation"
    - from: "apps/email-listener/app/container.py"
      to: "apps/email-listener/app/infrastructure/supabase/knowledge_graph_repository.py"
      via: "provider factory instantiating the adapter + service"
      pattern: "SupabaseKnowledgeGraphRepository"
---

<objective>
Activate the dormant D-13 synthesis hook: inject the KnowledgeSynthesizer into ConfirmRegionUseCase and
call it best-effort after the embedding persists, then wire the concrete SupabaseKnowledgeGraphRepository +
KnowledgeSynthesizerService through the DI container so a real region-confirm materializes knowledge_nodes
and EXTRACTED-tier edges.

Purpose: SYNTH-01 — confirming a region must materialize knowledge nodes and EXTRACTED-tier edges linking
the confirmed entity/field to co-occurring entities and importer scope; where the table was previously
empty, a reviewer can query rows immediately after a confirm. This plan is the trigger + wiring that turns
29-03's synthesizer into observable rows, under best-effort semantics that mirror the existing
PromoteEntityOnConfirmUseCase try/except convention (synthesis failure must not block or corrupt confirm).
Output: wired use case, DI factory, and use-case + endpoint tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-tier-ladder-edge-materialization/29-CONTEXT.md
@apps/email-listener/app/application/use_cases/confirm_region.py
@apps/email-listener/app/presentation/api/v1/components.py
@apps/email-listener/app/domain/ports/knowledge_synthesizer.py
@apps/email-listener/app/application/use_cases/synthesize_knowledge.py

<interfaces>
Current ConfirmRegionUseCase.__init__ (confirm_region.py:48-57): keyword-only ports components, extractions,
embedder. Add `knowledge_synthesizer: KnowledgeSynthesizer | None = None` (default None keeps existing tests
and the no-op contract intact).
Hook site (confirm_region.py:169-189): the comment block to REPLACE, positioned after
`await self._components.update_embedding(component_id, embedding)`. In scope there: component, importer_id,
candidate (may be None), confirmed_record (defined ONLY inside the candidate branch — must be hoisted to None
before the if), corrected_fields, effective_fields, embedding.
Best-effort convention (components.py:250-257): `try: await X.execute(...) except Exception: structlog...warning(..., exc_info=True)`.
DI: container.py line 763 `provider.provide(ConfirmRegionUseCase)` — replace with a factory, mirroring
_provide_promote_entity_use_case (container.py:311-328) which instantiates a concrete Supabase repo from Client.
KnowledgeSynthesizer.synthesize_from_confirmation kwargs: component_id, importer_id, confirmed_record,
corrected_fields, source (default "learned_from_correction").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the best-effort synthesis call into ConfirmRegionUseCase</name>
  <files>apps/email-listener/app/application/use_cases/confirm_region.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/confirm_region.py (full; note confirmed_record is only bound inside the candidate branch, lines 110-128)
    - apps/email-listener/app/domain/ports/knowledge_synthesizer.py (the port + kwargs)
    - apps/email-listener/app/presentation/api/v1/components.py (lines 248-257: best-effort try/except reference)
  </read_first>
  <action>
    Import `KnowledgeSynthesizer` from app.domain.ports.knowledge_synthesizer. Add a keyword-only constructor
    param `knowledge_synthesizer: KnowledgeSynthesizer | None = None` and store it as `self._knowledge_synthesizer`.
    Hoist `confirmed_record: ExtractionRecord | None = None` to before the `if candidate is not None:` block so it
    is always in scope (assign the promoted record inside the branch as today). REPLACE the comment-only block at
    lines 169-189 with a real best-effort call after `update_embedding`: `if self._knowledge_synthesizer is not
    None:` then a `try/except Exception:` that awaits
    `self._knowledge_synthesizer.synthesize_from_confirmation(component_id=component_id, importer_id=importer_id,
    confirmed_record=confirmed_record, corrected_fields=corrected_fields, source="learned_from_correction")`; on
    exception log `log.warning("confirm_region_synthesis_failed", exc_info=True)` and continue (confirm has already
    succeeded — never re-raise). Keep the final `confirm_region_done` log. Do not change the embedding or extraction
    logic above the hook.
  </action>
  <verify>
    <automated>cd apps/email-listener && python -m pytest tests/test_confirm_region.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ConfirmRegionUseCase.__init__ accepts knowledge_synthesizer: KnowledgeSynthesizer | None = None
    - confirmed_record is initialized to None before the candidate branch (no UnboundLocalError on the no-candidate path)
    - The synthesizer is called after update_embedding with source="learned_from_correction", inside a try/except that logs and swallows
    - Existing test_confirm_region.py tests (constructed without the new port) still pass unchanged
  </acceptance_criteria>
  <done>The D-13 hook is live and best-effort; confirm never fails because synthesis failed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SupabaseKnowledgeGraphRepository + KnowledgeSynthesizerService in the DI container</name>
  <files>apps/email-listener/app/container.py</files>
  <read_first>
    - apps/email-listener/app/container.py (imports block ~20-53; _provide_promote_entity_use_case factory 311-328; provide site line 763)
    - apps/email-listener/app/application/use_cases/synthesize_knowledge.py (constructor ports: components, knowledge, entity_instances)
    - apps/email-listener/app/infrastructure/supabase/knowledge_graph_repository.py (SupabaseKnowledgeGraphRepository(client) constructor)
  </read_first>
  <action>
    Import KnowledgeSynthesizerService (app.application.use_cases.synthesize_knowledge), the KnowledgeSynthesizer
    port, and SupabaseKnowledgeGraphRepository (app.infrastructure.supabase.knowledge_graph_repository). Add a
    factory `_provide_confirm_region_use_case(components: ComponentRepository, extractions: ExtractionRepository,
    embedder: EmbeddingProtocol, entity_instances: EntityInstanceRepository, client: Client) ->
    ConfirmRegionUseCase` that instantiates `SupabaseKnowledgeGraphRepository(client=client)` (concrete infra, no
    port Protocol — same reason the promote factory instantiates SupabaseEntityResolutionRepository directly),
    builds `KnowledgeSynthesizerService(components=components, knowledge=<that repo>, entity_instances=entity_instances)`,
    and returns `ConfirmRegionUseCase(components=components, extractions=extractions, embedder=embedder,
    knowledge_synthesizer=<that service>)`. REPLACE `provider.provide(ConfirmRegionUseCase)` at line 763 with
    `provider.provide(_provide_confirm_region_use_case, provides=ConfirmRegionUseCase)`. Do not alter the other
    providers.
  </action>
  <verify>
    <automated>cd apps/email-listener && python -c "from app.container import build_container" 2>&1 | tail -5 && python -m pytest tests/ -k "container or confirm" -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - container.py imports KnowledgeSynthesizerService and SupabaseKnowledgeGraphRepository
    - _provide_confirm_region_use_case builds the adapter + service + use case and is registered via provides=ConfirmRegionUseCase
    - The container imports/builds without dishka resolution errors
    - lint-imports passes (the factory lives in container.py, the composition root, where infra imports are allowed)
  </acceptance_criteria>
  <done>Confirm requests now flow through a use case with a live, Supabase-backed synthesizer.</done>
</task>

<task type="auto">
  <name>Task 3: Test best-effort wiring + end-to-end confirm materialization</name>
  <files>apps/email-listener/tests/test_confirm_region.py</files>
  <read_first>
    - apps/email-listener/tests/test_confirm_region.py (existing AsyncMock-port use-case tests + any TestClient/dishka-override endpoint test)
    - apps/email-listener/app/presentation/api/v1/components.py (confirm endpoint 221-259)
  </read_first>
  <action>
    Add use-case tests with an AsyncMock KnowledgeSynthesizer: (a) assert synthesize_from_confirmation is awaited
    once with source="learned_from_correction" and the correct component_id/importer_id, and that it is called
    AFTER embedder.embed + components.update_embedding (assert ordering via a shared parent mock's mock_calls);
    (b) best-effort — configure the synthesizer to raise, assert use_case.execute completes without raising and
    that update_status/update_embedding still occurred; (c) no-candidate path (only a prior confirmed record) —
    assert confirmed_record passed to the synthesizer is None and no UnboundLocalError. If the module already has
    a TestClient + dishka-override endpoint test, add one that overrides KnowledgeSynthesizer with an AsyncMock,
    POSTs /{component_id}/confirm, asserts 200, and asserts the synthesizer was invoked (proving the wired path is
    reachable through the HTTP seam). Reuse existing fixtures; do not add pytest-asyncio (use asyncio.run).
  </action>
  <verify>
    <automated>cd apps/email-listener && python -m pytest tests/test_confirm_region.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the synthesizer is awaited after update_embedding with source="learned_from_correction"
    - A test asserts a raising synthesizer does NOT fail confirm (best-effort) and prior side effects still ran
    - A test covers the no-candidate path with confirmed_record=None (no UnboundLocalError)
    - The endpoint-level test (if present) asserts POST confirm returns 200 and the synthesizer was invoked
    - Full test_confirm_region.py passes; mypy + ruff clean
  </acceptance_criteria>
  <done>The wired, best-effort synthesis path is proven at the use-case and (where present) HTTP seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP confirm request → use case → synthesizer | client-triggered write path into knowledge_nodes/edges |
| synthesis failure → confirm transaction | a synthesis error must not corrupt or block the confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Denial of service | synthesis failure blocking confirm | mitigate | The call is wrapped in try/except that logs (structlog, exc_info) and never re-raises; confirm side effects (status, embedding) complete before the hook — asserted by a raising-synthesizer test |
| T-29-11 | Elevation of privilege | tenant of materialized rows | mitigate | importer_id passed to the synthesizer is derived inside the use case from the loaded component (confirm_region.py:88), not from the request body; the D-18 mismatch guard already 404s cross-tenant confirms |
| T-29-12 | Repudiation | edge origin | mitigate | source="learned_from_correction" is passed explicitly so materialized edges are attributable to a human confirmation, distinct from future synthesis-derived edges |
| T-29-SC | Tampering | package installs | accept | No new npm/pip/cargo packages introduced |
</threat_model>

<verification>
- test_confirm_region.py green (ordering, best-effort swallow, no-candidate None, endpoint 200 + invoked)
- container imports/builds without dishka errors
- End-to-end: a confirm materializes a knowledge_node + EXTRACTED edges (the previously empty table becomes queryable)
</verification>

<success_criteria>
Confirming a region via the existing region-confirm review flow creates knowledge_nodes and EXTRACTED-tier
knowledge_node_edges rows linking the confirmed entity/field to co-occurring entities and importer scope
(SYNTH-01); the synthesis call is best-effort and never fails the confirm.
</success_criteria>

<output>
Create `.planning/phases/29-tier-ladder-edge-materialization/29-04-SUMMARY.md` when done.
</output>

---
phase: 53-mobile-responsive-answer
plan: 06
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - apps/web/src/app/knowledge/_components/filter-rail.tsx
  - apps/web/src/app/knowledge/_components/knowledge-mobile-list.tsx
  - apps/web/src/app/knowledge/_components/knowledge-surface.tsx
  - apps/web/src/app/knowledge/_components/node-detail-pane.tsx
  - apps/web/src/app/knowledge/page.tsx
  - apps/web/src/app/knowledge/_components/__tests__/knowledge-mobile-list.test.tsx
autonomous: true
requirements: [MOBL-01]

must_haves:
  truths:
    - "Below md, /knowledge renders a node list (with filter chips) instead of the React-Flow graph, and the graph island is NEVER mounted"
    - "Tapping a list row opens the existing NodeDetailPane inside a full-width right Sheet; backdrop/X/Escape returns to the list with scroll preserved"
    - "The mobile list reuses filter-rail's exact node-type facets and NodeDetailPane verbatim — no second vocabulary, no redesign"
    - "There is exactly one close affordance in the mobile detail sheet (NodeDetailPane's own X is suppressed below md)"
    - "Desktop (>=md) /knowledge graph is unchanged"
  artifacts:
    - path: "apps/web/src/app/knowledge/_components/knowledge-mobile-list.tsx"
      provides: "Filter-chip bar + node list + empty state + full-width detail Sheet wrapping NodeDetailPane; self-fetches knowledge.graph"
      exports: ["KnowledgeMobileList"]
      min_lines: 60
    - path: "apps/web/src/app/knowledge/_components/knowledge-surface.tsx"
      provides: "Client wrapper branching on useIsMobileViewport: KnowledgeMobileList below md, KnowledgeGraphIsland at/above"
      contains: "useIsMobileViewport"
  key_links:
    - from: "knowledge-surface.tsx"
      to: "apps/web/src/hooks/use-is-mobile-viewport"
      via: "useIsMobileViewport gates graph-island mount"
      pattern: "useIsMobileViewport"
    - from: "knowledge-mobile-list.tsx"
      to: "filter-rail NODE_TYPE_ROWS"
      via: "reused facet data + active-state color recipe"
      pattern: "NODE_TYPE_ROWS"
    - from: "knowledge-mobile-list.tsx detail Sheet"
      to: "NodeDetailPane"
      via: "full-width SheetContent renders the unchanged NodeDetailPane"
      pattern: "NodeDetailPane"
---

<objective>
Give `/knowledge` a real mobile answer below `md`: a node LIST (filter chips +
`InboxRow`-idiom rows) with tap-through to the existing `NodeDetailPane` inside a
full-width right `Sheet` — and the `dynamic(ssr:false)` React-Flow
`KnowledgeGraphIsland` is never mounted on a phone (its init cost is never paid,
same pattern as `/chat`'s canvas).

Purpose: MOBL-01 — "knowledge collapses to a list/feed below md; desktop keeps
the 2D graph." Reuse filter-rail's exact facets and NodeDetailPane verbatim
(53-UI-SPEC Component Inventory §3, Judgment Calls #4/#5). A spatial graph is a
desktop-native pattern; the mobile answer is a list, not a shrunk graph.
Output: a new `KnowledgeMobileList` + a client `knowledge-surface.tsx` wrapper
that branches on `useIsMobileViewport()`; `filter-rail.tsx` exports its facet
data; `node-detail-pane.tsx` suppresses its redundant internal close below md; a
vitest suite proving the graph island never mounts on mobile.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md
@.planning/phases/53-mobile-responsive-answer/53-01-SUMMARY.md

<interfaces>
apps/web/src/hooks/use-is-mobile-viewport.ts (from Plan 53-01):
  export function useIsMobileViewport(): boolean   // true below 768px

filter-rail.tsx (existing): NODE_TYPE_ROWS is a MODULE-PRIVATE const —
  [{type,label,dotClass}] for the 6 node types (Entity Types/Fields/Instances/
  Emails/Components/Knowledge Rules); active recipe `bg-primary/10 text-primary`.
  export type NodeTypeKey = "entity_type"|"entity_type_field"|"entity_instance"|"email"|"email_component"|"knowledge_node"

node-detail-pane.tsx (existing): export interface SelectedNode { id; type; label; [k]:unknown }
  props: selectedNode, onClose, onSelectNode; has its own internal close X button.

knowledge/page.tsx (existing SERVER component with metadata): renders <KnowledgeGraphIsland className="absolute inset-0" /> inside a relative flex-1 container.
knowledge-graph-island.tsx: KnowledgeGraphIsland — dynamic(ssr:false) React-Flow wrapper.
knowledge.graph query: api.knowledge.graph.useQuery({ includeInstances, includeEmails }) -> { nodes:[{id,type,label,...}], edges }

@polytoken/ui/sheet: Sheet, SheetContent (side="right"; default caps at w-3/4 sm:max-w-sm — override to w-full sm:max-w-full p-0)
InboxRow row-height idiom: min-h-16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export filter facets and build KnowledgeMobileList</name>
  <files>apps/web/src/app/knowledge/_components/filter-rail.tsx, apps/web/src/app/knowledge/_components/knowledge-mobile-list.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/_components/filter-rail.tsx (NODE_TYPE_ROWS + NodeTypeKey)
    - apps/web/src/app/knowledge/_components/node-detail-pane.tsx (SelectedNode type + props)
    - apps/web/src/app/knowledge/_components/knowledge-graph.tsx (lines ~264-330 — how visibleTypes/includeInstances/includeEmails feed api.knowledge.graph; the GraphNode shape)
    - apps/web/src/app/knowledge/_components/graph-states.tsx (GraphNoSchemaState empty copy to reuse)
    - .planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md (Component Inventory §3 — filter-chip bar, node list, empty state, detail sheet; Copywriting Contract)
  </read_first>
  <action>
    (a) In `filter-rail.tsx`, add `export` to `NODE_TYPE_ROWS` (currently
        module-private) so the mobile list reuses the EXACT facet data + dotClass
        recipe — do not duplicate it. No other filter-rail change.
    (b) Create `apps/web/src/app/knowledge/_components/knowledge-mobile-list.tsx`
        exporting `KnowledgeMobileList` (self-contained client component; it
        fetches `api.knowledge.graph.useQuery` itself since the desktop
        `KnowledgeGraph` only mounts at >=md). Owns local state: `visibleTypes`
        (Set<NodeTypeKey>, default the same `entity_type`+`entity_type_field` as
        the graph's DEFAULT_VISIBLE_TYPES) and `selectedNodeId` (string|null).
        Derive the graph query flags from `visibleTypes` exactly as
        knowledge-graph.tsx does (`includeInstances`/`includeEmails`). Structure,
        top to bottom (per 53-UI-SPEC §3 — USE `h-11` chips, NOT the draft's
        `h-9`, per the spec's own correction):
        1. Filter-chip bar — `flex gap-2 overflow-x-auto border-b border-border/50
           bg-background px-3 py-1`, one pill `<button>` per `NODE_TYPE_ROWS`
           entry: `flex h-11 shrink-0 items-center gap-2 rounded-pill border px-3
           text-xs transition-colors focus-visible:outline-none
           focus-visible:ring-2 focus-visible:ring-ring
           focus-visible:ring-offset-1`, `aria-pressed={visibleTypes.has(type)}`,
           active `border-primary/30 bg-primary/10 text-primary` vs resting
           `border-border bg-muted/40 text-muted-foreground hover:bg-accent
           hover:text-accent-foreground`, a `size-2 rounded-full border` dot using
           the entry's `dotClass`, then the label. Toggling adds/removes the type
           from `visibleTypes` (immutable Set update). `role="group"
           aria-label="Filter by type"`.
        2. Node list — the graph query's `nodes` filtered to `visibleTypes`, one
           `<button min-h-16 w-full ...>` per node (per §3's row markup): a
           `size-2 rounded-full border` dot (dotClass for the node's type), then a
           `truncate text-sm font-normal text-foreground` label and a `truncate
           text-xs text-muted-foreground` type label. `onClick` sets
           `selectedNodeId`. Build small `dotClassFor(type)`/`typeLabelFor(type)`
           helpers FROM `NODE_TYPE_ROWS` (label+dotClass are right there) — never
           a second vocabulary.
        3. Empty state — if the filtered set is empty: render "No nodes match your
           filters — try showing another type." (exact copy). For a genuinely
           empty/no-schema graph, reuse `GraphNoSchemaState` verbatim.
        4. Detail Sheet — `<Sheet open={selectedNodeId !== null}
           onOpenChange={(open) => !open && setSelectedNodeId(null)}>` with
           `<SheetContent side="right" className="w-full sm:max-w-full p-0">`
           containing `<NodeDetailPane selectedNode={selectedNode}
           onClose={() => setSelectedNodeId(null)} onSelectNode={setSelectedNodeId}
           />`. `selectedNode` is derived from the query nodes as a `SelectedNode`.
           `onSelectNode` re-targets `selectedNodeId` WITHOUT closing (content
           swaps in place, matching desktop re-selection). Token classes only.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web && npm test -w @polytoken/web -- src/app/__tests__/palette-ban.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_TYPE_ROWS` is exported from `filter-rail.tsx`; `KnowledgeMobileList` imports it (no duplicated facet array).
    - `knowledge-mobile-list.tsx` renders `h-11` filter chips (not `h-9`), `min-h-16` list rows, the exact empty-state copy, and a `side="right"` `w-full sm:max-w-full p-0` Sheet wrapping `NodeDetailPane`.
    - `npm run typecheck -w @polytoken/web` clean; palette-ban gate green (no raw palette classes in the new file).
  </acceptance_criteria>
  <done>filter-rail exports its facets; KnowledgeMobileList reuses them + NodeDetailPane to render a filterable list with a full-width detail sheet; gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch the knowledge surface on the hook + suppress the redundant detail close</name>
  <files>apps/web/src/app/knowledge/_components/knowledge-surface.tsx, apps/web/src/app/knowledge/page.tsx, apps/web/src/app/knowledge/_components/node-detail-pane.tsx, apps/web/src/app/knowledge/_components/__tests__/knowledge-mobile-list.test.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/page.tsx (server component; renders KnowledgeGraphIsland)
    - apps/web/src/app/knowledge/_components/knowledge-graph-island.tsx (the dynamic ssr:false wrapper to gate)
    - apps/web/src/app/knowledge/_components/node-detail-pane.tsx (its internal close X button)
    - .planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md (Component Inventory §3 detail-sheet + Judgment Call #5)
  </read_first>
  <behavior>
    - With `useIsMobileViewport()` mocked `true`: `KnowledgeGraphIsland` is never mounted; `KnowledgeMobileList` renders instead.
    - With the hook mocked `false`: `KnowledgeGraphIsland` mounts (desktop unchanged); `KnowledgeMobileList` does not render.
    - NodeDetailPane's own internal close X carries `hidden md:inline-flex` so it is suppressed inside the mobile Sheet (the Sheet ships its own corner close) but visible on desktop.
  </behavior>
  <action>
    (a) Create `apps/web/src/app/knowledge/_components/knowledge-surface.tsx` —
        a `"use client"` component that reads `useIsMobileViewport()` and renders
        `isMobile ? <KnowledgeMobileList /> : <KnowledgeGraphIsland
        className="absolute inset-0" />`. This wrapper is needed because
        `page.tsx` is a server component (owns `metadata`) and cannot read a
        client hook directly. Import `useIsMobileViewport` from
        `~/hooks/use-is-mobile-viewport`.
    (b) In `knowledge/page.tsx`, replace the direct `<KnowledgeGraphIsland ...>`
        render with `<KnowledgeSurface />` (keep the surrounding server-component
        shell + metadata + the `relative min-h-0 flex-1` container). Note: the
        mobile list needs the full content height — the mobile branch may render
        without the `absolute inset-0` positioning; keep the graph branch's
        `absolute inset-0` intact.
    (c) In `node-detail-pane.tsx`, append `hidden md:inline-flex` to the internal
        close `<Button>`/`X` element's className so exactly ONE close affordance
        shows inside the mobile Sheet (the Sheet's own corner X) while desktop
        keeps NodeDetailPane's own close. Change nothing else in that file.
    Write the test FIRST (RED) — see acceptance criteria.
  </action>
  <verify>
    <automated>npm test -w @polytoken/web -- src/app/knowledge/_components/__tests__/knowledge-mobile-list.test.tsx && npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge-mobile-list.test.tsx` (jsdom): `vi.mock("~/hooks/use-is-mobile-viewport", ...)` and `vi.mock` the `./knowledge-graph-island` module with a spy export; mock `~/trpc/react` (proxy) so `knowledge.graph.useQuery` returns a couple of fake nodes. Assert: (a) with the hook mocked true, the KnowledgeGraphIsland mock is NEVER called and KnowledgeMobileList's chips/rows render; (b) toggling a filter chip changes which rows show; (c) clicking a row opens the detail Sheet (NodeDetailPane content appears); (d) a second case with the hook mocked false calls the KnowledgeGraphIsland mock and does not render the mobile list.
    - `node-detail-pane.tsx` internal close button className contains `hidden md:inline-flex`.
    - `knowledge/page.tsx` renders `KnowledgeSurface` (not `KnowledgeGraphIsland` directly) and remains a server component with its metadata intact.
    - `npm run typecheck -w @polytoken/web` clean; palette-ban gate green.
  </acceptance_criteria>
  <done>Below md /knowledge is the mobile list (graph island never mounted) with a single-close detail sheet; desktop graph unchanged; proven by a committed hook-mocked test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport -> /knowledge presentation | Same `knowledge.graph` query + same NodeDetailPane drive list and graph |
| DB-origin node strings -> rendered rows/detail | NodeDetailPane's existing escaped-React-text rendering (T-11-05) reused unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06-01 | Information Disclosure | KnowledgeMobileList data source | mitigate | Reuses the SAME auth-gated `api.knowledge.graph` query and the SAME NodeDetailPane component (same T-11-05 escaped-text rendering, no dangerouslySetInnerHTML) — only the layout differs; no new endpoint or authorization change |
| T-53-06-02 | Tampering | visibleTypes / selectedNodeId state | accept | Client presentation state; gates only which nodes render and which detail is shown, never data access |
| T-53-06-SC | Tampering | npm installs | accept | No new dependency (Sheet already vendored, hook from Plan 53-01); no install task |
</threat_model>

<verification>
- `npm test -w @polytoken/web -- src/app/knowledge/_components/__tests__/knowledge-mobile-list.test.tsx` green (graph island never mounts under mocked-true hook; list renders; row-tap opens sheet).
- `npm run typecheck -w @polytoken/web` clean; palette-ban/token-contrast/token-registration gates green.
- Live 360/768/1024 confirmation (list below md, graph at/above md, full-width detail sheet, single close affordance) is DEFERRED to `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md` §G — do not fake as passed. A Playwright viewport spec MAY be authored but not run this session.
</verification>

<success_criteria>
- Below md /knowledge is the node list with reused filter facets; KnowledgeGraphIsland never mounts; tap-through opens the unchanged NodeDetailPane in a full-width right Sheet with exactly one close affordance.
- Desktop graph byte-identical.
- Committed hook-mocked test + typecheck + gates green.
</success_criteria>

<output>
Create `.planning/phases/53-mobile-responsive-answer/53-06-SUMMARY.md` when done.
</output>

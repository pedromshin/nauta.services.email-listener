---
phase: 53-mobile-responsive-answer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/_components/inbox-three-pane.tsx
  - apps/web/src/app/_components/__tests__/inbox-mobile-stack.test.tsx
autonomous: true
requirements: [MOBL-02]

must_haves:
  truths:
    - "Below md the inbox is a single-pane stack: filter (segmented Tabs) + list, then a stacked detail view — never three horizontal panes"
    - "Tapping an inbox row on mobile swaps the view from list to detail; a back affordance returns to the list with scroll/selection preserved"
    - "On first paint on a phone the user always lands on the list, never auto-deposited into a detail view"
    - "Desktop (>=md) three-pane ResizablePanelGroup is byte-identical to today"
    - "No horizontal overflow at 360/390/414 on the inbox flow"
  artifacts:
    - path: "apps/web/src/app/_components/inbox-three-pane.tsx"
      provides: "Desktop three-pane (hidden md:block) + mobile stack (flex md:hidden) with Tabs filter, list, stacked detail, back affordance"
      contains: "md:hidden"
    - path: "apps/web/src/app/_components/__tests__/inbox-mobile-stack.test.tsx"
      provides: "Assertions: Tabs filter below md, three-pane wrapper hidden md:block, row-tap->detail, back->list with selectedEmailId preserved"
  key_links:
    - from: "inbox mobile stack filter"
      to: "FiltersRail's 3 options"
      via: "Tabs/TabsList/TabsTrigger reusing All/Unread/With entities + bg-primary/10 text-primary active recipe"
      pattern: "TabsTrigger"
    - from: "inbox row tap (mobile)"
      to: "mobileView state"
      via: "onSelectMember also sets mobileView=detail (only on explicit tap)"
      pattern: "mobileView"
---

<objective>
Collapse the inbox's desktop three-pane (FiltersRail | list | ReadingPreview)
into a single-pane master->detail stack below `md`, so the inbox flow shows no
horizontal overflow and stays usable on a phone. Desktop keeps the exact
`ResizablePanelGroup` three-pane, byte-identical.

Purpose: MOBL-02 — "inbox three-pane collapses to single-pane master->detail
navigation below md; no horizontal panes." Two side panels of fixed width
cannot fit a 360px viewport, so the mobile presentation must stack.
Output: `inbox-three-pane.tsx` gains a `flex md:hidden` mobile stack (segmented
Tabs filter + full-width list + tap-through stacked detail with an `ArrowLeft`
back bar) alongside the unchanged `hidden md:block` desktop three-pane, plus a
vitest suite proving the swap and the desktop-unchanged invariant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md
@docs/design/breakpoint-decision.md

<interfaces>
inbox-three-pane.tsx (existing, this file):
  type InboxFilter = "all" | "unread" | "with-entities"
  state: filter (setFilter), selectedEmailId (setSelectedEmailId), visibleItems, selectedEmail
  FiltersRail options: {all:"All", unread:"Unread", with-entities:"With entities"}, active recipe `bg-primary/10 text-primary`
  InboxThreadGroup props: subject, messageCount, latestReceivedAt, latestSnippet, members, entitiesByEmailId, selectedEmailId, onSelectMember
  ReadingPreview({ email }): renders subject header (h-11) + sender/body; unchanged
  Default-select effect (lines ~272-277): sets selectedEmailId to first visible thread's latest member

@polytoken/ui/tabs: Tabs, TabsList, TabsTrigger (same idiom as ChatCanvasViewToggle)
@polytoken/ui/button: Button (variant="ghost" size="icon")
lucide-react: ArrowLeft (new usage this phase)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split inbox into desktop three-pane + mobile master->detail stack</name>
  <files>apps/web/src/app/_components/inbox-three-pane.tsx, apps/web/src/app/_components/__tests__/inbox-mobile-stack.test.tsx</files>
  <read_first>
    - apps/web/src/app/_components/inbox-three-pane.tsx (whole file — FiltersRail, ReadingPreview, InboxThreePane, the default-select effect)
    - apps/web/src/app/chat/_canvas/chat-canvas-view-toggle.tsx (Tabs idiom to mirror)
    - .planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md (Component Inventory §4 — points 1-6, including the auto-select guard)
  </read_first>
  <behavior>
    - Below md: a segmented `Tabs` filter (All/Unread/With entities) renders, and the desktop `ResizablePanelGroup` wrapper carries `hidden md:block`.
    - Tapping an inbox row (mobile) flips `mobileView` to "detail" and shows the detail (ReadingPreview) with a back bar.
    - Tapping the back button returns to the list; `selectedEmailId` remains resolved (state preserved, only the view swapped).
    - On mount, the background auto-select effect sets `selectedEmailId` but does NOT flip `mobileView` to "detail" — first paint shows the list.
    - Desktop render path never reads `mobileView`.
  </behavior>
  <action>
    Wrap the existing `ResizablePanelGroup` return in `<div className="hidden
    h-full md:block">` (byte-identical three-pane inside — do NOT edit its
    internals). Add a sibling `<div className="flex h-full flex-col md:hidden">`
    mobile stack (keep it in this file per Claude's Discretion; a co-located
    `inbox-mobile-stack.tsx` is acceptable but not required). Add local state
    `mobileView: "list" | "detail"` (default "list").
    Mobile stack, top to bottom (per 53-UI-SPEC §4):
    1. Filter segmented control — `Tabs value={filter} onValueChange={(v) =>
       setFilter(v as InboxFilter)}` with `TabsList` `h-11 w-full justify-start
       gap-1 bg-background p-1` and three `TabsTrigger` (`h-9 flex-1 text-xs`):
       All / Unread / With entities (reuse FiltersRail's exact labels; the
       active state is the shadcn `Tabs` token-driven default — no color
       override, mirrors ChatCanvasViewToggle). `aria-label="Inbox filter"` on
       the list.
    2. When `mobileView === "list"`: the `InboxThreadGroup` list, full width, no
       `ResizablePanel` wrapper (reuse the exact loading/error/empty/load-more
       blocks already in the file). Its `onSelectMember` handler must, on mobile,
       ALSO set `mobileView = "detail"` (wrap the existing `setSelectedEmailId`).
    3. When `mobileView === "detail"`: a sticky `h-11` back bar ABOVE
       `ReadingPreview` — `flex h-11 shrink-0 items-center gap-2 border-b
       border-border/50 bg-background px-2` containing `<Button variant="ghost"
       size="icon" aria-label="Back to inbox" className="size-11"
       onClick={() => setMobileView("list")}><ArrowLeft className="size-4"
       aria-hidden /></Button>` then a truncating `<span className="truncate
       text-sm font-semibold">{selectedEmail?.subject ?? "Message"}</span>` —
       then `<ReadingPreview email={selectedEmail} />` (byte-identical, only
       wrapped by the extra sticky row on mobile).
    GUARD (53-UI-SPEC §4 point 6): the existing default-select effect keeps
    setting `selectedEmailId` (feeds ReadingPreview prefetch) but must NEVER flip
    `mobileView` to "detail" — only the explicit `onSelectMember` tap does.
    Import `ArrowLeft` from lucide-react and Tabs primitives from
    `@polytoken/ui/tabs`. Token classes only (palette-ban stays green).
    Write the test FIRST (RED) — see acceptance criteria.
  </action>
  <verify>
    <automated>npm test -w @polytoken/web -- src/app/_components/__tests__/inbox-mobile-stack.test.tsx && npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `inbox-mobile-stack.test.tsx` (jsdom, `~/trpc/react` mocked with a tRPC proxy the same way `genui-panel-node-toolbar.test.tsx` mocks it; seed `emails.listThreads`/`emails.list`/`emails.entitySummary` with a couple of fake rows) asserts:
        (a) the segmented `Tabs` filter with role/aria-label "Inbox filter" is in the DOM;
        (b) the desktop three-pane wrapper carries `hidden md:block` and the mobile stack carries `md:hidden` (class-string / source assertion);
        (c) clicking a rendered inbox row shows the back bar (`aria-label="Back to inbox"`) — i.e. mobileView flipped to detail;
        (d) clicking the back button hides the back bar (returns to list) while the same email remains selected (ReadingPreview still resolves the selected subject).
    - Existing inbox behavior (filter switching, load-more, entity filter) is unchanged — the desktop `ResizablePanelGroup` internals are untouched.
    - `npm run typecheck -w @polytoken/web` clean; palette-ban gate green.
  </acceptance_criteria>
  <done>Below md the inbox is a single-pane stack with a Tabs filter, tap-through detail, and a back affordance; desktop three-pane is byte-identical; the swap + auto-select guard are proven by a committed test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport -> inbox presentation | Same `emails.listThreads`/`emails.list`/`emails.entitySummary` queries drive both layouts |
| DB-origin strings -> rendered rows | Existing escaped-React-text rendering path is reused unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Information Disclosure | Mobile stack data source | mitigate | Mobile stack reuses the SAME auth-gated tRPC queries + the SAME InboxThreadGroup/ReadingPreview components — no new endpoint, no altered authorization; only layout differs |
| T-53-03-02 | Tampering | selectedEmailId / mobileView state | accept | Plain client component state (matches the existing selectedEmailId posture); no privilege or data-access decision depends on it |
| T-53-03-SC | Tampering | npm installs | accept | No new dependency (`ArrowLeft` is a new usage of the already-installed lucide-react; Tabs/Button already vendored); no install task |
</threat_model>

<verification>
- `npm test -w @polytoken/web -- src/app/_components/__tests__/inbox-mobile-stack.test.tsx` green.
- `npm run typecheck -w @polytoken/web` clean; palette-ban/token-contrast/token-registration gates green.
- Live 360/390/414 no-horizontal-overflow confirmation of the inbox flow is DEFERRED to `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md` §G — do not fake as passed. A Playwright viewport spec MAY be authored but not run this session.
</verification>

<success_criteria>
- Below md the inbox is a single-pane master->detail stack (Tabs filter + list + stacked detail + back bar); no horizontal panes.
- First paint on a phone shows the list; tap-through and back preserve selection/scroll.
- Desktop three-pane byte-identical; committed test + typecheck + gates green.
</success_criteria>

<output>
Create `.planning/phases/53-mobile-responsive-answer/53-03-SUMMARY.md` when done.
</output>

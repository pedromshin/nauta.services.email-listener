---
phase: 53-mobile-responsive-answer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/emails/[id]/_components/canvas-shell.tsx
  - apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx
  - apps/web/src/app/emails/[id]/_components/__tests__/canvas-shell-mobile.test.tsx
autonomous: true
requirements: [MOBL-02]

must_haves:
  truths:
    - "Below md the email-detail canvas has exactly one persistent zone (the CANVAS); LAYERS and INSPECTOR/SUMMARY are collapsed into Sheets, opened from toolbar triggers"
    - "The email-detail flow shows no page-level horizontal overflow at 360/390/414"
    - "The Layers/Inspector Sheet triggers only render below md; desktop keeps its always-visible side panels unchanged"
    - "The editing tools themselves (draw mode, region/role/entity pickers) are unchanged — only their container changes on mobile"
  artifacts:
    - path: "apps/web/src/app/emails/[id]/_components/canvas-shell.tsx"
      provides: "Persistent side panels (hidden md:flex) + Sheet-collapsed panels (md:hidden) below md; CANVAS is the sole persistent zone on mobile"
      contains: "md:hidden"
    - path: "apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx"
      provides: "md:hidden Layers/PanelRight Sheet-trigger icon buttons in the toolbar right group"
      contains: "Show layers"
  key_links:
    - from: "canvas-toolbar.tsx Layers/Inspector triggers"
      to: "canvas-shell.tsx Sheet open state"
      via: "onOpenLayers/onOpenInspector callbacks driving Sheet open"
      pattern: "onOpen"
    - from: "canvas-shell.tsx mobile Sheets"
      to: "same layers/inspector/summary slot ReactNodes"
      via: "the identical slot content rendered inside SheetContent below md"
      pattern: "SheetContent"
---

<objective>
Collapse `/emails/[id]`'s four-zone `CanvasShell` (LAYERS w-64 | CANVAS flex-1 |
INSPECTOR w-72 | optional SUMMARY w-72) so that below `md` the two side panels
(256px + 288px = 544px, already wider than a 360px viewport before the canvas's
own content) stop being persistent flex siblings and instead render inside
`Sheet`s opened from new toolbar triggers. The CANVAS zone becomes the sole
persistent `w-full flex-1` zone on mobile.

Purpose: MOBL-02 explicitly names "email detail" in the required flow list;
fixed-width side panels make "no horizontal overflow at 360px" mathematically
unsatisfiable without a collapse (53-UI-SPEC Judgment Call #7). Reuse the same
`Sheet`-based pattern as the other mobile surfaces — invent nothing new.
Output: `canvas-shell.tsx` renders persistent panels at >=md and Sheet-collapsed
panels below md; `canvas-toolbar.tsx` gains two `md:hidden` Sheet triggers; a
vitest suite proves the collapse. `email-detail.tsx` needs no change (CanvasShell
owns the collapse internally around the slots it already receives).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md
@docs/design/breakpoint-decision.md

<interfaces>
canvas-shell.tsx (existing):
  props: state, showRegions/onShowRegionsChange, showHistory/onShowHistoryChange,
         showUnrelated/onShowUnrelatedChange, onClose, layers?, inspector?,
         summary?, canvas, banner? (all ReactNode slots)
  renders <CanvasToolbar .../> then a `flex flex-1 min-h-0 overflow-hidden` row:
    LAYERS `w-64 shrink-0 border-r`, CANVAS `flex-1 min-w-0 overflow-hidden relative`,
    INSPECTOR `w-72 shrink-0 border-l`, optional SUMMARY `w-72 shrink-0 border-l`

canvas-toolbar.tsx (existing): h-11 role="toolbar" bar; right group is a
  ghost close Button with `ml-auto`. Uses TooltipProvider/Tooltip already.

@polytoken/ui/sheet: Sheet, SheetContent, SheetTrigger (side="left"/"right")
@polytoken/ui/button: Button (variant="ghost" size="icon")
lucide-react: Layers, PanelRight (new usages of already-installed glyphs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sheet-collapse LAYERS/INSPECTOR/SUMMARY below md in CanvasShell + toolbar triggers</name>
  <files>apps/web/src/app/emails/[id]/_components/canvas-shell.tsx, apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx, apps/web/src/app/emails/[id]/_components/__tests__/canvas-shell-mobile.test.tsx</files>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/canvas-shell.tsx (whole file — the four-zone body)
    - apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx (whole file — the toolbar right group)
    - packages/ui/src/sheet.tsx (Sheet/SheetContent/SheetTrigger exports)
    - .planning/phases/53-mobile-responsive-answer/53-UI-SPEC.md (Component Inventory §5 + Judgment Call #7)
  </read_first>
  <behavior>
    - At >=md: LAYERS/INSPECTOR/(SUMMARY) render as persistent flex siblings exactly as today; no Sheet triggers visible.
    - Below md: the persistent side panels are hidden; the CANVAS zone is the sole persistent `w-full flex-1` zone; a `Layers` trigger and a `PanelRight` trigger (both `md:hidden`, 44px) appear in the toolbar right group.
    - Tapping the `Layers` trigger opens a left `Sheet` containing the SAME `layers` slot node; tapping `PanelRight` opens a right `Sheet` containing the SAME `inspector` (and `summary` when present) slot node(s).
    - The editing tools inside the slots are byte-identical — same nodes, same props, only the container differs.
  </behavior>
  <action>
    In `canvas-shell.tsx`: add local state `mobileLayersOpen` and
    `mobileInspectorOpen` (both default false). Wrap the persistent LAYERS panel
    div and the INSPECTOR/SUMMARY panel divs so they only show at >=md — add
    `hidden md:flex` to each (they are `flex flex-col` today; keep that, just
    gate visibility). Render, `md:hidden`, two `Sheet`s: a `side="left"` Sheet
    (`open={mobileLayersOpen}`, `onOpenChange={setMobileLayersOpen}`) whose
    `SheetContent` renders the SAME `layers` node; a `side="right"` Sheet
    (`open={mobileInspectorOpen}`) whose `SheetContent` renders the `inspector`
    node and, when `summary` is provided, the `summary` node below it. Keep the
    CANVAS zone (`banner` + `canvas`) as the persistent `flex-1 min-w-0
    overflow-hidden` zone at all widths — its internal pan/zoom stays a contained
    scrollable surface bounded by the existing `overflow-hidden` wrapper (never
    page-level horizontal scroll). Pass `onOpenLayers={() =>
    setMobileLayersOpen(true)}` and `onOpenInspector={() =>
    setMobileInspectorOpen(true)}` down to `CanvasToolbar` via two new OPTIONAL
    props.
    In `canvas-toolbar.tsx`: add two OPTIONAL props `onOpenLayers?`,
    `onOpenInspector?`. When provided, render two `md:hidden` `size-11` ghost
    icon `Button`s in the right group (before or beside the existing `ml-auto`
    close button): a `Layers` glyph with `aria-label="Show layers"` and a
    `PanelRight` glyph with `aria-label="Show inspector"`, each wired to its
    callback. Wrap each in the existing Tooltip idiom for parity with the other
    toolbar controls. Do NOT change any existing tool-mode/view-toggle/keybinding
    behavior. Import `Layers`/`PanelRight` from lucide-react and Sheet primitives
    from `@polytoken/ui/sheet`. Token classes only. `email-detail.tsx` is NOT
    modified — CanvasShell owns the collapse around the slots it already receives.
    Write the test FIRST (RED) — see acceptance criteria.
  </action>
  <verify>
    <automated>npm test -w @polytoken/web -- src/app/emails/[id]/_components/__tests__/canvas-shell-mobile.test.tsx && npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `canvas-shell-mobile.test.tsx` (jsdom; render `CanvasShell` with a minimal fake `state` and simple string slot nodes for layers/inspector/summary/canvas) asserts:
        (a) the persistent LAYERS div and INSPECTOR div carry `hidden md:flex` (source/class assertion) — i.e. not persistent below md;
        (b) a `Layers` trigger (`aria-label="Show layers"`) and a `PanelRight` trigger (`aria-label="Show inspector"`) are present and both carry `md:hidden`;
        (c) clicking the `Show layers` trigger reveals the `layers` slot content inside a Sheet (open state flips);
        (d) the CANVAS slot content is always present regardless of Sheet state.
    - `canvas-shell.tsx` has no side panel that renders persistently below md (every `w-64`/`w-72` panel is gated `hidden md:flex`).
    - Existing canvas-toolbar behavior (Select/Draw, Regions/History/Unrelated toggles, close) is unchanged when the new props are omitted.
    - `npm run typecheck -w @polytoken/web` clean; palette-ban gate green.
  </acceptance_criteria>
  <done>Below md the email-detail canvas has one persistent zone with Sheet-collapsed side panels reachable from toolbar triggers; desktop side panels unchanged; editing tools byte-identical; a committed test proves the collapse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport -> email-detail layout | Same canvas state + same slot content drive both layouts |
| PDF/region data -> canvas zone | Existing overlay/draw data path reused unchanged inside the Sheets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Information Disclosure | Sheet-collapsed side panels | mitigate | Sheets render the IDENTICAL `layers`/`inspector`/`summary` slot nodes (same `showRegions`/`showHistory`/`showUnrelated` state, same components) — no new data path, no altered authorization; only the container changes |
| T-53-04-02 | Tampering | mobileLayersOpen/mobileInspectorOpen state | accept | Plain client presentation state; gates nothing but Sheet visibility |
| T-53-04-SC | Tampering | npm installs | accept | No new dependency (`Layers`/`PanelRight` are new usages of installed lucide-react; Sheet/Button already vendored); no install task |
</threat_model>

<verification>
- `npm test -w @polytoken/web -- src/app/emails/[id]/_components/__tests__/canvas-shell-mobile.test.tsx` green.
- `npm run typecheck -w @polytoken/web` clean; palette-ban/token-contrast/token-registration gates green.
- Live 360/390/414 no-horizontal-overflow confirmation of the email-detail flow is DEFERRED to `.planning/phases/49-live-loop-gate-deploy-oauth-real-email/MORNING-CHECKLIST.md` §G — do not fake as passed.
</verification>

<success_criteria>
- Below md the email-detail canvas has a single persistent CANVAS zone; LAYERS/INSPECTOR/SUMMARY collapse into Sheets from `md:hidden` toolbar triggers.
- Desktop side panels and all editing tools unchanged.
- Committed test + typecheck + gates green.
</success_criteria>

<output>
Create `.planning/phases/53-mobile-responsive-answer/53-04-SUMMARY.md` when done.
</output>

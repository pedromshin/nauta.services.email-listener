---
phase: 24-dual-channel-genui
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - apps/web/src/app/chat/_components/genui-part-boundary.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/chat/_components/build-proposal-cards-spec.ts
  - apps/web/src/app/chat/_components/widget-status-badge.tsx
  - apps/web/src/app/chat/_components/compact-interaction-entry.tsx
  - apps/web/src/app/chat/_components/interactive-widget-boundary.tsx
  - apps/web/src/app/chat/_components/widget-display-state.ts
  - packages/api-client/src/router/chat/widget-interactions.ts
  - packages/api-client/src/router/chat/index.ts
  - apps/web/src/app/api/chat/widget/submit/route.ts
  - apps/web/src/app/chat/_hooks/use-chat-stream.ts
  - apps/web/src/app/chat/_hooks/use-conversation-controller.ts
  - apps/web/src/app/chat/_components/message-turn.tsx
  - apps/web/src/app/chat/_components/message-list.tsx
  - apps/web/src/app/chat/_canvas/canvas-spec-context.tsx
  - apps/web/src/app/chat/_canvas/use-canvas-persistence.ts
  - apps/web/src/app/chat/_components/__tests__/build-proposal-cards-spec.test.ts
  - apps/web/src/app/chat/_components/__tests__/interactive-widget-boundary.test.tsx
  - apps/web/src/app/chat/_components/__tests__/widget-display-state.test.ts
  - apps/web/src/app/chat/_canvas/__tests__/interactive-widget-canvas.test.tsx
autonomous: true
requirements: [DCUI-01]

must_haves:
  truths:
    - "An agent-emitted proposal-card group renders in the transcript as a vertical stack of catalog cards, each with one footer button (D-05, UI-SPEC layout)"
    - "Clicking a card disables the group, shows Submitting…, POSTs the optionId, and on success shows the chosen card with ring + Selected badge, others dimmed, plus a compact 'Selected \"{title}\"' entry (D-06/D-16)"
    - "Typing while a widget is pending marks it Superseded (dimmed, disabled, caption) and the composer is never blocked (D-02/D-07)"
    - "A widget whose turn was regenerated or superseded by a newer turn shows the Stale badge and a 409 reconciles to the same state (D-12)"
    - "The same widget renders inside a canvas genui-panel node with identical interaction state — one message-part source of truth, no duplication (D-08/DCUI-04)"
    - "GenuiPanelNode no longer triple-nests chrome: the bare GenuiPartBoundary variant removes the inner GenuiCard inside canvas panels (UI-SPEC mandatory prerequisite)"
  artifacts:
    - path: "apps/web/src/app/chat/_components/genui-part-boundary.tsx"
      provides: "variant?: 'default' | 'bare' prop — bare renders content with NO GenuiCard wrapper"
      contains: "bare"
    - path: "apps/web/src/app/chat/_components/build-proposal-cards-spec.ts"
      provides: "deterministic declaration -> SpecRoot builder (stack of card+footer-button nodes, onClick setState carrying optionId)"
      exports: ["buildProposalCardsSpec"]
    - path: "apps/web/src/app/chat/_components/interactive-widget-boundary.tsx"
      provides: "InteractiveWidgetBoundary — state chrome (badges/submitting/error rows) around the UNMODIFIED SpecRenderer path"
      exports: ["InteractiveWidgetBoundary"]
    - path: "packages/api-client/src/router/chat/widget-interactions.ts"
      provides: "chat.getWidgetInteractions tRPC query (authoritative interaction state for a conversation)"
      contains: "getWidgetInteractions"
    - path: "apps/web/src/app/api/chat/widget/submit/route.ts"
      provides: "Next SSE proxy for POST /v1/chat/widget/submit (server-side X-API-Key, passes 409/422 through as JSON)"
      contains: "widget/submit"
  key_links:
    - from: "interactive-widget-boundary.tsx"
      to: "GenuiPartBoundary"
      via: "actions prop — the boundary's own setState ActionRegistry receives the clicked option's onClick payload"
      pattern: "actions="
    - from: "use-chat-stream.ts"
      to: "/api/chat/widget/submit"
      via: "submitWidget() reusing the existing SSE reader loop for the continuation turn"
      pattern: "submitWidget"
    - from: "genui-panel-node.tsx"
      to: "interactive-widget-boundary.tsx"
      via: "provenance part-type branch renders the widget with variant='bare' inside the panel shell"
      pattern: "InteractiveWidgetBoundary"
---

<objective>
Make proposal cards observable end-to-end: the mandatory GenuiPartBoundary `bare` variant fix, the
deterministic declaration→SpecRoot builder, the InteractiveWidgetBoundary state chrome
(pending/submitting/selected/superseded/stale/error), the submit transport (Next proxy +
useChatStream continuation), and rendering in BOTH the transcript and the canvas genui-panel node
from one message-part source of truth.

Purpose: DCUI-01 — agent emits a proposal card; clicking it sends a structured result that resumes
the run. Everything here consumes 24-02's backend verbatim and renders exclusively through the
UNMODIFIED SpecRenderer + existing catalog (D-05/D-09).
Output: proposal-card round-trip working in the browser (transcript + canvas), with the UI-SPEC's
locked copy, badges, and accessibility contract.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-dual-channel-genui/24-CONTEXT.md
@.planning/phases/24-dual-channel-genui/24-UI-SPEC.md
@.planning/phases/24-dual-channel-genui/24-01-SUMMARY.md
@.planning/phases/24-dual-channel-genui/24-02-SUMMARY.md

<interfaces>
<!-- Part shapes (24-01 contract): -->
interactive_widget part: { type:"interactive_widget", interactionId, widgetKind:"proposal_cards"|"clarify_widget", declaration }
  proposal declaration: { prompt?: string, options: [{ id, title, description?, value }] }
interaction_result part: { type:"interaction_result", interactionId, widgetKind, summary }
  proposal summary: { chosenTitle: string }

<!-- Backend contract (24-02): POST /v1/chat/widget/submit
  body { conversation_id, interaction_id, model_id, result } — proposal result = { optionId }
  401 no key | 404 not_found | 409 {stale|conflict} | 422 invalid | 200 text/event-stream of ChatRunEvents -->

<!-- Catalog contracts already in the codebase (verified):
  CardNodeSchema: { type:"card", title?, description?, children, header?, footer? } (spec-schema.ts:179)
  ButtonNodeSchema: { type:"button", label, "aria-label" (a11y-required), action?: string, onClick?: ActionSchema }
  SetStateActionSchema: { type:"setState", key: string(<=64), value: string|number|boolean|null }
  ButtonComponent (manifest.ts, 23-06): fires registry[onClick.type]?.(onClick) — the FULL onClick
    object is the handler payload, so onClick={type:"setState", key:"proposal.choice", value:"<optionId>"}
    delivers the optionId to a boundary-supplied setState handler with ZERO schema changes.
  GenuiPartBoundary props: { specJson, isStreaming, data?, actions? } -->

<!-- tool_call run events carry data.tool_name (run_chat_turn._apply_delta) — the client can
     distinguish emit_ui_spec from emit_proposal_cards/emit_clarify_widget streams. -->
</interfaces>

Reference source (read before modifying):
@apps/web/src/app/chat/_components/genui-part-boundary.tsx
@apps/web/src/app/chat/_components/message-turn.tsx
@apps/web/src/app/chat/_hooks/use-chat-stream.ts
@apps/web/src/app/chat/_hooks/use-conversation-controller.ts
@apps/web/src/app/chat/_canvas/genui-panel-node.tsx
@apps/web/src/app/api/chat/stream/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GenuiPartBoundary `bare` variant + GenuiPanelNode call-site switch (UI-SPEC mandatory prerequisite)</name>
  <files>apps/web/src/app/chat/_components/genui-part-boundary.tsx, apps/web/src/app/chat/_canvas/genui-panel-node.tsx, apps/web/src/app/chat/_components/__tests__/genui-part-boundary.test.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/genui-part-boundary.tsx (GenuiCard wrapper at all 3 return paths: finalized, full-parse streaming, partial-tree streaming, skeleton-only — every one wraps in GenuiCard today)
    - apps/web/src/app/chat/_components/__tests__/genui-part-boundary.test.tsx (existing test conventions to extend, not replace)
    - apps/web/src/app/chat/_canvas/genui-panel-node.tsx (the node shell's own border + p-4 div — the two layers that must survive; GenuiPartBoundary's GenuiCard is the third layer being removed)
    - .planning/phases/24-dual-channel-genui/24-UI-SPEC.md § Component Inventory (the exact variant contract)
  </read_first>
  <behavior>
    - variant="bare" returns the identical rendered content (SpecRenderer / skeleton / partial+skeleton) with NO GenuiCard wrapper — no extra border, no extra padding, at ALL FOUR return paths.
    - variant unset/"default" is byte-for-byte the current behavior (MessageTurn call sites unchanged — GenuiCard stays the transcript's only chrome).
    - GenuiPanelNodeBody passes variant="bare"; its own single p-4 div remains the ONE surviving content-padding layer.
  </behavior>
  <action>
    Add `variant?: "default" | "bare"` to GenuiPartBoundaryProps (default "default"). Extract a tiny
    internal `Wrapper` that renders `GenuiCard` for default and a plain fragment for bare, and route
    all four return paths through it (finalized / streaming-full-parse / streaming-partial / skeleton).
    Switch genui-panel-node.tsx's GenuiPartBoundary call site to `variant="bare"` — keep the
    node shell, header row, ScrollArea, and the p-4 div exactly as-is. Do NOT touch
    packages/genui/src/renderer/spec-renderer.tsx (hard-locked). Tests: render a finalized valid spec
    with variant="bare" and assert NO element carries the GenuiCard classes (`rounded-lg border`)
    between the boundary root and the rendered spec content; render default and assert the wrapper is
    still present; render bare while streaming and assert skeleton bars render unwrapped.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/genui-part-boundary.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RED before GREEN.
    - `grep -c "variant" apps/web/src/app/chat/_components/genui-part-boundary.tsx` >= 3; genui-panel-node.tsx contains `variant="bare"`.
    - A test asserts bare renders zero `border-border` wrappers around the spec content; a default-variant test still passes unchanged.
    - `git status --porcelain packages/genui/src/renderer/spec-renderer.tsx` is empty.
  </acceptance_criteria>
  <done>Canvas panels render genui content with exactly one border (the node shell) and one padding layer (the p-4 div); transcript chrome unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildProposalCardsSpec + WidgetStatusBadge + CompactInteractionEntry + InteractiveWidgetBoundary (proposal-cards chrome)</name>
  <files>apps/web/src/app/chat/_components/build-proposal-cards-spec.ts, apps/web/src/app/chat/_components/widget-status-badge.tsx, apps/web/src/app/chat/_components/compact-interaction-entry.tsx, apps/web/src/app/chat/_components/interactive-widget-boundary.tsx, apps/web/src/app/chat/_components/__tests__/build-proposal-cards-spec.test.ts, apps/web/src/app/chat/_components/__tests__/interactive-widget-boundary.test.tsx</files>
  <read_first>
    - .planning/phases/24-dual-channel-genui/24-UI-SPEC.md (Copywriting Contract — EXACT badge/caption/error strings and aria rules; Interaction Contracts — the full selection flow; Design Decisions 3-5 — unboxed error row, no pending badge, no separate proposal Submitted badge)
    - packages/genui/src/schema/spec-schema.ts lines 88-110 + 178-190 (ButtonNodeSchema incl. required "aria-label"; CardNodeSchema children is REQUIRED — pass children: [])
    - apps/web/src/app/chat/_components/turn-status-badge.tsx (the sibling Badge pattern WidgetStatusBadge mirrors)
    - apps/web/src/app/chat/_canvas/panel-action-bridge.ts (buildPanelActionRegistry — the registry-shape precedent the boundary's own actions registry follows)
    - apps/web/src/app/chat/_components/message-turn.tsx (user-bubble classes CompactInteractionEntry reuses: bg-muted px-4 py-2 rounded-lg max-w-[85%])
  </read_first>
  <behavior>
    - buildProposalCardsSpec(declaration) returns a SpecRoot that passes SpecRootSchema.safeParse: {v:1, root:{type:"stack", gap:"sm"(or the stack schema's equivalent), children: one card per option}}; each card {type:"card", title, description?, children:[], footer:{type:"button", label:<per-option label or "Choose this option">, "aria-label":"<label> — <title>", onClick:{type:"setState", key:"proposal.choice", value:<option.id>}}}.
    - InteractiveWidgetBoundary renders: pending → live spec via GenuiPartBoundary (isStreaming=false) with an actions registry {setState:(payload)=>onSubmitOption(payload.value)}; submitting → group pointer-events-none + Loader2 "Submitting…" row; submitted → chosen card ring-2 ring-primary + "Selected" badge, others opacity-50 aria-disabled, actions registry replaced by a noop; superseded/stale → whole group dimmed + the matching badge and caption, noop actions; error (422) → widget RE-ENABLES with the unboxed destructive icon+text row above the content.
    - The group container carries role="group" aria-label (declaration.prompt or "Choose an option"); no badge renders in the pending state.
  </behavior>
  <action>
    build-proposal-cards-spec.ts: pure builder + a `PROPOSAL_CHOICE_ACTION_KEY = "proposal.choice"`
    export. Chosen-state variant: a second export `buildProposalCardsSpecWithChoice(declaration,
    chosenOptionId)` is NOT needed — the chosen/dimmed treatment is boundary chrome (CSS on the
    wrapper via data attributes/DOM), not a spec mutation; render the SAME spec and overlay state by
    wrapping each... NO — SpecRenderer output cannot be reliably per-card styled from outside without
    fragile DOM selectors. Instead: chosen state re-renders via the SAME builder with per-option
    presentation flags: give the builder an optional second arg `state?: { chosenOptionId?: string,
    disabled?: boolean }`; when present, non-chosen cards' buttons render with `action` omitted...
    ButtonNodeSchema has no disabled prop. FINAL contract (implement exactly this): the SUBMITTED view
    replaces the live spec entirely — render the chosen option as ONE card (title+description, no
    button) inside a `ring-2 ring-primary ring-offset-1 rounded-lg bg-primary/5` wrapper div with the
    "Selected" WidgetStatusBadge, and the other options as plain dimmed (opacity-50, aria-disabled)
    title/description rows — matching D-06's visual contract without needing per-node style injection
    through the renderer. The LIVE (pending) view is the pure catalog spec via GenuiPartBoundary.
    Superseded/stale keep the live spec rendered but wrap it in an opacity-50 pointer-events-none
    aria-disabled container with the badge + caption (buttons present but inert — cosmetic per D-11;
    the server lock is authoritative).

    widget-status-badge.tsx: `WidgetStatusBadge({kind:"selected"|"superseded"|"stale"|"submitted"})`
    using @nauta/ui/badge — selected: variant="default" + Check + "Selected"; superseded:
    variant="secondary" + Ban + "Superseded"; stale: variant="secondary" + Clock + "Stale";
    submitted: variant="secondary" + CheckCircle2 + "Submitted" (lucide icons, all already deps).

    compact-interaction-entry.tsx: `CompactInteractionEntry({widgetKind, summary})` — proposal:
    right-aligned user bubble (`bg-muted px-4 py-2 rounded-lg max-w-[85%]`) with text `Selected
    "{chosenTitle}"`; clarify branch stubs to the same bubble rendering `{label}: {value}` rows (full
    key-value-list treatment lands in 24-04, but the component accepts both kinds now so the part
    renderer never needs a second entry point).

    interactive-widget-boundary.tsx: props { part (interactive_widget), displayState:
    "pending"|"submitting"|"submitted"|"superseded"|"stale", submittedValue?: {optionId}, errorMessage?:
    string|null, onSubmitOption:(optionId:string)=>void, variant?: "default"|"bare", data?, }. Renders
    the badge row (only for non-pending deviations, UI-SPEC Design Decision 4), the unboxed error row
    (AlertTriangle + text-destructive text, NO border/background — Design Decision 3), the
    Loader2 "Submitting…" row (motion-safe:animate-spin, text-muted-foreground), and the content per
    &lt;behavior&gt;. Captions: superseded "You replied by typing instead."; stale "This is no longer
    the active response."; conflict error "This was already answered."; validation error "This
    response couldn't be saved. Please try again.". aria-describedby on the dimmed container points
    at the badge caption id. Uses GenuiPartBoundary (with the Task 1 variant prop) for the live spec
    — never SpecRenderer directly.
    Tests: builder output safeParses + carries composed aria-labels + optionId values; boundary tests
    mount each displayState and assert exact copy strings, aria-disabled, the noop-vs-live actions
    registry, and that clicking a live card's rendered button calls onSubmitOption with the right id
    (real SpecRenderer, mirroring packages/genui/src/__tests__/button-action.test.tsx's approach).
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/build-proposal-cards-spec.test.ts src/app/chat/_components/__tests__/interactive-widget-boundary.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RED before GREEN.
    - A test asserts SpecRootSchema.safeParse(buildProposalCardsSpec(decl)).success === true and every button's aria-label equals "<label> — <title>".
    - A live-render test clicks a card button and receives the exact optionId through onSubmitOption (via the setState registry payload's value).
    - Copy assertions match UI-SPEC verbatim: "Selected", "Superseded", "Stale", "Submitting…", "You replied by typing instead.", "This is no longer the active response.".
    - No new catalog component, no spec-renderer.tsx change, no new npm dependency.
  </acceptance_criteria>
  <done>The full proposal-card visual/interaction contract exists as tested components, driven entirely by props — no transport yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: transport + state plumbing — tRPC interactions query, Next proxy, submitWidget stream, part types, controller wiring</name>
  <files>packages/api-client/src/router/chat/widget-interactions.ts, packages/api-client/src/router/chat/index.ts, apps/web/src/app/api/chat/widget/submit/route.ts, apps/web/src/app/chat/_hooks/use-chat-stream.ts, apps/web/src/app/chat/_components/widget-display-state.ts, apps/web/src/app/chat/_hooks/use-conversation-controller.ts, apps/web/src/app/chat/_components/__tests__/widget-display-state.test.ts</files>
  <read_first>
    - packages/api-client/src/router/chat/history.ts (publicProcedure + Drizzle select + input schema export pattern) and packages/api-client/src/router/chat/index.ts (procedure registration shape)
    - packages/db/src/schema/chat-widget-interactions.ts (columns from 24-01)
    - apps/web/src/app/api/chat/stream/route.ts (the ENTIRE proxy pattern: request-time env read, Zod body validation, upstream pipe, jsonError helper — copy it, adapt the schema/path)
    - apps/web/src/app/chat/_hooks/use-chat-stream.ts (runStream's reader loop, parseSseChunk/applyRunEvent exports, onTerminal contract)
    - apps/web/src/app/chat/_hooks/use-conversation-controller.ts (handleTerminal invalidations, handleSubmit — the supersede hook point, groupTurnsFromHistory)
  </read_first>
  <behavior>
    - chat.getWidgetInteractions({conversationId}) returns rows {id, messageId, partIndex, widgetKind, state, submittedValue} for the conversation (UUID-validated input, row cap mirroring MAX_HISTORY_ROWS).
    - The Next proxy validates {conversation_id, interaction_id, model_id, result} and pipes the upstream stream on 200; on upstream 4xx it passes the status + a JSON body {error, reason?} through (409/422 are MEANINGFUL to the widget UI — never flattened to 502).
    - useChatStream gains submitWidget(interactionId, result, modelId) plus an onWidgetRejected?(status:number, reason:string) option: a non-ok response invokes onWidgetRejected (and does NOT set the global stream state to failed — the transcript turn is fine, only the widget shows the error); an ok response reuses the SAME reader/accumulator loop as send() so the continuation turn streams like any turn.
    - applyRunEvent routes tool_call events whose data.tool_name !== "emit_ui_spec" into a client-only {type:"interactive_widget_streaming", toolId, partialJson} part (rendered as skeleton by Task 4) instead of genui_spec_streaming; MessagePart union gains interactive_widget, interaction_result, interactive_widget_streaming.
    - deriveWidgetDisplayState (pure): submitted row → "submitted"; pending row + (emitting message inactive OR a newer turnIndex exists in history) → "stale"; pending + locally-superseded set contains the id → "superseded"; else "pending". Local in-flight submit overlays "submitting"; a 409-conflict reconciles to "submitted" state after invalidation, a 409-stale to "stale".
    - Typing supersedes: handleSubmit records every currently-pending interactionId into the superseded-locally set BEFORE sending (optimistic, D-02); handleTerminal also invalidates getWidgetInteractions.
  </behavior>
  <action>
    widget-interactions.ts: `getWidgetInteractionsInputSchema = z.object({conversationId: z.string().uuid()})`;
    query selects from ChatWidgetInteractions where conversationId, ordered by createdAt, limit 200.
    Register as `chat.getWidgetInteractions` in chat/index.ts.

    submit/route.ts: copy stream/route.ts verbatim structure; schema {conversation_id: uuid,
    interaction_id: uuid, model_id: min(1), result: z.record(z.unknown())}; POST to
    `${url}/v1/chat/widget/submit`; on `!upstream.ok`, read the upstream JSON (FastAPI HTTPException
    detail) and return `new Response(JSON.stringify({error: <friendly>, reason: <detail reason>}),
    {status: upstream.status})` for 404/409/422 (5xx keeps the existing 502 mapping); on ok pipe
    text/event-stream through unchanged.

    use-chat-stream.ts: extract the fetch+reader+accumulate body of runStream into a reusable inner
    (or parameterize runStream with {onHttpError}); add `submitWidget` to UseChatStreamResult; add
    `onWidgetRejected` to UseChatStreamOptions. Extend MessagePart + applyRunEvent per behavior
    (the tool_call branch keys on event.data.tool_name; unknown/missing tool_name defaults to the
    existing genui path for backward compatibility).

    widget-display-state.ts: `deriveWidgetDisplayState({interaction, historyRows, supersededLocally,
    inFlightInteractionId})` pure function + the WidgetDisplayState type — colocated with components
    so tests need no hook mounting.

    use-conversation-controller.ts: add `api.chat.getWidgetInteractions.useQuery({conversationId})`;
    a `supersededLocallyRef` (Set<string>) populated in handleSubmit from current pending rows; an
    `inFlightWidget` state {interactionId, status: "submitting"|"error", errorKind?: "conflict"|"stale"|"invalid"} —
    handleWidgetSubmit(interactionId, result) sets submitting, calls chatStream.submitWidget(interactionId,
    result, modelId); onWidgetRejected maps 409-conflict/409-stale/422 into errorKind + invalidates
    getWidgetInteractions (server truth reconciles the visual state); onTerminal (existing) additionally
    invalidates getWidgetInteractions. Expose on ConversationController: `widgetInteractions` rows,
    `widgetSubmit` {inFlight state}, `handleWidgetSubmit`, and `supersededLocally` (as a stable array/set
    snapshot) so Task 4 renders purely from controller output.
    Tests: deriveWidgetDisplayState across the six cases in behavior; applyRunEvent's
    interactive_widget_streaming routing; the tRPC input schema.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/widget-display-state.test.ts src/app/chat/_hooks/__tests__/use-chat-stream.test.ts && npx tsc --noEmit && cd ../../packages/api-client && npx vitest run src/router/chat/__tests__ 2>nul || cd ../../packages/api-client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RED before GREEN.
    - use-chat-stream tests: a tool_call event with tool_name "emit_proposal_cards" accumulates interactive_widget_streaming (NOT genui_spec_streaming); existing emit_ui_spec tests unchanged and green.
    - widget-display-state tests cover: submitted, stale-by-inactive-sibling, stale-by-newer-turn, superseded-locally, pending, submitting-overlay.
    - submit/route.ts passes upstream 409/422 status + reason through (assert in source: no jsonError(...502) on 4xx); EMAIL_LISTENER_API_KEY appears only in the route file, never client code.
    - packages/api-client + apps/web tsc clean; chat/index.ts registers getWidgetInteractions.
  </acceptance_criteria>
  <done>The client can read authoritative widget state, submit a result through the two-hop key posture, stream the continuation, and derive every display state purely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: surface wiring — transcript part rendering + canvas parity (D-08, DCUI-01 observable end-to-end)</name>
  <files>apps/web/src/app/chat/_components/message-turn.tsx, apps/web/src/app/chat/_components/message-list.tsx, apps/web/src/app/chat/_canvas/canvas-spec-context.tsx, apps/web/src/app/chat/_canvas/use-canvas-persistence.ts, apps/web/src/app/chat/_canvas/genui-panel-node.tsx, apps/web/src/app/chat/_canvas/__tests__/interactive-widget-canvas.test.tsx</files>
  <read_first>
    - apps/web/src/app/chat/_components/message-turn.tsx (the parts.map branch structure — add branches, do not restructure)
    - apps/web/src/app/chat/_components/message-list.tsx (MessageListItem + prop threading — widget props flow controller → list → turn)
    - apps/web/src/app/chat/_canvas/canvas-spec-context.tsx (how provenance resolves to specJson/isStreaming today — the seam to extend for part-type-aware resolution)
    - apps/web/src/app/chat/_canvas/use-canvas-persistence.ts (buildExpectedGenuiPanelSpecs — the Pass-2 materialization filter that currently only matches part.type === "genui_spec")
    - apps/web/src/app/chat/_canvas/__tests__/panel-data-flow.test.tsx (the zero-mock canvas mount harness pattern to reuse)
  </read_first>
  <behavior>
    - MessageTurn renders: interactive_widget → InteractiveWidgetBoundary (default variant) fed by widget state/handlers threaded from the controller; interaction_result → CompactInteractionEntry; interactive_widget_streaming → the skeleton placeholder (GenuiPartBoundary isStreaming with variant default is acceptable — it renders SkeletonBars for unparseable content).
    - The canvas materializes a genui-panel node for interactive_widget parts exactly like genui_spec parts (same provenance id scheme genuiPanelNodeId(messageId, partIndex)); inside the panel the widget renders via InteractiveWidgetBoundary variant="bare" with the SAME controller-derived state and submit handler — clicking in either surface updates both (one source of truth, D-08).
    - interaction_result parts do NOT materialize canvas panels (they are transcript entries, not panels).
  </behavior>
  <action>
    message-turn.tsx: extend MessageTurnProps with an optional `widgets` bundle prop
    `{ states: Readonly<Record<string, WidgetDisplayState>>, submittedValues: Readonly<Record<string,
    unknown>>, errorMessages: Readonly<Record<string, string|null>>, onSubmitOption:(interactionId:string,
    optionId:string)=>void }` (keyed by interactionId); add the three part branches. message-list.tsx:
    thread `widgets` through MessageListItem/MessageListProps. page.tsx's ConversationView passes the
    controller's widget surface down (build the Record maps in the controller, not in render — keep
    MessageList memo-friendly).

    Canvas: extend buildExpectedGenuiPanelSpecs' part filter to `part.type === "genui_spec" ||
    part.type === "interactive_widget"`. Extend canvas-spec-context so useCanvasSpec (or a new
    sibling `useCanvasPart(provenance)`) can return the RAW MessagePart for a provenance ref;
    GenuiPanelNodeBody branches: interactive_widget part → InteractiveWidgetBoundary variant="bare"
    (widget state/handlers read from the controller via the existing ChatControllerContext seam the
    canvas already threads — see chat-canvas.tsx's provider tree); genui_spec (default) → current
    GenuiPartBoundary path unchanged.

    Test (zero-mock, mirrors panel-data-flow.test.tsx): mount a transcript MessageTurn AND a canvas
    GenuiPanelNodeBody harness over the same interactive_widget part + shared state maps; click the
    card in the canvas harness; assert the SAME onSubmitOption fires with the same interactionId
    (one handler, both surfaces); flip the shared state map to submitted and assert BOTH surfaces
    show the Selected treatment.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/interactive-widget-canvas.test.tsx && npx vitest run src/app/chat && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RED before GREEN.
    - The dual-surface test proves one click handler + one state source drives both transcript and canvas rendering (D-08 "never duplicated").
    - buildExpectedGenuiPanelSpecs materializes an interactive_widget part as a genui-panel node id `genui-panel:{messageId}:{partIndex}` and does NOT materialize interaction_result parts (assert both).
    - Full apps/web chat vitest suite green (no regression in the 95 existing tests); tsc clean; next build compiles.
    - `git status --porcelain packages/genui/src/renderer/spec-renderer.tsx` empty.
  </acceptance_criteria>
  <done>Proposal cards are observable end-to-end in both surfaces: emit → render → click → submit → continuation streams → locked Selected state + compact entry persist across reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-authored declaration → DOM | untrusted declaration renders via the catalog only |
| browser → Next proxy → FastAPI | optionId submit crosses the two-hop key boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering | declaration → renderer | mitigate | buildProposalCardsSpec output still passes SpecRootSchema.safeParse inside GenuiPartBoundary (FOUND-6 gate unchanged); a declaration that produces an invalid spec renders SAFE_FALLBACK_SPEC |
| T-24-11 | Spoofing | client forges optionId | mitigate | Server resolves payload from the STORED declaration (24-02); a forged optionId fails the stored enum schema → 422 |
| T-24-12 | Tampering | client-side disable bypass (devtools re-enable) | mitigate | All client disables are cosmetic by design (D-11); the DB CAS + staleness checks are authoritative; 409 reconciles the UI |
| T-24-13 | Information Disclosure | API key exposure | mitigate | EMAIL_LISTENER_API_KEY read only in the route handler at request time (T-22-29 pattern copied); never NEXT_PUBLIC_ |
| T-24-14 | Denial of Service | auto-fire | mitigate | Only a real DOM click on a native button fires onSubmitOption; no effect ever calls handleWidgetSubmit (MCP Apps posture — explicit user action only) |
| T-24-SC | Supply chain | new packages | mitigate | none — lucide-react, @nauta/ui/badge, zod all existing deps |
</threat_model>

<verification>
- `cd apps/web && npx vitest run src/app/chat && npx tsc --noEmit` — full chat suite green including all new tests.
- `cd packages/api-client && npx tsc --noEmit` clean; getWidgetInteractions registered.
- No-eval grep on the new _components files = 0; spec-renderer.tsx untouched (git status).
- RED-before-GREEN commits for all four tasks.
</verification>

<success_criteria>
DCUI-01 is observably true: the agent emits a proposal-card group that renders in transcript and
canvas through the unmodified renderer; a click submits a structured result, the run resumes as a
streamed continuation, the group locks to the UI-SPEC's Selected/dimmed contract, typing supersedes,
staleness badges appear, and all state survives reload from message parts + interaction rows.
</success_criteria>

<output>
Create `.planning/phases/24-dual-channel-genui/24-03-SUMMARY.md` when done.
</output>

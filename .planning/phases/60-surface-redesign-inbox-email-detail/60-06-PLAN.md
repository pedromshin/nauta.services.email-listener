---
phase: 60-surface-redesign-inbox-email-detail
plan: 06
type: execute
wave: 6
depends_on: ["60-05"]
files_modified:
  - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts
  - apps/web/src/app/emails/[id]/_components/role-picker.tsx
  - apps/web/src/app/emails/[id]/_components/active-parent-banner.tsx
  - apps/web/src/app/emails/[id]/_components/inspector-panel.tsx
  - apps/web/src/app/emails/[id]/_components/fields-panel.tsx
  - apps/web/src/app/emails/[id]/_components/email-detail.tsx
  - apps/web/src/app/__tests__/role-hue-ban.test.ts
autonomous: true
requirements: [SURF-04]

must_haves:
  truths:
    - "No Phase-60 surface encodes a role or a type in a hue — enforced, not asserted"
    - "The email-detail header reads the subject as the user's material, not as chrome"
    - "A failed email load is not madder — madder means irreversible, never an error"
  artifacts:
    - path: "apps/web/src/app/__tests__/role-hue-ban.test.ts"
      provides: "The committed law-1/law-3 source gate for Phase 60's surfaces, extensible by 61-63"
      contains: "SCOPED_DIRS"
    - path: "apps/web/src/app/emails/[id]/_components/email-detail.tsx"
      provides: "The detail frame on the identity"
  key_links:
    - from: "apps/web/src/app/__tests__/role-hue-ban.test.ts"
      to: "apps/web/src/app/_components + apps/web/src/app/emails/[id]"
      via: "scoped source walk for graph-* role hues"
      pattern: "SCOPED_DIRS"
---

<objective>
Finish the detail surface's law-1 sweep, put the detail frame on the identity, and make the sweep
permanent with a committed gate.

Purpose: the overlays and the extraction registry are done, but four editor panels still role-code
in `graph-*` hues, and `email-detail.tsx` renders a failed load in madder — which 58-IDENTITY forbids
by name ("Never errors, never warnings"). A half-swept surface reads exactly like the
"ugly/experimental" verdict this milestone exists to answer. Then: a sweep that is not gated is a
sweep that comes back.

Output: four panels on the vocabulary, a redesigned detail frame, and `role-hue-ban.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-05-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly. -->

§A — Plan 04's vocabulary (confirm exact shapes in 60-04-SUMMARY.md):
  export type RegionTier = "confirmed" | "suggested" | "terminal";
  export function tierOf(status: string): RegionTier
  export const REGION_TIER: Record<RegionTier, { box: string; chip: string; ring: string }>
  export const REGION_ROLE_GEOMETRY: Record<"entity"|"field"|"unrelated"|"none", string>
  export function regionLabelFor(c): { kind: "type"|"text"|"status"; text: string }

§B — The exact role-hue debt left, by file and line (verified at plan time):
  role-picker.tsx           ~18 graph-entity · ~19 graph-email-component · ~20 graph-email
                            (a role SWATCH per option — the most literal role-as-hue on the surface)
  active-parent-banner.tsx  ~15 graph-entity-tinted · ~24 graph-entity
  inspector-panel.tsx       ~68 graph-entity · ~69 graph-email-component · ~70 graph-email
                            ~290 destructive
  fields-panel.tsx          ~133 destructive
  email-detail.tsx          ~49 destructive (in `parseStatusVariant`)

§C — email-detail.tsx as shipped (Task 2's target). It is a 790-line orchestrator; you are touching
  only its own rendered chrome, NOT its data/canvas logic:
    parseStatusVariant(status) -> "default" | "secondary" | "destructive"
       // "parsed" -> default, "failed" -> destructive, else secondary. Feeds <Badge variant>.
    isLoading  -> <main className="h-full p-6"> with 3 x <Skeleton className="h-28 w-full rounded-xl">
    isError    -> <Card className="border-destructive" role="alert"> + CardTitle "text-destructive text-base"
    not-found  -> <Card> "Email not found"
    header     -> <header className="flex flex-wrap items-center gap-4 border-b px-4 py-3 shrink-0">
                    <Link href="/">← Back to inbox</Link>  (text-muted-foreground hover:text-foreground)
                    <h1 ref={h1Ref} className="text-lg font-semibold tracking-tight outline-none truncate flex-1" tabIndex={-1}>{subject}</h1>
                    <Badge variant={parseStatusVariant(email.parseStatus)}>{email.parseStatus}</Badge>
                    <Button variant="outline" size="sm">Reprocess Email</Button>
                  </header>
    then <CanvasShell ... /> in a flex-1 min-h-0 wrapper.
  DO NOT TOUCH in this plan: every hook and derivation above the return — `useCanvasState`,
  `useRoleMutations`, `useAutofillFields`, `getCandidateValue` (WR-02's deterministic slug lookup),
  `isAutoDetectedOrigin`, the signed-URL cache + its 55-minute expiry, `layersComponents`,
  `confirmDenyComponentIds`, `autoDetectedComponentIds`, `parentOptions`, `candidateFieldIds`,
  `handleSelectRow` (D-10's active-parent arming, incl. B4's field-keeps-parent-active rule),
  `handleRectDrawn`, `handleConfirmSplit`, and the `h1Ref.current?.focus()` mount effect.

§D — Law 1's disposition rule for `destructive` (same as Plan 05 §D, restated because this plan
  meets four more occurrences): madder is CORRECT on a control that performs an irreversible action.
  It is WRONG on an error, a status, or a warning. `email-detail.tsx`'s `parseStatusVariant` maps a
  FAILED PARSE — a status — to `destructive`. That is a violation: a failed parse is not
  irreversible, it is reprocessable (the page has a Reprocess button that proves it).

§E — palette-ban.test.ts (apps/web/src/app/__tests__/) is the source-walk idiom Task 3 mirrors:
  `collectSourceFiles(dir)` recursive walk, `EXCLUDED_DIR_SEGMENTS = new Set(["dev","node_modules",".next"])`,
  `FILE_EXTENSIONS = new Set([".ts",".tsx"])`, per-line regex match, an inline `ALLOWLIST` of
  `{ file, pattern, reason }` that starts EMPTY, and a violation report listing `file:line -> "match"`.
  Note it walks its OWN source file too — so any literal example string in the gate must be
  constructed, never written literally, or the gate matches itself.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound email -> DB -> UI | the subject rendered in the detail header is attacker-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02 | Tampering (XSS) | `email-detail.tsx` | mitigate | The subject already renders as a React text node in the h1 — keep it. No `dangerouslySetInnerHTML`; never interpolate the subject into a class or style. |
| T-60-10 | Information Disclosure | `email-detail.tsx` | mitigate | The error/not-found branches must keep showing generic, friendly copy ("Unable to load this email", "Email not found") and must not be "improved" into surfacing a raw error object or a tRPC message — server detail stays server-side. Restyle the copy; do not enrich it. |
| T-60-08 | Repudiation | `role-picker.tsx`, `inspector-panel.tsx` | mitigate | These panels MUTATE role and entity type. Restyling must not alter which mutation any control fires or its arguments — a redesign that silently rewires a classification control would corrupt extraction provenance. Verify the existing detail suites stay green. |
| T-60-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The four role-coding panels move onto the vocabulary</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/role-picker.tsx (full)
    - apps/web/src/app/emails/[id]/_components/active-parent-banner.tsx (full)
    - apps/web/src/app/emails/[id]/_components/inspector-panel.tsx (lines 55-90 + ~285-295 — the role map and the destructive use)
    - apps/web/src/app/emails/[id]/_components/fields-panel.tsx (~line 133 — the destructive use)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts (Plan 04)
  </read_first>

  <files>
    apps/web/src/app/emails/[id]/_components/role-picker.tsx,
    apps/web/src/app/emails/[id]/_components/active-parent-banner.tsx,
    apps/web/src/app/emails/[id]/_components/inspector-panel.tsx,
    apps/web/src/app/emails/[id]/_components/fields-panel.tsx
  </files>

  <action>
    Four mechanical swaps onto Plan 04's vocabulary. Mechanical does not mean thoughtless — each one
    is the same judgement: does this hue mean a TIER (keep it, via `REGION_TIER`), or does it mean a
    ROLE or a TYPE (remove it, via `REGION_ROLE_GEOMETRY`)?

    `role-picker.tsx` is the most literal role-as-hue on the surface: it paints a coloured swatch per
    role option. Under law 3 those swatches must survive greyscale. Replace each hue swatch with the
    role's own geometry from `REGION_ROLE_GEOMETRY` — render a miniature of the actual box treatment
    the user will see on the document (entity = a `border-2` square, field = a 1px-bordered square,
    unrelated = a dotted, faded square). This is strictly better than a legend: the picker stops
    showing an arbitrary colour key and starts showing the real thing, so the picker teaches the
    document's vocabulary instead of a parallel one. Keep every option's value and mutation wiring
    identical (T-60-08).

    `active-parent-banner.tsx`: `graph-entity` and `graph-entity-tinted` say "entity" in a hue. The
    banner announces which entity is armed as the active parent — that is a MODE, and law 1 puts
    modes in ink: `bg-shade border-rule text-ink`, with the entity's label as a sans word. The
    entity's own tier badge may appear via `REGION_TIER[tier].chip` if the banner has the status to
    hand; if it does not, do not fetch it — a banner is not worth a query. Keep `onClear`.

    `inspector-panel.tsx`: the `graph-*` role map (~68-70) goes; take role from
    `REGION_ROLE_GEOMETRY` and tier from `REGION_TIER` via `tierOf`. Any value the inspector shows
    from the document (`candidateValue`, a content snippet) is evidence: `font-serif` + `tabular` +
    `data-evidence`. Its field/property labels stay muted sans. Judge the `destructive` at ~290 by
    §D and report it. Do not touch any `onSet*` handler's arguments.

    `fields-panel.tsx`: judge its `destructive` at ~133 by §D and report it. It consumes
    `getStatusBadge`, which Plan 05 already moved onto `tierOf` — verify it renders correctly against
    the new return shape rather than re-deriving tier locally (T-60-08: one mapping, not two).

    If any of the four needs a treatment the vocabulary lacks, ADD IT TO `region-vocabulary.ts` and
    note it in the summary — never improvise a local class map. A fifth local role map is exactly how
    this debt accumulated in the first place.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run "src/app/emails/[id]/_components/__tests__/" src/app/__tests__/palette-ban.test.ts src/app/__tests__/colour-law.test.ts && test $(grep -v '^\s*//' "src/app/emails/[id]/_components/role-picker.tsx" "src/app/emails/[id]/_components/active-parent-banner.tsx" "src/app/emails/[id]/_components/inspector-panel.tsx" "src/app/emails/[id]/_components/fields-panel.tsx" | grep -c "graph-") -eq 0</automated>
  </verify>

  <done>
    Zero `graph-` across the four files outside comments. The role picker shows real box geometry
    rather than a hue key. The active-parent banner is an ink mode indicator. The inspector takes
    role from geometry and tier from `tierOf`, and shows document-derived values in serif. No
    mutation wiring changed; the existing detail suites are green. Any vocabulary additions are
    recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: The detail frame — a serif subject, and an error that isn't madder</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/email-detail.tsx lines 44-53 + 316-360 + 691-730 (parseStatusVariant, the three states, the header)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md (law 1's madder rule; law 2)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 364-372 + 950-955 (.rp-head/.rp-meta — the header hierarchy this mirrors)
  </read_first>

  <files>apps/web/src/app/emails/[id]/_components/email-detail.tsx</files>

  <action>
    Redesign only this file's own rendered chrome (§C). Do not touch a single hook, derivation, or
    handler above the return — they are load-bearing and heavily specified by Phases 6-9.

    `parseStatusVariant`: "failed" -> `destructive` is a law-1 violation (§D). A failed parse is a
    STATUS, and it is reprocessable — this very header renders a Reprocess button that proves it is
    not irreversible. Replace the whole function with a small status treatment that reads on the ink
    ladder: parsed is a quiet confirmed-tier marker (it is a machine fact that succeeded — route it
    through `REGION_TIER` if it maps cleanly, otherwise a plain `text-faded` marker), failed is
    `text-ink` on a `border-rule` frame, anything else is `text-pencil`. Drop the shadcn `<Badge
    variant>` in favour of the designed `.count`-scale marker vocabulary the inbox header now uses
    (`text-2xs` semibold, `tabular`, `bg-bright`, `border-rule`), so the two surfaces agree.

    Header hierarchy, mirroring the reference's `.rp-head`: the subject is the user's own material,
    so law 2 puts the `h1` in `font-serif` at `text-xl` `text-ink`, `data-evidence`. KEEP `ref={h1Ref}`,
    `tabIndex={-1}`, `outline-none` and the mount-focus effect — that is the page's a11y entry point.
    "← Back to inbox" is chrome: `text-sm text-faded hover:text-ink`, no hue. Rule the header with
    `border-hair` and use the designed density steps for its padding.

    The three states:
      - isError: drop `border-destructive` and `text-destructive` (§D — an error is not
        irreversible). Render the same generic copy on a `border-rule` framed block with a `text-ink`
        title and a `text-faded` body. KEEP `role="alert"`. Do NOT enrich the message with the
        underlying error (T-60-10).
      - not-found: same framed treatment; keep the copy.
      - isLoading: the `h-28 rounded-xl` skeleton slabs predict nothing that arrives. Re-shape them
        to the frame they stand in — a header bar, then the canvas zone — so the load reads as the
        page assembling rather than three unrelated bricks. Keep `aria-busy="true"` and
        `aria-label="Loading…"`, and keep using `Skeleton`.

    Do not restyle `CanvasShell` or any panel it hosts — those are Task 1's and Plans 04-05's.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run "src/app/emails/[id]/_components/__tests__/" src/app/__tests__/palette-ban.test.ts src/app/__tests__/colour-law.test.ts && test $(grep -v '^\s*//' "src/app/emails/[id]/_components/email-detail.tsx" | grep -c "destructive\|parseStatusVariant") -eq 0 && grep -q "font-serif" "src/app/emails/[id]/_components/email-detail.tsx" && grep -q 'role="alert"' "src/app/emails/[id]/_components/email-detail.tsx" && grep -q "h1Ref" "src/app/emails/[id]/_components/email-detail.tsx"</automated>
  </verify>

  <done>
    `parseStatusVariant` and every `destructive` are gone from this file; the error state is ink on a
    rule and still carries `role="alert"` and its generic copy. The `h1` is a serif `text-xl`
    `data-evidence` subject with `h1Ref`/`tabIndex={-1}`/focus-on-mount intact. Skeletons predict the
    real frame. No hook, derivation, or handler above the return was modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: role-hue-ban — the sweep becomes permanent</name>

  <read_first>
    - apps/web/src/app/__tests__/palette-ban.test.ts (full — the idiom you mirror, including its self-match warning)
    - apps/web/src/app/globals.css lines 595-610 + 685-695 (the graph-* token definitions and what they resolve to)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md (laws 1 and 3)
  </read_first>

  <files>apps/web/src/app/__tests__/role-hue-ban.test.ts</files>

  <action>
    Commit the gate that keeps role-as-hue out of Phase 60's surfaces permanently. Without it, the
    next phase to touch these files reintroduces `graph-entity` in an afternoon — it is still a
    registered, autocompleting token, because Phases 61-63's surfaces legitimately still use it.

    Mirror `palette-ban.test.ts`'s structure exactly (§E): the recursive `collectSourceFiles` walk,
    `.ts`/`.tsx` only, the `dev`/`node_modules`/`.next` exclusions, per-line matching, a violation
    report of `file:line -> "match"`, and an inline `ALLOWLIST` of `{ file, pattern, reason }` that
    starts EMPTY with a comment forbidding additions without a documented reason.

    THE SCOPE IS THE POINT, so document it prominently at the top of the file: this gate is SCOPED,
    not global. Export `SCOPED_DIRS` naming exactly the two Phase-60 surface roots —
    `_components` (the inbox) and `emails/[id]` (the detail view) — with a comment explaining that
    `graph-*` remains legitimately in use on the canvas and knowledge surfaces, which Phases 61-63
    own, and that each of those phases should ADD its root to `SCOPED_DIRS` as it sweeps. That turns
    a one-off cleanup into a ratchet: the banned area only ever grows. Add a test asserting
    `SCOPED_DIRS` contains both Phase-60 roots, so the scope itself cannot be silently narrowed to
    make a future violation pass.

    Ban the `graph-` role-hue family on colour-bearing utility prefixes. Construct the pattern from
    parts rather than writing a literal example (§E: the gate walks its own source, so a literal
    would match itself — palette-ban.test.ts documents this exact trap in its own comments).

    Add a second assertion in the same file, since it is the same law from the other side: within
    `SCOPED_DIRS`, `text-destructive` and `border-destructive` are banned outright, while
    `variant="destructive"` and `bg-destructive` are ALLOWED — madder is legitimate on a control that
    performs an irreversible action (the reject/deny/reprocess buttons), and forbidden on an error,
    a status, or a warning (58-IDENTITY: "Never errors, never warnings"). Encoding the distinction as
    "a destructive FILL on a button is fine; destructive TEXT or a destructive BORDER is a state
    talking" is the closest a source-level gate can get to that rule; say so in the comment, and note
    honestly that it is a proxy, not a proof.

    NEGATIVE PROOF — required, to 59-02's standard. Run all three and paste the output:
      1. Reintroduce a `graph-entity` background class into `region-overlay-box.tsx`. Gate MUST go
         red naming that file and line. Revert.
      2. Reintroduce `text-destructive` into `email-detail.tsx`'s error branch. Gate MUST go red.
         Revert.
      3. Add a `variant="destructive"` button to any scoped file. Gate MUST stay GREEN — proving the
         gate distinguishes an irreversible action from a state, rather than crudely banning a token.
         Revert.
      Confirm `git diff --stat` against the Task 2 commit is empty afterwards.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run src/app/__tests__/role-hue-ban.test.ts && npx vitest run && npx tsc --noEmit && npm run build:local</automated>
  </verify>

  <done>
    `role-hue-ban.test.ts` is committed and green: no `graph-*` colour utility and no
    `text-destructive`/`border-destructive` in `_components` or `emails/[id]`, with an empty
    allowlist and an exported `SCOPED_DIRS` documented as a ratchet for Phases 61-63. All three
    negative proofs were run — two RED, one deliberately GREEN — and their output is in the summary.
    `npm run build:local` succeeds.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npx vitest run` — every suite green.
- `cd apps/web && npm run build:local` succeeds (this script exists precisely because a bare
  `next build` cannot see the monorepo-root `.env.local` — use it; do not copy env files around).
- All three negative proofs recorded in 60-06-SUMMARY.md.
</verification>

<success_criteria>
- Zero role-as-hue on either Phase-60 surface, enforced by a committed, scoped, ratcheting gate.
- The detail header reads the subject as evidence; the error state is ink, not madder.
- The gate distinguishes an irreversible action from a state — proven by a deliberately GREEN proof.
</success_criteria>

<output>
Create `.planning/phases/60-surface-redesign-inbox-email-detail/60-06-SUMMARY.md` when done.
Record: every `destructive` kept vs. changed with its §D justification; any `region-vocabulary.ts`
additions; the three negative proofs; and the `SCOPED_DIRS` ratchet note for Phases 61-63.
</output>

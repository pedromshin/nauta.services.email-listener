---
phase: 60-surface-redesign-inbox-email-detail
plan: 03
type: execute
wave: 3
depends_on: ["60-02"]
files_modified:
  - apps/web/src/app/_components/inbox-three-pane.tsx
  - apps/web/src/app/_components/inbox-entities-rail.tsx
  - apps/web/src/app/_components/__tests__/inbox-structure.test.tsx
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "The reading pane shows the email body as a document (serif, measured), not as muted chrome"
    - "A fourth pane names what was found in the selected email, with each fact carrying its tier"
    - "The inbox's error state carries no madder — law 1 reserves madder for irreversible actions"
    - "The mobile feed is the same designed row rhythm, with touch-sized targets"
    - "The desktop inbox has four named panes where it had three"
  artifacts:
    - path: "apps/web/src/app/_components/inbox-entities-rail.tsx"
      provides: "The 'What I found in this email' rail — the new fourth pane"
      min_lines: 40
    - path: "apps/web/src/app/_components/inbox-three-pane.tsx"
      provides: "Four-pane desktop shell + mobile feed on the designed system"
  key_links:
    - from: "apps/web/src/app/_components/inbox-three-pane.tsx"
      to: "apps/web/src/app/_components/inbox-entities-rail.tsx"
      via: "entitiesByEmailId lookup for the selected email"
      pattern: "InboxEntitiesRail"
---

<objective>
Rebuild the inbox shell: the reading pane becomes a document, a fourth pane names the facts found in
it, the states stop breaking law 1, and the mobile feed inherits the designed rhythm.

Purpose: Plan 02 fixed the row. The shell around it is still stock — a `text-destructive` error
(madder on an error, which the identity forbids by name), a body rendered as `text-sm
text-muted-foreground`, and no answer anywhere to "what did you pull out of this?" The reference's
inbox is a registry with a provenance rail; this plan builds it.

Output: a four-pane desktop inbox, `InboxEntitiesRail`, a law-clean state set, a designed mobile
feed, and the pane-level half of the criterion-1 gate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase + Plans 01-02. Use directly. -->

§A — The shell you rewrite (apps/web/src/app/_components/inbox-three-pane.tsx), as shipped:

  export interface InboxEmailItem extends InboxEmail { bodyText: string | null; toAddresses: ReadonlyArray<string> }
  export interface InboxThreadItem { key; threadId; importerId; subject; messageCount; latestReceivedAt; latestSnippet; memberEmailIds }
  export interface InboxData { items: ReadonlyArray<InboxThreadItem>; hasMore: boolean; nextOffset: number }
  export function InboxThreePane(props: { data: InboxData | undefined; isLoading: boolean; isError: boolean }): React.ReactElement

  Internal sub-views: `FiltersRail({ filter, onFilterChange })` and `ReadingPreview({ email })`.
  Structure today — a CSS DUAL TREE (53-03), both rendered, CSS picks one:
    <div className="hidden h-full md:block">   -> desktop: ResizablePanelGroup(horizontal)
        ResizablePanel defaultSize={18} minSize={14}  -> <FiltersRail>
        ResizableHandle withHandle
        ResizablePanel defaultSize={42} minSize={28}  -> list header + rows + Load more
        ResizableHandle withHandle
        ResizablePanel defaultSize={40}               -> <ReadingPreview>
    <div className="flex h-full flex-col md:hidden"> -> mobile: Tabs filter + list OR detail
        `mobileView` useState<"list"|"detail">, default "list"; `handleSelectMemberMobile` is the
        ONLY thing that flips it to "detail" — the background default-select effect must NEVER
        deposit a mobile user into detail (53-UI-SPEC §4 point 6; there is a test for this).

  State/data already computed in this component — the rail needs NO new query:
    `entitiesByEmailId: ReadonlyMap<string, ReadonlyArray<EntityChipEntry>>` (from the single
    batched `emails.entitySummary` call), `selectedEmailId`, `selectedEmail: InboxEmailItem | null`,
    `emailsById`, `visibleItems`, `showLoading`, `showError`, `hasMore`, `handleLoadMore`.

  DO NOT REGRESS: the `useMemo` on `seedItems` (without it the reset effect loops — "Maximum update
  depth exceeded"), the single batched entitySummary call (never a per-row fetch, D-23), the
  EMAIL_LOOKUP_LIMIT/SUMMARY_BATCH_CAP bounds, and the default-select effect's list-first guard.

§B — Plan 01/02 contracts (confirm exact signatures in 60-01/60-02-SUMMARY.md before wiring):
  EntitySummaryEntry { componentId; entityTypeId; typeLabel; value: string | null; tier: "confirmed" | "suggested"; entityInstanceId? }
  EntityChips(props: { entities; totalCount; emailId })
  fingerprintTree(root: Element): StructuralFingerprint   // colour-blind: no className/style/data-*
  Baseline: apps/web/src/app/_components/__tests__/__baselines__/inbox-pre-60.json (FROZEN)
  Gate: apps/web/src/app/_components/__tests__/inbox-structure.test.tsx (Plan 02 — you EXTEND it)

§C — Law-1 violations sitting in this file today (these are the point of Task 1, not incidental):
  - `text-destructive` on both the desktop and mobile error branches. `--destructive: var(--bad)`
    (globals.css line 562, commented "madder — irreversible actions only"). 58-IDENTITY: madder means
    irreversible and "Never errors, never warnings." A failed query is not irreversible. This is a
    law-1 violation shipped in the current tree.
  - `bg-primary/10 text-primary` on the active filter item — hue on chrome. `--primary: var(--ink)`
    post-59, so it is already an ink wash pretending to be an accent. The reference's `.fitem.on` is
    `bg-shade` + `text-ink` + semibold.
  - `bg-background/95`, `border-border/50`, `bg-muted` — stock shadcn aliases. They resolve onto the
    ladder now, but they are not the designed ground vocabulary; use `bg-leaf`/`bg-bright`/
    `border-hair`/`border-rule` so the intent is legible and Phases 61-63 inherit the pattern.

§D — Reference target (sketches/direction-final.html). Realize the DESIGN, not the DOM:
  CSS: .filters/.paneh/.fitem/.fhint 287-301 · .threads/.lhead/.count 302-315 ·
       .reading/.rp-head/.rp-meta/.rp-body 364-372 · .entities/.ent/.e1/.et/.ent-actions/.flink 373-389 ·
       .badge/.badge.c/.badge.s 235-245 · 1120px guard 746-769
  Markup: filters 840-846 · list head 849 · reading pane 950-961 · entities rail 963-986
  The 1120px guard exists for a reason the sketch review found: at 1024 four panes crush the reading
  measure, so `.entities` collapses and `.reading`'s flex:1 gets its width back.

§E — Mobile constraints (53-UI-SPEC §4, MOBL-02): keep the CSS dual tree (`hidden md:block` /
  `md:hidden`) — do NOT convert to `useIsMobileViewport()`, which would change the render model and
  is not this phase's job. Touch targets >= 44px via `pointer-coarse:` variants. Canvas islands
  never mount below `md` (not applicable here, but do not introduce any).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound email -> DB -> UI | body text and extracted values render in the reading pane and the new rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02 | Tampering (XSS) | `inbox-entities-rail.tsx`, `inbox-three-pane.tsx` | mitigate | Body text and entity `value` render as React text nodes only. No `dangerouslySetInnerHTML` — the reading pane keeps `whitespace-pre-line` on a text node, which is the safe way to honour newlines. Plan 02's gate already asserts this across the inbox component set; extend its file list to cover the rail. |
| T-60-04 | Denial of Service (client) | `inbox-three-pane.tsx` | mitigate | Keep the existing `bodyText.slice(0, 2000)` bound on the reading pane — do not remove it while restyling. Keep EMAIL_LOOKUP_LIMIT and SUMMARY_BATCH_CAP as they are. |
| T-60-06 | Information Disclosure | `inbox-entities-rail.tsx` | mitigate | The rail renders only entities the already-importer-scoped `entitySummary` returned for the SELECTED email; it introduces no new query and must not widen the lookup beyond `entitiesByEmailId.get(selectedEmailId)`. |
| T-60-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The filters rail, the list header, and states that obey law 1</name>

  <read_first>
    - apps/web/src/app/_components/inbox-three-pane.tsx (full — you rewrite its sub-views and shell)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md (law 1's madder rule, verbatim)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 287-315 + 840-849
    - apps/web/src/app/globals.css lines 549-575 (the shadcn semantic mapping — see what --destructive/--primary resolve to)
  </read_first>

  <files>apps/web/src/app/_components/inbox-three-pane.tsx</files>

  <action>
    Rebuild the shell chrome around Plan 02's rows and fix the law-1 violations sitting in this file
    (§C). These are not cosmetic: `text-destructive` is madder, and 58-IDENTITY says madder means
    irreversible and "Never errors, never warnings."

    `FiltersRail`: the reference's `.filters` — `bg-leaf`, `border-r border-hair`, `p-4`-scale
    padding from the designed steps. Add the `.paneh` section head ("Filters", `text-2xs` semibold,
    uppercase, `tracking-[0.07em]`, `text-pencil`) — a ruled section start is what makes the
    reference read as a registry rather than a nav list. Items: `text-sm text-faded`, hover
    `bg-shade text-ink`; ACTIVE is `bg-shade text-ink font-semibold` — NOT `bg-primary/10
    text-primary`. Keep `aria-pressed` and the three options exactly as they are. Add the
    reference's `.fhint` footnote below a `border-t border-hair` rule, telling the user they can
    forward mail in (the reference's own copy names the forwarding address; use the product's real
    copy or a neutral equivalent — do not hardcode a fake address).

    List header (`.lhead`): make it `sticky top-0` on `bg-leaf` with a `border-b border-hair`, an
    `h2` at `text-base` semibold, and the count as the reference's `.count` marker — `text-2xs`
    semibold `tabular` on `bg-bright` with `border-rule` — replacing `<Badge variant="secondary">`.
    Mark it `data-field="count"`.

    States — all three, on BOTH the desktop and mobile branches (the dual tree duplicates them; fix
    both or the mobile feed keeps the violation):
      - ERROR: remove `text-destructive`. Madder is reserved for irreversible ACTIONS (a destructive
        button), never for a state. Render the error in `text-ink` with a `text-faded` explanation
        inside a `border-rule` framed block. Keep `role="alert"` if present; add it if not.
      - EMPTY: keep both existing messages (the `with-entities` variant and the default) — they are
        good copy. Restyle onto `text-faded`/`text-sm`, centred, with generous `spacing-panel`.
      - LOADING: the skeletons are `h-16 rounded-md` slabs sized to the OLD row. Re-shape them to the
        new row's rhythm (a short sender line, a longer subject line, a longest snippet line) so the
        loading state predicts the content that arrives instead of flashing a differently-shaped
        block. Keep using the existing `Skeleton` component.

    Ground vocabulary: replace `bg-background/95` with `bg-leaf`, `border-border/50` with
    `border-hair`, `bg-muted`/`bg-muted/50` hovers with `bg-shade` (§C). Keep the panel structure —
    `ResizablePanelGroup` and its three `ResizablePanel` sizes stay as they are; Task 2 adds the
    rail INSIDE the reading panel, not as a fourth resizable panel.

    Mark the panes for the gate: `data-pane="filters"` on the rail, `data-pane="threads"` on the
    list column, and `data-tree="desktop"` / `data-tree="mobile"` on the two top-level wrappers.
    These attributes are invisible to `fingerprintTree` by design — they cannot be used to game the
    shape gate, only to make the named assertions readable.

    Do not touch the data logic. The `seedItems` `useMemo`, the single batched entitySummary call,
    the caps, `handleLoadMore`, and the default-select effect all stay exactly as they are (§A).
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/_components/__tests__/ src/app/__tests__/palette-ban.test.ts src/app/__tests__/colour-law.test.ts && test $(grep -v '^\s*//' src/app/_components/inbox-three-pane.tsx | grep -c "text-destructive\|bg-primary/10\|bg-background/95\|border-border/50") -eq 0 && grep -q 'data-pane="filters"' src/app/_components/inbox-three-pane.tsx && grep -q 'data-tree="mobile"' src/app/_components/inbox-three-pane.tsx</automated>
  </verify>

  <done>
    Zero `text-destructive`, `bg-primary/10`, `bg-background/95`, `border-border/50` outside comments.
    The filters rail has a section head, an ink-well active state, and a footnote under a rule. The
    list header is sticky with a designed tabular count marker (no stock Badge). Error/empty/loading
    are restyled on both trees, with skeletons shaped like the new row. Panes and trees carry data
    markers. All existing inbox tests are green; `tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: The reading pane becomes a document, and gains the provenance rail</name>

  <read_first>
    - apps/web/src/app/_components/inbox-three-pane.tsx (ReadingPreview + the reading ResizablePanel)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md (law 2; "Signature element")
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 364-389 + 235-245 + 746-769 + 950-986
    - .planning/phases/59-visual-identity-designed-token-set-brand-guide/59-02-SUMMARY.md ("The Derived Type Scale" — why --text-lg IS this pane's body)
    - apps/web/src/app/globals.css lines 405-440 (tabular / pmark / pmark-confirmed / pmark-suggested)
  </read_first>

  <files>
    apps/web/src/app/_components/inbox-entities-rail.tsx,
    apps/web/src/app/_components/inbox-three-pane.tsx
  </files>

  <action>
    Two moves: make the reading pane read like a document, and add the fourth pane that answers
    "what did you find in this?"

    READING PANE (`ReadingPreview`). Today the body is `text-sm text-muted-foreground` — the mail is
    rendered as muted chrome, which is precisely law 2 inverted. Rebuild to the reference's
    `.reading`:
      - `.rp-head`: the subject as an `h2` in `font-serif text-xl text-ink` (it is the user's own
        material), with the "Open email →" ghost button beside it. Mark `data-evidence`.
      - `.rp-meta`: From/To on one `text-xs text-faded` line under a `border-b border-hair` rule.
        Addresses are the user's material but they are metadata, not prose — keep them sans, and
        keep them `tabular` where they are numeric. This mirrors the reference exactly.
      - `.rp-body`: `font-serif text-lg text-ink max-w-[56ch] whitespace-pre-line`, marked
        `data-field="body" data-evidence`. `--text-lg` is 15.5px/1.7 and Phase 59 anchored that step
        on THIS pane's body specifically (59-02's scale table) — it is the designed value, not a
        guess. The `max-w-[56ch]` measure is the reference's and is what makes long mail readable.
        KEEP the existing `bodyText.slice(0, 2000)` bound (T-60-04) and the existing no-plain-text
        fallback copy.
      - The empty "No email selected" state: restyle onto `text-faded`, keep the copy.

    THE ENTITIES RAIL — a new file, `inbox-entities-rail.tsx`, exporting `InboxEntitiesRail`. This
    is the fourth pane and the most visible layout change in the phase. Props: the selected email's
    `entities: ReadonlyArray<EntityChipEntry>` and its `emailId`. It needs NO new query — the
    three-pane already has `entitiesByEmailId` from the single batched call; pass
    `entitiesByEmailId.get(selectedEmailId) ?? []` (T-60-06: do not widen the lookup).

    Build it as the reference's `.entities`: an `<aside data-pane="entities"
    aria-label="What I found in this email">`, `w-72 shrink-0`, `border-l border-hair`, `bg-leaf`,
    `p-panel`, with an `h3` head. Each `.ent` row is a `border-b border-hair` band carrying:
      - the VALUE as the provenance mark — `pmark` plus `pmark-confirmed`/`pmark-suggested` by tier
        (the same one mark language as the row chips; that identity across surfaces is the whole
        point of the signature), truncating, `data-evidence`;
      - a tier BADGE pushed right — the reference's `.badge.c`/`.badge.s`: a small swatch plus the
        word "Confirmed"/"Suggested". Under law 3 the badge shows TIER, and tier is the one thing
        that earns colour. Mark it `data-field="tier-badge"` and `data-tier={tier}`;
      - the type WORD underneath (`.et`, `text-2xs text-pencil`) — the type name, no hue, and NO
        `tshape` glyph (the reference's own "Chanel rule", lines 44-50 and 382-387: the word is
        right there, and "date" has no shape of its own so a glyph would say something false).
    Close with the reference's `.flink` — a link onward. Render `null` for an empty entity list
    rather than an empty rail with a heading (the reference has no empty-rail state, and a heading
    over nothing is noise).

    SCOPE CALL, stated openly rather than silently dropped: the reference's `.ent-actions`
    Confirm/Dismiss buttons are NOT built here. Rationale — confirm/deny is an existing, canonical
    capability (`ConfirmDenyControls`, the `components/{id}/confirm` mutation) that lives on
    `/emails/[id]`, the surface designed for it and the one Plan 04 redesigns. Adding a second
    mutation path in the inbox is NEW product behaviour, not a redesign of an existing interaction,
    and neither D-58-01 nor the ROADMAP criteria ask for it. Instead, each suggested row links to
    the detail view where the real control lives. Record this in the summary as a deviation from the
    sketch WITH this reasoning so it is a visible decision, not an omission.

    WIRING: inside the reading `ResizablePanel`, make a horizontal flex — the reading column
    (`flex-1 min-w-0`) plus `<InboxEntitiesRail>`. Do NOT add a fourth `ResizablePanel`: the
    reference's `.entities` is a fixed-width aside, not a resizable one, and adding a panel would
    renegotiate all three existing `defaultSize`s for no design gain.

    The 1120px guard (reference lines 746-769) is load-bearing and the sketch review found the bug it
    fixes: at 1024 the four panes crush the reading measure. Hide the rail below the nearest
    registered breakpoint — `hidden xl:flex` (xl = 1280px). Note in the summary that 1280 is the
    nearest registered step to the reference's 1120 and that a custom breakpoint was deliberately not
    added for one rail.

    Do not mount the rail in the mobile tree at all — below `md` the reading pane is a full-screen
    detail view and a 288px aside has nowhere to go.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/_components/__tests__/ src/app/__tests__/palette-ban.test.ts src/app/__tests__/colour-law.test.ts && grep -q 'data-pane="entities"' src/app/_components/inbox-entities-rail.tsx && grep -q "pmark-suggested" src/app/_components/inbox-entities-rail.tsx && grep -q "max-w-\[56ch\]" src/app/_components/inbox-three-pane.tsx && grep -q "font-serif" src/app/_components/inbox-three-pane.tsx && test $(grep -v '^\s*//' src/app/_components/inbox-entities-rail.tsx | grep -c "tshape\|graph-") -eq 0</automated>
  </verify>

  <done>
    The reading pane renders a serif `text-xl` subject, a ruled meta line, and a serif `text-lg`
    body at a 56ch measure with the 2000-char bound intact. `InboxEntitiesRail` exists, renders each
    fact as a tier-marked provenance mark with a tier badge and a hue-free type word, uses no new
    query, carries no `tshape` and no `graph-*`, returns null when empty, and mounts inside the
    reading panel as a fixed aside hidden below `xl`. `tsc --noEmit` clean; all inbox tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: The mobile feed + the pane-level half of the criterion-1 gate</name>

  <read_first>
    - apps/web/src/app/_components/inbox-three-pane.tsx (the mobile tree — the `md:hidden` branch)
    - apps/web/src/app/_components/__tests__/inbox-structure.test.tsx (Plan 02's gate — you EXTEND it)
    - apps/web/src/app/_components/__tests__/inbox-mobile-stack.test.tsx (the behaviour you must not regress)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-02-SUMMARY.md (the negative proof to re-run)
  </read_first>

  <files>
    apps/web/src/app/_components/inbox-three-pane.tsx,
    apps/web/src/app/_components/__tests__/inbox-structure.test.tsx
  </files>

  <action>
    MOBILE FEED. The mobile tree inherits Plan 02's redesigned rows for free (it renders the same
    `InboxThreadGroup`), so this is about its own chrome:
      - The `Tabs`/`TabsList` filter is stock shadcn with `h-11`/`h-9`/`text-xs`. Restyle onto the
        designed system: `bg-leaf`, `border-b border-hair`, triggers at `text-sm` with `bg-shade
        text-ink` for the active state — matching Task 1's desktop filter vocabulary so the two
        trees read as one product. No hue on the active tab.
      - Touch targets: the back button and the filter triggers must be >= 44px under
        `pointer-coarse:` (§E). The back button is already `size-11` (44px) — keep it and add the
        `pointer-coarse:` guarantee to the tab triggers.
      - The detail bar's subject is the user's material: `font-serif`, truncating, `data-evidence`.
      - Apply Task 1's state fixes here if any were missed (the dual tree duplicates all three).

    DO NOT REGRESS, and there is an existing test for each: `mobileView` defaults to `"list"`; only
    `handleSelectMemberMobile` flips it to `"detail"`; the background default-select effect never
    does; back preserves `selectedEmailId`.

    EXTEND THE GATE with the pane-level assertions Plan 02 could not make (they did not exist yet).
    Scope every query to `[data-tree="desktop"]` — both trees are in the jsdom DOM simultaneously
    and would otherwise double every count:
      - Four named panes exist: `filters`, `threads`, `reading`, `entities`. Assert the set, not just
        the count, so a rename cannot silently pass. This is the layout delta criterion 1 asks for:
        the pre-60 inbox had three panes and no answer to "what did you find in this email".
      - The entities rail's facts each carry a `data-tier` of exactly "confirmed" or "suggested",
        and the rail renders nothing when the selected email has no entities.
      - The reading body `[data-field="body"]` carries `font-serif` and a bounded measure class.
      - The `font-serif` <-> `data-evidence` mutual implication (Plan 02's leg 3) still holds across
        the WHOLE tree now that the rail and reading pane are in it — this is where serif is most
        likely to drift onto chrome.
      - Re-assert the frozen-baseline legs (`shape !== baseline.shape`, `elementCount >`,
        `leafTextCount >`) — they now cover the full redesign, and the deltas should be much larger
        than Plan 02's. Record the final numbers in the summary.
      - Extend leg 4's source-walk (no `dangerouslySetInnerHTML`) to include
        `inbox-entities-rail.tsx`.

    RE-RUN THE NEGATIVE PROOF, now over the complete inbox component set, and paste the output:
      1. `git stash push apps/web/src/app/_components/inbox-row.tsx apps/web/src/app/_components/inbox-thread-group.tsx apps/web/src/app/_components/entity-chips.tsx apps/web/src/app/_components/inbox-three-pane.tsx apps/web/src/app/_components/inbox-entities-rail.tsx`
         — Phase 59's colour system stays fully in place; only the Phase-60 DOM goes away. That state
         IS "the inbox re-tokened but not redesigned."
      2. Run the suite. It MUST be RED (assertion failure or render error — either proves the point).
      3. `git stash pop`; confirm green; confirm `git diff --stat` against the Task 2 commit is empty.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run && npx vitest run src/app/_components/__tests__/inbox-structure.test.tsx src/app/_components/__tests__/inbox-mobile-stack.test.tsx</automated>
  </verify>

  <done>
    The mobile filter/back/detail chrome is on the designed system with >= 44px coarse-pointer
    targets and a serif detail subject; all four mobile-stack behaviours still pass. The gate asserts
    the four named desktop panes, the rail's tiers, the serif body measure, the serif/evidence mutual
    implication across the whole tree, and the three baseline-delta legs. The negative proof was
    re-run over the full component set, its RED output is in the summary, and the tree is restored
    and green.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npx vitest run` — every suite green.
- `cd apps/web && npm run build:local` succeeds (this script exists precisely because a bare
  `next build` cannot see the monorepo-root `.env.local` — use it, do not copy env files around).
- The negative proof's RED output over the full inbox set is recorded in 60-03-SUMMARY.md.
</verification>

<success_criteria>
- The desktop inbox has four named panes where it had three.
- The email body renders as a serif document at a designed measure; no madder anywhere in the states.
- Every fact in the rail carries its tier in the one mark language.
- Criterion 1's gate covers layout, hierarchy, and density, and is proven unable to pass on colour.
</success_criteria>

<output>
Create `.planning/phases/60-surface-redesign-inbox-email-detail/60-03-SUMMARY.md` when done.
Record: final baseline-vs-current `elementCount`/`leafTextCount`/pane-count deltas; the negative
proof output; the Confirm/Dismiss scope call and its reasoning; the `xl`-vs-1120px breakpoint note.
</output>

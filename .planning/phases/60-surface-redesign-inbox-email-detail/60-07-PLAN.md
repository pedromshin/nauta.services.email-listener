---
phase: 60-surface-redesign-inbox-email-detail
plan: 07
type: execute
wave: 7
depends_on: ["60-06"]
files_modified:
  - .planning/phases/60-surface-redesign-inbox-email-detail/artifacts/60-SCREENSHOT-REVIEW.md
  - docs/design/brand-guide.md
  - .claude/skills/polytoken-design-system/SKILL.md
autonomous: true
requirements: [SURF-01, SURF-04]

must_haves:
  truths:
    - "Both redesigned surfaces are captured under the new design, in both themes, or the reason they could not be is documented honestly"
    - "The other 14 harness surfaces show no unintended regression from Phase 60"
    - "Phases 61-63 can read the realized surface patterns instead of re-deriving them from the sketch"
  artifacts:
    - path: ".planning/phases/60-surface-redesign-inbox-email-detail/artifacts/60-SCREENSHOT-REVIEW.md"
      provides: "Criterion 4's evidence — or its honest absence"
    - path: "docs/design/brand-guide.md"
      provides: "The realized surface patterns, not just the token set"
  key_links:
    - from: "docs/design/brand-guide.md"
      to: "apps/web/src/app/emails/[id]/_components/region-vocabulary.ts"
      via: "documented tier/role vocabulary"
      pattern: "region-vocabulary|tierOf"
---

<objective>
Capture the redesigned surfaces through the 16-surface harness, prove nothing else regressed, and
write down what Phases 61-63 must inherit.

Purpose: ROADMAP criterion 4 is the harness capture. It needs Docker and the local stack, which is
intermittent overnight — so this plan is deliberately LAST and ISOLATED: if the stack is down, an
honest gap is recorded and no code plan's acceptance is held hostage. The deterministic layer
(Plans 01-06's gates) already carries the proof of criteria 1-3. Then: Plan 04's `region-vocabulary`
and the chip/mark patterns are system-level decisions that Phases 61-63 will otherwise re-derive —
or contradict.

Output: a screenshot review artifact (or a documented gap), an updated brand guide, and a corrected
design-system skill.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-06-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly. -->

§A — The harness (apps/web):
  `npm run screenshot:review` -> `playwright test --config playwright.screenshot.config.ts`
  Config: testDir ./e2e, testMatch /screenshot-review\.spec\.ts$/, workers 1, fullyParallel false,
  baseURL http://localhost:3000, webServer `npm run dev` (which is
  `dotenv -e ../../.env.local -- next dev`) with `reuseExistingServer: true`, timeout 120s.
  Surfaces (e2e/screenshot-review.spec.ts): BASE_SURFACES = login(/login), inbox(/), chat(/chat),
  knowledge(/knowledge), studio(/studio), forwarding(/settings/forwarding); PLUS emails(/emails/{id})
  appended for the LOCAL target only, from a seeded fixture (e2e/helpers/screenshot-fixtures.ts,
  which loads the monorepo-root .env.local and seeds a real owned email so /emails/[id] renders real
  content). Captures full-page into a timestamped RUN_DIR under .planning/ui-reviews/.
  Prior runs exist under .planning/ui-reviews/<ISO-timestamp>/ and are gitignored — the review NOTES
  are the committed artifact, not the PNGs.

§B — What this phase changed, i.e. what the capture must show:
  inbox (/):        four named panes (filters/threads/reading/entities), serif subjects + snippets,
                    tier-coloured provenance chips, tabular times, ink selection.
  emails/[id]:      serif subject, tier-coloured region overlays (solid=confirmed, dashed=suggested),
                    role by weight/style not hue, the extraction registry with serif values + tier
                    badges, ink error state.
  Everything else (login, chat, knowledge, studio, forwarding): Phase 60 touched NO file behind these
  surfaces. They should be pixel-identical to the most recent pre-60 run apart from unrelated drift
  (fonts, dates, seeded data). Any difference on those five is a REGRESSION and must be reported,
  not waved through.

§C — Known environment facts, so time is not lost rediscovering them:
  - `npm run build -w @polytoken/web` fails at "Collecting page data" with missing auth env vars,
    because apps/web has no .env.local (only the monorepo root does). This is PRE-EXISTING and NOT a
    regression — 59-01 and 59-02 both hit and documented it. Use `npm run build:local` (which is
    `dotenv -e ../../.env.local -- next build`). Do NOT copy env files around.
  - The sidebar pointer-events E2E interception bug (backlog 999.21) and a `packages/genui`
    artifacts.test.ts hash drift are both pre-existing and explicitly NOT this phase's to fix
    (60-CONTEXT). If either surfaces, name it and move on.
  - The harness needs Docker + the local Supabase stack for the seeded /emails/[id] fixture.

§D — The precedent for an honest gap: 51-07 and 55-01 both recorded a blocked verification leg
  rather than fabricating one. Follow it exactly. A fabricated or "representative" screenshot is
  worse than no screenshot — it is the only failure mode in this phase that cannot be caught by a
  later gate.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local .env.local -> harness -> committed artifact | the capture runs against real local credentials and seeded data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-11 | Information Disclosure | `60-SCREENSHOT-REVIEW.md` | mitigate | The capture runs against a real local DB with a real seeded session. The committed artifact is PROSE — never paste a signed attachment URL, a session cookie, an access token, or a `.env.local` value into it. Screenshots stay in the gitignored `.planning/ui-reviews/` run dir; do not `git add -f` them. |
| T-60-12 | Repudiation | `60-SCREENSHOT-REVIEW.md` | mitigate | If the stack is unavailable, the artifact must say so plainly and state exactly which criterion is unproven. Do not describe an un-run capture as if it ran, and do not substitute a hand-made image (§D). |
| T-60-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. Playwright and its browsers are already installed. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture both surfaces through the harness — or record the gap honestly</name>

  <read_first>
    - apps/web/e2e/screenshot-review.spec.ts (the surface list, the RUN_DIR convention, the fixture gate)
    - apps/web/e2e/helpers/screenshot-fixtures.ts (what the /emails/[id] surface needs to seed)
    - apps/web/playwright.screenshot.config.ts
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-03-SUMMARY.md + 60-06-SUMMARY.md (what the capture should show)
  </read_first>

  <files>.planning/phases/60-surface-redesign-inbox-email-detail/artifacts/60-SCREENSHOT-REVIEW.md</files>

  <action>
    Run `cd apps/web && npm run screenshot:review`. The config boots its own dev server and reuses a
    running one; the /emails/[id] surface only appends when the fixture seeds, which needs Docker and
    the local Supabase stack.

    IF THE CAPTURE RUNS: review every surface at every captured viewport, in both themes, and write
    `60-SCREENSHOT-REVIEW.md` containing:
      - The RUN_DIR path and the list of files captured.
      - Per redesigned surface (inbox, emails), a written verdict against §B's checklist — is the
        four-pane layout there; are subjects and snippets serif; do overlays read solid-for-confirmed
        and dashed-for-suggested; is role legible without hue; is anything hue-coloured that should
        be ink. Be specific and be willing to say something looks wrong: this is the ONLY leg of the
        phase that can see what the gates cannot, and the user's standing verdict on v1.9 was
        "ugly/experimental" precisely because no one looked.
      - Per untouched surface (login, chat, knowledge, studio, forwarding), a regression verdict
        against the most recent pre-60 run in `.planning/ui-reviews/`. Phase 60 touched no file
        behind them, so a difference is a regression — report it with the surface name and what
        changed. Ignore unrelated drift (dates, seeded content, font loading).
      - Any defect you find that is NOT worth fixing in this phase, named for the backlog.
    Do not commit the PNGs (§A: the run dir is gitignored; T-60-11).

    IF THE STACK IS DOWN (Docker not running, seed fails, dev server will not boot): do NOT fake it
    and do NOT substitute a hand-made image (§D — this is the one failure mode no later gate can
    catch). Write `60-SCREENSHOT-REVIEW.md` recording: the exact command run, the exact failure
    output, which precondition was missing, and the explicit statement that ROADMAP criterion 4 is
    UNPROVEN pending a run with the local stack up — plus the one-line command a human needs to run
    it later. Then state plainly which criteria ARE proven and by what: criteria 1-3 rest on the
    committed deterministic gates from Plans 01-06 (`inbox-structure`, `region-overlay-law`,
    `extraction-summary-structure`, `role-hue-ban`, `colour-law`, `palette-ban`, `token-contrast`,
    `token-registration`), which need no stack. Mark this plan complete — the gap is the honest
    outcome, not a failure.
  </action>

  <verify>
    <automated>test -s .planning/phases/60-surface-redesign-inbox-email-detail/artifacts/60-SCREENSHOT-REVIEW.md && grep -qiE "RUN_DIR|criterion 4 is UNPROVEN" .planning/phases/60-surface-redesign-inbox-email-detail/artifacts/60-SCREENSHOT-REVIEW.md && cd apps/web && npx vitest run</automated>
  </verify>

  <done>
    `60-SCREENSHOT-REVIEW.md` exists and either (a) names the RUN_DIR, gives a per-surface verdict for
    inbox and emails against §B, and a regression verdict for the five untouched surfaces, or (b)
    records the exact command, the exact failure, the missing precondition, the statement that
    criterion 4 is UNPROVEN, the re-run command, and which criteria the deterministic gates do prove.
    No PNGs or secrets are committed. The full vitest suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write down what Phases 61-63 inherit</name>

  <read_first>
    - docs/design/brand-guide.md §3 (the visual-identity section 59-03 wrote — you EXTEND it, not replace)
    - .claude/skills/polytoken-design-system/SKILL.md (flagged by 59-01/59-02 as still documenting the PRE-59 token source)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts (Plan 04 + any Plan 06 additions)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-01-SUMMARY.md + 60-04-SUMMARY.md + 60-06-SUMMARY.md
  </read_first>

  <files>
    docs/design/brand-guide.md,
    .claude/skills/polytoken-design-system/SKILL.md
  </files>

  <action>
    Phase 59 documented the token SET. This phase produced the first realized surface PATTERNS on top
    of it, and 60-CONTEXT is explicit that Phases 61-63 must inherit them rather than re-derive them
    from a static sketch. Write them down.

    Extend `docs/design/brand-guide.md` §3 with a "Realized surface patterns" subsection — additive;
    do not rewrite 59-03's token documentation. Cover, with the real class vocabulary and a pointer
    to the file that owns each:
      - THE PROVENANCE CHIP: value (serif + tabular) then a subordinate `· type` word, coloured only
        by tier via `pmark-confirmed`/`pmark-suggested`. One mark language on inbox chips, region
        label chips, entity rails, and extraction values. Point at `entity-chips.tsx` as the canonical
        implementation.
      - THE TIER/ROLE VOCABULARY: `region-vocabulary.ts` — `tierOf` (with its never-"confirmed"
        default and why), `REGION_TIER` (tier owns colour + solid/dashed), `REGION_ROLE_GEOMETRY`
        (role owns weight/style, never hue; `unrelated` uses DOTTED because tier already owns
        dashed). State the orthogonality rule explicitly — it is the single most reusable decision
        this phase made, and Phases 61-63 will need it for canvas nodes and edges.
      - LAW 2 IN PRACTICE: the `data-evidence` convention and the rule the gates enforce — `font-serif`
        and `data-evidence` mutually imply each other. Name what counts as evidence (subjects, bodies,
        snippets, extracted values, content-derived labels) and what does not (type names, property
        labels, status words, counts, addresses-as-metadata).
      - THE MADDER RULE IN PRACTICE: `variant="destructive"`/`bg-destructive` on an irreversible
        control is correct; `text-destructive`/`border-destructive` on a state is banned. Point at
        `role-hue-ban.test.ts` and its `SCOPED_DIRS` ratchet, and state that each of Phases 61-63 adds
        its own surface root as it sweeps.
      - THE DENSITY STEPS IN PRACTICE: which of Phase 59's `--spacing-*` steps ended up where
        (`row-x/y` on list rows, `chip-x/y` on chips, `panel` on rails), so 61-63 reach for a named
        step instead of a fresh number.
      - The documented deviations from the sketch and their reasoning: no `tshape` on inbox/detail
        surfaces (the reference's own Chanel rule); no unread dot and no attachment row (no
        read-state model, no attachment metadata on the list row); no Confirm/Dismiss in the inbox
        rail (the canonical control lives on the detail view); the entities rail hides below `xl`
        rather than the reference's 1120px.

    Correct `.claude/skills/polytoken-design-system/SKILL.md`: 59-01 and 59-02 both flagged that it
    still points at the PRE-59 token source, so any agent reading it today is given a stale palette —
    an active trap for Phases 61-63. Repoint it at `globals.css`'s current ladder and at the brand
    guide's §3, and add the three laws plus this phase's realized patterns in brief. Keep the skill
    SHORT and pointer-shaped: it is loaded into context, so it should route to the source of truth
    rather than duplicate it and drift.

    Do not restate the whole token ladder in either file. Both should point at `globals.css` and
    58-IDENTITY.md, which are the sources of truth.
  </action>

  <verify>
    <automated>grep -qi "region-vocabulary\|tierOf" docs/design/brand-guide.md && grep -qi "data-evidence" docs/design/brand-guide.md && grep -qi "globals.css" .claude/skills/polytoken-design-system/SKILL.md && test $(grep -ci "nauta" .claude/skills/polytoken-design-system/SKILL.md) -eq 0</automated>
  </verify>

  <done>
    The brand guide's §3 gains a "Realized surface patterns" subsection covering the provenance chip,
    the tier/role orthogonality rule, the `data-evidence` law-2 convention, the madder rule and the
    `SCOPED_DIRS` ratchet, the density steps in practice, and every documented sketch deviation with
    its reasoning. The design-system skill points at the current token source rather than the pre-59
    one. Neither file restates the ladder.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run` — every suite green.
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npm run build:local` succeeds.
- `60-SCREENSHOT-REVIEW.md` exists and is either a real review or an honest, actionable gap.
</verification>

<success_criteria>
- Criterion 4 is either satisfied with a per-surface verdict, or recorded as UNPROVEN with the exact
  blocker and re-run command — never faked.
- No regression on the five surfaces Phase 60 did not touch.
- Phases 61-63 inherit the realized patterns and the sketch deviations, in writing.
</success_criteria>

<output>
Create `.planning/phases/60-surface-redesign-inbox-email-detail/60-07-SUMMARY.md` when done.
State plainly whether criterion 4 was proven or is an open gap, and list any defects found for the
backlog.
</output>

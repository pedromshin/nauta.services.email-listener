---
phase: 60-surface-redesign-inbox-email-detail
plan: 05
type: execute
wave: 5
depends_on: ["60-04"]
files_modified:
  - apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx
  - apps/web/src/app/emails/[id]/_components/status-badge.ts
  - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx
  - apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx
  - apps/web/src/app/emails/[id]/_components/fields-panel.tsx
  - apps/web/src/app/emails/[id]/_components/entities-list.tsx
  - apps/web/src/app/emails/[id]/_components/__tests__/extraction-summary-structure.test.tsx
  - apps/web/src/app/emails/[id]/_components/__tests__/__baselines__/extraction-summary-pre-60.json
autonomous: true
requirements: [SURF-04]

must_haves:
  truths:
    - "An extracted VALUE reads as the document's own words (serif), not as muted secondary text"
    - "A candidate fact is pencil-amber, not an entity-type grey pretending to be a tier"
    - "Tier is stated in a word, not only in a colour dot with a screen-reader-only label"
    - "The extraction surface's DOM provably differs from pre-60 with colour excluded"
  artifacts:
    - path: "apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx"
      provides: "The 'what did we pull out of this document' registry"
    - path: "apps/web/src/app/emails/[id]/_components/__tests__/__baselines__/extraction-summary-pre-60.json"
      provides: "The frozen pre-60 extraction-summary structure"
  key_links:
    - from: "apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx"
      to: "apps/web/src/app/emails/[id]/_components/region-vocabulary.ts"
      via: "tierOf + REGION_TIER lookup"
      pattern: "tierOf|REGION_TIER"
---

<objective>
Put the extraction surface on the tier vocabulary and turn law 2 the right way up.

Purpose: 60-CONTEXT flags `extraction-summary-panel.tsx`'s `candidate: "bg-graph-email-component"`
by name — a node-TYPE hue used to mean a TIER, which breaks laws 1 and 3 at once and, post-59,
renders as `var(--faded)` grey. Underneath that sits a quieter and worse problem: `FieldRow` puts the
property LABEL in `font-medium text-foreground` and the extracted VALUE in `text-muted-foreground`.
The value is the document's own words — the entire product — rendered as the least important thing
on the row. Law 2 is inverted here.

Output: a restructured extraction registry, three more files on the vocabulary, and the shape gate
for this surface.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-04-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase + Plan 04. Use directly. -->

§A — Plan 04 delivered these (confirm exact shapes in 60-04-SUMMARY.md):
  // apps/web/src/app/emails/[id]/_components/region-vocabulary.ts
  export type RegionTier = "confirmed" | "suggested" | "terminal";
  export function tierOf(status: string): RegionTier          // unknown -> "suggested", never "confirmed"
  export const REGION_TIER: Record<RegionTier, { box: string; chip: string; ring: string }>
  export const REGION_ROLE_GEOMETRY: Record<"entity"|"field"|"unrelated"|"none", string>
  export function regionLabelFor(c): { kind: "type"|"text"|"status"; text: string }

  // apps/web/src/app/__tests__/support/structural-fingerprint.ts  (Plan 01)
  export function fingerprintTree(root: Element): StructuralFingerprint   // colour-blind: no className/style/data-*

§B — extraction-summary-panel.tsx as shipped (Task 1 rewrites it):
  export function ExtractionSummaryPanel(props: {
    components: readonly LayersComponent[];
    onConfirmEntity?: (componentId: string) => void;
    confirmingEntityIds?: ReadonlySet<string>;
  })
  // LayersComponent (from ./layers-panel) carries: id, sourceType, role, parentComponentId,
  // entityTypeLabel, entityTypeFieldId, extractionStatus, location, contentText,
  // candidateValue: string | null, propertyLabel: string | null.
  //
  // Current internals, ALL of which this plan replaces:
  //   HIDDEN_STATUSES = new Set(["rejected","superseded"])   <- keep this filter's behaviour
  //   isMeaningfulField(c) = c.candidateValue !== null || c.entityTypeFieldId !== null  <- keep
  //   statusTone(status) -> "confirmed" | "candidate" | "other"   <- REPLACE with tierOf
  //   TONE_DOT = { confirmed: "bg-success", candidate: "bg-graph-email-component",
  //                other: "bg-muted-foreground/50" }            <- the CONTEXT-flagged violation
  //   TONE_LABEL = { confirmed: "Confirmed", candidate: "Candidate", other: "—" }
  //   StatusDot -> a 2x2 dot + an sr-only label (the word is invisible to sighted users)
  //   FieldRow  -> label: "text-xs font-medium text-foreground"     <- chrome, over-weighted
  //                value: "text-sm text-muted-foreground"           <- EVIDENCE, under-weighted
  //   entity <section> -> "border-graph-entity/30 bg-graph-entity/10", header
  //                       "text-sm font-semibold text-graph-entity"  <- role hue
  //   confirmed note -> "text-success" + Check icon
  // Data grouping to PRESERVE: entities = visible.filter(role==="entity");
  //   meaningfulFields grouped into fieldsByParent by parentComponentId; orphanFields = meaningful
  //   fields with no parent among entities; isEmpty when no entities and no orphans.

§C — The other three files this plan moves onto the vocabulary:
  // status-badge.ts — getStatusBadge(status) -> { variant: "default"|"secondary"|"outline"|"destructive"; className? }
  //   Consumed by fields-panel.tsx and entities-list.tsx. Today: candidate -> "default" (= --primary
  //   = ink), pending -> "secondary", rejected -> outline + line-through, superseded -> secondary +
  //   opacity-60. So a CANDIDATE currently renders in full ink — louder than a CONFIRMED fact.
  //   Note the `destructive` variant is in the return TYPE but is never returned.
  // confirm-deny-controls.tsx — uses bg-success/text-success (line ~61) and destructive (~72).
  // layers-tree-row.tsx — uses bg-success (~62), destructive (~168), graph-email (~16),
  //   graph-email-component (~15). The graph-* pair is role-as-hue and must go.

§D — Law 1 disposition for the `destructive` occurrences you will meet (do NOT blanket-remove):
  madder is CORRECT on a button that performs an irreversible action (reject/deny/reprocess). It is
  WRONG on an error state, a status, or a warning. Judge each occurrence by that rule and say which
  you changed and why in the summary.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR/LLM extraction -> panel | `candidateValue`, `contentText`, `propertyLabel` derive from attacker-authored documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02 | Tampering (XSS) | `extraction-summary-panel.tsx` | mitigate | Values already render as React text nodes (the file's header says so explicitly) — keep it. Never `dangerouslySetInnerHTML`; never interpolate a value into a class or style. Tier classes come from `REGION_TIER` lookups keyed by the `tierOf` union, never concatenation. |
| T-60-08 | Repudiation | `status-badge.ts`, `extraction-summary-panel.tsx` | mitigate | Every tier decision routes through `tierOf`, whose unknown-status default is "suggested". No local re-derivation of tier is permitted in these files — a second, divergent mapping is how a UI starts claiming confirmations that never happened. |
| T-60-09 | Denial of Service (client) | `extraction-summary-panel.tsx` | accept | The panel renders every meaningful field for one email. Pre-existing, unchanged by this plan; `isMeaningfulField` already excludes the hundreds of raw-OCR boxes, which is the real bound. |
| T-60-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the pre-60 shape, then rebuild the extraction registry</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx (full — you rewrite it)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts (Plan 04)
    - apps/web/src/app/__tests__/support/structural-fingerprint.ts (Plan 01)
    - apps/web/src/app/_components/__tests__/capture-inbox-baseline.test.tsx (the freeze convention to mirror)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 373-389 + 963-986 (.entities/.ent — the registry band this mirrors) and 586-609 (.kd-rows — a label/value pair done right)
  </read_first>

  <files>
    apps/web/src/app/emails/[id]/_components/__tests__/__baselines__/extraction-summary-pre-60.json,
    apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx
  </files>

  <action>
    ORDER MATTERS AND IS NOT NEGOTIABLE: capture the baseline and commit it BEFORE editing the
    component. `extraction-summary-panel.tsx` is untouched by Plans 01-04, so its current state IS
    the pre-Phase-60 state — but only until you edit it. If you capture after editing, the gate in
    Task 3 becomes vacuous. (It would go red rather than falsely green, so the mistake is safe, but
    it costs a rework.)

    STEP 1 — Freeze. Mirror `capture-inbox-baseline.test.tsx` exactly: a `describe.skipIf` gated on
    `CAPTURE_STRUCTURE_BASELINE === "1"`, a refuse-to-overwrite guard that throws if the JSON already
    exists, and one always-on test asserting the committed artifact exists and parses.
    `ExtractionSummaryPanel` is a pure presentational component — it takes `components` as a prop and
    makes no tRPC call — so it needs NO `vi.mock("~/trpc/react")`. Build a fixture with at least: one
    confirmed entity with two fields, one candidate entity with one field, and one orphan field, so
    the baseline covers every branch the redesign touches. Run the capture, inspect, commit.

    STEP 2 — Rebuild. Replace `statusTone`/`TONE_DOT`/`TONE_LABEL` with `tierOf` + `REGION_TIER`
    (§A). `candidate: "bg-graph-email-component"` is the violation 60-CONTEXT names: a node-TYPE hue
    standing in for a TIER. Under the vocabulary a candidate IS `suggested` and wears pencil-amber.
    `confirmed: "bg-success"` resolves to `--conf` and is right by accident — make it explicit
    through `REGION_TIER` so it is right on purpose and cannot drift.

    Replace `StatusDot` with a tier BADGE on the reference's `.badge.c`/`.badge.s` pattern: a small
    swatch PLUS the visible word ("Confirmed" / "Suggested"), coloured via `REGION_TIER[tier].chip`.
    Today the word exists only as `sr-only` — the tier is communicated to sighted users by a 2x2 dot
    and nothing else, which is exactly the "tier meter" accessibility concern 58-IDENTITY rejected in
    Direction C. Say the word. Mark it `data-field="tier-badge" data-tier={tier}`.

    TURN LAW 2 THE RIGHT WAY UP in `FieldRow`. Today the property label is `font-medium
    text-foreground` and the value is `text-sm text-muted-foreground`. Invert the hierarchy to match
    what the data means:
      - the property LABEL is polytoken's word for a slot — chrome: `text-2xs text-pencil`, sans.
      - the VALUE is the document's own words — evidence: `font-serif text-sm text-ink`, wrapped in
        the `pmark` language tinted by the field's OWN tier, marked `data-field="value"
        data-evidence`. Use `tabular` on it — amounts and dates live here and 58-IDENTITY says
        "tabular numerals everywhere".
      - the "no value" case stays a `text-pencil` italic sans note — an absence is not evidence, so
        it does not get the serif.
    Structure the pair as the reference's `.kd-rows` label-over-value band rather than the current
    `justify-between` label/value/dot row, and rule the bands with `border-hair`.

    The entity `<section>`: drop `border-graph-entity/30 bg-graph-entity/10` and the
    `text-graph-entity` header (role-as-hue). The section is a FRAME — chrome — so it is
    `border-rule bg-leaf rounded-card`, and its header shows the entity type as a sans word with the
    tier badge pushed right. The entity's own tier colours the badge, not the frame: tinting a whole
    section by tier would flood the panel with wash and drown the individual field marks.

    Keep the confirm affordance and its Loader2 spinner exactly as they are behaviourally, and keep
    "In the entities gallery" — but as `text-conf` via the vocabulary rather than `text-success`.
    Keep the panel head, the counts line, the `ScrollArea`, the empty-state copy, and every data
    grouping in §B (`HIDDEN_STATUSES`, `isMeaningfulField`, `fieldsByParent`, `orphanFields`,
    `isEmpty`) byte-identical in behaviour. This is a redesign of the rendering, not of the logic.
  </action>

  <verify>
    <automated>cd apps/web && test -s "src/app/emails/[id]/_components/__tests__/__baselines__/extraction-summary-pre-60.json" && npx tsc --noEmit && npx vitest run "src/app/emails/[id]/_components/__tests__/" src/app/__tests__/palette-ban.test.ts src/app/__tests__/colour-law.test.ts && test $(grep -v '^\s*//' "src/app/emails/[id]/_components/extraction-summary-panel.tsx" | grep -c "graph-\|bg-success\|text-success\|statusTone\|TONE_DOT") -eq 0 && grep -q "tierOf" "src/app/emails/[id]/_components/extraction-summary-panel.tsx" && grep -q "font-serif" "src/app/emails/[id]/_components/extraction-summary-panel.tsx"</automated>
  </verify>

  <done>
    `extraction-summary-pre-60.json` is committed, frozen, and was captured before the component was
    edited. `statusTone`/`TONE_DOT`/`TONE_LABEL` are gone; tier routes through `tierOf`/`REGION_TIER`.
    Candidates are pencil-amber, not a type grey. Tier is a visible word plus a swatch. Values render
    serif+tabular in the pmark language and labels are muted sans. Zero `graph-`/`bg-success`/
    `text-success` outside comments. Every data grouping in §B behaves identically.
  </done>
</task>

<task type="auto">
  <name>Task 2: The tier-bearing trio — status badges, confirm/deny, layers rows</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/status-badge.ts (full — 25 lines)
    - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx (full)
    - apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx (full)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts (Plan 04)
    - apps/web/src/app/emails/[id]/_components/fields-panel.tsx + entities-list.tsx (getStatusBadge's two callers — check both still compile)
  </read_first>

  <files>
    apps/web/src/app/emails/[id]/_components/status-badge.ts,
    apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx,
    apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx
  </files>

  <action>
    Move the three tier-bearing files onto the one vocabulary, so the detail surface stops saying
    "confirmed" three different ways.

    `status-badge.ts`: today `candidate -> variant "default"` renders a candidate in full ink —
    LOUDER than a confirmed fact, which is the tier ladder upside down. Rewrite `getStatusBadge` to
    resolve through `tierOf` and return the `REGION_TIER[tier].chip` vocabulary: confirmed reads
    confirmed, candidate and pending read suggested, rejected keeps its `line-through` (a
    struck-through record is a structural signal, not a colour one — keep it) and superseded keeps
    `opacity-60`. Both are hue-free, which is correct: neither makes a tier claim. Drop
    `"destructive"` from the return type — it is never returned, and leaving it there invites a
    future edit to paint a status madder, which law 1 forbids by name. Update both callers
    (`fields-panel.tsx`, `entities-list.tsx`) only as far as compilation requires; their own
    redesign is Plan 06's.

    `confirm-deny-controls.tsx`: `bg-success`/`text-success` -> the vocabulary's confirmed tokens.
    Judge the `destructive` occurrence by §D: the DENY control performs an irreversible action, so
    madder is CORRECT there — keep it, and say in the summary that you kept it and why. Do not
    blanket-remove `destructive`; that would break law 1 from the other side by leaving the one
    genuinely irreversible action indistinguishable from a benign one.

    `layers-tree-row.tsx`: `graph-email` and `graph-email-component` are role-as-hue — remove both
    and take role from `REGION_ROLE_GEOMETRY` (weight/style), tier from `REGION_TIER`. `bg-success`
    -> the confirmed token. Judge its `destructive` occurrence by §D and report it. The row is a
    tree row, not a box, so use the vocabulary's `chip` treatment for its tier marker rather than
    `box`; if a genuinely new shape is needed, add it to `region-vocabulary.ts` — not to this file.
    That is the CONTEXT's rule: the system grows, the component does not improvise.

    Any value the layers row renders from the document (a content snippet, a candidate value) is
    evidence: `font-serif`, `data-evidence`. Its property/type labels stay sans.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run "src/app/emails/[id]/_components/__tests__/" src/app/__tests__/palette-ban.test.ts && test $(grep -v '^\s*//' "src/app/emails/[id]/_components/layers-tree-row.tsx" "src/app/emails/[id]/_components/confirm-deny-controls.tsx" "src/app/emails/[id]/_components/status-badge.ts" | grep -c "graph-\|bg-success\|text-success") -eq 0 && grep -q "tierOf" "src/app/emails/[id]/_components/status-badge.ts"</automated>
  </verify>

  <done>
    `getStatusBadge` resolves through `tierOf`; a candidate no longer outranks a confirmed fact; the
    unused `destructive` variant is gone from its return type and both callers compile. Zero
    `graph-`/`bg-success`/`text-success` across the three files. Each surviving `destructive` is
    justified in the summary against §D's rule. Document-derived text in the layers row is serif.
  </done>
</task>

<task type="auto">
  <name>Task 3: The extraction-surface shape gate</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/__tests__/__baselines__/extraction-summary-pre-60.json (Task 1)
    - apps/web/src/app/_components/__tests__/inbox-structure.test.tsx (Plans 02/03 — the standard to mirror)
    - apps/web/src/app/__tests__/support/structural-fingerprint.ts
  </read_first>

  <files>apps/web/src/app/emails/[id]/_components/__tests__/extraction-summary-structure.test.tsx</files>

  <action>
    Gate the extraction surface the same way the inbox is gated — this surface, unlike the overlay
    box, genuinely restructures, so a colour-blind shape delta IS the honest evidence here.

    Mount `ExtractionSummaryPanel` against the SAME fixture Task 1 froze the baseline with (an unfair
    fixture invalidates the comparison). Assert:
      - `shape !== baseline.shape` — and comment WHY this proves criterion 3's "visibly improved in
        hierarchy": `fingerprintTree` reads no className, no style and no data-* attribute, so no
        amount of recolouring can move it. The tier badge replacing the dot+sr-only label, and the
        label-over-value band replacing the justify-between row, are what move it.
      - `elementCount > baseline.elementCount` and `leafTextCount > baseline.leafTextCount` — the
        tier word is now rendered text rather than an `sr-only` whisper, and the value gained its
        own band. Report both numbers in the failure message.
      - Every `[data-field="value"]` carries `font-serif` AND `data-evidence`; every
        `[data-evidence]` carries `font-serif`; and no element carries `font-serif` without
        `data-evidence` (serif must not drift onto chrome).
      - Every `[data-field="tier-badge"]` has `data-tier` in {confirmed, suggested, terminal} and
        renders a visible word — assert its `textContent` is non-empty, which is what the old
        `sr-only` dot would fail.
      - A candidate field's badge carries a `sugg` token class and NOT a `conf` one, and not any
        `graph-` class. This is the CONTEXT-flagged regression, asserted directly.
      - Source-walk (mirror `palette-ban.test.ts`'s idiom, filtering comment lines first so a header
        comment cannot self-invalidate the gate): `extraction-summary-panel.tsx` contains no
        `dangerouslySetInnerHTML` (T-60-02).

    NEGATIVE PROOF — required, to 59-02's standard. Temporarily restore `TONE_DOT.candidate` to
    `"bg-graph-email-component"` (the exact violation 60-CONTEXT named) and confirm the candidate-tier
    assertion goes RED. Revert, confirm green, and confirm `git diff --stat` against the Task 2
    commit is empty. Paste the RED output in the summary.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run "src/app/emails/[id]/_components/__tests__/extraction-summary-structure.test.tsx" && npx vitest run && npx tsc --noEmit</automated>
  </verify>

  <done>
    The gate is committed and green: shape/elementCount/leafTextCount all differ from the frozen
    baseline, values are serif evidence, serif and `data-evidence` mutually imply each other, tier
    badges say a real word, and a candidate is amber and never a graph hue. The negative proof was
    run, its RED output is in the summary, and the tree is restored and fully green.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npx vitest run` — every suite green (inbox gates, overlay matrix, this gate).
- The negative proof's RED output recorded in 60-05-SUMMARY.md.
</verification>

<success_criteria>
- Extracted values read as the document's own words; property labels recede.
- A candidate is pencil-amber everywhere on the detail surface, resolved from one `tierOf`.
- Tier is a word, not a dot with an invisible label.
- The extraction surface's structural delta is proven independent of colour.
</success_criteria>

<output>
Create `.planning/phases/60-surface-redesign-inbox-email-detail/60-05-SUMMARY.md` when done.
Record: the baseline-vs-current deltas; the negative proof output; and every `destructive`
occurrence you kept or changed, judged against §D's rule.
</output>

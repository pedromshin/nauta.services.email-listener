---
phase: 55-platform-migration-tailwind-v4-react-19
plan: 06
type: execute
wave: 6
depends_on: ["55-05"]
files_modified:
  - docs/design/radix-vs-base-ui.md
  - .claude/skills/polytoken-design-system/SKILL.md
  - packages/ui/components.json
  - packages/ui/src/
  - apps/web/src/app/dev/design/page.tsx
  - apps/web/src/app/dev/design/design-data.json
autonomous: true
requirements: [STCK-03, STCK-04]
must_haves:
  truths:
    - "The Radix-vs-Base-UI stance is DECIDED (stay on Radix) and documented in docs/design/radix-vs-base-ui.md with the July-2026 shadcn changelog citation, the -b radix mechanism, and a re-evaluation trigger"
    - "The design-system skill (SKILL.md) is updated to reflect Tailwind v4 + React 19 + the Radix pin (no longer says 'NOT Tailwind v4, NOT React 19')"
    - "components.json has tailwind.config: \"\" (v4) and a real `shadcn add @kibo-ui/<component>` registry install of ≥1 component succeeds, renders, and typechecks — the vendor-and-adapt workflow is no longer required for that component"
    - "The /dev/design reference page renders oklch swatches (Pitfall 6 cleanup)"
  artifacts:
    - path: "docs/design/radix-vs-base-ui.md"
      provides: "STCK-03 decision record"
      min_lines: 25
    - path: "packages/ui/components.json"
      provides: "v4 registry-install config"
      contains: "\"config\": \"\""
  key_links:
    - from: "packages/ui/components.json"
      to: "@kibo-ui registry"
      via: "shadcn add with tailwind.config blank"
      pattern: "config.*\"\""
---

<objective>
Stage 5 — close STCK-03 and STCK-04, the payoff that justifies the whole migration. STCK-03 is a DOCUMENTED DECISION (not a primitive swap): stay on Radix, because shadcn's July-2026 changelog explicitly states Radix is not deprecated and ships `-b radix`, and a Base-UI swap would revalidate all 37 forwardRef-based Radix-wrapping components against a different a11y/DOM contract for zero requirement credit (55-RESEARCH.md Pattern 3). STCK-04 proves the migration's benefit: with `components.json` on the v4 shape, a direct `shadcn add @kibo-ui/<component>` registry install works in place of the vendor-and-adapt workflow for ≥1 real component. Also fold in the cheap Pitfall 6 cleanup so the design-system's own reference page (/dev/design) renders oklch swatches.

Purpose: Convert the platform migration into a documented stance + a proven capability the user can rely on going into the Phase-58 design gate.

Output: docs/design/radix-vs-base-ui.md decision record; updated SKILL.md; components.json on v4; ≥1 kibo-ui component installed via the registry, rendering and typechecking; /dev/design swatches fixed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Gate commands (scope with `-w`):
- Typecheck: `npm run typecheck -w @polytoken/ui`, `npm run typecheck -w @polytoken/web`
- Vitest (incl. palette-ban): `npm run test -w @polytoken/web`
- Production build: `npm run web:build`
- The shadcn CLI is invoked via `npx shadcn@latest` from `packages/ui/` (never a devDependency) — SKILL.md documents the workflow.
</execution_context>

<context>
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-RESEARCH.md
@.claude/skills/polytoken-design-system/SKILL.md
@packages/ui/components.json
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-05-SUMMARY.md

<interfaces>
- packages/ui/components.json: `"tailwind": { "config": "tailwind.config.ts", "css": "../../apps/web/src/app/globals.css", "baseColor": "neutral", "cssVariables": true }`, aliases mapping components/ui/lib/utils/hooks → @polytoken/ui, registries incl. `@kibo-ui: https://www.kibo-ui.com/r/{name}.json`.
- SKILL.md "Stack pin (hard constraints)" today says: "Tailwind v3.4 + React 18 + Next 15. NOT Tailwind v4, NOT React 19." and "Primitives: Radix ... stay on the Radix track; diff any payload before vendoring." — the stack line is now false and must change; the Radix line is correct and gets a citation.
- SKILL.md "shadcn CLI workflow (vendor + adapt)" documents the DO-NOT-plain-add rule (write path resolves through the exports map and breaks). The v4 registry-install path for STCK-04 must reconcile with this — verify the payload with `--dry-run --view` first and, if a direct `add` still mis-resolves the write path due to the `exports` map, use the documented copy-from-view approach but confirm it required NO v3/Base-UI adaptation (that "no adaptation needed" is the STCK-04 payoff).
- Pitfall 6 targets (untracked scratch, app/dev/** is excluded from palette-ban): apps/web/src/app/dev/design/page.tsx (`Swatch` has `const isHslTriplet = /^[\d.]+ [\d.%]+ [\d.%]+$/.test(value); if (!isHslTriplet) return null;` — rejects oklch), and design-data.json (regenerated from globals.css by `.claude/skills/polytoken-design-system/scripts/build-design-data.mjs`; header comment references the stale `nauta-design-system` path).
</interfaces>

RESEARCH references: Pattern 3 (Radix stays, changelog + `-b radix`), Code Examples "components.json update" (`config: ""` diff, `--dry-run --view` first), Pitfall 6 (dev/design cleanup + regenerate via build-design-data.mjs).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the STCK-03 Radix-stays decision record + update SKILL.md</name>
  <read_first>
    - .claude/skills/polytoken-design-system/SKILL.md (Stack pin + Approved external sources sections)
    - docs/design/ (existing decision-record conventions, e.g. brand-guide.md, EXTERNAL-IDENTITY-DECISIONS.md if present)
    - 55-RESEARCH.md Pattern 3 + Sources (the changelog URL: ui.shadcn.com/docs/changelog/2026-07-base-ui-default)
  </read_first>
  <action>
Create `docs/design/radix-vs-base-ui.md` recording the DECISION: stay on Radix. Include: (a) the decision statement; (b) rationale — shadcn's July-2026 changelog explicitly states Radix is not deprecated and every update/new component ships for both libraries unless Base-UI-only, plus this repo has 37 forwardRef-based Radix-wrapping packages/ui components with zero forcing function to swap; (c) the mechanism to stay pinned — `shadcn init/add -b radix` for non-interactive/CI use, and the components.json implications; (d) a RE-EVALUATION TRIGGER (e.g. "revisit if a needed component ships Base-UI-only"); (e) the changelog citation URL. Keep it a concise decision record, not an essay.

Update `.claude/skills/polytoken-design-system/SKILL.md` "Stack pin (hard constraints)": change the now-false line "Tailwind v3.4 + React 18 + Next 15. NOT Tailwind v4, NOT React 19." to reflect the migrated stack (Tailwind v4 `@theme`/oklch, React 19, Next 15). Update the tokens line ("HSL CSS variables, shadcn v3 convention") to describe the oklch `@theme inline` shape. Update the Primitives line to cite the changelog + `-b radix` pin and point to docs/design/radix-vs-base-ui.md. Update the "vendor + adapt" workflow section to note that v4 payloads no longer need v3 adaptation (registry-direct install is now viable — cross-reference Task 2). Keep the file accurate as the GSD-auto-read design-system source of truth.
  </action>
  <acceptance_criteria>
    - `docs/design/radix-vs-base-ui.md` exists, ≥25 lines, contains the changelog URL, the `-b radix` mechanism, and a re-evaluation trigger (`grep -iE "radix|base-ui|-b radix|re-evaluat|changelog" docs/design/radix-vs-base-ui.md` returns matches for each).
    - SKILL.md no longer asserts "NOT Tailwind v4, NOT React 19": `grep -c "NOT Tailwind v4" .claude/skills/polytoken-design-system/SKILL.md` returns 0; `grep -iE "tailwind v4|react 19|oklch|@theme" .claude/skills/polytoken-design-system/SKILL.md` returns matches.
    - SKILL.md references docs/design/radix-vs-base-ui.md.
  </acceptance_criteria>
  <done>STCK-03 stance decided + documented in a decision record and the design-system skill; both reflect Radix-stays + the migrated stack.</done>
</task>

<task type="auto">
  <name>Task 2: STCK-04 — v4 components.json + prove a direct @kibo-ui registry install</name>
  <read_first>
    - packages/ui/components.json
    - .claude/skills/polytoken-design-system/SKILL.md (shadcn CLI workflow — the DO-NOT-plain-add write-path caveat)
    - apps/web/src/app/dev/components/page.tsx (the visual smoke surface where vendored registry components are rendered)
    - 55-RESEARCH.md Code Examples "components.json update" + Pattern 3 (registry-install ⇄ Radix decision)
  </read_first>
  <action>
1. Update `packages/ui/components.json`: set `tailwind.config` from `"tailwind.config.ts"` to `""` (v4 — "leave this blank" per shadcn v4 docs). Leave `css`, `baseColor`, `cssVariables`, aliases, and registries as-is.
2. Choose ONE real @kibo-ui component to install as the proof (pick a self-contained one with low dependency surface — consult references/component-catalog.md if present, else `npx shadcn@latest search @kibo-ui -q <term>`). From `packages/ui/`, first inspect the payload: `npx shadcn@latest add @kibo-ui/<component> --dry-run --view` — confirm it resolves cleanly against the migrated components.json and that the payload needs NO v3/Base-UI adaptation (Radix-track, v4-native). If it pulls runtime deps, re-verify each via `npm view <pkg> version` + repository.url before they land (supply-chain gate — the shadcn install can add transitive deps).
3. Perform the actual install for the proof component. Use `-b radix` to honor the STCK-03 pin in this non-interactive context. If a plain `add` mis-resolves the write path through the package `exports` map (the documented SKILL.md caveat), fall back to writing the `--dry-run --view` payload into `packages/ui/src/<component>.tsx` verbatim — but the STCK-04 payoff is that it required NO manual v3/Base-UI adaptation; record that explicitly.
4. Render the installed component on `/dev/components` (extend apps/web/src/app/dev/components/page.tsx per the SKILL.md "extend it when vendoring more" convention) so it has a visual smoke home.
  </action>
  <acceptance_criteria>
    - `grep '"config"' packages/ui/components.json` shows `""`.
    - The chosen @kibo-ui component file exists under `packages/ui/src/` and is exported/importable via the `@polytoken/ui` convention.
    - `npm run typecheck -w @polytoken/ui` and `-w @polytoken/web` exit 0 with the new component in the tree.
    - `npm run test -w @polytoken/web` exits 0 — palette-ban.test.ts still passes with the newly installed component (no stock palette classes leaked in).
    - `npm run web:build` exits 0 and the component renders on `/dev/components` (confirmed via the build + a screenshot capture or a targeted render).
    - The SUMMARY records: the component chosen, whether a direct `add` worked or the copy-from-view fallback was used, and explicit confirmation NO v3/Base-UI adaptation was needed (the STCK-04 payoff).
  </acceptance_criteria>
  <done>components.json is on the v4 shape and a real @kibo-ui component was installed via the registry with no vendor-and-adapt work — STCK-04 proven on ≥1 component.</done>
</task>

<task type="auto">
  <name>Task 3: Pitfall 6 — regenerate /dev/design data + fix the oklch swatch renderer</name>
  <read_first>
    - apps/web/src/app/dev/design/page.tsx (the `Swatch` isHslTriplet regex + the stale nauta-design-system header comment)
    - .claude/skills/polytoken-design-system/scripts/build-design-data.mjs (the authoritative regenerator)
    - 55-RESEARCH.md Pitfall 6
  </read_first>
  <action>
This is the design-system's own reference/consultation surface (SKILL.md documents /dev/design as load-bearing tooling); it is untracked scratch and excluded from palette-ban, so it will not fail CI — it will just silently break after the oklch port. Low-priority but cheap:
1. Update the `Swatch` component's `isHslTriplet` check to ALSO accept `oklch(...)` strings (or better, since design-data.json is regenerated not hand-edited, make the renderer format-agnostic — accept any non-empty CSS color value).
2. Regenerate the data: `node .claude/skills/polytoken-design-system/scripts/build-design-data.mjs` (the documented refresh command) so design-data.json holds the new oklch token values.
3. Fix the stale header comment referencing `.claude/skills/nauta-design-system/scripts/build-design-data.mjs` → the actual `polytoken-design-system` path.
If build-design-data.mjs itself parses globals.css assuming HSL triplets and breaks on oklch, fix that parse too (it is the authoritative generator). Do not expand scope beyond making /dev/design render the oklch tokens.
  </action>
  <acceptance_criteria>
    - `grep -c "nauta-design-system" apps/web/src/app/dev/design/page.tsx` returns 0 (stale path fixed).
    - `grep -iE "oklch" apps/web/src/app/dev/design/design-data.json` returns matches (regenerated with oklch values) OR the regenerator was run and design-data.json reflects the current globals.css tokens.
    - The `Swatch` renderer no longer rejects oklch (`grep -n "isHslTriplet\|oklch" apps/web/src/app/dev/design/page.tsx` shows the updated/format-agnostic check).
    - `npm run web:build` exits 0 (the /dev/design route still builds).
  </acceptance_criteria>
  <done>The /dev/design reference page renders oklch swatches; stale nauta path removed; data regenerated from the migrated globals.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| shadcn registry (@kibo-ui) → packages/ui source | A registry install writes code + may pull transitive runtime deps into the vendored tree |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SC | Tampering | `shadcn add @kibo-ui/<component>` payload + any transitive deps | mitigate | `--dry-run --view` inspection BEFORE install (the repo's documented vendor workflow); re-verify any pulled runtime dep via `npm view <pkg> version` + repository.url before it lands; palette-ban.test.ts guards against stock-palette leakage from the payload. |
| T-55-06 | Tampering (config drift) | non-interactive shadcn install defaulting to Base UI | mitigate | Explicit `-b radix` flag per the STCK-03 decision — do not rely on the interactive-prompt default in this autonomous/overnight context (55-RESEARCH.md Security Domain threat pattern). |
</threat_model>

<verification>
- `docs/design/radix-vs-base-ui.md` decision record exists with changelog cite + `-b radix` + re-eval trigger.
- SKILL.md updated to the migrated stack + Radix pin.
- components.json `tailwind.config: ""`; ≥1 @kibo-ui component installed via registry, typechecks, renders, palette-ban green.
- /dev/design renders oklch swatches.
- `npm run typecheck -w @polytoken/ui` + `-w @polytoken/web` → 0; `npm run test -w @polytoken/web` → 0; `npm run web:build` → 0.
</verification>

<success_criteria>
- STCK-03: Radix-stays stance decided + documented (decision record + SKILL.md), with citation, pin mechanism, and re-evaluation trigger.
- STCK-04: a direct `shadcn add @kibo-ui/<component>` registry install works for ≥1 real component with no vendor-and-adapt work.
- Pitfall 6 cleanup done: /dev/design renders oklch.
- All gates green; the migration's payoff is proven and documented.
</success_criteria>

<output>
Create `.planning/phases/55-platform-migration-tailwind-v4-react-19/55-06-SUMMARY.md`. Record: the decision record path, the @kibo-ui component chosen + direct-add vs copy-from-view outcome + "no adaptation needed" confirmation, any transitive deps verified, and the /dev/design regeneration result.
</output>

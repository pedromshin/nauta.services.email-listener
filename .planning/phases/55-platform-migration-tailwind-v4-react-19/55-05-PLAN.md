---
phase: 55-platform-migration-tailwind-v4-react-19
plan: 05
type: execute
wave: 5
depends_on: ["55-04"]
files_modified:
  - packages/ui/package.json
  - packages/ui/src/calendar.tsx
autonomous: true
requirements: [STCK-02]
must_haves:
  truths:
    - "react-day-picker is on v9 and packages/ui/src/calendar.tsx is rewritten to the v9 API (classNames/modifiers/components) with correct day-highlighting and range styling"
    - "react-resizable-panels is on v3 and the chat/canvas resizable dock still resizes correctly"
    - "typecheck + the full E2E suite (incl. the token-render guard) are green; each major bump is a separate commit so a regression bisects to one dependency"
  artifacts:
    - path: "packages/ui/src/calendar.tsx"
      provides: "react-day-picker v9-compatible Calendar wrapper"
      contains: "DayPicker"
    - path: "packages/ui/package.json"
      provides: "react-day-picker ^9 + react-resizable-panels ^3"
  key_links:
    - from: "packages/ui/src/calendar.tsx"
      to: "react-day-picker v9 DayPicker API"
      via: "classNames/components props (v9 shape)"
      pattern: "DayPicker"
---

<objective>
Stage 4b — the two genuinely-breaking major-version bumps, isolated from the Stage-4a batch precisely so a screenshot/interaction regression bisects to ONE dependency, not a batch of nine (55-RESEARCH.md Anti-Patterns + Pitfall 3). react-day-picker v8→v9 is a documented full API rewrite that `packages/ui/src/calendar.tsx` wraps directly; react-resizable-panels v2→v3 is a major with no 2.x version that adds React 19 support, driving the chat/canvas resizable dock.

Purpose: Both are the kind of change TypeScript only partially catches — v9's changed default CSS class names and v3's panel behavior surface only in the E2E/interaction gates, not `typecheck`. Isolating and separately committing them keeps the bisect narrow.

Output: react-day-picker@^9 with calendar.tsx rewritten to the v9 API; react-resizable-panels@^3 with the dock resize revalidated; each in its own task/commit; all gates green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Gate commands (scope with `-w`):
- Typecheck: `npm run typecheck -w @polytoken/ui`, `npm run typecheck -w @polytoken/web`
- E2E (incl. token-render guard): `npm run test:e2e -w @polytoken/web`
- Production build: `npm run web:build`
- Screenshot capture (artifact — the eventual human review confirms Calendar day-highlighting): `npm run screenshot:review -w @polytoken/web`

Documentation lookup: prefer Context7 MCP for the react-day-picker v9 API; fetch the v9 migration guide fresh at execution time (55-RESEARCH.md Pitfall 3 + Assumption A4 flag this as LOW-confidence in research — verify against the version actually installed, do not trust a cached characterization).
</execution_context>

<context>
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-RESEARCH.md
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-04-SUMMARY.md

<interfaces>
- packages/ui/src/calendar.tsx (read fully): wraps `DayPicker` from react-day-picker; `export type CalendarProps = React.ComponentProps<typeof DayPicker>`; passes a large v8 `classNames` object (months/month/caption/nav_button/day_selected/day_today/day_outside/day_range_* etc.) and `components={{ IconLeft, IconRight }}`. v9 renamed/reshaped `classNames`, `modifiers`, `mode` types AND changed default class names + the `components` slot names (e.g. IconLeft/IconRight → Chevron). The v8 classNames keys (nav_button, day_selected, ...) are v8-specific and must map to v9 equivalents.
- react-resizable-panels is consumed by apps/web for the chat/canvas resizable dock (PanelGroup/Panel/PanelResizeHandle). v2→v3 is API-compatible for the common case but is a major — smoke the actual resize.
- Current pins: react-day-picker `^8.10.1`, react-resizable-panels `^2.0.19` (both left untouched by 55-04).

Target versions (55-RESEARCH.md, verified live 2026-07-15): react-day-picker → ^9.0.0 (react: >=16.8.0); react-resizable-panels → ^3.0.0 (adds ^19.0.0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump react-day-picker to v9 and rewrite calendar.tsx to the v9 API (isolated)</name>
  <read_first>
    - packages/ui/src/calendar.tsx (entire file)
    - packages/ui/src/button.tsx (buttonVariants used for nav/day styling)
    - 55-RESEARCH.md Pitfall 3, Assumption A4
    - The react-day-picker v9 migration guide (fetch fresh — Context7 or official docs — against the installed v9 version)
  </read_first>
  <action>
Re-verify `npm view react-day-picker@^9.0.0 version` + repository.url (gpbl/react-day-picker) before install. Then `npm install -w @polytoken/ui react-day-picker@^9.0.0` and `npm install` at root.

Rewrite `packages/ui/src/calendar.tsx` to the v9 API using the freshly-fetched v9 migration guide — do NOT guess from the v8 shape. Map every v8 `classNames` key the file currently sets to its v9 equivalent (v9 changed the class-name keys and default class names), migrate the `components={{ IconLeft, IconRight }}` slots to v9's slot names (v9 consolidates the nav chevrons), and preserve the existing VISUAL contract: selected-day = `bg-primary text-primary-foreground`, today = `bg-accent`, outside/disabled dimming, range start/middle/end rounding, `showOutsideDays` default true, the `p-3` container. Keep `export type CalendarProps` typed against v9's `DayPicker` props (named export, explicit type). Keep the `cn(...)` + `buttonVariants(...)` styling idiom. Commit this bump on its own (`feat(55): react-day-picker v9`) so it bisects independently.

Because TypeScript cannot catch a class-name mismatch (v9's changed defaults can silently break day highlighting), the acceptance below requires an interaction-level check, not just typecheck.
  </action>
  <acceptance_criteria>
    - `grep '"react-day-picker"' packages/ui/package.json` shows `^9`.
    - `npm run typecheck -w @polytoken/ui` and `-w @polytoken/web` exit 0 (no references to removed/renamed v8 exports remain in calendar.tsx).
    - `npm run test:e2e -w @polytoken/web` exits 0 (full suite incl. token-render guard).
    - Calendar render/interaction proof: identify each `<Calendar>` consumer (grep `Calendar` importers across apps/web/src + packages/ui/src, and /dev/components) and confirm via the screenshot capture (or a targeted Playwright interaction if a consumer is on a reachable route) that a selected day shows the `bg-primary` highlight and today shows `bg-accent` — record which surface was used to verify in the SUMMARY. (Day-highlighting is exactly the silent visual break Pitfall 3 warns of.)
    - `npm run web:build` exits 0.
  </acceptance_criteria>
  <done>react-day-picker v9 installed; calendar.tsx rewritten to v9 with day-highlighting/range styling intact; committed on its own for clean bisect.</done>
</task>

<task type="auto">
  <name>Task 2: Bump react-resizable-panels to v3 and revalidate the resizable dock (isolated)</name>
  <read_first>
    - The apps/web consumer(s) of react-resizable-panels (grep `react-resizable-panels` / `PanelGroup` / `PanelResizeHandle` across apps/web/src to locate the chat/canvas dock)
    - 55-RESEARCH.md Standard Stack "Supporting" table (react-resizable-panels row, MEDIUM risk) + Anti-Patterns (isolate the major bumps)
  </read_first>
  <action>
Re-verify `npm view react-resizable-panels@^3.0.0 version` + repository.url (bvaughn/react-resizable-panels) before install. Then `npm install -w @polytoken/ui react-resizable-panels@^3.0.0` and `npm install` at root. Fix any v2→v3 API fallout at the dock consumer (v3 is largely compatible for PanelGroup/Panel/PanelResizeHandle; adjust any renamed prop/import surfaced by typecheck). Commit on its own (`feat(55): react-resizable-panels v3`) so a resize regression bisects independently from the calendar bump.
  </action>
  <acceptance_criteria>
    - `grep '"react-resizable-panels"' packages/ui/package.json` shows `^3`.
    - `npm run typecheck -w @polytoken/ui` and `-w @polytoken/web` exit 0.
    - `npm run test:e2e -w @polytoken/web` exits 0.
    - Dock-resize proof: a Playwright interaction (or an added assertion to the token-render guard) drags the `PanelResizeHandle` on the chat/canvas dock and confirms the panel width changes (the dock still resizes under v3) — record the surface used. If no reachable route exercises the dock in E2E, note it and confirm via the screenshot capture that the dock renders at its default split.
    - `npm run web:build` exits 0.
  </acceptance_criteria>
  <done>react-resizable-panels v3 installed; the chat/canvas dock still resizes; committed separately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → runtime deps | Two major-version bumps enter the client bundle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SC | Tampering | npm bumps react-day-picker@^9, react-resizable-panels@^3 | mitigate | Both existing committed deps (major version bump, not new supply-chain surface); re-verify `npm view <pkg> version` + repository.url (gpbl/react-day-picker, bvaughn/react-resizable-panels) before each install; RESEARCH audit disposition Approved (flagged elevated-review for major blast radius, mitigated by per-dep isolation + separate commits). |
| T-55-05 | Denial of Service (silent visual/interaction regression) | calendar.tsx day-highlighting, resizable dock | mitigate | Each bump isolated + separately committed; interaction-level proof (day highlight, panel drag) beyond typecheck since v9's class-name changes and v3's panel behavior are invisible to `typecheck`. |
</threat_model>

<verification>
- `npm run typecheck -w @polytoken/ui` + `-w @polytoken/web` → 0
- `npm run test:e2e -w @polytoken/web` → 0 (guard green)
- `npm run web:build` → 0
- Calendar day-highlighting + dock resize proven at interaction level (not just typecheck)
- Two separate commits (one per major) for clean bisect
</verification>

<success_criteria>
- react-day-picker@^9 with calendar.tsx correctly rewritten to the v9 API; visual contract preserved.
- react-resizable-panels@^3 with the dock resize revalidated.
- STCK-02's "every vendored component revalidated / zero runtime regression" bar met for the two high-risk components; all gates green.
</success_criteria>

<output>
Create `.planning/phases/55-platform-migration-tailwind-v4-react-19/55-05-SUMMARY.md`. Record: the v9 class-name/slot mapping applied to calendar.tsx, the surface used to prove day-highlighting, any v3 dock API fixes, the surface used to prove resize, and confirmation each bump is its own commit.
</output>

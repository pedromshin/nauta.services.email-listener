---
phase: 55-platform-migration-tailwind-v4-react-19
plan: 04
type: execute
wave: 4
depends_on: ["55-03"]
files_modified:
  - apps/web/package.json
  - packages/ui/package.json
autonomous: true
requirements: [STCK-02]
must_haves:
  truths:
    - "apps/web + packages/ui run on react@19 / react-dom@19 with @types/react@19"
    - "packages/ui declares ^19 in its react/react-dom peerDependencies so consumers are not warned"
    - "The five low-risk runtime deps (vaul, sonner, react-hook-form, next-themes, lucide-react) + tailwind-merge are bumped to their React-19-compatible / v4-aware versions"
    - "typecheck, the full E2E suite (incl. the token-render guard), and vitest are all green after the bump"
  artifacts:
    - path: "apps/web/package.json"
      provides: "react/react-dom ^19 + @types ^19"
      contains: "19"
    - path: "packages/ui/package.json"
      provides: "widened ^19 peerDependencies + bumped runtime deps"
  key_links:
    - from: "packages/ui/package.json peerDependencies"
      to: "react ^19"
      via: "widened range"
      pattern: "react.*19"
---

<objective>
Stage 4a of the migration — the React 18→19 core bump plus the SIX low-risk dependency bumps (55-RESEARCH.md Standard Stack "Supporting" table + Pitfall 3 isolation guidance). This is deliberately the LOW-risk half of the React work: RESEARCH found zero `useRef()` no-arg / defaultProps / propTypes / string-ref usages in the repo, `next@15.5.20` already accepts React 19, and every `@radix-ui/*` already declares React 19 — so the only React-19 blockers are the specifically version-capped runtime deps. The two genuinely-breaking major bumps (react-day-picker v9, react-resizable-panels v3) are isolated into the next plan (55-05).

Purpose: Move the runtime to React 19 with the smallest possible blast radius per bump, so any regression bisects cleanly. `forwardRef` (37 usages) is deprecated-not-removed in React 19 and is intentionally NOT rewritten — STCK-02 is "revalidated," not "rewritten."

Output: react/react-dom/@types on 19; packages/ui peerDeps widened; the six low-risk deps bumped; all gates green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Gate commands (scope with `-w`):
- Typecheck (catches RefObject<T> → RefObject<T|null> type changes): `npm run typecheck -w @polytoken/web`, `npm run typecheck -w @polytoken/ui`
- E2E (incl. token-render guard): `npm run test:e2e -w @polytoken/web`
- Vitest: `npm run test -w @polytoken/web`
- Production build: `npm run web:build`
- Screenshot capture (artifact): `npm run screenshot:review -w @polytoken/web`
</execution_context>

<context>
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-RESEARCH.md
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-03-SUMMARY.md

<interfaces>
Current pins (verified in-repo):
- apps/web/package.json: react/react-dom `^18.3.1`, @types/react `^18.3.3`, @types/react-dom `^18.3.0`, next `^15.3.3` (installed 15.5.20 — DO NOT bump; already React-19-peer-compatible).
- packages/ui/package.json: peerDependencies react/react-dom `^18.3.1`; devDependencies react/react-dom `^18.3.1`, @types/react `^18.3.3`; runtime deps vaul `^0.9.1`, sonner `^1.4.41`, react-hook-form `^7.51.4`, next-themes `^0.3.0`, lucide-react `^0.364.0`, tailwind-merge `^2.3.0`, react-day-picker `^8.10.1` (LEFT for 55-05), react-resizable-panels `^2.0.19` (LEFT for 55-05).
- packages/ui declares react/react-dom as peerDependencies + devDependencies only (no direct runtime react dep); apps/web is the only workspace with a direct react/react-dom dependency.

Target versions (55-RESEARCH.md, verified live 2026-07-15 — minimum version that adds React 19 to peerDeps, staying in the same major where possible):
- react/react-dom → ^19.2.7 ; @types/react ^19 ; @types/react-dom ^19
- vaul → ^1.1.2 (1.1.0/1.1.1 still cap at 18) ; sonner → ^1.7.4 (stay 1.x) ; react-hook-form → ^7.81.0 (stay 7.x) ; next-themes → ^0.4.6 (0.x — review changelog) ; lucide-react → ^0.400.0 (stay 0.x) ; tailwind-merge → ^3.6.0 (v3 targets Tailwind v4's expanded utility surface — required so cn() dedupes v4-only variants).
</interfaces>

RESEARCH notes: next-themes is 0.x (any minor is technically breaking-eligible) — skim its 0.3→0.4 changelog before/after bump. tailwind-merge v2 on a v4 project can silently fail to merge new v4-only utility variants — the v3 bump is functional, not just peer-range.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump React to 19 + widen peerDeps + bump the six low-risk deps</name>
  <read_first>
    - apps/web/package.json, packages/ui/package.json
    - 55-RESEARCH.md Standard Stack "Core" + "Supporting" tables, "Installation" block, "Already React-19-compatible... no bump required" paragraph
    - packages/ui/src/index.ts (cn() helper location — for the tailwind-merge dedupe smoke)
  </read_first>
  <action>
Re-verify registry state immediately before install (RESEARCH audit fallback, slopcheck did not run): for each target package run `npm view <pkg>@<target-range> version` and confirm it resolves and its repository.url matches the RESEARCH audit (facebook/react, emilkowalski/vaul, emilkowalski/sonner, react-hook-form/react-hook-form, pacocoursey/next-themes, lucide-icons/lucide, dcastil/tailwind-merge). Record versions in the SUMMARY. Then, from repo root:

1. `npm install -w @polytoken/web react@^19.2.7 react-dom@^19.2.7`
2. `npm install -w @polytoken/web -D @types/react@^19 @types/react-dom@^19`
3. `npm install -w @polytoken/ui -D react@^19.2.7 react-dom@^19.2.7 @types/react@^19`
4. Widen `packages/ui/package.json` `peerDependencies.react` and `.react-dom` to include ^19 (e.g. `^18.3.1 || ^19.0.0`) so downstream consumers are not peer-warned. Edit the file directly (immutable JSON edit — change only these two ranges).
5. `npm install -w @polytoken/ui vaul@^1.1.2 sonner@^1.7.4 react-hook-form@^7.81.0 next-themes@^0.4.6 lucide-react@^0.400.0 tailwind-merge@^3.6.0`
6. Do NOT touch react-day-picker or react-resizable-panels (55-05). Do NOT bump next. Do NOT run the forwardRef codemod (out of scope — forwardRef is valid in React 19).
7. Run `npm install` at repo root to reconcile the workspace lockfile.
  </action>
  <acceptance_criteria>
    - `grep -E '"react"|"react-dom"' apps/web/package.json` shows `^19.2.7`; `grep -E '@types/react' apps/web/package.json` shows `^19`.
    - `grep -A3 peerDependencies packages/ui/package.json` shows react/react-dom ranges including `19`.
    - `grep -E '"vaul"|"sonner"|"react-hook-form"|"next-themes"|"lucide-react"|"tailwind-merge"' packages/ui/package.json` shows `^1.1.2`, `^1.7.4`, `^7.81.0`, `^0.4.6`, `^0.400.0`, `^3.6.0` respectively.
    - `grep -E '"react-day-picker"|"react-resizable-panels"' packages/ui/package.json` still shows `^8.10.1` and `^2.0.19` (untouched — proves the isolation).
    - `grep '"next"' apps/web/package.json` unchanged (`^15.3.3`).
  </acceptance_criteria>
  <done>React 19 + the six low-risk deps installed; peerDeps widened; the two high-risk majors deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Revalidate the stack on React 19 (typecheck + E2E + build)</name>
  <read_first>
    - packages/ui/src/index.ts (cn = tailwind-merge + clsx)
    - 55-RESEARCH.md Pitfall 3 (what E2E/screenshot catch vs typecheck), "Don't Hand-Roll" (tailwind-merge v3 dedupe)
  </read_first>
  <action>
Run the full revalidation gate and fix any React-19 type fallout that surfaces. Expected fallout per RESEARCH is narrow: `RefObject<T>` became `RefObject<T | null>` in @types/react@19, which can surface false-negative-turned-error typechecks at ref sites — fix by correcting the ref typing at the call site (do NOT downgrade types). If tailwind-merge v3 changes `cn()` behavior, confirm `cn()` still dedupes conflicting classes (e.g. `cn("p-2", "p-4")` → `p-4`) — add/keep a lightweight assertion if one exists; otherwise smoke it once and note the result. Do NOT introduce the forwardRef codemod. If any Radix component's animation regressed (from the Stage-2 animate port interacting with React 19), that surfaces in the E2E/guard run — fix the specific keyframe/utility, not the React version.
  </action>
  <acceptance_criteria>
    - `npm run typecheck -w @polytoken/web` and `npm run typecheck -w @polytoken/ui` both exit 0.
    - `npm run test -w @polytoken/web` exits 0 (vitest incl. both rewritten gates + palette-ban).
    - `npm run test:e2e -w @polytoken/web` exits 0 — the full suite including the token-render guard passes on React 19 (this is the runtime-regression gate; typecheck cannot catch a render/interaction regression).
    - `npm run web:build` exits 0.
    - `cn()` dedupe confirmed working post tailwind-merge v3 (assertion green or documented smoke).
    - `npm run screenshot:review -w @polytoken/web` runs and writes a capture dir (artifact for later human review).
  </acceptance_criteria>
  <done>The stack typechecks, tests, and builds on React 19 with the six deps bumped; no runtime regression in the E2E suite or the token-render guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → runtime deps | React 19 + six runtime dep bumps enter the app's client bundle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SC | Tampering | npm bumps (react/react-dom, vaul, sonner, react-hook-form, next-themes, lucide-react, tailwind-merge) | mitigate | All are existing committed deps (version bump, not new supply-chain surface); re-verify `npm view <pkg> version` + repository.url per RESEARCH audit before install; every version was live-verified in RESEARCH 2026-07-15. RESEARCH audit disposition: Approved for all → automated `npm view` re-check is the gate (no blocking-human checkpoint in the unattended run). |
| T-55-04 | Denial of Service (regression) | React 19 runtime + Radix animations | mitigate | Full E2E suite + token-render guard + production build gate before Stage 4b/5 proceed. |
</threat_model>

<verification>
- `npm run typecheck -w @polytoken/web` + `-w @polytoken/ui` → 0
- `npm run test -w @polytoken/web` → 0
- `npm run test:e2e -w @polytoken/web` → 0 (guard green on React 19)
- `npm run web:build` → 0
- react-day-picker + react-resizable-panels confirmed untouched (deferred to 55-05)
</verification>

<success_criteria>
- apps/web + packages/ui run on React 19; packages/ui peerDeps include ^19.
- The six low-risk deps are on their React-19-compatible / v4-aware versions.
- forwardRef left intact (deprecated-not-removed); no codemod run.
- All gates green; the two breaking majors isolated for 55-05.
</success_criteria>

<output>
Create `.planning/phases/55-platform-migration-tailwind-v4-react-19/55-04-SUMMARY.md`. Record: exact installed versions, any RefObject/ref-typing fixes made, the tailwind-merge v3 cn() dedupe result, the next-themes 0.3→0.4 changelog note, and confirmation react-day-picker/react-resizable-panels are untouched.
</output>

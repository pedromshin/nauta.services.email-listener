---
phase: 55-platform-migration-tailwind-v4-react-19
plan: 03
type: execute
wave: 3
depends_on: ["55-02"]
files_modified:
  - apps/web/src/app/__tests__/token-contrast.test.ts
  - apps/web/src/app/__tests__/token-registration.test.ts
autonomous: true
requirements: [STCK-01]
must_haves:
  truths:
    - "The WCAG-AA contrast gate parses oklch(...) token values and re-asserts every gated neutral pair clears 4.5:1 on the migrated globals.css"
    - "The token-family-registration gate proves every family globals.css declares is registered in the v4 @theme, WITHOUT tailwindcss/resolveConfig (which does not exist in v4)"
    - "npm run test -w @polytoken/web is fully green again — no gate is silently skipped or hollowed out"
  artifacts:
    - path: "apps/web/src/app/__tests__/token-contrast.test.ts"
      provides: "oklch-aware WCAG-AA regression gate"
      contains: "oklch"
    - path: "apps/web/src/app/__tests__/token-registration.test.ts"
      provides: "resolveConfig-free @theme registration gate"
  key_links:
    - from: "token-registration.test.ts"
      to: "globals.css @theme inline block"
      via: "string-parse of --color-* mapping lines"
      pattern: "@theme"
---

<objective>
Stage 3 — rewrite the two named STCK-01 gates so they audit the migrated (v4/oklch) artifacts instead of the v3 (JS-config/HSL-triplet) artifacts. Both broke in Stage 2 for two INDEPENDENT reasons (55-RESEARCH.md Pitfall 5): token-contrast's `parseHslTriplet` regex cannot match `oklch(...)`, and token-registration imports `tailwindcss/resolveConfig` which does not exist in v4. Neither is a value update — both need a rewrite.

Purpose: STCK-01 requires the WCAG-AA contrast + token-family-registration gates to STAY GREEN on the new engine. A gate that is deleted, skipped, or rewritten to assert nothing would pass CI while testing nothing — the exact rot these committed gates exist to prevent (PROJECT.md Key Decision "v1.4: committed regression gates over one-off checks"). The rewrites must preserve the original intent: the contrast gate still fails if a token drops below 4.5:1; the registration gate still fails if a declared family is not wired into the compiled theme.

Output: both gate files rewritten to parse the new CSS shape directly (no resolveConfig, no HSL regex); `npm run test -w @polytoken/web` fully green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Gate commands: `npm run test -w @polytoken/web` (vitest), `npm run typecheck -w @polytoken/web`.
</execution_context>

<context>
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-RESEARCH.md
@.planning/phases/55-platform-migration-tailwind-v4-react-19/55-02-SUMMARY.md

<interfaces>
Existing gate internals to preserve/reuse (from the current files, read both fully before editing):
- token-contrast.test.ts EXPORTS reusable helpers: `readTokenBlock(css, blockSelector)` (engine-agnostic — regexes `--token: value;` out of a named CSS block; KEEP IT), `relativeLuminance`, `contrastRatio`, plus the HSL-specific `parseHslTriplet`/`hslToLinearRgb` (REPLACE these two with an oklch path). NEUTRAL_PAIRS gated today: muted/muted-foreground, secondary/secondary-foreground, accent/accent-foreground — in both `:root` and `.dark`. Threshold: 4.5:1.
- token-registration.test.ts currently: `resolveConfig(appConfig).theme` then asserts `theme.colors.sidebar.*` match `var(--sidebar-*)`, `theme.colors.chart.1..5` = `hsl(var(--chart-N))`, `theme.boxShadow.elevation-1..3` = `var(--elevation-N)`, `theme.borderRadius.xl/2xl` = `var(--radius-xl/2xl)`. The v4 equivalents now live as `--color-sidebar-*`, `--color-chart-*`, `--shadow-elevation-*`, `--radius-xl/2xl` mapping lines inside globals.css's `@theme inline`/`@theme` blocks (per 55-02).
- 55-02-SUMMARY.md records how the elevation/chart/radius families were expressed in @theme — read it to know the exact mapping-line shape to assert.
</interfaces>

RESEARCH guidance (Pitfall 5 + Don't Hand-Roll):
- Do NOT add culori or any new dependency. Per Open Question 2, oklch values were precomputed as literals in 55-02, so the contrast gate parses `oklch(L C H)` directly and computes luminance from oklch WITHOUT runtime HSL↔oklch conversion.
- For the registration gate, reuse token-contrast's `readTokenBlock` string-parsing technique against the `@theme` blocks — do not write a third parser.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite token-contrast.test.ts to parse oklch and re-assert WCAG-AA</name>
  <read_first>
    - apps/web/src/app/__tests__/token-contrast.test.ts (entire file)
    - apps/web/src/app/globals.css (the migrated :root/.dark oklch values)
    - 55-RESEARCH.md Pitfall 5 (gate A) + "Don't Hand-Roll" (oklch luminance math)
  </read_first>
  <behavior>
    - Given the migrated globals.css, `readTokenBlock(css, ":root")` and `readTokenBlock(css, ".dark")` still extract the token map (helper unchanged).
    - A new `parseOklch(value)` parses `oklch(L C H)` / `oklch(L% C H)` into components; throws on a value it cannot parse (same fail-loud contract as the old parseHslTriplet).
    - A new luminance path converts oklch → relative luminance (via oklch→linear-sRGB→WCAG luminance, or by parsing to sRGB with a correct, self-contained conversion) feeding the UNCHANGED `contrastRatio`.
    - For each of the 3 neutral pairs × 2 modes, the computed ratio ≥ 4.5:1 against the file's ACTUAL current oklch values (never hardcoded expected numbers — the same regression contract as before).
    - A deliberately-broken token value would fail the gate (self-verify the math against the 55-02 recorded ratios: the test's computed ratios should match the throwaway-script numbers from Stage 2 within rounding).
  </behavior>
  <action>
Replace `parseHslTriplet`/`hslToLinearRgb` with an oklch parser + oklch→luminance conversion; keep `readTokenBlock`, `relativeLuminance`, `contrastRatio`, the NEUTRAL_PAIRS/MODES structure, and the `>= 4.5` assertion loop. Type everything explicitly; conversion functions are pure and return new objects (no mutation). Do NOT add a dependency — implement the oklch→sRGB math self-contained (RESEARCH notes the gamut-clipping edge cases; a security/accessibility gate must be correct, so validate the converted-luminance numbers against the WCAG ratios recorded in 55-02-SUMMARY.md). Keep the exact `describe`/`it` naming so the gate's intent stays legible in CI output.
  </action>
  <acceptance_criteria>
    - `grep -c "resolveConfig\|parseHslTriplet\|hslToLinearRgb" apps/web/src/app/__tests__/token-contrast.test.ts` returns 0; `grep -c "oklch" apps/web/src/app/__tests__/token-contrast.test.ts` returns ≥1.
    - `npm run test -w @polytoken/web -- token-contrast` runs the 6 (3 pairs × 2 modes) assertions and all pass.
    - The computed ratios match the Stage-2 throwaway-script numbers (sanity-log or comment them); a temporary local edit that pushes a gated token below 4.5:1 makes the test FAIL (verify once, then revert — do not commit the broken edit).
    - `npm run typecheck -w @polytoken/web` exits 0.
  </acceptance_criteria>
  <done>The WCAG-AA gate parses the oklch tokens and still fails on any real contrast regression; green on the migrated globals.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite token-registration.test.ts off resolveConfig</name>
  <read_first>
    - apps/web/src/app/__tests__/token-registration.test.ts (entire file)
    - apps/web/src/app/globals.css (@theme inline + @theme blocks from 55-02)
    - 55-02-SUMMARY.md (exact @theme mapping-line shapes)
    - 55-RESEARCH.md Pitfall 5 (gate B)
  </read_first>
  <behavior>
    - The test imports NO `tailwindcss/resolveConfig` and NO JS `tailwind.config` (both gone/obsolete in v4).
    - It string-parses globals.css's `@theme`/`@theme inline` blocks (reuse token-contrast's exported `readTokenBlock` or an equivalent block reader adapted for `@theme { ... }`) and asserts every family globals.css declares vars for is registered as a Tailwind theme mapping:
      - sidebar family: `--color-sidebar`, `--color-sidebar-foreground`, `-primary`, `-primary-foreground`, `-accent`, `-accent-foreground`, `-border`, `-ring` each mapped (→ `var(--sidebar-*)`).
      - chart: `--color-chart-1..5` mapped (→ `var(--chart-N)`).
      - elevation shadow scale: `--shadow-elevation-1..3` mapped (→ `var(--elevation-N)`).
      - radius: `--radius-xl`, `--radius-2xl` present.
    - A missing mapping line (e.g. a family declared in :root but never mapped in @theme — the original "var exists but utility never registered" bug class) makes the test FAIL.
  </behavior>
  <action>
Rewrite the file to read globals.css and assert the `@theme inline`/`@theme` mapping lines exist for every declared family, preserving the original intent (guarding the unregistered-utility bug class that once left `bg-sidebar` emitting no CSS). Reuse `readTokenBlock` from token-contrast (export it if not already, or add a small `@theme`-aware reader). Keep the four `it(...)` groupings (sidebar / chart / elevation / radius) and their descriptive names. Explicit types; no new dependency; no mutation.
  </action>
  <acceptance_criteria>
    - `grep -c "resolveConfig" apps/web/src/app/__tests__/token-registration.test.ts` returns 0; no `import ... from "../../../tailwind.config"` remains.
    - `npm run test -w @polytoken/web -- token-registration` passes all groups.
    - Deleting one `--color-sidebar-ring` mapping line locally makes the test FAIL (verify once, then revert — do not commit).
    - `npm run typecheck -w @polytoken/web` exits 0.
    - FULL GREEN: `npm run test -w @polytoken/web` exits 0 (both rewritten gates + palette-ban + all other vitest files pass) — the Stage-2 expected failures are now resolved.
  </acceptance_criteria>
  <done>The registration gate proves @theme registration without resolveConfig and still catches an unregistered family; the whole web vitest suite is green again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI gate → shipped tokens | These vitest gates are the CI-time audit of the token surface; a hollowed gate would let contrast/registration regressions ship |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Repudiation / integrity (silent gate rot) | rewritten token-contrast + token-registration gates | mitigate | Each rewrite is proven to still FAIL on a deliberately-broken token (verified-then-reverted), so the gate is not hollowed; computed ratios cross-checked against 55-02's recorded numbers. No new dependency added (no supply-chain surface) → no T-55-SC row. |
</threat_model>

<verification>
- `npm run test -w @polytoken/web` → exit 0 (fully green, both gates rewritten).
- Each gate proven to still fail on an injected regression (verified locally, reverted, not committed).
- `npm run typecheck -w @polytoken/web` → exit 0.
</verification>

<success_criteria>
- Both named STCK-01 gates parse the migrated v4/oklch artifacts and stay green, with their original failure-detection intent intact.
- No `tailwindcss/resolveConfig` and no HSL-triplet regex remain anywhere in the gate files.
- The full web vitest suite is green — no gate skipped or hollowed.
</success_criteria>

<output>
Create `.planning/phases/55-platform-migration-tailwind-v4-react-19/55-03-SUMMARY.md`. Record: the oklch→luminance approach chosen, confirmation the injected-regression check made each gate fail (then reverted), and the final green vitest count.
</output>

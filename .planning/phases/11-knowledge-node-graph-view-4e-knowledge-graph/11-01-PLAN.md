---
phase: 11-knowledge-node-graph-view-4e-knowledge-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/knowledge-node-edges.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/0019_*.sql
  - packages/db/scripts/assert-knowledge-node-edges.ts
  - packages/api-client/src/router/knowledge/graph.ts
  - packages/api-client/src/router/knowledge/list.ts
  - packages/api-client/src/router/knowledge/detail.ts
  - packages/api-client/src/router/knowledge/index.ts
  - packages/api-client/src/router/knowledge/graph.test.ts
  - packages/api-client/src/router/knowledge/list.test.ts
  - packages/api-client/src/root.ts
  - apps/email-listener/app/application/use_cases/confirm_region.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10, D-11, D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "The DB has an empty knowledge_node_edges table that 4e can later write node-edges into with no migration — shipping the simple read-now path while seaming the real synthesis moat for later (D-01/D-05/D-10)"
    - "api.knowledge.graph returns { nodes, edges } unioning derived-FK edges with the (empty) knowledge_node_edges table behind one provider boundary (D-04/D-06/D-11)"
    - "api.knowledge.graph returns the importer's entire knowledge/entity network (importer-wide, not a per-email subgraph): always entity_type + entity_type_field nodes and edges from existing FKs, with instance/component/email/knowledge nodes added only when requested or present (D-02/D-03/D-04)"
    - "api.knowledge.list returns a paginated knowledge_nodes feed and api.knowledge.byId returns one node + its (empty today) edges (D-06)"
    - "Every knowledge query scopes importer_id from a data filter, never from a trusted caller claim, and writes 0 rows to knowledge_node_edges (D-09/D-12)"
    - "The Python confirm/correction use case documents the future synthesis-trigger injection point with no code path built (D-13)"
  artifacts:
    - path: "packages/db/src/schema/knowledge-node-edges.ts"
      provides: "Empty 4e write-seam table KnowledgeNodeEdges + inferred types (D-05/D-10)"
      contains: "knowledge_node_edges"
      min_lines: 35
    - path: "packages/db/src/schema/index.ts"
      provides: "Barrel export of knowledge-node-edges"
      contains: 'export * from "./knowledge-node-edges"'
    - path: "packages/db/scripts/assert-knowledge-node-edges.ts"
      provides: "Verify-gate script that asserts the table exists in the live local DB via information_schema (defeats schema-source false-green)"
      contains: "information_schema.tables"
    - path: "packages/api-client/src/router/knowledge/graph.ts"
      provides: "knowledge.graph procedure + inferred edge-provider boundary + shapeGraphResponse helper (D-04/D-06/D-11/D-12)"
      exports: ["knowledgeGraphProcedures", "graphInputSchema", "shapeGraphResponse"]
      min_lines: 120
    - path: "packages/api-client/src/router/knowledge/list.ts"
      provides: "knowledge.list paginated knowledge_nodes feed (limit+1)"
      exports: ["knowledgeListProcedures"]
    - path: "packages/api-client/src/router/knowledge/detail.ts"
      provides: "knowledge.byId node + edges (empty today)"
      exports: ["knowledgeDetailProcedures"]
    - path: "packages/api-client/src/router/knowledge/index.ts"
      provides: "Composed knowledgeRouter"
      exports: ["knowledgeRouter"]
    - path: "packages/api-client/src/root.ts"
      provides: "knowledge: knowledgeRouter registration"
      contains: "knowledge: knowledgeRouter"
  key_links:
    - from: "packages/api-client/src/root.ts"
      to: "packages/api-client/src/router/knowledge/index.ts"
      via: "import { knowledgeRouter }"
      pattern: 'knowledge:\\s*knowledgeRouter'
    - from: "packages/api-client/src/router/knowledge/graph.ts"
      to: "@nauta/db/schema KnowledgeNodeEdges"
      via: "provider seam union read (empty today)"
      pattern: "KnowledgeNodeEdges"
    - from: "packages/api-client/src/router/knowledge/graph.ts"
      to: "ComponentEntityCandidateLinks"
      via: "component to entity_instance derived edge (D-04 item 3 — NOT a direct FK on email_components)"
      pattern: "ComponentEntityCandidateLinks"
---

<objective>
Build the entire read-only backend for the knowledge graph: the empty `knowledge_node_edges`
4e write-seam table (with its [BLOCKING] migration), the new `knowledge` tRPC router
(`graph` + `list` + `byId`) reading edges through a single source-agnostic provider boundary,
and the documented-only synthesis-trigger injection point in the Python confirm use case.

Purpose: Ship the SIMPLE, demoable-today graph data from existing FKs (D-01) while leaving
clean, documented seams (D-05/D-10/D-11/D-12/D-13) so the future 4e knowledge-synthesis backend
drops in with no rework. The graph reads; the synthesis path (a future phase) writes.
This is also the backend that implements the importer-wide scoped query (D-03 — "the moat made
visible" served from one importer-filtered data path, the origin of the D-12 tenant-isolation rule).

Output: `knowledge_node_edges` table + migration 0019, `packages/api-client/src/router/knowledge/`
(graph/list/detail/index + DB-free tests), `knowledge: knowledgeRouter` in root.ts, and a
documented injection point in confirm_region.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-CONTEXT.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do NOT re-explore. -->

Derived-edge source columns (D-04, all verified against live schema):
- entity_type → entity_type_field:   EntityTypeFields.entityTypeId  (entity-types.ts:92)
- entity_instance → entity_type:     EntityInstances.entityTypeId   (entity-instances.ts:43)
- component ↔ entity_instance:       ComponentEntityCandidateLinks.componentId + .entityInstanceId
                                     (component-links.ts:75,79) — carries .matchType, .wasSelected.
                                     THIS IS THE JOIN PATH, NOT a direct FK on email_components
                                     (UI-SPEC Note #3 is WRONG; PATTERNS "Schema Discrepancy" governs)
- component → entity_type:           ComponentEntityCandidateLinks.entityTypeId (component-links.ts:83)
- component → email:                 EmailComponents.emailId        (components.ts:45)
- component → component (nesting):   EmailComponents.parentComponentId (components.ts:56, self-FK)
- component ↔ knowledge_node:        ComponentKnowledgeNodeLinks.componentId + .knowledgeNodeId
                                     (component-links.ts:38,42)
- knowledge_node → scope:            KnowledgeNodes.scopeRefId + .scopeRefType (knowledge-nodes.ts:54-55)

KnowledgeNodes (knowledge-nodes.ts): id, importerId(NOT NULL FK→importers), title, content,
  scope(enum), scopeRefId(uuid), scopeRefType(text), source(text default 'manual'),
  confidence(real default 1.0), isActive(bool default true), createdAt.

DB connection facts the verify gate depends on (client.ts / migrate.ts):
- The tRPC router and the migrator both connect via `env.POSTGRES_URL_NON_POOLING` (session-mode, port 5432) — this is the live local DB the router queries.
- env is validated by @t3-oss/env-core in packages/db/src/client.ts and is only populated when the dotenv `with-env` wrapper (`dotenv -e ../../.env.local`) runs the process. A bare `tsx`/`node` invocation WITHOUT `npm run with-env --` will have no POSTGRES_URL_* and cannot connect — the existence assertion MUST run through `with-env`.
- `npm run migrate:local` = `with-env -- tsx src/migrate.ts`; migrate.ts exits 0 on success (its finally calls process.exit(0)), so chaining `&&` after it works.

tRPC context (trpc.ts): ctx.db = Drizzle handle; publicProcedure is the only (unauth) procedure.
  Use publicProcedure for ALL knowledge procedures (read-only, D-09).

Existing analog procedure-object + DB-free-test idiom (entities/gallery.ts):
  export const entityGalleryProcedures = { list: publicProcedure.input(listInputSchema).query(...) }
  exported pure shaping helper (shapeGalleryItem) + exported input schema (listInputSchema) for tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: knowledge_node_edges schema + barrel export + [BLOCKING] migration generate/apply (live-table-asserted)</name>
  <read_first>
    - packages/db/src/schema/knowledge-nodes.ts (exact analog: imports, real/uuid/text columns, index idiom, $inferSelect/$inferInsert)
    - packages/db/src/schema/index.ts (barrel — append order; knowledge-nodes already exported at line 19)
    - packages/db/src/schema/component-links.ts (FK reference idiom + onDelete cascade)
    - packages/db/src/client.ts (env shape: POSTGRES_URL_NON_POOLING is the session-mode URL the router uses; @t3-oss/env-core validation; postgres-js client) + packages/db/src/migrate.ts (the with-env-loaded migrator pattern the assertion script mirrors)
    - packages/db/migrations/0006_bitter_white_queen.sql (migration SQL style analog — CREATE TABLE + ADD CONSTRAINT + CREATE INDEX with --> statement-breakpoint)
    - packages/db/package.json (scripts: migration:generate, migrate:local, with-env = `dotenv -e ../../.env.local --`)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md (the full table shape for knowledge-node-edges.ts is in the "Full table shape" block)
  </read_first>
  <action>
    Create packages/db/src/schema/knowledge-node-edges.ts exporting `KnowledgeNodeEdges` pgTable
    "knowledge_node_edges" with columns (D-05): id uuid primaryKey defaultRandom; sourceNodeId
    uuid("source_node_id") notNull references KnowledgeNodes.id onDelete cascade; targetRefId
    uuid("target_ref_id") (NO FK — polymorphic, mirrors knowledge_nodes.scope_ref_id idiom);
    targetRefType text("target_ref_type"); relationType text("relation_type") notNull default
    "related"; confidence real("confidence") notNull default 1.0 (real to match
    knowledge_nodes.confidence); source text("source") notNull default "manual"; createdAt
    timestamp withTimezone notNull defaultNow. Two indexes: idx_knowledge_node_edges_source_node_id
    on sourceNodeId, idx_knowledge_node_edges_target_ref_id on targetRefId. Export inferred types
    KnowledgeNodeEdgeRow (= $inferSelect) and InsertKnowledgeNodeEdge (= $inferInsert). Import only
    the pg-core members used + `import { KnowledgeNodes } from "./knowledge-nodes"` — no pgEnum
    (relationType stays plain text), no halfvec. Append `export * from "./knowledge-node-edges";`
    to packages/db/src/schema/index.ts AFTER the component-links export (line 22) — it depends on
    knowledge-nodes (line 19), which precedes it.

    THEN [BLOCKING] generate + apply the migration before any downstream work:
    (1) from packages/db run `npm run migration:generate` (drizzle-kit generate) — it writes a new
        numbered file packages/db/migrations/0019_<random>.sql (last is 0018_many_scarecrow.sql; do
        NOT hand-number it). Inspect the generated SQL: it must CREATE TABLE knowledge_node_edges
        with exactly the 8 columns above, the source_node_id FK to knowledge_nodes ON DELETE cascade,
        and the two btree indexes — and NOTHING ELSE (if drizzle-kit re-emits unrelated drift from
        un-snapshotted custom migrations 0009..0018, scope the file to the Phase-11 change only and
        add IF NOT EXISTS guards, per the 09-01 drift-fix precedent in STATE.md).
    (2) apply locally: from packages/db run `npm run migrate:local` (tsx src/migrate.ts).

    THEN create the live-table assertion script the verify gate runs (this is what makes the gate a
    REAL gate, not a schema-source false-green): packages/db/scripts/assert-knowledge-node-edges.ts —
    a tiny tsx script that imports `postgres` (already a dependency) and `env` from "../src/client",
    connects to `env.POSTGRES_URL_NON_POOLING` (the SAME session-mode URL the tRPC router and migrator
    use), runs `select 1 from information_schema.tables where table_schema = 'public' and table_name =
    'knowledge_node_edges'`, and `process.exit(1)` with a clear "table NOT present in local DB" message
    if zero rows come back, else `process.exit(0)`. It must be invoked THROUGH the dotenv wrapper
    (`npm run with-env -- tsx scripts/assert-knowledge-node-edges.ts`) so POSTGRES_URL_NON_POOLING is
    loaded from ../../.env.local exactly as migrate:local loads it — a bare tsx call has no env and
    would fail env validation. The whole point: typecheck + the 0019 file presence pass even if
    migrate:local was never applied (Drizzle types compile from schema source), so the gate ONLY goes
    green when the table actually exists in the live local Postgres.

    Staging/prod application (`migrate:staging`/`migrate:prod`) follows the migrations-first deploy
    playbook AFTER the phase — note it in the SUMMARY as a PENDING DEPLOY follow-up; local apply +
    the live-table assertion is the gate here.
  </action>
  <verify>
    <automated>cd packages/db &amp;&amp; npm run typecheck &amp;&amp; ls migrations/ | grep -E '^0019_.*\.sql$' &amp;&amp; grep -q 'knowledge_node_edges' migrations/0019_*.sql &amp;&amp; npm run migrate:local &amp;&amp; npm run with-env -- tsx scripts/assert-knowledge-node-edges.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/knowledge-node-edges.ts exports `KnowledgeNodeEdges`, `KnowledgeNodeEdgeRow`, `InsertKnowledgeNodeEdge`
    - knowledge-node-edges.ts contains columns source_node_id, target_ref_id, target_ref_type, relation_type, confidence, source, created_at
    - source_node_id references KnowledgeNodes.id with onDelete "cascade"; target_ref_id has NO .references() call (polymorphic)
    - packages/db/src/schema/index.ts contains `export * from "./knowledge-node-edges"` AFTER the component-links export
    - A new migration file matching packages/db/migrations/0019_*.sql exists and contains `CREATE TABLE "knowledge_node_edges"`
    - packages/db/scripts/assert-knowledge-node-edges.ts queries `information_schema.tables` for `knowledge_node_edges` against POSTGRES_URL_NON_POOLING and exits non-zero when the table is absent
    - The migration applied locally (migrate:local exit 0) AND the assertion script (run via `with-env`) confirms knowledge_node_edges exists in the live local Postgres the tRPC router queries — a green verify run is IMPOSSIBLE if the table was never created (typecheck + file-presence checks alone do not prove the table; the assertion does)
    - packages/db typecheck (tsc --noEmit) exits 0
  </acceptance_criteria>
  <done>
    knowledge_node_edges table exists (schema + barrel + applied migration 0019, asserted present in the
    live local DB via information_schema through the with-env-loaded script, not just compiled from
    schema source); 0 rows written; packages/db typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: knowledge tRPC router (graph + list + byId) behind the inferred edge-provider seam</name>
  <read_first>
    - packages/api-client/src/router/entities/gallery.ts (analog: exported input schema, exported pure shaping helper, procedure object, parameterized Drizzle, limit+1, immutable spreads)
    - packages/api-client/src/router/entities/detail.ts (analog: byId, null-on-missing never throws, KnowledgeNodes read with scope/scopeRefId/isActive)
    - packages/api-client/src/router/entities/index.ts (compose pattern for index.ts)
    - packages/api-client/src/router/entities/gallery.test.ts (DB-free test idiom — import pure helper + input schema, assert immutability + enum allow-lists)
    - packages/api-client/src/root.ts (registration site — add knowledge alongside emails/entityTypes/entities)
    - packages/api-client/src/trpc.ts (ctx.db, publicProcedure)
    - packages/db/src/schema/component-links.ts + entity-instances.ts + entity-types.ts + components.ts + emails.ts + knowledge-nodes.ts (the derived-edge source columns — listed in <interfaces>)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md ("Schema Discrepancy" + per-file pattern assignments for graph.ts/list.ts/detail.ts/index.ts)
  </read_first>
  <behavior>
    - shapeGraphResponse(nodes, edges) returns { nodes, edges } as NEW immutable arrays (spread); never mutates inputs.
    - graphInputSchema: importerId z.string().uuid().optional(); includeInstances z.boolean().default(false);
      includeEmails z.boolean().default(false); nodeTypes z.array(z.enum([the 6 node types])).optional().
      Defaults: includeInstances=false, includeEmails=false. nodeTypes rejects values outside the 6-item allow-list.
    - knowledge.list input: importerId uuid optional, limit z.number().int().min(1).max(100).default(25),
      offset z.number().int().min(0).default(0); limit+1 hasMore detection; returns { items, hasMore, nextOffset }.
    - A pure edge-provider helper (the D-11 seam) maps derived-FK rows AND knowledge_node_edges rows to the
      SAME GraphEdge shape; with the edges table empty it contributes 0 edges and the response is identical to
      derived-only — proving the seam without a UI change when 4e populates it.
  </behavior>
  <action>
    Create packages/api-client/src/router/knowledge/ with four files mirroring entities/ exactly.

    graph.ts — export `graphInputSchema` (+ `type GraphInput`), exported pure interfaces `GraphNode`
    (readonly id, type, label, plus type-specific fields) + `GraphEdge` (readonly id, source, target,
    relationType/edgeType), an exported pure `shapeGraphResponse(nodes, edges)` returning spread copies,
    and `knowledgeGraphProcedures = { graph: publicProcedure.input(graphInputSchema).query(...) }`. The
    query, using parameterized Drizzle builders only (eq/and/inArray; NEVER string-interpolate input):
    (1) ALWAYS select entity_type nodes (EntityTypes) + entity_type_field nodes (EntityTypeFields) and
        emit entity_type→entity_type_field edges from EntityTypeFields.entityTypeId (D-04 item 1 —
        taxonomy, present even with zero instances);
    (2) when includeInstances (or the future auto-threshold) — entity_instance nodes (EntityInstances)
        + entity_instance→entity_type edges (EntityInstances.entityTypeId, item 2) + component↔instance
        and component→entity_type edges via ComponentEntityCandidateLinks (items 3+4 — JOIN PATH per the
        PATTERNS Schema Discrepancy, NOT a direct FK on email_components);
    (3) when includeEmails — email_component nodes (EmailComponents) + email nodes (Emails) + component→email
        edges (EmailComponents.emailId, item 5) + component→component nesting edges
        (EmailComponents.parentComponentId, item 6);
    (4) knowledge_node nodes (KnowledgeNodes where isActive) only when count > 0, + component↔knowledge_node
        edges (ComponentKnowledgeNodeLinks, item 7) + knowledge_node→scope edges (KnowledgeNodes.scopeRefId
        /scopeRefType, item 8);
    (5) the D-11 provider seam: ALSO read knowledge_node_edges (KnowledgeNodeEdges) and map each row to a
        GraphEdge through the SAME pure mapper as the derived edges, then UNION into the edge list (empty
        today → 0 extra edges). Return shapeGraphResponse(nodes, edges). D-12: importerId is an OPTIONAL
        data filter applied via eq on the *.importerId columns — never trust a caller claim; never derive
        tenant from a URL alone. WRITE 0 ROWS to knowledge_node_edges anywhere in this router (read-only, D-09).

    list.ts — `knowledgeListProcedures = { list: publicProcedure.input(...).query(...) }` selecting from
    KnowledgeNodes where isActive (+ optional importerId eq), limit+1, returning { items, hasMore, nextOffset }.

    detail.ts — `knowledgeDetailProcedures = { byId: publicProcedure.input(z.object({id: z.string().uuid()})).query(...) }`
    selecting one KnowledgeNodes row (isActive) → return null when missing (never throw), and reading
    KnowledgeNodeEdges where sourceNodeId = id (empty today) → return { node, edges }.

    index.ts — compose `knowledgeRouter = createTRPCRouter({ ...knowledgeGraphProcedures,
    ...knowledgeListProcedures, ...knowledgeDetailProcedures })` (mirror entities/index.ts).

    Register in root.ts: `import { knowledgeRouter } from "./router/knowledge";` and add
    `knowledge: knowledgeRouter,` to appRouter (alongside emails/entityTypes/entities).

    TDD: write graph.test.ts + list.test.ts FIRST (RED) against the pure exports — shapeGraphResponse
    immutability + identity-of-shape, graphInputSchema enum/default/bounds (the 6 node-type allow-list,
    includeInstances/includeEmails defaults false), and list input limit/offset bounds — then implement to GREEN.
  </action>
  <verify>
    <automated>cd packages/api-client &amp;&amp; npm run test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api-client/src/router/knowledge/graph.ts exports `knowledgeGraphProcedures`, `graphInputSchema`, `shapeGraphResponse`
    - graphInputSchema nodeTypes rejects a value outside ["entity_type","entity_type_field","entity_instance","email_component","email","knowledge_node"] (test asserts ZodError)
    - shapeGraphResponse returns a new object whose nodes/edges arrays are not the same reference as the inputs (immutability test passes)
    - graph.ts references `ComponentEntityCandidateLinks` for the component↔instance edge (NOT a direct email_components FK)
    - graph.ts references `KnowledgeNodeEdges` (the D-11 provider seam union read)
    - packages/api-client/src/router/knowledge/index.ts exports `knowledgeRouter`
    - packages/api-client/src/root.ts contains `knowledge: knowledgeRouter`
    - graph.ts/list.ts/detail.ts contain NO insert/update/delete against KnowledgeNodeEdges (read-only)
    - importerId appears only as an optional `.input` filter applied via eq() — never read from a header/session as a trusted tenant claim
    - api-client vitest passes (npm run test exit 0) and tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    knowledge.graph/list/byId compile, are registered in appRouter, read derived FKs + the empty edges table
    through one provider mapper, are read-only and importer-filtered from data, and the DB-free tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the 4e synthesis-trigger injection point in confirm_region.py (D-13, no code path)</name>
  <read_first>
    - apps/email-listener/app/application/use_cases/confirm_region.py (the existing confirm flow — the documented injection point lands here; note the domain-ports-only import contract on lines 10-12)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-CONTEXT.md (D-13 — the future correction→synthesis call hooks here; an optional knowledge_synthesizer dependency, None today; source discriminator already exists on knowledge_nodes, no schema change needed for 4e)
  </read_first>
  <action>
    Add a documentation-only seam to ConfirmRegionUseCase (D-13) WITHOUT building any code path,
    LLM call, or write. Add a module/class docstring section AND an inline comment at the natural
    injection point (immediately after the component is confirmed and embedded, near the end of
    execute(), before the final "confirm_region_done" log) describing the FUTURE 4e hook: when a human
    correction is present (corrected_fields differs from the candidate's extracted_fields), the real 4e
    phase will dispatch an ISOLATED Bedrock synthesis call (per context/6 §6 — never derived from raw
    email content, S14/S15 isolation) that proposes a knowledge_nodes row with source=
    'learned_from_correction', gated by a human rule-approval queue. State explicitly that this is an
    optional `knowledge_synthesizer` dependency that is None today, that NO schema change is needed
    (knowledge_nodes.source already discriminates manual vs learned), and that nothing is built or
    wired in this phase. Do NOT add a constructor parameter, import, or branch — comment/docstring
    ONLY, so the domain-ports-only import contract and lint-imports stay clean and behavior is unchanged.
  </action>
  <verify>
    <automated>cd apps/email-listener &amp;&amp; uv run ruff check app/application/use_cases/confirm_region.py &amp;&amp; uv run lint-imports &amp;&amp; grep -q "knowledge_synthesizer" app/application/use_cases/confirm_region.py</automated>
  </verify>
  <acceptance_criteria>
    - confirm_region.py contains the string `knowledge_synthesizer` in a comment/docstring (the documented injection point)
    - confirm_region.py contains a reference to `learned_from_correction` (the future source value) and notes "None today"/no code path
    - No new constructor parameter, no new import, no new branch added (git diff shows only comment/docstring lines; execute() signature unchanged)
    - ruff check on the file passes; lint-imports passes (no new infrastructure import)
  </acceptance_criteria>
  <done>
    The 4e synthesis injection point is documented in confirm_region.py with zero behavioral change;
    Python gates (ruff, lint-imports) stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client → knowledge tRPC | Untrusted query input (importerId, includeInstances/Emails, nodeTypes, id) crosses here |
| tRPC router → Postgres | Drizzle query builders cross into the DB; user input must be parameterized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | knowledge.graph/list/byId IDOR — cross-importer data leak | mitigate | importerId is an OPTIONAL data filter applied via `eq(*.importerId, input.importerId)`; tenant is never read from a trusted caller claim/header as the sole identifier — mirrors Phase 9 D-18 / Phase 10 D-21 / D-12. Reads partition by data, never by an auth boundary that does not exist yet. |
| T-11-02 | Tampering (SQL injection) | nodeTypes/search/id inputs into Drizzle | mitigate | All inputs validated by Zod (z.enum 6-item allow-list for nodeTypes, z.string().uuid() for ids, capped limit max 100); all SQL uses parameterized Drizzle builders / bound `sql` params — never string interpolation (gallery.ts T-10-32 precedent). |
| T-11-03 | Denial of service | unbounded graph fan-out on a large importer | mitigate | Instances/components/emails are OFF by default (includeInstances/includeEmails=false); list is limit+1 capped at max 100; the heavy ranks render only on explicit request — render-depth caps enforced in the frontend plan (UI-SPEC default render depth). |
| T-11-04 | Elevation / unauthorized write | a new mutation surface introduced | accept (verified absent) | Phase is strictly read-only (D-09): all procedures are `publicProcedure().query`, no `.mutation`; knowledge_node_edges receives 0 writes. No auth/CSRF write concerns apply because no mutation is introduced (acceptance criteria assert no insert/update/delete). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO new package (router + schema + a verify-only tsx script reusing the existing `postgres` dep only); no install task → package-legitimacy gate not triggered for plan 11-01. Frontend package installs are gated in 11-02. |
</threat_model>

<verification>
- packages/db: tsc --noEmit exit 0; migration 0019_*.sql exists, applied locally, AND asserted present in the live local DB via the with-env-loaded information_schema script (knowledge_node_edges table truly exists — not merely compiled from schema source).
- packages/api-client: `npm run test` (vitest) exit 0; `npm run typecheck` exit 0; knowledge router registered in root.ts.
- apps/email-listener: ruff + lint-imports clean on confirm_region.py; D-13 seam documented, behavior unchanged.
- 0 rows written to knowledge_node_edges; no .mutation in the knowledge router (read-only, D-09).
</verification>

<success_criteria>
- knowledge_node_edges exists (empty 4e write-seam, D-05/D-10) with migration 0019 applied locally and table-existence confirmed against the live DB.
- api.knowledge.graph returns { nodes, edges } from derived FKs unioned with the empty edges table behind
  one provider boundary (D-04/D-06/D-11); list + byId work (D-06); all importer-filtered from data (D-12), serving the importer-wide network view (D-03) under the governing ship-simple/seam-real decision (D-01).
- The 4e synthesis injection point is documented in confirm_region.py (D-13) with no code path.
- Both stacks' gates green (packages/db tsc + live-table assertion, api-client vitest+tsc, Python ruff+lint-imports).
</success_criteria>

<output>
Create `.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-01-SUMMARY.md` when done.
Record the PENDING DEPLOY follow-up: `migrate:staging` / `migrate:prod` to apply migration 0019 to
staging+prod Supabase before/with the next web+listener deploy.
</output>

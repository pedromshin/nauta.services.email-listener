---
phase: 11-knowledge-node-graph-view-4e-knowledge-graph
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/web/package.json
  - apps/web/src/app/knowledge/page.tsx
  - apps/web/src/app/knowledge/_components/knowledge-graph.tsx
  - apps/web/src/app/knowledge/_components/knowledge-graph-skeleton.tsx
  - apps/web/src/app/knowledge/_components/graph-nodes.tsx
  - apps/web/src/app/knowledge/_components/graph-layout.ts
  - apps/web/src/components/app-sidebar.tsx
autonomous: false
requirements: [D-02, D-04, D-07, D-08, D-11]

must_haves:
  truths:
    - "Visiting /knowledge renders the entity-type + entity-type-field taxonomy as a hierarchical React Flow graph from real data — never a blank screen (D-02/D-08)"
    - "The graph is a client island loaded via dynamic(..., { ssr: false }) so Canvas/SSR never collide (D-08)"
    - "@xyflow/react (React Flow v12) + @dagrejs/dagre are installed in apps/web and the six custom node types + edge styling render per UI-SPEC (D-07)"
    - "Nodes are laid out top-to-bottom by dagre (rankdir TB, ranksep 64, nodesep 32) (D-07)"
    - "The Knowledge sidebar item is a live /knowledge link, removed from the Soon list (UI-SPEC Sidebar Nav Change)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "@xyflow/react + @dagrejs/dagre dependencies"
      contains: "@xyflow/react"
    - path: "apps/web/src/app/knowledge/page.tsx"
      provides: "Server-component route mounting the graph as a dynamic ssr:false client island (D-08)"
      contains: "ssr: false"
    - path: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      provides: "use client React Flow graph consuming api.knowledge.graph + 6 custom node types + edge styles"
      min_lines: 120
    - path: "apps/web/src/app/knowledge/_components/graph-layout.ts"
      provides: "dagre TB layout util mapping nodes+edges → positioned nodes (D-07)"
      contains: "rankdir"
    - path: "apps/web/src/app/knowledge/_components/graph-nodes.tsx"
      provides: "The six custom React Flow node components per UI-SPEC Node Visual Language"
      min_lines: 80
    - path: "apps/web/src/components/app-sidebar.tsx"
      provides: "Knowledge promoted to LIVE_NAV_ITEMS with href /knowledge"
      contains: '/knowledge'
  key_links:
    - from: "apps/web/src/app/knowledge/page.tsx"
      to: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      via: "dynamic(() => import(...), { ssr: false, loading })"
      pattern: "ssr:\\s*false"
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "api.knowledge.graph"
      via: "api.knowledge.graph.useQuery"
      pattern: "knowledge\\.graph\\.useQuery"
    - from: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      to: "apps/web/src/app/knowledge/_components/graph-layout.ts"
      via: "dagre layout import"
      pattern: "graph-layout"
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "/knowledge"
      via: "LIVE_NAV_ITEMS entry"
      pattern: 'href:\\s*"/knowledge"'
---

<objective>
Stand up the /knowledge route and the React Flow graph foundation: install the graph-viz deps,
create the server-component page + ssr:false client island, the dagre TB layout util, the six
custom node components + edge styling from real api.knowledge.graph data, and flip the sidebar
nav item to live. This delivers the "what we extract" taxonomy map that renders real data on day
one (D-02) — the filter rail, detail pane, toolbar, banner, and all states land in plan 11-03.

Purpose: Establish the client-island graph rendering boundary (D-07/D-08) and a legible
taxonomy-first default so the graph is never a blank screen, consuming the 11-01 knowledge.graph API.

Output: @xyflow/react + @dagrejs/dagre in apps/web; apps/web/src/app/knowledge/ (page + graph +
skeleton + nodes + layout); Knowledge live in the sidebar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-CONTEXT.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-UI-SPEC.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md
@.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do NOT re-explore the codebase for these. -->

api.knowledge.graph (from 11-01) → { nodes: GraphNode[], edges: GraphEdge[] }.
  Input: { importerId?: string; includeInstances?: boolean; includeEmails?: boolean; nodeTypes?: string[] }.
  D-12: pass importerId: undefined — tenant is derived server-side, never from the URL.

Six node types (UI-SPEC Node Visual Language — fixed dimensions + role-color, NO new hues, NO font-medium/500):
  entity_type        160×48  bg-primary/10 border-primary/40   icon Shapes size-4 text-primary    text-sm font-semibold
  entity_type_field  128×32  bg-muted/60 border-border/60      icon Hash size-3 text-muted-foreground  text-xs
  entity_instance    160×44  bg-violet-500/10 border-violet-500/40  icon Box size-4 text-violet-500  text-sm font-semibold + sub-label text-xs
  email_component    128×36  bg-amber-500/10 border-amber-500/40   icon Layers size-3 text-amber-500  text-xs
  email              144×40  bg-slate-100/60 border-slate-400/40 (dark: bg-slate-800/40 border-slate-600/40)  icon Mail size-4 text-slate-500  text-sm + sub-label
  knowledge_node     160×48  bg-primary/15 border-primary/60 + shadow-[0_0_8px_hsl(164_39%_22%/0.25)]  icon Share2 size-4 text-primary  text-sm font-semibold text-primary + sub-label
  All nodes: rounded-lg border shadow-sm cursor-pointer; selected adds ring-2 ring-primary ring-offset-1; hover adds shadow-md transition-shadow 150ms.

dagre config (UI-SPEC Layout Algorithm): rankdir "TB", ranksep 64, nodesep 32, edgesep 16.
  Ranks: 0 entity_type, 1 entity_type_field, 2 entity_instance, 3 email_component, 4 email; knowledge_node pinned rank 0–1 right of rightmost entity_type.

Edge styles (UI-SPEC Edge Visual Language) — per edge type stroke/width/dashed/animated; arrows
  (MarkerType.ArrowClosed size 6) only on instance/email edges; taxonomy edges no arrow; labels on hover only.

React Flow island convention (PATTERNS page.tsx block): dynamic(() => import('./_components/knowledge-graph').then(m => m.KnowledgeGraph), { ssr: false, loading: () => <KnowledgeGraphSkeleton/> }).
  Import "@xyflow/react/dist/style.css" inside the "use client" graph file. No existing dynamic() usage to copy — derive from Next.js App Router docs + the entities/page.tsx structural analog.

@nauta/ui primitives available (invent nothing): Skeleton, Card, Badge, ScrollArea, Separator, Switch,
  ResizablePanelGroup/ResizablePanel/ResizableHandle. tRPC client: import { api } from "~/trpc/react".

app-sidebar.tsx CURRENT (lines 37-45): LIVE_NAV_ITEMS = [Inbox "/", Entity Types "/entity-types", Entities "/entities"];
  SOON_NAV_ITEMS = [{ label: "Knowledge", icon: Share2, soon: true }]. Share2 already imported (line 4).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: package-legitimacy gate — runs BEFORE the Task 1 install (this checkpoint is intentionally first in document order)</name>
  <what-built>About to add two new npm packages to apps/web: @xyflow/react and @dagrejs/dagre. No RESEARCH.md Package Legitimacy table exists for this phase, so both are treated as [ASSUMED] (T-11-SC). This gate is FIRST in execution order: do not run the Task 1 install until it clears.</what-built>
  <how-to-verify>
    1. Open https://npmjs.com/package/@xyflow/react — confirm it is the maintained React Flow v12 package (xyflow org, millions of weekly downloads), not a typosquat.
    2. Open https://npmjs.com/package/@dagrejs/dagre — confirm it is the maintained dagre layout fork (dagrejs org).
    3. Confirm the pinned versions resolve to those packages.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the Task 1 install, or describe a concern to halt.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Install @xyflow/react + @dagrejs/dagre; flip the Knowledge sidebar nav to live</name>
  <read_first>
    - apps/web/package.json (dependency list + scripts; add deps here)
    - package.json (root — npm workspaces; install runs from repo root, web:build = api-client build then web build)
    - package-lock.json (root lockfile — check whether @dagrejs/dagre is already a resolved transitive dep of @xyflow/react, and which version satisfies @xyflow/react's peer/optional range, before pinning)
    - apps/web/src/components/app-sidebar.tsx (lines 36-45: LIVE_NAV_ITEMS / SOON_NAV_ITEMS; isActiveRoute handles active state automatically; Share2 already imported)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md (app-sidebar target-state block; how Phase 10 promoted "Entities")
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-UI-SPEC.md (Registry Safety table — @xyflow/react + @dagrejs/dagre are npm packages, not shadcn registry blocks)
  </read_first>
  <action>
    PREREQUISITE: the Task 0 package-legitimacy human-verify gate (FIRST in this <tasks> block, above)
    must read "approved" before this install runs — both packages are [ASSUMED] (T-11-SC). Do not install
    until it clears.

    Add `@xyflow/react` (React Flow v12, ~85-100KB gzipped, D-07) and `@dagrejs/dagre` (the dagre
    layout engine, UI-SPEC Layout Algorithm) to apps/web/package.json dependencies, then install via
    the repo package manager (npm workspaces — run `npm install` from the repo ROOT; this project uses
    npm, NOT pnpm, per root package.json "workspaces" + npm-run-based scripts).

    PATTERNS Note #4 — dagre dependency resolution (handle the conflict path concretely):
    First inspect the root lockfile for an existing @dagrejs/dagre resolution
    (`npm ls @dagrejs/dagre` from the repo root, or grep package-lock.json).
    - If @dagrejs/dagre is NOT already resolved: add it with the current latest stable range.
    - If @dagrejs/dagre IS already resolved transitively (e.g. pulled by @xyflow/react): DO NOT force a
      different/broader range. Pin the explicit dependency to the EXACT version already in the lockfile
      that satisfies @xyflow/react's peer/optional-dependency requirement — i.e. read the version
      @xyflow/react resolved and pin apps/web to that same version (`@dagrejs/dagre@<resolved-version>`),
      so adding the explicit dep is dedupe-clean and never introduces a second, mismatched copy or a peer
      warning. The goal is a single deduped @dagrejs/dagre that both apps/web (direct import) and
      @xyflow/react agree on. Verify post-install with `npm ls @dagrejs/dagre` showing one resolved version.
    Either way, @dagrejs/dagre ends up an explicitly declared dependency so the layout-util import is not
    relying on a transitive that could vanish.

    These are npm packages (UI-SPEC Registry Safety: shadcn registry vetting NOT triggered) — but they
    ARE new package-manager installs, gated by Task 0 (above) per T-11-SC.

    Then flip the sidebar (UI-SPEC Sidebar Nav Change): in app-sidebar.tsx move Knowledge into
    LIVE_NAV_ITEMS as `{ href: "/knowledge", label: "Knowledge", icon: Share2 }` (append after Entities),
    remove it from SOON_NAV_ITEMS (leaving SOON_NAV_ITEMS = []), and update the D-20 nav-order comment to
    "Inbox · Entity Types · Entities · Knowledge (all live)". isActiveRoute already handles active state —
    do not change it.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; node -e "const p=require('./package.json');if(!p.dependencies['@xyflow/react']||!p.dependencies['@dagrejs/dagre'])process.exit(1)" &amp;&amp; grep -q '"/knowledge"' src/components/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies contains both `@xyflow/react` and `@dagrejs/dagre`
    - The packages are installed (node_modules/@xyflow/react and node_modules/@dagrejs/dagre resolvable from apps/web)
    - `npm ls @dagrejs/dagre` from the repo root shows a single resolved version (no duplicate/mismatched copy; if it was transitive, the explicit pin matches @xyflow/react's resolved version)
    - app-sidebar.tsx LIVE_NAV_ITEMS contains an entry with `href: "/knowledge"` and `label: "Knowledge"`
    - app-sidebar.tsx SOON_NAV_ITEMS no longer contains a Knowledge entry (is now empty)
    - apps/web tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Graph-viz deps installed (after the Task 0 legitimacy gate cleared) with a single deduped @dagrejs/dagre,
    and Knowledge is a live /knowledge nav link.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: dagre TB layout util + the six custom React Flow node components</name>
  <read_first>
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-UI-SPEC.md (Node Visual Language — exact dimensions/colors/icons/typography per type; Layout Algorithm — rankdir/ranksep/nodesep/edgesep + rank assignments; Implementation Note #5: no font-medium/500)
    - apps/web/src/app/entities/[id]/_components/entity-knowledge.tsx (KnowledgeNode interface shape to reuse for the knowledge_node node sub-label, lines 13-20)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md ("No Analog Found" — derive node components from @xyflow/react docs + UI-SPEC; dagre util from @dagrejs/dagre API + UI-SPEC)
    - apps/web/src/components/app-sidebar.tsx (lucide-react import idiom + teal/glass token usage as a styling reference)
  </read_first>
  <action>
    Create graph-layout.ts — a pure exported `layoutGraph(nodes, edges)` util using
    `import dagre from "@dagrejs/dagre"`: build a dagre.graphlib.Graph, setGraph({ rankdir: "TB",
    ranksep: 64, nodesep: 32, edgesep: 16 }), set each node with its fixed width/height from the
    UI-SPEC per-type dimensions table, add edges, run dagre.layout, then return React Flow nodes with
    { position: { x, y } } computed from dagre (offset by half-width/height so dagre's center becomes
    React Flow's top-left). Keep it pure + immutable (return new node objects, never mutate inputs).

    Create graph-nodes.tsx — the six custom React Flow node components (entity_type,
    entity_type_field, entity_instance, email_component, email, knowledge_node), each rendering @nauta/ui
    primitives + Tailwind teal/glass tokens to the EXACT UI-SPEC Node Visual Language spec: fixed
    dimensions, role-color bg/border, lucide icon (Shapes/Hash/Box/Layers/Mail/Share2) at the specified
    size+tint, typography (text-sm font-semibold or text-xs — NEVER font-medium/500 per Note #5), the
    knowledge_node glow shadow, the shared base (rounded-lg border shadow-sm cursor-pointer), and the
    selected (ring-2 ring-primary ring-offset-1) + hover (shadow-md transition-shadow 150ms) treatments.
    Include the React Flow <Handle> source/target placements per the per-type "Handle" lines. Export a
    `nodeTypes` map object keyed by the six type strings for ReactFlow's nodeTypes prop. Reuse the
    KnowledgeNode shape from entity-knowledge.tsx for the knowledge_node sub-label (confidence %).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npm run typecheck &amp;&amp; grep -q 'rankdir' src/app/knowledge/_components/graph-layout.ts &amp;&amp; grep -q 'export const nodeTypes' src/app/knowledge/_components/graph-nodes.tsx &amp;&amp; grep -Fq 'shadow-[0_0_8px' src/app/knowledge/_components/graph-nodes.tsx &amp;&amp; grep -Fq 'bg-violet-500/10' src/app/knowledge/_components/graph-nodes.tsx &amp;&amp; grep -Fq 'bg-amber-500/10' src/app/knowledge/_components/graph-nodes.tsx &amp;&amp; ! grep -Fq 'font-medium' src/app/knowledge/_components/graph-nodes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - graph-layout.ts exports a pure layout function and contains `rankdir: "TB"`, `ranksep: 64`, `nodesep: 32`, `edgesep: 16`
    - graph-layout.ts imports dagre from `@dagrejs/dagre` and returns nodes with a position {x,y} (does not mutate inputs)
    - graph-nodes.tsx defines six node components and exports a `nodeTypes` map with keys entity_type, entity_type_field, entity_instance, email_component, email, knowledge_node (verify asserts `export const nodeTypes` is present)
    - graph-nodes.tsx contains the role-color classes bg-primary/10, bg-muted/60, bg-violet-500/10, bg-amber-500/10, bg-slate-100/60, bg-primary/15 (one per node type); the verify gate greps for the entity_instance `bg-violet-500/10` and email_component `bg-amber-500/10` markers specifically so a wrong/missing role-color is caught before the 11-03 human gate, and asserts NO occurrence of `font-medium`
    - knowledge_node component contains the glow class `shadow-[0_0_8px_hsl(164_39%_22%/0.25)]` (verify greps the `shadow-[0_0_8px` prefix)
    - apps/web tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    A pure dagre TB layout util + six on-brand custom node components compile against the UI-SPEC dimensions/colors,
    with the critical structural strings (nodeTypes export, knowledge-node glow, violet/amber role-colors, no font-medium) grep-asserted.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: /knowledge route (server page) + ssr:false client island consuming api.knowledge.graph</name>
  <read_first>
    - apps/web/src/app/entities/page.tsx (server-component wrapper + metadata convention — the structural analog for page.tsx)
    - apps/web/src/app/entities/_components/entities-gallery.tsx ("use client" + tRPC useQuery + local state idiom for knowledge-graph.tsx)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-PATTERNS.md (page.tsx dynamic ssr:false block + knowledge-graph.tsx "use client"/import/useQuery/skeleton blocks)
    - .planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-UI-SPEC.md (Default Render Depth — ALWAYS render entity_type + entity_type_field taxonomy; instances hidden by default; knowledge_nodes appear only when count>0; Loading skeleton 3-up/5-below ghost arrangement; Graph Canvas Background dots / Controls / MiniMap)
    - graph-layout.ts + graph-nodes.tsx (this plan, Task 2 — the layout util + nodeTypes map this island consumes)
  </read_first>
  <action>
    Create page.tsx — a server component exporting `metadata` (title "Knowledge — Nauta") that renders
    `<KnowledgeGraph />` loaded via `const KnowledgeGraph = dynamic(() => import('./_components/knowledge-graph').then(m => m.KnowledgeGraph), { ssr: false, loading: () => <KnowledgeGraphSkeleton /> })`
    (D-08 client island — Canvas/WebGL break SSR). Import the skeleton from its own module so the
    server file stays light.

    Create knowledge-graph-skeleton.tsx — exported `KnowledgeGraphSkeleton` rendering the UI-SPEC
    Loading state: role="status" aria-busy="true" aria-label="Loading knowledge graph", 3 entity_type
    ghost rectangles (Skeleton h-12 w-40 rounded-lg) above 5 entity_type_field ghosts (Skeleton h-8 w-32
    rounded-md), no edges, static divs (not React Flow).

    Create knowledge-graph.tsx — a "use client" component that imports "@xyflow/react/dist/style.css",
    calls `api.knowledge.graph.useQuery({ importerId: undefined, includeInstances: false, includeEmails:
    false })` (D-12 importerId undefined — derived server-side), maps the returned { nodes, edges } into
    React Flow nodes (applying the per-type style via the Task-2 `nodeTypes` map) + styled edges (per the
    UI-SPEC Edge Visual Language: stroke/width/dashed/animated per edge type; ArrowClosed size 6 on
    instance/email edges only), runs the Task-2 dagre `layoutGraph` to position them, and renders
    `<ReactFlow nodes edges nodeTypes={nodeTypes}>` with `<Background variant="dots" color="hsl(var(--border))" gap={20} size={1}/>`,
    `<Controls/>`, `<MiniMap/>` (Controls + MiniMap styled className="border border-border/50
    bg-background/70 backdrop-blur-md rounded-md"; MiniMap nodeColor mapped to the role-color palette).
    Default render depth (UI-SPEC): the taxonomy (entity_type + entity_type_field) always renders so the
    page is never blank (D-02); instances/components/emails stay hidden until plan 11-03 wires the
    "Show instances" toggle; knowledge_node nodes appear automatically only when the query returns them.
    Wrap ReactFlow in a `<ReactFlowProvider>` and give the viewport role="application"
    aria-label="Knowledge graph". This task delivers a SCROLL/ZOOM-able taxonomy graph; the surrounding
    three-zone chrome (filter rail / detail pane / toolbar / banner / error+empty states) is plan 11-03.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npm run typecheck &amp;&amp; cd ../.. &amp;&amp; npm run web:build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/knowledge/page.tsx exports `metadata` and mounts the graph via `dynamic(...)` with `{ ssr: false, loading: ... }`
    - knowledge-graph.tsx starts with "use client", imports "@xyflow/react/dist/style.css", and calls `api.knowledge.graph.useQuery`
    - knowledge-graph.tsx passes `importerId: undefined` to the query (D-12 — not from a URL param)
    - knowledge-graph.tsx renders ReactFlow with `<Background variant="dots" .../>`, `<Controls/>`, `<MiniMap/>` and applies the Task-2 `nodeTypes` map
    - knowledge-graph-skeleton.tsx exports `KnowledgeGraphSkeleton` with role="status" aria-busy="true" aria-label="Loading knowledge graph"
    - `npm run web:build` (api-client build + next build) exits 0 and the /knowledge route compiles
  </acceptance_criteria>
  <done>
    /knowledge renders the taxonomy graph from api.knowledge.graph as a dynamic ssr:false client island with
    dagre layout + custom nodes/edges; web:build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/web | New third-party packages (@xyflow/react, @dagrejs/dagre) enter the build/runtime |
| browser → /knowledge tRPC | Read-only query input crosses to the 11-01 router (mitigated there: T-11-01/02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering (supply chain) | @xyflow/react + @dagrejs/dagre installs | mitigate | Blocking human-verify gate (Task 0, FIRST in document + execution order) before install: confirm each package on npmjs.com (xyflow is the maintained successor to react-flow, ~100KB; dagrejs/dagre is the maintained dagre fork) — both are RESEARCH-named, mainstream graph libs, but no RESEARCH.md Package Legitimacy table exists for this phase, so treat as [ASSUMED] and verify. Checkpoint is never auto-approvable. |
| T-11-03 | Denial of service | unbounded graph render on a large importer | mitigate | Default render depth = taxonomy only (includeInstances/includeEmails=false); heavy ranks render only on explicit request (toggle lands in 11-03 with the "may slow rendering" warning). React Flow is right-sized for dozens-to-hundreds of nodes (D-07). |
| T-11-04 | Elevation / unauthorized write | a write surface introduced in the UI | accept (verified absent) | Read-only (D-09): the page only calls api.knowledge.graph.useQuery; no mutation, no node CRUD UI, no edges-table write. |

## Package Legitimacy Gate

| Package | Status | Verify at |
|---------|--------|-----------|
| @xyflow/react | [ASSUMED] | npmjs.com/package/@xyflow/react |
| @dagrejs/dagre | [ASSUMED] | npmjs.com/package/@dagrejs/dagre |
</threat_model>

<verification>
- apps/web tsc --noEmit exit 0; `npm run web:build` exit 0; /knowledge route present in the build output.
- @xyflow/react + @dagrejs/dagre installed (single deduped @dagrejs/dagre); Knowledge live in the sidebar, removed from Soon.
- Taxonomy renders from api.knowledge.graph (D-02 — never blank); no write surface (D-09).
</verification>

<success_criteria>
- /knowledge loads a dagre-laid-out React Flow taxonomy graph as an ssr:false client island (D-07/D-08).
- The six custom node types + edge styles render per UI-SPEC; the page is never blank (D-02).
- Knowledge is a live sidebar link; web:build green.
</success_criteria>

<output>
Create `.planning/phases/11-knowledge-node-graph-view-4e-knowledge-graph/11-02-SUMMARY.md` when done.
</output>

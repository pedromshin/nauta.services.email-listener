---
phase: 31-recall-measurement
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - packages/db/migrations/0028_autofill_retrieval_events.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/scripts/verify-0028-live.ts
  - packages/db/src/schema/autofill-retrieval-events.ts
  - apps/email-listener/app/domain/ports/autofill_retrieval_event_repository.py
  - apps/email-listener/app/domain/entities/autofill_retrieval_event.py
  - apps/email-listener/app/infrastructure/supabase/autofill_retrieval_event_repository.py
  - apps/email-listener/app/application/use_cases/autofill.py
  - apps/email-listener/app/container.py
  - packages/db/scripts/retrieval-miss-rate.ts
  - packages/db/scripts/RETRIEVAL-MISS-RATE.md
  - apps/email-listener/tests/test_autofill_instrumentation.py
autonomous: true
requirements: [RECALL-02]
must_haves:
  truths:
    - "Every autofill run persists one instrumentation record of its retrieval outcome"
    - "The record captures seed hits (example ids/scores/count), what alias-identifier context was injected (entity_instance_id + counts), routing_reason, and created_at"
    - "An instrumentation-write failure never breaks autofill (best-effort)"
    - "Human-correction linkage is derivable at query time by joining corrected_fields against the events (no event mutation)"
    - "A retrieval-miss rate is computable from the persisted records and returns a number over seeded data"
  artifacts:
    - path: "packages/db/migrations/0028_autofill_retrieval_events.sql"
      provides: "autofill_retrieval_events table + RLS deny-all"
      contains: "CREATE TABLE"
    - path: "packages/db/scripts/verify-0028-live.ts"
      provides: "Live pg verification of the table + RLS against local Postgres"
    - path: "apps/email-listener/app/infrastructure/supabase/autofill_retrieval_event_repository.py"
      provides: "Best-effort event writer"
    - path: "packages/db/scripts/retrieval-miss-rate.ts"
      provides: "Computable miss-rate query/report over persisted events"
    - path: "packages/db/scripts/RETRIEVAL-MISS-RATE.md"
      provides: "Written-down miss definition (the stage-3 go/no-go gate)"
  key_links:
    - from: "apps/email-listener/app/application/use_cases/autofill.py"
      to: "AutofillRetrievalEventRepository.save"
      via: "best-effort instrumentation write per run"
      pattern: "retrieval_event"
    - from: "packages/db/scripts/retrieval-miss-rate.ts"
      to: "autofill_retrieval_events + extraction records (corrected_fields)"
      via: "query-time join on component_id"
      pattern: "corrected_fields"
---

<objective>
Persist one best-effort instrumentation record per autofill run (seed hits, injected
alias/identifier context, routing_reason) and ship a committed artifact that computes a
retrieval-miss rate from that data (RECALL-02) — the concrete number that gates the deferred
stage-3 BFS work (KGX-01..03).

Purpose: The design case cites a measurement-gated architecture decision: build graph-expand ONLY
if a real retrieval-miss rate justifies it. This plan makes that answerable with a number, not a vibe.
Output: Migration 0028 (new event table, RLS deny-all, live-verified), a best-effort event repo
wired into the autofill use case, and a miss-rate query/report + written miss definition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-recall-measurement/31-CONTEXT.md
@.planning/phases/31-recall-measurement/31-01-PLAN.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Migration style (packages/db/migrations/):
  - CREATE TABLE IF NOT EXISTS with uuid PK `DEFAULT gen_random_uuid()` (see 0021_genui_generation_events.sql).
  - RLS deny-all: ENABLE ROW LEVEL SECURITY + RESTRICTIVE deny-all policies for anon AND authenticated,
    `USING (false) WITH CHECK (false)`, idempotent DROP POLICY IF EXISTS first (see 0020_knowledge_node_edges_rls.sql).
  - Statements separated by `--> statement-breakpoint`.
  - Register in meta/_journal.json: append entry `{ idx: 28, version: "7", when: <ms>, tag: "0028_autofill_retrieval_events", breakpoints: true }` (last idx is 27).

Live verify script (packages/db/scripts/verify-0027-live.ts is the template):
  - Uses `pg.Client({ connectionString: env.POSTGRES_URL_NON_POOLING })`, queries information_schema, exit 0/1.
  - Run via `npm run with-env -- tsx scripts/verify-0028-live.ts`. Local Postgres apply: `npm run migrate:local`.

AutofillUseCase.execute (app/application/use_cases/autofill.py after 31-01):
  - Has `examples` (RetrievedExample dicts w/ score), resolved `entity_context` (aliases/identifiers +
    entity_instance_id logged), `routing_reason`, `component_id`, `importer_id`, `entity_type.id`.
  - Best-effort posture reference: confirm_region.py synthesis hook (try/except log-and-swallow, structlog).

Correction signal for query-time join: `corrected_fields` on ExtractionRecord / ConfirmRegionUseCase.execute.

container.py: add the event-repo provider + bind it into `_provide_autofill_use_case` (lines ~229-252).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migration 0028 — autofill_retrieval_events table, RLS deny-all, live-verify</name>
  <files>packages/db/migrations/0028_autofill_retrieval_events.sql, packages/db/migrations/meta/_journal.json, packages/db/scripts/verify-0028-live.ts, packages/db/src/schema/autofill-retrieval-events.ts</files>
  <read_first>
    - packages/db/migrations/0021_genui_generation_events.sql (CREATE TABLE shape + indexes)
    - packages/db/migrations/0020_knowledge_node_edges_rls.sql (RESTRICTIVE deny-all policy pattern)
    - packages/db/migrations/meta/_journal.json (last entry idx=27, tag 0027)
    - packages/db/scripts/verify-0027-live.ts (verify-script template)
    - packages/db/src/schema/entity-instances.ts (Drizzle schema style for the ORM-side type)
  </read_first>
  <action>
    Hand-write `0028_autofill_retrieval_events.sql`: `CREATE TABLE IF NOT EXISTS
    "autofill_retrieval_events"` with columns — `id uuid PK DEFAULT gen_random_uuid()`,
    `component_id uuid NOT NULL`, `importer_id uuid`, `entity_type_id uuid`,
    `seed_hits jsonb` (example ids/scores/count), `seed_hit_count integer NOT NULL DEFAULT 0`,
    `injected_entity_instance_id uuid`, `injected_alias_count integer NOT NULL DEFAULT 0`,
    `injected_identifier_count integer NOT NULL DEFAULT 0`, `routing_reason text NOT NULL`,
    `created_at timestamptz NOT NULL DEFAULT now()`. Add btree indexes on `component_id`, `importer_id`,
    `created_at`. Append RLS: ENABLE ROW LEVEL SECURITY + RESTRICTIVE deny-all policies for anon AND
    authenticated (`USING (false) WITH CHECK (false)`), idempotent DROP POLICY IF EXISTS first —
    copy 0020's exact wording. Separate statements with `--> statement-breakpoint`. Register the
    migration in `meta/_journal.json` (idx 28). Add the matching Drizzle table in
    `src/schema/autofill-retrieval-events.ts` (schema source of truth; do not rely on the live DB for
    types). Write `scripts/verify-0028-live.ts` by copying `verify-0027-live.ts` and asserting: the
    table exists, each expected column has the expected udt/nullable, AND `rowsecurity=true` for the
    table (query `pg_tables`/`pg_class.relrowsecurity`). Apply locally via `npm run migrate:local`,
    then run the verify script.
  </action>
  <verify>
    <automated>cd packages/db && npm run migrate:local && npm run with-env -- tsx scripts/verify-0028-live.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `verify-0028-live.ts` prints "VERIFICATION PASSED" and exits 0 against local Postgres (postgresql://postgres:postgres@localhost:54322/postgres).
    - Source assertion: `grep -c "RESTRICTIVE FOR ALL" packages/db/migrations/0028_autofill_retrieval_events.sql` returns 2 (anon + authenticated deny-all).
    - Source assertion: meta/_journal.json contains an entry with tag `0028_autofill_retrieval_events`.
    - Live assertion: the verify script confirms `relrowsecurity = true` for `autofill_retrieval_events`.
  </acceptance_criteria>
  <done>Table + RLS deny-all applied to local Postgres and live-verified; journal + Drizzle schema updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Best-effort instrumentation write — one event per autofill run</name>
  <files>apps/email-listener/app/domain/entities/autofill_retrieval_event.py, apps/email-listener/app/domain/ports/autofill_retrieval_event_repository.py, apps/email-listener/app/infrastructure/supabase/autofill_retrieval_event_repository.py, apps/email-listener/app/application/use_cases/autofill.py, apps/email-listener/app/container.py, apps/email-listener/tests/test_autofill_instrumentation.py</files>
  <read_first>
    - apps/email-listener/app/application/use_cases/autofill.py (post-31-01: examples, entity_context, routing_reason, ids available near the end of execute)
    - apps/email-listener/app/application/use_cases/confirm_region.py (best-effort try/except log-and-swallow posture)
    - apps/email-listener/app/infrastructure/supabase/entity_instance_repository.py (Supabase repo `.table(...).insert(...)` style + row mapping)
    - apps/email-listener/app/container.py (provider + `_provide_autofill_use_case` wiring)
  </read_first>
  <behavior>
    - Test (AsyncMock port): after `execute`, `AutofillRetrievalEventRepository.save` is called exactly once with an event carrying component_id, importer_id, entity_type_id, seed_hit_count == len(examples), injected_entity_instance_id + alias/identifier counts (matching the injected entity_context), and routing_reason.
    - Test (few-shot case): seed_hits carries per-example score/count from the retrieved examples.
    - Test (cold-start case): seed_hit_count == 0 and injected counts == 0 when nothing was retrieved/injected.
    - Test (best-effort isolation): when `save` raises, `execute` still returns the AutofillResult and logs a warning — instrumentation NEVER breaks autofill.
  </behavior>
  <action>
    Add a frozen `AutofillRetrievalEvent` domain entity (fields mirroring the 0028 columns) and an
    `AutofillRetrievalEventRepository` domain Protocol with `async def save(event) -> None`. Implement
    `SupabaseAutofillRetrievalEventRepository` inserting into `autofill_retrieval_events` (parameterized
    via the supabase client, mirror existing repo insert style). In `AutofillUseCase.__init__` accept an
    optional `retrieval_events: AutofillRetrievalEventRepository | None = None`; at the END of `execute`
    (after the ExtractionRecord save) build the event from the run's examples (ids/scores/count),
    injected entity_context (entity_instance_id + alias/identifier counts), and routing_reason, and call
    `save` inside a try/except that logs a warning and swallows (mirror confirm_region synthesis hook —
    an instrumentation failure must NEVER break autofill). Do NOT mutate any existing record and do NOT
    add a second write path for corrections (correction linkage is query-time only, Task 3). Add the
    Supabase provider and pass it through `_provide_autofill_use_case` in container.py. Keep the use case
    importing domain ports only (lint-imports).
  </action>
  <verify>
    <automated>cd apps/email-listener && python -m pytest tests/test_autofill_instrumentation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: test proves exactly one `save` per `execute` with the correct seed/injection fields for both few-shot and cold-start cases.
    - Behavior assertion: best-effort test proves a raising `save` does not propagate (execute returns a result).
    - Source assertion: `grep -n "retrieval_event" apps/email-listener/app/application/use_cases/autofill.py` shows the write; no in-place mutation of prior events.
    - Test command: `pytest tests/test_autofill_instrumentation.py` passes; `ruff`/`mypy`/lint-imports clean.
  </acceptance_criteria>
  <done>Every autofill run writes one best-effort instrumentation event; failures isolated; container wired.</done>
</task>

<task type="auto">
  <name>Task 3: Retrieval-miss-rate artifact + written miss definition</name>
  <files>packages/db/scripts/retrieval-miss-rate.ts, packages/db/scripts/RETRIEVAL-MISS-RATE.md</files>
  <read_first>
    - packages/db/scripts/verify-0027-live.ts (pg.Client connection + env pattern to reuse)
    - packages/db/migrations/0028_autofill_retrieval_events.sql (the event columns from Task 1)
    - apps/email-listener/app/domain/entities/extraction_record.py (the `corrected_fields` field used for the join)
  </read_first>
  <action>
    Write `RETRIEVAL-MISS-RATE.md` documenting the miss definition next to the artifact: a MISS =
    (a) a run that HAD retrieval context (seed_hits or injected entity_context) yet the human
    subsequently corrected the autofilled field(s) for that component, OR (b) a run whose retrieval
    returned nothing for a component a human later hand-filled. State this is the stage-3 (KGX-01..03)
    go/no-go gate. Write `retrieval-miss-rate.ts`: a pg query that joins `autofill_retrieval_events`
    to the extraction/confirm records ON `component_id`, evaluates `corrected_fields` presence for the
    correction signal, and prints total runs, miss count, and the miss rate as a number (0..1) plus a
    breakdown by miss type (a vs b). No event mutation — correction linkage is derived purely at query
    time. Run via `npm run with-env -- tsx scripts/retrieval-miss-rate.ts`. The script must succeed and
    print a numeric rate even when the event table is empty (report 0 runs, rate defined as 0 or N/A —
    document which).
  </action>
  <verify>
    <automated>cd packages/db && npm run with-env -- tsx scripts/retrieval-miss-rate.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: the script prints a numeric retrieval-miss rate (e.g. `miss_rate=0.0`) over the persisted events and exits 0.
    - Source assertion: `retrieval-miss-rate.ts` joins events to corrections via `corrected_fields` at query time (grep `corrected_fields`); no UPDATE/mutation of events.
    - Doc assertion: `RETRIEVAL-MISS-RATE.md` contains the written miss definition (both miss types) and names it the stage-3 go/no-go gate.
    - Behavior assertion: after seeding one event row + one corrected extraction for the same component (manual insert in the script's own smoke path or a fixture), the script counts that run as a miss and the rate reflects it (a number).
  </acceptance_criteria>
  <done>A committed query/report computes the retrieval-miss rate as a number over persisted data, with the miss definition written down beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autofill run -> new event write path | Instrumentation is a new write; must be isolated from the autofill critical path |
| autofill_retrieval_events table -> external clients | Deny-all RLS keeps anon/authenticated from reading tenant retrieval metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Denial of Service (availability) | instrumentation write in autofill.py | mitigate | Best-effort try/except log-and-swallow — an event-write failure never breaks autofill (mirrors confirm_region synthesis hook) |
| T-31-05 | Information Disclosure (cross-tenant read) | autofill_retrieval_events | mitigate | RESTRICTIVE RLS deny-all for anon + authenticated; live-verified `relrowsecurity=true`; service-role writer bypasses by design |
| T-31-06 | Tampering (SQL) | retrieval-miss-rate.ts / event insert | mitigate | Parameterized pg queries / supabase client params only — no string-interpolated SQL |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (uses existing `pg`/`tsx`); Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `npm run migrate:local` applies 0028 and `verify-0028-live.ts` prints VERIFICATION PASSED (RLS on).
- `pytest tests/test_autofill_instrumentation.py` passes; one event per run; best-effort isolation proven.
- `retrieval-miss-rate.ts` prints a numeric miss rate over seeded data.
- `ruff`/`mypy`/lint-imports clean on edited Python; no event mutation for correction linkage.
</verification>

<success_criteria>
Every autofill run persists one best-effort instrumentation record (seed hits, injected context,
routing_reason), human-correction linkage is derivable at query time via `corrected_fields`, and a
committed artifact computes the retrieval-miss rate as a number — the stage-3 BFS go/no-go gate — with
the miss definition written down beside it.
</success_criteria>

<output>
Create `.planning/phases/31-recall-measurement/31-02-SUMMARY.md` when done.
</output>

---
phase: 51-total-ui-re-skin
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/knowledge/_components/knowledge-graph.tsx
  - apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx
  - apps/web/src/app/chat/_hooks/use-conversation-controller.ts
  - apps/web/src/app/chat/_hooks/__tests__/use-conversation-controller-invalidate.test.ts
  - .planning/todos/pending/2026-07-09-knowledge-cache-invalidation-gap.md
autonomous: true
requirements: [RSKN-07]
must_haves:
  truths:
    - "The /knowledge promote path (promoteEdge) invalidates knowledge.expandNode in addition to knowledge.byId + knowledge.graph"
    - "The chat-driven promotion terminal (handleTerminal on a widget-submit continuation) invalidates knowledge.byId + knowledge.graph + knowledge.expandNode"
    - "Bound genui panels and knowledge-preview nodes refresh tier data immediately after a promotion, not only after staleTime elapses"
  artifacts:
    - path: "apps/web/src/app/knowledge/_components/knowledge-graph.tsx"
      provides: "promoteEdge extended to invalidate expandNode"
      pattern: "expandNode.invalidate"
    - path: "apps/web/src/app/chat/_hooks/__tests__/use-conversation-controller-invalidate.test.ts"
      provides: "Regression test proving the chat-driven promotion path invalidates all three knowledge.* keys"
      exports: []
  key_links:
    - from: "apps/web/src/app/chat/_hooks/use-conversation-controller.ts"
      to: "utils.knowledge.{byId,graph,expandNode}.invalidate"
      via: "handleTerminal widget-submit continuation"
      pattern: "knowledge\\.(byId|graph|expandNode)\\.invalidate"
---

<objective>
Close the RSKN-07 cache-invalidation gap (todo 2026-07-09-knowledge-cache-invalidation-gap,
resolves_phase 51) — a FUNCTIONAL fix, not styling. Today the BIND-02 invalidation contract only
covers the `/knowledge` page promote button, and even that never invalidates `knowledge.expandNode`.
Two paths show stale tier data for up to ~10s (staleTime) after a promotion: (1) chat-driven
promotion via the confirm_action widget (`handleTerminal`) invalidates only `chat.*`; (2)
`knowledge.expandNode` (the KnowledgePreviewNode data source) is invalidated by NO promotion path.
Extend both to invalidate the three `knowledge.*` query keys, with a regression test on the widget
path mirroring the existing Phase-33 invalidation test.

Purpose: RSKN-07 — knowledge-canvas cache invalidation extends to chat-driven promotions + expandNode.
Output: Both promotion paths invalidate knowledge.byId + knowledge.graph + knowledge.expandNode; two
regression tests; todo closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/todos/pending/2026-07-09-knowledge-cache-invalidation-gap.md

Existing test to mirror (the pattern):
@apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx

<interfaces>
From knowledge-graph.tsx (research read):
  export type PromoteEdgeUtils = {
    readonly knowledge: {
      readonly byId: { readonly invalidate: () => void };
      readonly graph: { readonly invalidate: () => void };
      // MISSING today: expandNode
    };
  };
  export async function promoteEdge(edgeId, importerId, utils): Promise<{ok, errorMessage?}>
    // lines ~181-182 invalidate byId + graph AFTER a successful ok response only.
  // expandNode is a tRPC .query fetched lazily via utils.knowledge.expandNode.fetch (line ~424).

From use-conversation-controller.ts (research read):
  handleTerminal (line ~345) currently:
    utils.chat.getHistory.invalidate({ conversationId })     // ~348
    utils.chat.sessionCost.invalidate({ conversationId })    // ~351
    utils.chat.getWidgetInteractions.invalidate({ conversationId })  // ~354
  // fires on the widget-submit continuation turn (Phase 40 chat-driven promotion) but never touches knowledge.*
</interfaces>

<constraints>
- Do NOT change the staleTime self-healing mechanism itself (UI-SPEC per-surface checklist) — only ADD
  event-driven invalidation.
- Preserve the existing contract: invalidation fires ONLY after a successful promote (the !ok branch
  must still NOT invalidate — the existing test's second/third cases must stay green).
- Immutability + explicit typing (CLAUDE.md): extend PromoteEdgeUtils type explicitly; no `any`.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend promoteEdge to invalidate expandNode (knowledge-graph.tsx)</name>
  <files>apps/web/src/app/knowledge/_components/knowledge-graph.tsx, apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx</files>
  <read_first>
    - apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx (the full existing pattern — makeUtils shape, the 3 cases)
    - apps/web/src/app/knowledge/_components/knowledge-graph.tsx (PromoteEdgeUtils type ~146, promoteEdge ~156-182, expandNode usage ~424)
  </read_first>
  <behavior>
    - RED first: extend `makeUtils()` in the existing test to add `expandNode: { invalidate: vi.fn() }`
      under `knowledge`, and assert in the success case that `utils.knowledge.expandNode.invalidate`
      is called exactly once (alongside byId + graph). This must FAIL against current code.
    - The two !ok cases must continue to assert `expandNode.invalidate` was NOT called (early return).
  </behavior>
  <action>
    Extend the `PromoteEdgeUtils` type to add `readonly expandNode: { readonly invalidate: () => void }`
    under `knowledge`. In `promoteEdge`, after the successful-response `byId.invalidate()` +
    `graph.invalidate()` calls, add `utils.knowledge.expandNode.invalidate()` (same success-only
    branch — never in the !ok path). Update the real call site(s) of `promoteEdge` in this file to pass
    `expandNode` from the tRPC utils object (`utils.knowledge.expandNode`). Keep the change minimal and
    typed — no `any`. GREEN: the extended test passes.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web/src && grep -n "expandNode.invalidate" app/knowledge/_components/knowledge-graph.tsx` matches.
    - `cd apps/web && npx vitest run src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx` passes (incl. the new expandNode success assertion AND the two !ok cases still asserting no invalidation).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>promoteEdge invalidates byId + graph + expandNode on success only; test extended and green; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat-driven promotion — invalidate knowledge.* in handleTerminal + regression test + close todo</name>
  <files>apps/web/src/app/chat/_hooks/use-conversation-controller.ts, apps/web/src/app/chat/_hooks/__tests__/use-conversation-controller-invalidate.test.ts, .planning/todos/pending/2026-07-09-knowledge-cache-invalidation-gap.md</files>
  <read_first>
    - apps/web/src/app/chat/_hooks/use-conversation-controller.ts (handleTerminal ~345-358; the utils object shape; how it knows a turn was a widget-submit continuation)
    - apps/web/src/app/knowledge/_components/__tests__/knowledge-graph-invalidate.test.tsx (the mock-utils + assert-invalidate pattern to mirror)
  </read_first>
  <behavior>
    - RED first: a new test that drives the terminal path (extract the invalidation logic into a
      testable exported helper if `handleTerminal` isn't directly reachable — mirror how
      knowledge-graph.tsx exports `promoteEdge` as a standalone for testing) with a mocked utils object
      exposing `chat.*` + `knowledge.{byId,graph,expandNode}.invalidate` spies. Assert that on a
      widget-submit continuation terminal, all three `knowledge.*` invalidate spies fire (and the
      existing `chat.*` invalidations still fire). This must FAIL before the code change.
  </behavior>
  <action>
    In `handleTerminal` (or a small exported helper it delegates to, extracted for testability per the
    promoteEdge precedent), add `utils.knowledge.byId.invalidate()`, `utils.knowledge.graph.invalidate()`,
    and `utils.knowledge.expandNode.invalidate()` alongside the existing `chat.*` invalidations. Per the
    todo's proposed solution, these may fire unconditionally on the terminal (the queries are cheap and
    staleTime-guarded) OR be gated to the widget-submit continuation — choose unconditional-on-terminal
    only if simpler and it does not add invalidations on ordinary non-promotion turns beyond the cheap
    staleTime-guarded refetch (Claude's Discretion; document the choice). Do not change the staleTime
    mechanism. Then move the todo `.planning/todos/pending/2026-07-09-knowledge-cache-invalidation-gap.md`
    to `.planning/todos/done/` with a one-line resolution note pointing at this plan.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_hooks/__tests__/use-conversation-controller-invalidate.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web/src && grep -nE "knowledge\.(byId|graph|expandNode)\.invalidate" app/chat/_hooks/use-conversation-controller.ts` returns at least 3 matches.
    - `cd apps/web && npx vitest run src/app/chat/_hooks/__tests__/use-conversation-controller-invalidate.test.ts` passes (asserts all three knowledge.* keys invalidate on the widget-submit terminal AND chat.* still invalidate).
    - Todo cleared: `test ! -f .planning/todos/pending/2026-07-09-knowledge-cache-invalidation-gap.md`.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Chat-driven promotion terminal invalidates all three knowledge.* keys; regression test green; todo moved to done; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client cache → server | React Query invalidation triggers refetch of already-authorized tRPC queries |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07-A | Information Disclosure | knowledge.* invalidation | mitigate | Invalidation only re-runs the SAME authenticated tRPC queries the user already loads (byId/graph/expandNode) with the same inputs — it widens NO data exposure and adds no new query surface; assert no new query keys are introduced. |
| T-51-07-B | Denial of Service | extra invalidations on every terminal | accept | Queries are cheap + staleTime-guarded (todo rationale); worst case is a bounded refetch on turn settle — no unbounded fan-out. |
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | Zero new deps — assert no `package.json` diff. |
</threat_model>

<verification>
- Both new/extended tests green; the existing knowledge-graph-invalidate !ok cases still green.
- `cd apps/web && npx tsc --noEmit` green.
- Grep confirms the three knowledge.* invalidations in both promotion paths.
- Both RSKN-07 todos-of-record moved to done.
</verification>

<success_criteria>
Both promotion paths (/knowledge promote button + chat-driven widget promotion) invalidate
knowledge.byId + knowledge.graph + knowledge.expandNode on success; two regression tests prove it;
staleTime mechanism unchanged; no widened data exposure; todo closed.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-06-SUMMARY.md` when done.
</output>

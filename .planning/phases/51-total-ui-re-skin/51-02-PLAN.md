---
phase: 51-total-ui-re-skin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx
  - apps/web/src/app/emails/[id]/_components/extraction-summary-panel.tsx
  - apps/web/src/app/emails/[id]/_components/inspector-panel.tsx
  - apps/web/src/app/emails/[id]/_components/role-picker.tsx
  - apps/web/src/app/emails/[id]/_components/layers-tree-row.tsx
  - apps/web/src/app/emails/[id]/_components/active-parent-banner.tsx
  - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx
autonomous: true
requirements: [RSKN-02, RSKN-05]
must_haves:
  truths:
    - "Region/entity type-coding in email detail renders through the color.graph.* closed palette (violet→graph-entity, amber→graph-email-component, slate→graph-email)"
    - "confirm-deny-controls' deny button carries text-destructive-foreground (no raw text-white) and the filled-semantic hover/active recipe"
    - "Email detail contains zero classic Tailwind palette classes and zero raw hex"
  artifacts:
    - path: "apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx"
      provides: "Region overlay coding on graph.* tokens (was 17 palette occurrences)"
      pattern: "graph-(entity|email-component|email)"
  key_links:
    - from: "apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx"
      to: "--graph-entity / --graph-email-component / --graph-email"
      via: "semantic Tailwind alias"
      pattern: "graph-(entity|email-component|email)"
---

<objective>
Execute the D-49-03 palette-class conversion map across the email-detail surface — the heaviest
off-token cluster the Phase-48 scout found (~30 occurrences, top offender `region-overlay-box.tsx`
at 17). Land region/entity type-coding on the `color.graph.*` CLOSED palette (the tokens designed
with the SAME violet/amber/slate semantics as the region coding — this is the palette landing on its
intended consumers, not a reinterpretation), convert neutrals to semantic aliases, and apply the
D-48-06 hover/active convention. Refinement pass, no layout change (D-49-01).

Purpose: RSKN-02 (email detail re-skin) + RSKN-05 (token discipline holds on this surface).
Output: Email-detail components on graph.*/semantic tokens, zero palette classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md

Design contracts (binding):
@apps/web/docs/design/hover-active-convention.md

Exemplars to imitate (already correct):
@apps/web/src/components/provenance-link.tsx

<conversion_map>
D-49-03 target aliases (from 51-UI-SPEC.md "palette-class conversion map"):
- `violet-*`        → `graph-entity` / `graph-entity-foreground`
- `amber-*`         → `graph-email-component` / `-foreground` when coding a REGION/COMPONENT TYPE
                      (classify by what it codes; confidence/candidate semantics take `tier-*`, but
                      no confidence badges live in THIS file set — email detail is region-type coding)
- `slate-*`/`gray-*`/`zinc-*` neutrals → `background`/`card`/`foreground`/`muted-foreground` per
                      structural role (Claude's Discretion: page bg→background, row/card bg→card,
                      body text→foreground, secondary text→muted-foreground)
- `bg-white`/`text-white` → `bg-background`/`text-background`, or on a filled semantic surface the
                      matching `-foreground` (deny button in confirm-deny-controls.tsx:72 sits on
                      `bg-destructive` → `text-destructive-foreground`)
If a region kind has NO graph.* alias, use the nearest semantic alias and LOG a deferred item —
DO NOT mint a new alias (Phase 48 closed the token surface).
</conversion_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert region/entity type-coding — region-overlay-box (17x) + extraction-summary + inspector</name>
  <files>apps/web/src/app/emails/[id]/_components/{region-overlay-box,extraction-summary-panel,inspector-panel}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "D-49-03 palette-class conversion map" table (exact target aliases) + "Extended tokens" table (graph.* CLOSED set values)
    - Each of the 3 files (read once; enumerate every palette occurrence + what it codes in a single pass)
    - packages/ui/tailwind.config.ts / apps/web/tailwind.config — confirm `graph-entity`/`graph-email-component`/`graph-email` + `-foreground` families are registered utilities (they are, 48-02)
  </read_first>
  <action>
    In `region-overlay-box.tsx` (17 occurrences + 3 bg-white), convert each palette class per the
    map: violet family → `graph-entity`(+`-foreground`), amber family → `graph-email-component`
    (+`-foreground`), slate/gray/zinc neutrals → `background`/`card`/`foreground`/`muted-foreground`
    by structural role, `bg-white`/`text-white` → the semantic equivalent for the surface it sits on.
    Preserve opacity modifiers (e.g. `/10`, `/20`) by re-attaching them to the alias
    (`bg-graph-entity/10`). Repeat for `extraction-summary-panel.tsx` (4) and `inspector-panel.tsx`
    (3), classifying each occurrence by what it codes (region/entity TYPE → graph.*; never mint a new
    alias — log a deferred item if a kind has no graph.* home). Do not reorder or restructure any
    content; classes only. If the same class combination now duplicates 3+ times, extract a small
    local constant (Claude's Discretion) rather than repeating.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero palette classes in the 3 files: `cd apps/web/src && grep -rnE "(bg|text|border|ring|from|via|to|fill|stroke|decoration|outline|divide)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}|(bg|text|border|fill|stroke)-white\b|(bg|text|border)-black\b" "app/emails/[id]/_components/region-overlay-box.tsx" "app/emails/[id]/_components/extraction-summary-panel.tsx" "app/emails/[id]/_components/inspector-panel.tsx"` returns nothing.
    - graph.* aliases now present: `cd apps/web/src && grep -rnE "graph-(entity|email-component|email)" "app/emails/[id]/_components/region-overlay-box.tsx"` matches.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All region/entity coding in the 3 files renders through graph.*/semantic aliases; zero palette classes; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining email-detail files + deny-button + hover/active + surface grep gate</name>
  <files>apps/web/src/app/emails/[id]/_components/{role-picker,layers-tree-row,active-parent-banner,confirm-deny-controls}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → conversion map + "Interactive-State Contract" (filled-semantic recipe) + "Color" (destructive reserved for deny/irreversible; stop/cancel use muted — never relabel)
    - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx (UI-SPEC filled-semantic exemplar; line 72 `text-white` on `bg-destructive`)
    - Each of the 4 files (read once, enumerate occurrences)
  </read_first>
  <action>
    Convert `role-picker.tsx` (3), `layers-tree-row.tsx` (3 palette + 1 bg-white),
    `active-parent-banner.tsx` (1) per the D-49-03 map (violet→graph-entity, amber→
    graph-email-component, neutrals→semantic). In `confirm-deny-controls.tsx`, change the deny
    button's `text-white` (line ~72) to `text-destructive-foreground` and confirm the filled-semantic
    hover/active recipe is complete: confirm button `bg-success hover:bg-success/90 active:bg-success/80
    text-success-foreground`, deny button `bg-destructive hover:bg-destructive/90 active:bg-destructive/80
    text-destructive-foreground` — this control is the register's exemplar, so it must be exactly on
    pattern. Do NOT relabel deny→destructive semantics onto any stop/cancel affordance (locked
    convention). Add `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1` to any
    interactive element in these 4 files missing it. Then run the surface-wide palette grep across ALL
    seven email-detail files this plan owns (both tasks) — it must be zero.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero palette classes across ALL 7 email-detail files: `cd apps/web/src && grep -rnE "(bg|text|border|ring|from|via|to|fill|stroke|decoration|outline|divide)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}|(bg|text|border|fill|stroke)-white\b|(bg|text|border)-black\b" "app/emails/[id]/_components/region-overlay-box.tsx" "app/emails/[id]/_components/extraction-summary-panel.tsx" "app/emails/[id]/_components/inspector-panel.tsx" "app/emails/[id]/_components/role-picker.tsx" "app/emails/[id]/_components/layers-tree-row.tsx" "app/emails/[id]/_components/active-parent-banner.tsx" "app/emails/[id]/_components/confirm-deny-controls.tsx"` returns nothing.
    - Deny button uses the token foreground: `cd apps/web/src && grep -n "text-destructive-foreground" "app/emails/[id]/_components/confirm-deny-controls.tsx"` matches; `grep -n "text-white" "app/emails/[id]/_components/confirm-deny-controls.tsx"` returns nothing.
    - Contrast + registration gates green: `cd apps/web && npx vitest run src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts`.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All 7 email-detail files are palette-clean; confirm/deny controls on exact filled-semantic recipe; contrast + registration gates green; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Presentational class conversion only; no new input path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | Zero vendoring — no `npm install`/`shadcn add`; assert no `package.json` diff. |
| T-51-02-A | Information Disclosure | region-overlay data | accept | No data-layer change — overlay reads the same props; only CSS classes change. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` green.
- Surface-wide palette/raw-hex grep over all 7 files returns zero.
- token-contrast + token-registration gates green.
- Before-state = `.planning/ui-reviews/2026-07-11T04-32-30-989Z/emails-{desktop,mobile}.png`; after-pixels validated by 51-07 re-capture.
</verification>

<success_criteria>
Email-detail region/entity coding lands on the graph.* closed palette; neutrals on semantic aliases;
deny/confirm on the filled-semantic token recipe; zero palette classes; contrast/registration gates
green; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-02-SUMMARY.md` when done.
</output>

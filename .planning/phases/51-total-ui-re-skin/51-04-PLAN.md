---
phase: 51-total-ui-re-skin
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/knowledge/_components/graph-toolbar.tsx
  - apps/web/src/app/knowledge/_components/filter-rail.tsx
  - apps/web/src/app/knowledge/_components/node-detail-pane.tsx
  - apps/web/src/app/knowledge/_components/taxonomy-banner.tsx
  - .planning/todos/pending/2026-07-07-knowledge-preexisting-ui-debt.md
autonomous: true
requirements: [RSKN-03]
must_haves:
  truths:
    - "No /knowledge chrome surface uses backdrop-blur — all four violations are solid bg-background/95"
    - "The graph-toolbar grid-layout control renders a lucide-react LayoutGrid icon, not the raw ⊞ glyph"
    - "Tier/graph badges on /knowledge remain on their TOKN-04/05 tokens (confirmed unchanged)"
    - "edge-detail-popover content order (header/Relation/Tier/Confidence) is unchanged"
  artifacts:
    - path: "apps/web/src/app/knowledge/_components/graph-toolbar.tsx"
      provides: "Solid toolbar (no blur) + LayoutGrid lucide icon (glyph replaced)"
      contains: "LayoutGrid"
  key_links:
    - from: "apps/web/src/app/knowledge/_components/graph-toolbar.tsx"
      to: "lucide-react"
      via: "icon import"
      pattern: "LayoutGrid"
---

<objective>
Clear the pre-existing `/knowledge` UI debt (todo 2026-07-07-knowledge-preexisting-ui-debt,
resolves_phase 51) and finish the RSKN-03 re-skin: burn down the four remaining glassmorphism-ban
violations (`backdrop-blur-md` → solid `bg-background/95`), swap the raw `⊞` glyph for a
`lucide-react` `LayoutGrid` icon, apply the D-48-06 hover/active convention to the toolbar/filter-rail
controls, and CONFIRM (do not re-touch) that the tier/graph badges already sit on TOKN-04/05 tokens
(48-02..04). The `edge-detail-popover` content order is UI-SPEC LOCKED — restyle classes only, never
reorder.

Purpose: RSKN-03 — /knowledge canvas re-skinned + pre-existing UI debt cleared.
Output: De-glassed /knowledge chrome, lucide grid icon, todo moved to done.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md
@.planning/todos/pending/2026-07-07-knowledge-preexisting-ui-debt.md

Design contracts (binding):
@apps/web/docs/design/product-register-and-bans.md
@apps/web/docs/design/hover-active-convention.md

Exemplars (already correct — CONFIRM, do not re-touch):
@apps/web/src/app/knowledge/_components/tier-filter-control.tsx
@apps/web/src/app/knowledge/_components/tier-edge-style.ts

<constraints>
- DO NOT touch `apps/web/src/app/knowledge/_components/knowledge-graph.tsx` — owned by 51-06 (RSKN-07).
- `edge-detail-popover.tsx` content order (header / Relation / Tier / Confidence) is UI-SPEC LOCKED —
  do NOT open it for reorder; if it needs a class touch it is restyle-only (it has no palette debt per
  scout, so leave it unless a class drift is found — and it is NOT in this plan's files_modified).
- D-49-07: locked-renderer boundary holds; no packages/genui changes.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glassmorphism burn-down (4 files) + ⊞ → lucide LayoutGrid</name>
  <files>apps/web/src/app/knowledge/_components/{graph-toolbar,filter-rail,node-detail-pane,taxonomy-banner}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Glassmorphism-Ban Burn-down" table (exact file/line/target rows) + D-49-06
    - apps/web/docs/design/product-register-and-bans.md item 3 (blur ban) + v1.4 conversation-rail precedent
    - Each of the 4 files (read once; locate the `backdrop-blur-md` line + graph-toolbar.tsx:73 `⊞`)
  </read_first>
  <action>
    In each of `graph-toolbar.tsx` (~42), `filter-rail.tsx` (~96), `node-detail-pane.tsx` (~373),
    `taxonomy-banner.tsx` (~46): replace `bg-background/70 backdrop-blur-md` with solid
    `bg-background/95` (remove the `backdrop-blur-md` utility entirely — the v1.4 precedent). In
    `graph-toolbar.tsx` (~73), replace the raw `⊞` Unicode glyph with a `lucide-react` `LayoutGrid`
    icon (`import { LayoutGrid } from "lucide-react"`), sized/styled to match the adjacent toolbar
    icons. Do not restructure the toolbar or panes — classes/icon swap only.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero backdrop-blur across all 4 files: `cd apps/web/src && grep -rn "backdrop-blur" app/knowledge/_components/graph-toolbar.tsx app/knowledge/_components/filter-rail.tsx app/knowledge/_components/node-detail-pane.tsx app/knowledge/_components/taxonomy-banner.tsx` returns nothing.
    - Solid surface present: `cd apps/web/src && grep -rn "bg-background/95" app/knowledge/_components/graph-toolbar.tsx app/knowledge/_components/filter-rail.tsx app/knowledge/_components/node-detail-pane.tsx app/knowledge/_components/taxonomy-banner.tsx` returns 4 matches.
    - No raw glyph: `cd apps/web/src && grep -n "⊞" app/knowledge/_components/graph-toolbar.tsx` returns nothing; `grep -n "LayoutGrid" app/knowledge/_components/graph-toolbar.tsx` matches.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All 4 blur violations solid; ⊞ replaced with lucide LayoutGrid; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Hover/active on toolbar+filter controls, confirm tier/graph badges, grep gate, close todo</name>
  <files>apps/web/src/app/knowledge/_components/{graph-toolbar,filter-rail}.tsx, .planning/todos/pending/2026-07-07-knowledge-preexisting-ui-debt.md</files>
  <read_first>
    - 51-UI-SPEC.md → "Interactive-State Contract" + the pinned-state exception (tier-filter-control precedent)
    - apps/web/src/app/knowledge/_components/tier-filter-control.tsx (confirm — already correct, the pinned-state exemplar)
  </read_first>
  <action>
    Apply the D-48-06 hover/active convention to the interactive controls in `graph-toolbar.tsx` and
    `filter-rail.tsx` (neutral/ghost family: `hover:bg-accent hover:text-accent-foreground` +
    `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1`; any persistent
    selected/pinned segment reasserts rather than intensifies on hover — the documented exception).
    CONFIRM ONLY (read, do not edit): tier badges + node/edge type coding already consume TOKN-04/05
    tokens (48-02..04) — if a badge/edge is genuinely drifting off its token, note it, but the
    expectation per the UI-SPEC per-surface checklist is "confirm, don't re-touch." Run the
    /knowledge palette grep. Finally, move the todo file
    `.planning/todos/pending/2026-07-07-knowledge-preexisting-ui-debt.md` to `.planning/todos/done/`
    (or mark it done per repo convention) with a one-line resolution note pointing at this plan.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero palette classes across the 4 knowledge chrome files: `cd apps/web/src && grep -rnE "(bg|text|border|ring)-(slate|gray|zinc|neutral|violet|amber|emerald|green|red|blue|indigo)-[0-9]{2,3}|(bg|text|border)-white\b|(bg|text|border)-black\b" app/knowledge/_components/graph-toolbar.tsx app/knowledge/_components/filter-rail.tsx app/knowledge/_components/node-detail-pane.tsx app/knowledge/_components/taxonomy-banner.tsx` returns nothing.
    - At least one control each in graph-toolbar.tsx and filter-rail.tsx has `hover:bg-accent` + `focus-visible:ring-ring` (grep).
    - Contrast + registration gates green: `cd apps/web && npx vitest run src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts`.
    - Todo cleared: `test ! -f .planning/todos/pending/2026-07-07-knowledge-preexisting-ui-debt.md` (moved to done/).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Toolbar/filter controls carry hover/active; tier/graph badges confirmed on tokens; palette grep clean; gates green; UI-debt todo moved to done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Presentational-only; icon swap uses existing lucide-react dep |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | `LayoutGrid` is from the already-installed `lucide-react` — no new dep; assert no `package.json` diff. |
| T-51-04-A | Tampering | knowledge-graph.tsx boundary | accept | This plan does not open knowledge-graph.tsx (51-06 owns it) — no functional-seam collision. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` green.
- No backdrop-blur, no ⊞ glyph, palette grep clean across the 4 files.
- token-contrast + token-registration gates green.
- Before-state = `.planning/ui-reviews/2026-07-11T04-32-30-989Z/knowledge-{desktop,mobile}.png`; after-pixels validated by 51-07.
</verification>

<success_criteria>
/knowledge chrome de-glassed (4 files solid), raw ⊞ replaced with lucide LayoutGrid, hover/active
applied, tier/graph badges confirmed on TOKN-04/05 tokens, edge-detail-popover order untouched, gates
green, pre-existing UI-debt todo closed.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-04-SUMMARY.md` when done.
</output>

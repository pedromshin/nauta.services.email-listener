---
phase: 51-total-ui-re-skin
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/_components/entity-chips.tsx
  - apps/web/src/app/entities/_components/entities-table.tsx
  - apps/web/src/app/entities/_components/entities-mosaic.tsx
  - apps/web/src/app/entities/_components/entities-gallery.tsx
  - apps/web/src/app/entities/[id]/_components/entity-detail.tsx
  - apps/web/src/app/entities/[id]/_components/entity-fields.tsx
autonomous: true
requirements: [RSKN-02, RSKN-06, RSKN-05]
must_haves:
  truths:
    - "Inbox entity chips render on color.graph.entity + radius.pill (rounded-pill), not raw violet-* + rounded-md"
    - "/entities/[id] StatusBadge renders confirmed→tier-extracted and candidate→tier-inferred (was bg-primary/10 + raw amber-*)"
    - "The /entities pages carry the palette conversion (light-touch, no redesign per D-49-04) and are palette-clean"
  artifacts:
    - path: "apps/web/src/app/_components/entity-chips.tsx"
      provides: "Entity chip on graph-entity + rounded-pill (closes backlog 999.16 inbox half)"
      pattern: "graph-entity|rounded-pill"
    - path: "apps/web/src/app/entities/[id]/_components/entity-detail.tsx"
      provides: "StatusBadge on the color.tier.* ladder (closes backlog 999.16 entities half)"
      pattern: "tier-extracted|tier-inferred"
  key_links:
    - from: "apps/web/src/app/_components/entity-chips.tsx"
      to: "--graph-entity / --radius-pill"
      via: "semantic Tailwind alias"
      pattern: "graph-entity"
---

<objective>
Close backlog 999.16 — the two off-token chip/badge stragglers the Phase-48 audit named: land inbox
entity chips on `color.graph.entity` + `radius.pill` (RSKN-06 inbox half), and `/entities/[id]`'s
`StatusBadge` on the `color.tier.*` ladder (RSKN-06 entities half). Also apply the D-49-03 palette
conversion to the `/entities` list pages (D-49-04: light-touch, palette conversion + register check
only — these pages are NOT named by RSKN-01..04 and get no structural attention). The inbox
three-pane itself is an exemplar (already correct) — confirm-only, do not restructure.

Purpose: RSKN-06 (both stragglers), RSKN-02 (inbox chips half), RSKN-05 (token discipline holds).
Output: Entity chips + StatusBadge on their purpose-built tokens; /entities pages palette-clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md

Design contracts (binding):
@apps/web/docs/design/hover-active-convention.md

Exemplars (already correct — DO NOT touch/restructure):
@apps/web/src/app/_components/inbox-three-pane.tsx
@apps/web/src/components/provenance-link.tsx

<interfaces>
StatusBadge current shape — entity-detail.tsx:66-81 (from research read):
  confirmed → `bg-primary/10 text-primary border-primary/20` (raw)
  candidate → `border-amber-200 bg-amber-50 text-amber-700 dark:bg-amber-950/40 dark:text-amber-300` + `bg-amber-400` dot (raw)
Target (51-UI-SPEC "Extended tokens" tier row):
  confirmed → `tier-extracted` pair (bg-tier-extracted / text-tier-extracted-foreground)
  candidate → `tier-inferred` pair (bg-tier-inferred / text-tier-inferred-foreground)
radius.pill: registered Tailwind key `rounded-pill` (--radius-pill: 9999px). Imitate
provenance-link.tsx's CHIP_CLASS_NAME (`rounded-pill`), NOT `rounded-full`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inbox entity chips → graph-entity + rounded-pill + hover/active (RSKN-06 inbox)</name>
  <files>apps/web/src/app/_components/entity-chips.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Extended tokens" (radius.pill + graph.* rows; the entity-chips note about rounded-pill vs the shared Badge default rounded-md)
    - apps/web/src/components/provenance-link.tsx (CHIP_CLASS_NAME — the pill neutral-ghost target recipe)
    - apps/web/src/app/_components/entity-chips.tsx (read fully — 5 palette occurrences; note whether it wraps the shared Badge primitive)
  </read_first>
  <action>
    Convert the 5 `violet-*` occurrences in `entity-chips.tsx` to `graph-entity`
    (+`graph-entity-foreground`), preserving opacity modifiers. Ensure the chip renders with
    `rounded-pill` (radius.pill): if the chip uses the shared `Badge` primitive (default
    `rounded-md`), add an explicit `rounded-pill` override on the call site — do NOT modify the shared
    `Badge` primitive this phase (Registry Safety). Apply the neutral-ghost hover/active recipe if the
    chip is interactive (`hover:bg-accent hover:text-accent-foreground` + `focus-visible:ring-2
    focus-visible:ring-ring focus-visible:ring-offset-1`); if the chip is a static tag, no hover is
    needed — do not invent interactivity. Match the ProvenanceLink pill pattern.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web/src && grep -nE "graph-entity" app/_components/entity-chips.tsx` matches AND `grep -nE "rounded-pill" app/_components/entity-chips.tsx` matches.
    - Zero palette classes: `cd apps/web/src && grep -nE "(bg|text|border|ring)-(violet|amber|slate|gray|zinc|emerald|green|red|blue)-[0-9]{2,3}|(bg|text|border)-white\b" app/_components/entity-chips.tsx` returns nothing.
    - Shared Badge primitive untouched: `git status --porcelain packages/ui/src/badge.tsx` returns nothing.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Inbox entity chips render on graph-entity + rounded-pill (RSKN-06 inbox half closed); shared Badge untouched; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge → tier ladder + /entities pages palette conversion (RSKN-06 entities, D-49-04)</name>
  <files>apps/web/src/app/entities/[id]/_components/{entity-detail,entity-fields}.tsx, apps/web/src/app/entities/_components/{entities-table,entities-mosaic,entities-gallery}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Extended tokens" tier row (StatusBadge target: confirmed→tier-extracted, candidate→tier-inferred) + conversion map + D-49-04 (entities light-touch, no redesign)
    - apps/web/src/app/entities/[id]/_components/entity-detail.tsx (StatusBadge at lines 66-81 — the interfaces block above has its current class shape)
    - Each of the 5 files (read once; enumerate palette occurrences — table 5, mosaic 4, detail 2, fields 1)
  </read_first>
  <action>
    In `entity-detail.tsx` `StatusBadge`, replace the `confirmed` branch `bg-primary/10 text-primary
    border-primary/20` with the `tier-extracted` pair (`bg-tier-extracted text-tier-extracted-foreground`
    + matching border) and the `candidate` branch `amber-*` classes (incl. the `bg-amber-400` dot) with
    the `tier-inferred` pair (`bg-tier-inferred text-tier-inferred-foreground`, dot `bg-tier-inferred`).
    This is confidence-tier semantics → `tier-*`, NOT `graph-email-component` (per the UI-SPEC's
    explicit entity-detail note). For the `/entities` LIST pages (`entities-table`, `entities-mosaic`,
    `entities-gallery`) and `entity-fields.tsx`, apply the D-49-03 map (violet→graph-entity,
    amber→graph-email-component for type-coding OR tier-* for confidence — classify each occurrence,
    neutrals→semantic) — palette conversion + register check ONLY, no structural changes (D-49-04). Log
    a deferred item for any kind with no token home (never mint an alias). Then run the surface-wide
    palette grep across all 6 files this plan owns — it must be zero.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge on the tier ladder: `cd apps/web/src && grep -nE "tier-extracted|tier-inferred" "app/entities/[id]/_components/entity-detail.tsx"` matches; `grep -nE "amber-|bg-primary/10" "app/entities/[id]/_components/entity-detail.tsx"` returns nothing.
    - Zero palette classes across all 6 files: `cd apps/web/src && grep -rnE "(bg|text|border|ring|from|via|to|fill|stroke)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}|(bg|text|border)-white\b|(bg|text|border)-black\b" "app/_components/entity-chips.tsx" "app/entities/_components/entities-table.tsx" "app/entities/_components/entities-mosaic.tsx" "app/entities/_components/entities-gallery.tsx" "app/entities/[id]/_components/entity-detail.tsx" "app/entities/[id]/_components/entity-fields.tsx"` returns nothing.
    - Contrast + registration gates green: `cd apps/web && npx vitest run src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts`.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>StatusBadge on tier-extracted/tier-inferred (RSKN-06 entities half closed); /entities pages palette-clean (D-49-04 light-touch); contrast/registration gates green; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Presentational class conversion only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | Zero vendoring; shared `Badge` primitive NOT modified — assert no `packages/ui` diff. |
| T-51-03-A | Tampering | shared `@polytoken/ui/badge` | accept | `rounded-pill` applied at call site, not on the shared primitive — avoids un-vetted shared change. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` green.
- Surface-wide palette grep over all 6 files returns zero.
- token-contrast + token-registration gates green.
- Before-state = `.planning/ui-reviews/2026-07-11T04-32-30-989Z/inbox-{desktop,mobile}.png`; after-pixels validated by 51-07.
</verification>

<success_criteria>
Backlog 999.16 closed: inbox entity chips on graph-entity + rounded-pill, /entities/[id] StatusBadge
on the tier ladder; /entities pages palette-clean (light-touch); gates green; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-03-SUMMARY.md` when done.
</output>

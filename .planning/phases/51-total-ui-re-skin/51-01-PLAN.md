---
phase: 51-total-ui-re-skin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/chat/_components/composer.tsx
  - apps/web/src/app/chat/_components/message-list.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
  - apps/web/src/app/chat/_components/tool-round-activity-row.tsx
  - apps/web/src/app/chat/_components/tool-invocation-result-row.tsx
  - apps/web/src/app/chat/_components/turn-action-row.tsx
  - apps/web/src/app/chat/_components/conversation-row.tsx
  - apps/web/src/app/chat/_components/jump-to-bottom-button.tsx
  - apps/web/src/app/chat/_canvas/chat-node.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/chat/_canvas/save-status-indicator.tsx
  - apps/web/src/app/chat/_canvas/chat-canvas-view-toggle.tsx
  - apps/web/src/components/app-sidebar.tsx
  - apps/web/src/app/layout.tsx
autonomous: true
requirements: [RSKN-01]
must_haves:
  truths:
    - "The /chat composer send control, tool-round activity rows, and citation chips carry the D-48-06 hover/active + focus-visible classes"
    - "The app sidebar renders as a solid surface (no backdrop-blur) and every nav item has a hover/active state"
    - "Chat + global chrome contain zero classic Tailwind palette classes and zero raw hex"
    - "Citation chips render via the ProvenanceLink rounded-pill neutral-ghost recipe wherever they appear"
  artifacts:
    - path: "apps/web/src/components/app-sidebar.tsx"
      provides: "Solid sidebar surface + hover/active nav (glassmorphism ban cleared, D-49-08)"
      contains: "bg-background/95"
    - path: "apps/web/src/app/chat/_components/composer.tsx"
      provides: "Send affordance with focus-visible ring + hover/active"
  key_links:
    - from: "apps/web/src/app/chat/_components/tool-invocation-result-row.tsx"
      to: "apps/web/src/components/provenance-link.tsx"
      via: "citation chip render"
      pattern: "ProvenanceLink"
---

<objective>
Re-skin the `/chat` interactive surfaces (composer, message stream, tool-round activity rows,
citation chips, canvas chrome) and the global chrome (app sidebar, root layout + Toaster) in the
polytoken register: apply the D-48-06 hover/active + focus-visible convention to every interactive
element, clear the last glassmorphism-ban violation on the sidebar (`app-sidebar.tsx:117`), and
confirm the copy register holds. This is a refinement pass (D-49-01) — no layout restructuring, no
new components.

Purpose: RSKN-01 — `/chat` speaks the polytoken register on extended tokens; global chrome
(cross-cutting RSKN-01..04) carries the same convention.
Output: Chat + chrome surfaces with hover/active convention applied, sidebar de-glassed, zero
palette classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md

Design contracts (binding):
@apps/web/docs/design/hover-active-convention.md
@apps/web/docs/design/product-register-and-bans.md

Exemplars to imitate (already correct — DO NOT "fix" these):
@apps/web/src/components/provenance-link.tsx
@apps/web/src/app/chat/_canvas/save-status-indicator.tsx

<constraints>
- DO NOT touch `apps/web/src/app/chat/_hooks/use-conversation-controller.ts` — owned by 51-06 (RSKN-07).
- DO NOT touch `apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx` this plan.
- D-49-07: `packages/genui/src/renderer/*` stays byte-identical; do not touch renderer or generation
  artifacts. Chat re-skin lands only in `apps/web` consumer components.
- D-49-02: consume token aliases only (`hsl(var(--x))` / semantic Tailwind utilities); no pack value edits.
- Icons stay `lucide-react`-only (D-49-08).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat interactive surfaces — hover/active + citation-chip + copy register pass</name>
  <files>apps/web/src/app/chat/_components/{composer,message-list,message-turn,tool-round-activity-row,tool-invocation-result-row,turn-action-row,conversation-row,jump-to-bottom-button}.tsx, apps/web/src/app/chat/_canvas/{chat-node,genui-panel-node,save-status-indicator,chat-canvas-view-toggle}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Interactive-State Contract (D-48-06)" + "Color" (primary reserved for CTA/active only) + "Copywriting Contract" table
    - apps/web/docs/design/hover-active-convention.md (the binding recipe + pinned-state exception)
    - apps/web/src/components/provenance-link.tsx (CHIP_CLASS_NAME: rounded-pill + bg-muted/hover:bg-accent neutral-ghost recipe — the citation-chip target pattern)
    - Each file being modified (read once, extract every interactive element + its current classes in one pass)
  </read_first>
  <action>
    Classify every interactive element in these files into one of the two D-48-06 families before
    writing any class: (a) neutral/ghost — resting `bg-muted`/`bg-background` + `text-muted-foreground`
    → hover `hover:bg-accent hover:text-accent-foreground`; (b) filled-semantic — resting `bg-{alias}` +
    `text-{alias}-foreground` → hover `bg-{alias}/90` → active `bg-{alias}/80`. Add
    `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1` to any interactive
    element missing it (orthogonal, always present). The composer send control is the primary CTA
    (filled `bg-primary` family); tool-round activity rows + jump-to-bottom + view-toggle are
    neutral/ghost. Confirm citation chips (`tool-invocation-result-row.tsx`) render through
    `ProvenanceLink` and inherit its `rounded-pill` neutral-ghost recipe — if any chip is hand-rolled
    with `rounded-full`/`rounded-md`, route it through `ProvenanceLink` or match its exact class set.
    Copy register check ONLY (D-49-01 — no new copy): confirm `chat-home-empty-state` reads "Ask me
    anything" and `canvas-empty-state` reads "Panels will appear here" (both already present — do not
    edit); if any visible string in these files carries infrastructure vocabulary ("node/pipeline/
    daemon/compute/mesh") fix it to the warm first-person register per brand-guide.md §2. Preserve the
    `conversation-row.tsx` `isActive` primary-selected state and any pinned/selected suppression of the
    hover intensify step (documented exception). Do not restructure layout.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero palette classes in the touched chat files: `cd apps/web/src && grep -rnE "(bg|text|border|ring|from|via|to|fill|stroke|decoration|outline|divide)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}|(bg|text|border|fill|stroke)-white\b|(bg|text|border)-black\b" app/chat/_components app/chat/_canvas | grep -vE "knowledge-preview-node|use-conversation-controller"` returns nothing.
    - `cd apps/web && npx tsc --noEmit` exits 0.
    - The composer send control and at least the tool-round-activity-row + jump-to-bottom-button each contain `focus-visible:ring-ring` and a `hover:` class (grep each file).
    - Empty-state strings unchanged: `grep -rn "Ask me anything" app/chat/_components/chat-home-empty-state.tsx` and `grep -rn "Panels will appear here" app/chat/_canvas/canvas-empty-state.tsx` both match.
  </acceptance_criteria>
  <done>Every interactive element in the named chat files carries the correct D-48-06 family classes + focus-visible ring; citation chips use the ProvenanceLink pill recipe; typecheck green; zero palette classes.</done>
</task>

<task type="auto">
  <name>Task 2: Global chrome — sidebar de-glass + hover/active + Toaster/layout register</name>
  <files>apps/web/src/components/app-sidebar.tsx, apps/web/src/app/layout.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Glassmorphism-Ban Burn-down" table (app-sidebar.tsx:117 row) + D-49-08
    - apps/web/docs/design/product-register-and-bans.md item 3 (glassmorphism ban)
    - apps/web/src/components/app-sidebar.tsx (read fully — locate line 117 backdrop-blur + every nav item)
    - apps/web/src/app/layout.tsx (Toaster mount at line 46, import from @polytoken/ui/sonner)
  </read_first>
  <action>
    In `app-sidebar.tsx`, replace `bg-background/70 backdrop-blur-md` (line ~117) with a solid
    `bg-background/95` surface (no blur) per the v1.4 conversation-rail precedent (D-49-08). Apply the
    D-48-06 hover/active convention to every nav item / interactive sidebar control: neutral/ghost
    family (resting `text-muted-foreground` → `hover:bg-accent hover:text-accent-foreground`), with the
    active/selected nav item on the `bg-primary`/selected treatment and its hover reasserting (not
    intensifying) per the pinned-state exception. Add `focus-visible:ring-2 focus-visible:ring-ring
    focus-visible:ring-offset-1` where missing. Icons stay `lucide-react` (D-49-08) — do not introduce
    inline SVG. In `layout.tsx`, this is a register CONFIRM only: verify the `<Toaster />` needs no
    palette-class override (the sonner primitive is token-based); do NOT edit the shared
    `@polytoken/ui/sonner` primitive — if the Toaster visibly drifts from tokens, log it as a deferred
    item rather than modifying the shared package this phase (Registry Safety: no shared-primitive
    changes without the vetting gate). Do not restructure the layout tree.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No `backdrop-blur` remains in app-sidebar: `cd apps/web/src && grep -n "backdrop-blur" components/app-sidebar.tsx` returns nothing; `grep -n "bg-background/95" components/app-sidebar.tsx` matches.
    - Zero palette classes / raw white-black in chrome: `cd apps/web/src && grep -nE "(bg|text|border|ring)-(slate|gray|zinc|neutral|violet|amber|emerald|green|red|blue)-[0-9]{2,3}|(bg|text|border)-white\b|(bg|text)-black\b" components/app-sidebar.tsx app/layout.tsx` returns nothing.
    - At least one nav item in app-sidebar.tsx contains `hover:bg-accent` and `focus-visible:ring-ring` (grep).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Sidebar renders solid (no blur) with hover/active + focus-visible on nav items; layout/Toaster confirmed token-clean; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure presentational change; no new input crosses any boundary this plan |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | Zero vendoring this phase (Registry Safety); no `shadcn add`/`npm install` runs — assert no `package.json` diff on chrome/chat files. |
| T-51-01-A | Tampering | shared `@polytoken/ui` primitives | accept | Toaster/sonner primitive NOT edited (drift logged, not modified) — avoids un-vetted change to a shared surface. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` green.
- Palette/raw-hex greps over all files_modified return zero.
- Sidebar blur removed; hover/active + focus-visible present on interactive elements.
- Surface before-state = `.planning/ui-reviews/2026-07-11T04-32-30-989Z/{chat,login}-{desktop,mobile}.png`; after-pixels validated by the Wave-2 full re-capture (51-07).
</verification>

<success_criteria>
Chat interactive surfaces + global chrome carry the D-48-06 hover/active + focus-visible convention;
sidebar glassmorphism cleared; zero palette classes / raw hex; ProvenanceLink pill recipe holds for
citation chips; copy register confirmed; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-01-SUMMARY.md` when done.
</output>

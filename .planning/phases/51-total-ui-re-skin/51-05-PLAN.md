---
phase: 51-total-ui-re-skin
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/studio/page.tsx
  - apps/web/src/app/studio/_components/studio-tabs.tsx
  - apps/web/src/app/studio/_components/generation-state-chrome.tsx
  - apps/web/src/app/studio/_components/history-island.tsx
  - apps/web/src/app/studio/_components/page-ideas-island.tsx
  - apps/web/src/app/studio/_components/catalog-browser-island.tsx
  - apps/web/src/app/settings/forwarding/page.tsx
  - apps/web/src/app/_components/forwarding-address-card.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/login/_components/google-signin-button.tsx
autonomous: true
requirements: [RSKN-04]
must_haves:
  truths:
    - "/studio chrome interactive controls carry the D-48-06 hover/active + focus-visible convention"
    - "/settings/forwarding and its address card carry the polytoken register and are palette-clean"
    - "/login shows the brand-guide copy ('Welcome back to your workspace', 'Sign in with Google') and is palette-clean"
  artifacts:
    - path: "apps/web/src/app/login/page.tsx"
      provides: "Login card on the polytoken register (brand-guide §2 copy confirmed)"
      contains: "Welcome back to your workspace"
  key_links:
    - from: "apps/web/src/app/login/_components/google-signin-button.tsx"
      to: "--primary"
      via: "primary CTA token"
      pattern: "bg-primary"
---

<objective>
Re-skin `/studio`, `/settings/forwarding`, and `/login` in the polytoken register (RSKN-04): apply
the D-48-06 hover/active + focus-visible convention to interactive chrome, confirm the brand-guide
copy already landed on `/login` (D-49-09: re-skin what exists — no settings hub/index page), and hold
token discipline (these surfaces were scout-clean of palette classes; this is a hover/active +
register-confirm pass with a regression grep). Refinement pass, no layout change (D-49-01).

Purpose: RSKN-04 — /studio, /settings/*, /login re-skinned in the polytoken register.
Output: Three surfaces on the hover/active convention, copy register confirmed, palette-clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md

Design contracts (binding):
@apps/web/docs/design/hover-active-convention.md
@apps/web/docs/design/brand-guide.md

Exemplars (already correct — imitate, do not "fix"):
@apps/web/src/app/studio/_components/catalog-browser-island.tsx

<constraints>
- D-49-09: re-skin ONLY `/settings/forwarding` — do NOT create a settings hub/index page (deferred idea).
- D-49-02: consume token aliases only; no style-pack machinery edits (studio's pack machinery stays untouched).
- D-49-07: no packages/genui renderer changes.
- Icons stay lucide-react-only.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /studio chrome — hover/active + register pass</name>
  <files>apps/web/src/app/studio/page.tsx, apps/web/src/app/studio/_components/{studio-tabs,generation-state-chrome,history-island,page-ideas-island,catalog-browser-island}.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Interactive-State Contract" + "Color" (primary reserved for CTA/active) + Per-Surface checklist (/studio row: "token-class + hover/active pass on existing chrome; NOT layout/style-pack machinery")
    - apps/web/src/app/studio/_components/catalog-browser-island.tsx (exemplar — correct pattern)
    - Each file (read once; enumerate interactive elements + any palette drift)
  </read_first>
  <action>
    Apply the D-48-06 hover/active convention to interactive controls in the studio chrome
    (`studio-tabs` segments, `generation-state-chrome` controls, `history-island`/`page-ideas-island`
    row/button affordances): neutral/ghost segments → `hover:bg-accent hover:text-accent-foreground`
    with the active tab on the selected treatment (pinned-state exception: hover reasserts, not
    intensifies); any primary action → filled-primary recipe. Add `focus-visible:ring-2
    focus-visible:ring-ring focus-visible:ring-offset-1` where missing. Do NOT touch style-pack
    machinery, the code-island frames, or generation logic — chrome classes only. Convert any palette
    class found (scout expected ~0; regression-grep guards it).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero palette classes across the studio files: `cd apps/web/src && grep -rnE "(bg|text|border|ring)-(slate|gray|zinc|neutral|violet|amber|emerald|green|red|blue|indigo)-[0-9]{2,3}|(bg|text|border)-white\b|(bg|text|border)-black\b" app/studio/page.tsx app/studio/_components/studio-tabs.tsx app/studio/_components/generation-state-chrome.tsx app/studio/_components/history-island.tsx app/studio/_components/page-ideas-island.tsx app/studio/_components/catalog-browser-island.tsx` returns nothing.
    - At least one interactive control in studio-tabs.tsx carries `hover:bg-accent` + `focus-visible:ring-ring` (grep).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Studio chrome interactive controls on the hover/active convention; palette-clean; typecheck green; style-pack machinery untouched.</done>
</task>

<task type="auto">
  <name>Task 2: /settings/forwarding + /login — register/hover pass + brand-copy confirm + grep gate</name>
  <files>apps/web/src/app/settings/forwarding/page.tsx, apps/web/src/app/_components/forwarding-address-card.tsx, apps/web/src/app/login/page.tsx, apps/web/src/app/login/_components/google-signin-button.tsx</files>
  <read_first>
    - 51-UI-SPEC.md → "Copywriting Contract" table (login copy: "Welcome back to your workspace" / "Sign in with Google") + Per-Surface checklist (login/settings rows)
    - apps/web/docs/design/brand-guide.md §2 (voice register)
    - apps/web/src/app/login/page.tsx (CardTitle already "Welcome back to your workspace" per research) + google-signin-button.tsx (the primary CTA)
  </read_first>
  <action>
    `/settings/forwarding` + `forwarding-address-card.tsx`: apply the D-48-06 hover/active convention
    to interactive controls (copy-address button, any toggle) + `focus-visible` ring; convert any
    palette class found; confirm the copy carries the warm first-person register (no infrastructure
    vocabulary). `/login`: this is a register CONFIRM — verify the CardTitle reads "Welcome back to your
    workspace" and the Google button label reads "Sign in with Google" (both already present per
    research — do NOT rewrite copy). Confirm the Google sign-in button is on the `bg-primary` filled
    CTA recipe with hover `bg-primary/90` + `focus-visible:ring-ring`; fix only if drifting. Do NOT add
    a settings hub/index page (D-49-09). Run the palette grep across all 4 files.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Login copy intact: `cd apps/web/src && grep -n "Welcome back to your workspace" app/login/page.tsx` matches; `grep -rn "Sign in with Google" app/login` matches.
    - Google CTA on primary token: `cd apps/web/src && grep -nE "bg-primary" app/login/_components/google-signin-button.tsx` matches.
    - Zero palette classes across all 4 files: `cd apps/web/src && grep -rnE "(bg|text|border|ring)-(slate|gray|zinc|neutral|violet|amber|emerald|green|red|blue|indigo)-[0-9]{2,3}|(bg|text|border)-white\b|(bg|text|border)-black\b" app/settings/forwarding/page.tsx app/_components/forwarding-address-card.tsx app/login/page.tsx app/login/_components/google-signin-button.tsx` returns nothing.
    - Contrast + registration gates green: `cd apps/web && npx vitest run src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts`.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Forwarding + login on the register/hover convention; brand-guide copy confirmed; no settings hub created; palette-clean; gates green; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→OAuth | /login initiates Google OAuth — unchanged by this plan (chrome-only), noted for completeness |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm installs (supply chain) | mitigate | Zero vendoring; no new dep — assert no `package.json` diff. |
| T-51-05-A | Spoofing | Google sign-in button | accept | Chrome-only re-skin; the OAuth initiation/redirect logic is untouched — no auth-surface change. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` green.
- Palette grep clean across studio/settings/login files.
- token-contrast + token-registration gates green.
- Login brand copy confirmed present.
- Before-state = `.planning/ui-reviews/2026-07-11T04-32-30-989Z/{studio,forwarding,login}-{desktop,mobile}.png`; after-pixels validated by 51-07.
</verification>

<success_criteria>
/studio, /settings/forwarding, /login carry the polytoken register + hover/active convention; login
brand-guide copy confirmed; no settings hub created (D-49-09); palette-clean; gates green; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-05-SUMMARY.md` when done.
</output>

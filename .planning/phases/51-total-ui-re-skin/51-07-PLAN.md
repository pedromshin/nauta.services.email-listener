---
phase: 51-total-ui-re-skin
plan: 07
type: execute
wave: 2
depends_on: [51-01, 51-02, 51-03, 51-04, 51-05, 51-06]
files_modified:
  - apps/web/src/app/__tests__/palette-ban.test.ts
autonomous: true
requirements: [RSKN-05]
must_haves:
  truths:
    - "A committed palette-ban regression gate bans classic Tailwind palette classes across apps/web/src (excluding token sources + dev scratch) and passes green"
    - "The WCAG-AA contrast gate and the token-family registration gate stay green after the whole re-skin"
    - "The E2E regression suite (live-loop-green + uat-39/41/43/45/48) stays green — the re-skin broke nothing"
    - "All 16 authenticated surfaces re-captured and diffed against the 2026-07-11T04-32-30-989Z baseline show the token conversions with no layout regression"
  artifacts:
    - path: "apps/web/src/app/__tests__/palette-ban.test.ts"
      provides: "D-49-05 committed regression gate making RSKN-05 enforceable"
      contains: "dev"
  key_links:
    - from: "apps/web/src/app/__tests__/palette-ban.test.ts"
      to: "apps/web/src (excl globals.css, tailwind.config, src/app/dev/**)"
      via: "file-walk grep gate + allowlist"
      pattern: "allowlist"
---

<objective>
Land the RSKN-05 enforceability deliverable and run the phase-wide integration gates that only make
sense once every Wave-1 conversion is in: (1) author the D-49-05 committed palette-ban regression gate
(mirrors the `token-registration.test.ts` grep idiom) — banning classic Tailwind palette classes
across `apps/web/src`, excluding token sources + `src/app/dev/**` scratch, with an explicit inline
allowlist for justified cases — and make it pass green; (2) confirm the WCAG-AA contrast + token
registration gates stay green; (3) run the E2E regression suite to prove the re-skin broke nothing;
(4) re-capture all 16 authenticated surfaces and diff against the real-pixel baseline.

Purpose: RSKN-05 — token discipline holds and is ENFORCEABLE, not aspirational; phase-wide regression
+ full-surface visual evidence.
Output: Committed palette-ban gate (green), E2E regression pass, before/after screenshot diff.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-total-ui-re-skin/51-CONTEXT.md
@.planning/phases/51-total-ui-re-skin/51-UI-SPEC.md
@.claude/skills/polytoken-design-system/SKILL.md

Gate to mirror (grep idiom + registration precedent):
@apps/web/src/app/__tests__/token-registration.test.ts

<local_stack>
- Bring the stack up before E2E/screenshots: `scripts/preflight-local.ps1` (servers WITHOUT --reload;
  verify via DB/pixels not terminal — MEMORY: zombie-process ambiguity).
- E2E + screenshot harness use the seeded authenticated session (16 authenticated surfaces captured
  for real, LIVE-06).
- Real-pixel before-baseline (do NOT re-derive it): `.planning/ui-reviews/2026-07-11T04-32-30-989Z/`
  (login/inbox/chat/knowledge/studio×2-packs/forwarding/emails, each desktop+mobile).
</local_stack>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the D-49-05 palette-ban regression gate + make it green</name>
  <files>apps/web/src/app/__tests__/palette-ban.test.ts</files>
  <read_first>
    - 51-CONTEXT.md D-49-05 (exact exclusions: token sources globals.css + tailwind.config, src/app/dev/**, inline-allowlist mechanism) + 51-UI-SPEC.md "Verification Gates" (new-this-phase row)
    - apps/web/src/app/__tests__/token-registration.test.ts (the committed-vitest grep-idiom shape to mirror)
  </read_first>
  <behavior>
    - The gate walks `apps/web/src` (`.ts`/`.tsx`), greps each file for classic Tailwind palette
      classes — the family list `slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|
      emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose` with a numeric scale on any
      utility prefix (`bg|text|border|ring|from|via|to|fill|stroke|decoration|outline|divide|
      placeholder|caret|accent|shadow`), plus literal `-white`/`-black` on `bg|text|border|fill|stroke`.
    - EXCLUDES: `apps/web/src/app/globals.css`, the tailwind config, and `apps/web/src/app/dev/**`.
    - An explicit inline allowlist (an array of `{ file, line/pattern, reason }`) lets genuinely-justified
      occurrences pass — start it EMPTY (all production surfaces are converted by Wave 1).
    - Assert: the set of violations minus the allowlist is empty.
  </behavior>
  <action>
    Write `apps/web/src/app/__tests__/palette-ban.test.ts` as a committed vitest test implementing the
    behavior above (implementation shape — regex list vs file-walk, allowlist as array — is Claude's
    Discretion per 51-CONTEXT.md). Run it. If it reports any violation, that is a real straggler a
    Wave-1 plan missed: fix the offending file by converting the class per the D-49-03 map (do NOT add
    it to the allowlist unless it is genuinely justified — e.g. a token-source-internal literal — and
    document the reason inline). Note the pre-known `globals.css:242` `#000` mask literal is
    token-source-internal and lives in the EXCLUDED globals.css, so it is compliant and not converted.
    Re-run until green. Also run the two existing gates to confirm they stayed green through the whole
    phase.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/app/__tests__/palette-ban.test.ts src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/__tests__/palette-ban.test.ts` exists and `cd apps/web && npx vitest run src/app/__tests__/palette-ban.test.ts` passes with an empty (or fully-justified+documented) allowlist.
    - The test's exclusions cover globals.css, the tailwind config, and `src/app/dev/**` (grep the test file for `dev` + `globals`).
    - `cd apps/web && npx vitest run src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts` passes.
    - Independent confirmation of zero production violations: `cd apps/web/src && grep -rnE "(bg|text|border|ring|from|via|to|fill|stroke|decoration|outline|divide)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}|(bg|text|border|fill|stroke)-white\b|(bg|text|border)-black\b" app --include="*.tsx" --include="*.ts" | grep -v "/dev/"` returns nothing.
  </acceptance_criteria>
  <done>Committed palette-ban gate exists and is green; contrast + registration gates green; zero production palette classes outside token sources/dev scratch.</done>
</task>

<task type="auto">
  <name>Task 2: E2E regression run — prove the re-skin broke nothing</name>
  <files>(no source files authored — verification run; any fix lands in the offending Wave-1 file)</files>
  <read_first>
    - scripts/preflight-local.ps1 (stack-up procedure; servers WITHOUT --reload)
    - apps/web/e2e/ (live-loop-green.spec.ts + uat-39/41/43/45/48 specs — the suites that must stay green)
  </read_first>
  <action>
    Bring the local stack up via `scripts/preflight-local.ps1` (verify via DB/pixels, not the
    terminal — kill any zombie processes first per MEMORY). Run the E2E regression suite:
    `live-loop-green`, `uat-39-tool-round`, `uat-41-knowledge-preview`, `uat-43-auth`, `uat-45-threads`,
    `uat-48-token-surfaces`. These assert computed-style / behavior; the re-skin (token-class swaps +
    hover/active + RSKN-07 invalidation) must not regress any of them. If a spec fails because a token
    swap changed a resolved value it asserts on, fix the offending Wave-1 source file (not the test)
    unless the test's expectation is genuinely stale — document either way. uat-48-token-surfaces in
    particular proves chip pill radius + success/destructive + graph/tier edge colors resolve correctly:
    it must stay green after the conversions. uat-41-knowledge-preview exercises the KnowledgePreviewNode
    data path RSKN-07 touched — confirm it stays green.
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test e2e/live-loop-green.spec.ts e2e/uat-39-tool-round.spec.ts e2e/uat-41-knowledge-preview.spec.ts e2e/uat-43-auth.spec.ts e2e/uat-45-threads.spec.ts e2e/uat-48-token-surfaces.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six spec files pass (playwright exit 0). Record the pass/fail line per spec in the SUMMARY.
    - If any fix was required, the change landed in an `apps/web/src` source file (not a weakened test), documented in the SUMMARY.
  </acceptance_criteria>
  <done>The E2E regression suite (live-loop-green + uat-39/41/43/45/48) is green against the re-skinned stack; any regression fixed at source.</done>
</task>

<task type="auto">
  <name>Task 3: Full-surface screenshot re-capture + diff against the real-pixel baseline</name>
  <files>(no source files — evidence capture into .planning/ui-reviews/{new-timestamp}/)</files>
  <read_first>
    - 51-UI-SPEC.md "Verification Gates" (screenshot row: baseline `.planning/ui-reviews/2026-07-11T04-32-30-989Z/` is the literal before-state — do not re-derive it)
    - apps/web/playwright.screenshot.config.ts + apps/web/e2e/screenshot-review.spec.ts (the harness — 16 authenticated surfaces via seeded session)
  </read_first>
  <action>
    With the stack up + seeded session, run `npm run screenshot:review` (`playwright test --config
    playwright.screenshot.config.ts`) to capture all 16 authenticated surfaces (login/inbox/chat/
    knowledge/studio×2-packs/forwarding/emails, desktop+mobile) into a new
    `.planning/ui-reviews/{timestamp}/` directory. Diff each new capture against the same-named file in
    the before-baseline `.planning/ui-reviews/2026-07-11T04-32-30-989Z/`. Confirm per surface: (a) the
    intended token conversions are visible (e.g. email-detail region coding now on graph.* hues, entity
    chips pill-shaped, knowledge chrome solid not frosted, StatusBadge on tier hues); (b) NO layout
    regression, NO horizontal overflow, NO missing element vs baseline. Record a per-surface
    before→after row (surface | expected delta | observed | pass/regression) in the SUMMARY, citing both
    the baseline and new PNG paths. Any regression is fixed at source in the offending Wave-1 file, then
    re-captured.
  </action>
  <verify>
    <automated>cd apps/web && npm run screenshot:review</automated>
  </verify>
  <acceptance_criteria>
    - A new `.planning/ui-reviews/{timestamp}/` dir exists with all 16 authenticated surface PNGs: `ls .planning/ui-reviews/ | tail -1` shows a fresh timestamp and that dir contains chat/inbox/emails/knowledge/studio/forwarding/login desktop+mobile PNGs.
    - The SUMMARY contains a per-surface before→after table with an explicit pass/regression verdict per surface, citing baseline `2026-07-11T04-32-30-989Z/` paths + the new capture paths.
    - Zero surfaces marked regression (or every regression fixed-at-source + re-captured before finalizing).
  </acceptance_criteria>
  <done>All 16 authenticated surfaces re-captured and diffed against the real-pixel baseline; token conversions visible; no layout regression; per-surface verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Gate authoring + verification runs only; no product-surface behavior change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-SC | Tampering | npm/registry installs (supply chain) | mitigate | Zero vendoring this phase; the palette-ban gate itself uses only devDeps already present (vitest + node fs) — assert no `package.json` diff. |
| T-51-08-A | Repudiation | re-skin regressions slipping through | mitigate | Committed palette-ban gate + contrast/registration gates + E2E suite + full-surface pixel diff form the auditable evidence trail; screenshot dir gitignored but referenced in SUMMARY. |
</threat_model>

<verification>
- `cd apps/web && npx vitest run src/app/__tests__/palette-ban.test.ts src/app/__tests__/token-contrast.test.ts src/app/__tests__/token-registration.test.ts` all green.
- E2E: `npx playwright test e2e/live-loop-green.spec.ts e2e/uat-39-tool-round.spec.ts e2e/uat-41-knowledge-preview.spec.ts e2e/uat-43-auth.spec.ts e2e/uat-45-threads.spec.ts e2e/uat-48-token-surfaces.spec.ts` green.
- `npm run screenshot:review` produces a fresh 16-surface capture; per-surface diff vs baseline recorded, zero regressions.
</verification>

<success_criteria>
The D-49-05 palette-ban gate is committed and green (RSKN-05 enforceable); contrast + registration
gates green; the E2E regression suite green; all 16 authenticated surfaces re-captured and diffed
against the real-pixel baseline with no layout regression.
</success_criteria>

<output>
Create `.planning/phases/51-total-ui-re-skin/51-07-SUMMARY.md` when done.
</output>

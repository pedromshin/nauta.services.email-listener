---
phase: 05-review-ui-inbox-email-detail-with-document-preview-and-entit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-client/src/router/emails/detail.ts
  - packages/api-client/src/router/emails/index.ts
  - packages/api-client/src/geometry.ts
  - packages/api-client/src/geometry.test.ts
  - packages/api-client/src/index.ts
  - packages/api-client/package.json
  - packages/api-client/vitest.config.ts
autonomous: true
requirements: ["Decision-driven"]

must_haves:
  truths:
    - "emails.detail returns the email row, its attachments, and its components joined to entity-type label/slug and latest extraction fields"
    - "polygonToRect converts a 0-1 four-corner polygon to {left,top,width,height} fractions and is unit-tested"
    - "vitest runs in packages/api-client and the geometry suite passes"
  artifacts:
    - path: "packages/api-client/src/router/emails/detail.ts"
      provides: "emails.detail tRPC procedure (email + attachments + components + entity-type/extraction joins)"
      exports: ["emailDetailProcedures"]
      min_lines: 60
    - path: "packages/api-client/src/geometry.ts"
      provides: "polygonToRect pure geometry helper"
      exports: ["polygonToRect"]
    - path: "packages/api-client/src/geometry.test.ts"
      provides: "unit tests for polygonToRect"
    - path: "packages/api-client/vitest.config.ts"
      provides: "vitest runner config for the api-client package"
  key_links:
    - from: "packages/api-client/src/router/emails/index.ts"
      to: "emailDetailProcedures"
      via: "spread merge into emailsRouter"
      pattern: "\\.\\.\\.emailDetailProcedures"
    - from: "packages/api-client/src/index.ts"
      to: "polygonToRect"
      via: "re-export from geometry.ts"
      pattern: "export.*polygonToRect"
---

<objective>
Build the read-only tRPC data layer for the email detail page and the shared geometry helper the overlay UI depends on.

Purpose: The detail page (05-03) and overlay layer (05-04) need a single `emails.detail` query that returns the email, its attachments, and its components (joined to entity-type label/slug and latest extraction fields), plus a unit-tested `polygonToRect` helper that maps normalized polygons to CSS-rect fractions. This plan also bootstraps vitest for the TS workspace (none exists yet).
Output: `emails.detail` procedure, `polygonToRect` + tests, vitest wired into `packages/api-client`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-review-ui-inbox-email-detail-with-document-preview-and-entit/05-UI-SPEC.md
@.planning/phases/05-review-ui-inbox-email-detail-with-document-preview-and-entit/05-PATTERNS.md
@.planning/phases/05-review-ui-inbox-email-detail-with-document-preview-and-entit/05-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not re-explore. -->

Existing router to extend — packages/api-client/src/router/emails/index.ts:
  export const emailsRouter = createTRPCRouter({ list: publicProcedure..., byId: publicProcedure... });
  Imports: `import { desc, eq } from "drizzle-orm"`, `import { z } from "zod"`,
           `import { Emails } from "@nauta/db/schema"`,
           `import { createTRPCRouter, publicProcedure } from "../../trpc"`.
  Context shape (packages/api-client/src/trpc.ts): ctx.db is the Drizzle handle; publicProcedure is unauthenticated.

Drizzle tables (import from "@nauta/db/schema"):
  Emails            -> id, subject, senderName, senderAddress, toAddresses(text[]), receivedAt(Date),
                       bodyText, bodyHtml, parseStatus, importerId
  EmailAttachments  -> id, emailId, filename, contentType, fileExt, storageKey, sizeBytes
  EmailComponents   -> id, emailId, attachmentId, sourceType, contentText, extractionStatus,
                       location(jsonb: { page_index?, polygon?, type }), parentComponentId, sequenceIndex
  ExtractionRecords -> id, componentId, entityTypeId, extractedFields(jsonb), confidenceScore(numeric),
                       status (enum: candidate|confirmed|superseded|pending|error)
  EntityTypes       -> id, slug, label

Output shape required by 05-UI-SPEC §9.1 (EmailView). Field names are camelCase (Drizzle columns already camelCase):
  { email: {...}, attachments: [...], components: [{ id, attachmentId, sourceType, contentText,
    extractionStatus, location, entityTypeLabel, entityTypeSlug, extractedFields, confidenceScore }] }

Root export (packages/api-client/src/index.ts) already re-exports appRouter/types; add a value re-export of geometry there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: polygonToRect helper + vitest bootstrap + unit tests</name>
  <files>packages/api-client/src/geometry.ts, packages/api-client/src/geometry.test.ts, packages/api-client/src/index.ts, packages/api-client/package.json, packages/api-client/vitest.config.ts</files>
  <read_first>
    - packages/api-client/src/index.ts (current re-exports; add geometry re-export)
    - packages/api-client/package.json (no test script / no vitest yet — must add)
    - .planning/phases/05-.../05-UI-SPEC.md §5.6 (polygonToRect contract + signature)
    - .planning/phases/05-.../05-PATTERNS.md "geometry.ts" + "geometry.test.ts" sections (named-export-only, ReadonlyArray input, test idiom; analog examples/acme-os-dev/packages/api-client/__tests__/router/records.test.ts)
  </read_first>
  <behavior>
    - polygonToRect([[0.1,0.2],[0.5,0.2],[0.5,0.6],[0.1,0.6]]) === { left:0.1, top:0.2, width:0.4, height:0.4 }
    - Full-page polygon [[0,0],[1,0],[1,1],[0,1]] -> { left:0, top:0, width:1, height:1 }
    - Skewed / non-axis-aligned polygon collapses to its min/max bounding box
    - Single-point degenerate polygon -> width:0, height:0 (no NaN, no throw)
    - Returns a NEW object each call (immutability); does not mutate the input array
  </behavior>
  <action>
    Create geometry.ts with a single named export `polygonToRect(polygon: ReadonlyArray<readonly [number, number]>): { readonly left; readonly top; readonly width; readonly height }` computing left=min(xs), top=min(ys), width=max(xs)-min(xs), height=max(ys)-min(ys). Named export only (CLAUDE.md). Re-export `polygonToRect` from packages/api-client/src/index.ts as a value export (keep the existing type-only exports intact). Add devDependency `vitest` (^2) to packages/api-client/package.json and a `"test": "vitest run"` script (plus `"test:watch": "vitest"`). Create vitest.config.ts (defineConfig from "vitest/config", node environment, include src/**/*.test.ts). Write geometry.test.ts covering the five behaviors above using the vitest `describe/it/expect` idiom from the PATTERNS analog (pure function — no mock DB).
  </action>
  <verify>
    <automated>cd packages/api-client && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/api-client && npm run test` exits 0 with the geometry suite green (≥5 assertions covering the five behaviors)
    - `grep -q "export function polygonToRect" packages/api-client/src/geometry.ts` succeeds
    - `grep -q "polygonToRect" packages/api-client/src/index.ts` succeeds (re-exported)
    - `grep -q '"vitest"' packages/api-client/package.json` succeeds
  </acceptance_criteria>
  <done>polygonToRect exists, is re-exported from the package root, and its vitest suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: emails.detail tRPC procedure + router registration</name>
  <files>packages/api-client/src/router/emails/detail.ts, packages/api-client/src/router/emails/index.ts</files>
  <read_first>
    - packages/api-client/src/router/emails/index.ts (the byId procedure is the exact query template; extend, do not rewrite list/byId)
    - packages/api-client/src/trpc.ts (ctx.db, publicProcedure, superjson transformer keeps Date intact)
    - packages/db/src/schema/components.ts, attachments.ts, extractions.ts, entity-types.ts, emails.ts (column names already loaded in <interfaces>)
    - .planning/phases/05-.../05-UI-SPEC.md §9.1 (exact output shape)
    - .planning/phases/05-.../05-PATTERNS.md "detail.ts" section (query pattern, return-null-on-not-found, schema import convention)
  </read_first>
  <action>
    Create detail.ts exporting `emailDetailProcedures` (a plain object of procedures, mergeable by spread). Define `detail: publicProcedure.input(z.object({ id: z.string().uuid() })).query(...)`. In the query: (1) select the email row by `eq(Emails.id, input.id)` limit 1 — return `null` if absent (NOT a thrown error; matches byId). (2) select attachments by `eq(EmailAttachments.emailId, input.id)` projecting id, filename, contentType, storageKey, fileExt. (3) select components by `eq(EmailComponents.emailId, input.id)` with `leftJoin(ExtractionRecords, eq(ExtractionRecords.componentId, EmailComponents.id))` and `leftJoin(EntityTypes, eq(EntityTypes.id, ExtractionRecords.entityTypeId))`, projecting EmailComponents.id/attachmentId/sourceType/contentText/extractionStatus/location, EntityTypes.label AS entityTypeLabel, EntityTypes.slug AS entityTypeSlug, ExtractionRecords.extractedFields, ExtractionRecords.confidenceScore. D-18: filter by email id ONLY — no DEFAULT_IMPORTER_ID / importerId filter anywhere. Shape the email object to the §9.1 field list (subject, senderName, senderAddress, toAddresses, receivedAt, bodyText, bodyHtml, parseStatus, importerId, id). Return `{ email, attachments, components }`. In index.ts, import `emailDetailProcedures` and spread it into the existing `createTRPCRouter({ list, byId, ...emailDetailProcedures })` — keep list and byId unchanged.
  </action>
  <verify>
    <automated>cd packages/api-client && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/api-client && npm run typecheck` exits 0
    - `grep -q "export const emailDetailProcedures" packages/api-client/src/router/emails/detail.ts` succeeds
    - `grep -q "\.\.\.emailDetailProcedures" packages/api-client/src/router/emails/index.ts` succeeds
    - `grep -q "entityTypeLabel" packages/api-client/src/router/emails/detail.ts` succeeds (entity-type label projected)
    - `grep -Eq "leftJoin\(.*ExtractionRecords" packages/api-client/src/router/emails/detail.ts` succeeds (extraction join present)
    - No occurrence of DEFAULT_IMPORTER_ID in detail.ts: `! grep -q "DEFAULT_IMPORTER_ID" packages/api-client/src/router/emails/detail.ts`
  </acceptance_criteria>
  <done>emails.detail is registered on emailsRouter, returns the EmailView shape, typechecks clean, and applies no importer filter (D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC query | email id (uuid) crosses from the client into the detail query |
| query → Postgres (Drizzle) | parameterized reads only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | emails.detail input | mitigate | `z.string().uuid()` validates id at the procedure boundary; malformed ids reject before reaching SQL |
| T-05-02 | Information Disclosure | emails.detail output | accept | D-18: reads are cross-importer by design (no auth boundary yet); no per-user secrets are returned; documented seam in STATE.md decisions log |
| T-05-03 | Tampering | Drizzle query construction | mitigate | All filters use Drizzle `eq()` parameterized builders; no string interpolation in SQL |
| T-05-04 | Denial of Service | unbounded joins | accept | Per-email row counts are small (attachments/components scoped to one email id); no pagination needed for a single detail view |
| T-05-SC | Tampering | npm install (vitest) | mitigate | vitest is a first-party Vitest/Vite package on npmjs.com; no [ASSUMED]/[SUS] third-party packages added — no legitimacy checkpoint required |
</threat_model>

<verification>
- `cd packages/api-client && npm run test` — geometry suite green.
- `cd packages/api-client && npm run typecheck` — exits 0 (detail procedure + router merge typecheck clean).
- Output of `emails.detail` matches 05-UI-SPEC §9.1 field-for-field (manual spot-check against the projection in detail.ts).
</verification>

<success_criteria>
- emails.detail returns `{ email, attachments, components }` with entity-type label/slug and extraction fields joined per §9.1.
- polygonToRect is re-exported from `@nauta/api-client` and unit-tested (≥5 cases).
- vitest is wired into packages/api-client; both `npm run test` and `npm run typecheck` exit 0.
</success_criteria>

<output>
Create `.planning/phases/05-review-ui-inbox-email-detail-with-document-preview-and-entit/05-01-SUMMARY.md` when done.
</output>

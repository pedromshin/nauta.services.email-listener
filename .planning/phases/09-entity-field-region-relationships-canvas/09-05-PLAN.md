---
phase: 09-entity-field-region-relationships-canvas
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/sidebar.tsx
  - packages/ui/src/index.ts
  - packages/ui/package.json
  - apps/web/src/components/theme-provider.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-21: @nauta/ui exposes a canonical shadcn `sidebar` block (SidebarProvider/Sidebar/SidebarInset/SidebarContent/SidebarHeader/SidebarFooter/SidebarMenu/SidebarMenuItem/SidebarMenuButton/SidebarTrigger) reusing the existing --sidebar-* HSL tokens"
    - "D-21: a next-themes ThemeProvider wrapper exists (apps/web) ready to wire into the root layout"
    - "The sidebar primitive imports cn from @nauta/ui and uses default Tailwind glass utilities over themed tokens — no new design tokens invented"
  artifacts:
    - path: "packages/ui/src/sidebar.tsx"
      provides: "shadcn sidebar block (named exports)"
      contains: "SidebarProvider"
    - path: "apps/web/src/components/theme-provider.tsx"
      provides: "next-themes ThemeProvider wrapper"
      contains: "next-themes"
  key_links:
    - from: "packages/ui/src/sidebar.tsx"
      to: "globals.css --sidebar-* tokens"
      via: "bg-sidebar / text-sidebar-foreground utility classes (or the frosted bg-background/70 recipe)"
      pattern: "sidebar"
    - from: "apps/web/src/components/theme-provider.tsx"
      to: "next-themes"
      via: "ThemeProvider as NextThemesProvider re-export"
      pattern: "NextThemesProvider"
---

<objective>
Add the missing navigation primitive and theming wrapper (D-21): a canonical shadcn `sidebar` block in `@nauta/ui` (none exists today) reusing the already-present `--sidebar-*` HSL tokens in `globals.css` + `lucide-react`, and a `next-themes` ThemeProvider wrapper in `apps/web` (the dep is present in `@nauta/ui` but unwired). No new design tokens — the brand primary (`164 39% 22%`) and the sidebar token set already exist.

Purpose: This is a structural frontend prerequisite for the app shell (09-06) and the entity-types page (09-07). It is pure presentation with no schema/backend dependency, so it runs in Wave 1 alongside the migration.

Output: `packages/ui/src/sidebar.tsx` (named exports), the `sidebar` export added to the `@nauta/ui` barrel, any sidebar-only dependency (none expected — react/lucide/cn already present), and `apps/web/src/components/theme-provider.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-CONTEXT.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-PATTERNS.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-UI-SPEC.md

<interfaces>
<!-- Source-of-truth analogs. Read the cited files before coding. -->
From packages/ui/src/resizable.tsx (the export/registry shape to mirror):
- `"use client"`; import a primitive package; import `cn` from `@nauta/ui`; define wrapper components `({ className, ...props }) => <X className={cn("base", className)} {...props} />`; `export { ... }`. This is the @nauta/ui component convention.

From packages/ui/src/index.ts:
- The barrel re-exports each component module. Add the sidebar exports here (match the existing re-export style).

From apps/web/src/app/globals.css (lines 40-47 light, 74-81 dark):
- `--sidebar-background/-foreground/-primary/-primary-foreground/-accent/-accent-foreground/-border/-ring` HSL tokens ALREADY EXIST in both :root and .dark. The sidebar uses these (do NOT invent tokens). Brand primary `164 39% 22%`.

From packages/ui/package.json:
- `lucide-react ^0.364.0`, `next-themes ^0.3.0`, `react-resizable-panels ^2.0.19` are already dependencies. The shadcn sidebar block depends on Slot/Button/Input/Separator/Sheet/Skeleton/Tooltip — confirm each exists in packages/ui/src/ (button.tsx, input.tsx, separator.tsx, sheet.tsx, skeleton.tsx, tooltip.tsx all confirmed present) and a `useIsMobile` hook + a tiny `cn` util (cn is in the @nauta/ui barrel).

From 09-UI-SPEC.md (Registry Safety + App-Wide Surfaces):
- Sidebar family blocks used: SidebarProvider/Sidebar/SidebarInset/SidebarMenu/SidebarMenuItem/SidebarMenuButton (+ Header/Footer/Content/Trigger). Frosted recipe: `bg-background/70 backdrop-blur-md border-r border-border/50`. next-themes is NOT a registry component — just wire its provider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shadcn sidebar block to @nauta/ui</name>
  <read_first>
    - packages/ui/src/resizable.tsx (the @nauta/ui component export convention — "use client", cn from barrel, named exports)
    - packages/ui/src/index.ts (barrel re-export style)
    - packages/ui/src/button.tsx, sheet.tsx, separator.tsx, skeleton.tsx, tooltip.tsx, input.tsx (the sidebar block composes these — confirm import paths within @nauta/ui)
    - apps/web/src/app/globals.css (the --sidebar-* tokens to reference, NOT redefine)
    - 09-UI-SPEC.md sections "Registry Safety" + "R1 — Global App Shell" (which sidebar exports are needed; the glass recipe)
  </read_first>
  <action>
    Create `packages/ui/src/sidebar.tsx` from the canonical shadcn `sidebar` block. Use the official shadcn sidebar source verbatim, then adapt imports to the @nauta/ui convention: import `cn` from the `@nauta/ui` barrel (not `@/lib/utils`); import the composed primitives (`Button`, `Input`, `Separator`, `Sheet`/`SheetContent`, `Skeleton`, `Tooltip`/`TooltipContent`/`TooltipTrigger`) from their sibling modules in `packages/ui/src/`. Provide the `useIsMobile` hook inline (or in a sibling `use-mobile.ts`) if the block needs it. Keep the block's use of the `--sidebar-*` tokens (its `bg-sidebar`/`text-sidebar-foreground` classes resolve against the existing tokens via tailwind config). Export the named members the app shell needs: at minimum `SidebarProvider, Sidebar, SidebarInset, SidebarTrigger, SidebarContent, SidebarHeader, SidebarFooter, SidebarMenu, SidebarMenuItem, SidebarMenuButton, useSidebar`.

    If a shadcn CLI init is required to generate the block and `components.json` is absent at the @nauta/ui root, do NOT run an interactive shadcn init — instead author `sidebar.tsx` by hand from the published block source (the project already vendors shadcn components this way; resizable.tsx is the precedent). Confirm `tailwind.config` maps the sidebar color utilities to the `--sidebar-*` vars; if the `sidebar` color keys are missing from the tailwind theme `colors`, add them (referencing the existing CSS vars) — this is wiring existing tokens, not inventing new ones.

    Add the sidebar exports to `packages/ui/src/index.ts` following the existing barrel pattern. Do NOT add any new npm dependency unless the block strictly requires one already absent (lucide-react/next-themes/react are present) — if a new dep is unavoidable, STOP and surface it (none is expected).
  </action>
  <verify>
    <automated>cd packages/ui && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/sidebar.tsx` exists, begins with `"use client"`, imports `cn` from the `@nauta/ui` barrel, and exports `SidebarProvider, Sidebar, SidebarInset, SidebarTrigger, SidebarContent, SidebarHeader, SidebarFooter, SidebarMenu, SidebarMenuItem, SidebarMenuButton` (grep each name).
    - `packages/ui/src/index.ts` re-exports the sidebar module.
    - No `@/lib/utils` import remains (cn comes from the barrel); the sidebar references `--sidebar-*`/`bg-sidebar` styling, not new tokens.
    - `cd packages/ui && npx tsc --noEmit` exits 0; `git diff packages/ui/package.json` shows NO new dependency added (or, if one was unavoidable, the SUMMARY documents it with a legitimacy note).
  </acceptance_criteria>
  <done>@nauta/ui exposes a working shadcn sidebar block over the existing --sidebar-* tokens; barrel updated; tsc clean; no new deps.</done>
</task>

<task type="auto">
  <name>Task 2: next-themes ThemeProvider wrapper</name>
  <read_first>
    - apps/web/src/app/layout.tsx (current root layout — the wrapper will be wired here in 09-06; this task only creates the component)
    - 09-PATTERNS.md section "theme-provider.tsx" (standard next-themes boilerplate; no existing analog)
    - 09-UI-SPEC.md section "R1 — Global App Shell" (ThemeProvider attribute="class" defaultTheme="system" enableSystem)
  </read_first>
  <action>
    Create `apps/web/src/components/theme-provider.tsx`: `"use client"`; re-export a `ThemeProvider` that wraps `next-themes`' `ThemeProvider as NextThemesProvider`, forwarding all props (`React.ComponentProps<typeof NextThemesProvider>`). Type everything explicitly. Do NOT modify `layout.tsx` here (09-06 wires it with `attribute="class" defaultTheme="system" enableSystem` + `suppressHydrationWarning` on `<html>`).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/theme-provider.tsx` exists, is `"use client"`, imports `ThemeProvider as NextThemesProvider` from `next-themes`, and exports a typed `ThemeProvider`.
    - `layout.tsx` is unchanged in this plan (wired in 09-06).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A typed next-themes ThemeProvider wrapper exists, ready for the root layout in 09-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored shadcn block -> @nauta/ui | New UI source vendored into the shared package; must not pull an unvetted dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-40 | Tampering | new npm dependency via shadcn init | mitigate | Author sidebar.tsx by hand from the published block (resizable.tsx precedent); no new dependency expected (lucide-react/next-themes/react present). If a new dep is unavoidable, STOP and surface it for a legitimacy check before install. |
| T-09-41 | Information Disclosure | client-side component | accept | sidebar + theme-provider are pure presentational client components; no secrets, no data access. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Default path installs NOTHING new; the `git diff package.json` acceptance check enforces "no new dependency". |
</threat_model>

<verification>
- `packages/ui/src/sidebar.tsx` exports the sidebar family; barrel updated; `packages/ui` tsc clean.
- `apps/web/src/components/theme-provider.tsx` wraps next-themes; `apps/web` tsc clean.
- No new npm dependency added (package.json diff empty), or a surfaced+documented exception.
</verification>

<success_criteria>
- D-21: @nauta/ui has a canonical sidebar block over the existing --sidebar-* tokens + a next-themes provider wrapper.
- No new design tokens; no new (unvetted) dependency.
- Both stacks typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/09-entity-field-region-relationships-canvas/09-05-SUMMARY.md` when done
</output>

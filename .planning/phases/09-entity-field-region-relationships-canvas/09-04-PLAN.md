---
phase: 09-entity-field-region-relationships-canvas
plan: 04
type: execute
wave: 5
depends_on: ["09-01", "09-02a", "09-02b", "09-03"]
files_modified:
  - packages/api-client/src/router/emails/detail.ts
  - packages/api-client/src/router/emails/mutations.ts
  - packages/api-client/src/router/emails/entity-summary.ts
  - packages/api-client/src/router/emails/index.ts
  - packages/api-client/src/router/_listener-config.ts
  - packages/api-client/src/router/entity-types.ts
  - packages/api-client/src/router/entity-types-write.ts
  - packages/api-client/src/router/__tests__/component-relationship-mutations.test.ts
  - packages/api-client/src/router/__tests__/entity-summary.test.ts
  - packages/api-client/src/router/__tests__/entity-types-write.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-15: emails.detail exposes role / entityTypeId / entityTypeFieldId per component; new tRPC mutations (setRole, setEntityType, setFieldRelationship, autofillFields, denyField, confirmField) proxy the FastAPI endpoints with X-API-Key server-side only"
    - "D-23: a per-email entity rollup (distinct entity-type labels + counts) is available via emails.entitySummary, derived from existing joins + the new role/entity_type_id columns — no new table"
    - "D-26: entity-type write mutations (create/update type; create/update/delete/reorder field) proxy the new /v1/entity-types endpoints"
    - "The browser never holds EMAIL_LISTENER_API_KEY — all writes go through getListenerConfig() read at call time, never NEXT_PUBLIC_"
  artifacts:
    - path: "packages/api-client/src/router/emails/mutations.ts"
      provides: "setRole/setEntityType/setFieldRelationship/autofillFields/denyField/confirmField mutations"
      contains: "autofillFields"
    - path: "packages/api-client/src/router/emails/entity-summary.ts"
      provides: "emails.entitySummary batch query (per-email entity-type label+count rollup, D-23)"
      contains: "entitySummary"
    - path: "packages/api-client/src/router/entity-types-write.ts"
      provides: "entityTypes write mutations (D-26) via getListenerConfig proxy"
      contains: "getListenerConfig"
    - path: "packages/api-client/src/router/_listener-config.ts"
      provides: "shared getListenerConfig + parseErrorDetail extracted for reuse"
      contains: "getListenerConfig"
  key_links:
    - from: "packages/api-client/src/router/emails/mutations.ts"
      to: "FastAPI /v1/components/{id}/role|entity-type|field-relationship|autofill-fields|deny"
      via: "fetch with X-API-Key from getListenerConfig (server-side)"
      pattern: "X-API-Key"
    - from: "packages/api-client/src/router/entity-types.ts"
      to: "entity-types-write.ts mutations"
      via: "spread into entityTypesRouter"
      pattern: "entityTypesWriteProcedures|...write"
    - from: "packages/api-client/src/router/emails/detail.ts"
      to: "EmailComponents.role / .entityTypeId / .entityTypeFieldId"
      via: "Drizzle .select() column reads"
      pattern: "EmailComponents.role"
---

<objective>
Wire the TypeScript data layer over the new backend (09-01/02/03): expose `role` / `entityTypeId` / `entityTypeFieldId` on each component in `emails.detail`; add the new component mutations (`setRole`, `setEntityType`, `setFieldRelationship`, `autofillFields`, `denyField`, plus `confirmField` proxying the existing `/confirm`) following the `getListenerConfig` server-side proxy idiom; add a per-email entity rollup (`emails.entitySummary`) for the glassy inbox chips (D-23); and add the entity-type write mutations (`entityTypes.create/update` type + `createField/updateField/deleteField/reorderFields`) proxying the new `/v1/entity-types` endpoints (D-26). The browser NEVER holds `EMAIL_LISTENER_API_KEY`.

Purpose: This is the seam between the FastAPI backends (09-02/03) and the React surfaces (canvas 09-08/09, inbox 09-06, entity-types page 09-07). Those frontends consume these procedures.

Output: extended `detail.ts`, new component mutations in `mutations.ts`, a shared `_listener-config.ts` (extract the duplicated `getListenerConfig`/`parseErrorDetail`), `entity-summary.ts`, `entity-types-write.ts` spread into `entityTypesRouter`, and vitest coverage for the new mutations + the entity-summary rollup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-CONTEXT.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-PATTERNS.md

<interfaces>
<!-- Source-of-truth analogs. Read the cited files before coding. -->
From packages/api-client/src/router/emails/mutations.ts:
- `getListenerConfig()` (lines 22-31): reads process.env.EMAIL_LISTENER_URL + EMAIL_LISTENER_API_KEY at CALL TIME (never module init, never NEXT_PUBLIC_); throws if unset.
- `parseErrorDetail(res, fallback)` (lines 45-55): extracts FastAPI {detail}.
- mutation shape (accept, lines 66-85): `publicProcedure.input(z.object({componentId: z.string().uuid()})).mutation(async ({input}) => { const {url,apiKey}=getListenerConfig(); fetch(`${url}/v1/components/${id}/...`, {method, headers:{"X-API-Key":apiKey,"Content-Type":"application/json"}, body}); if(!res.ok) throw new Error(await parseErrorDetail(...)); return res.json() as Promise<unknown>; })`.
- Exported as `componentMutationProcedures` (plain object) spread into emailsRouter.

From packages/api-client/src/router/emails/detail.ts:
- The component `.select({...})` (lines 87-102) already returns id/attachmentId/parentComponentId/sourceType/contentText/extractionStatus/location/entityTypeLabel/entityTypeSlug/extracted+correctedFields/confidence*/extractionRecordStatus. Add the three new EmailComponents columns here.
- Dedupe-preferring-confirmed (lines 120-131) is the join-collapse to reuse for entitySummary.

From packages/api-client/src/router/emails/index.ts:
- `emailsRouter = createTRPCRouter({ ...emailDetailProcedures, ...componentMutationProcedures, list, byId })`. `list` returns `{ items, hasMore, nextOffset }` (limit+1 paging). Spread new procedures the same way (e.g. `...emailEntitySummaryProcedures`).

From packages/api-client/src/router/entity-types.ts:
- `entityTypesRouter = createTRPCRouter({ list })` reading EntityTypes leftJoin EntityTypeFields, grouped by `groupEntityTypeRows`. Spread new write procedures into this router (createTRPCRouter merges objects).

From packages/db/src/schema (after 09-01):
- `EmailComponents.role`, `.entityTypeId`, `.entityTypeFieldId` columns; `EntityTypes` / `EntityTypeFields`; `Emails`. Import from "@nauta/db/schema".

From STATE.md decisions:
- 07-01: `z.string().uuid()` validates ids before URL interpolation (path-injection guard). SKIP_ENV_VALIDATION in vitest.config so db/client doesn't throw on POSTGRES_URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared getListenerConfig + add the six component mutations + detail.ts column exposure</name>
  <read_first>
    - packages/api-client/src/router/emails/mutations.ts (getListenerConfig, parseErrorDetail, polygonSchema, the accept mutation shape)
    - packages/api-client/src/router/emails/detail.ts (the component .select shape to extend)
    - 09-02 endpoints (PATCH /role, /entity-type, /field-relationship; POST /autofill-fields, /deny; existing POST /confirm)
    - 09-PATTERNS.md sections "mutations.ts (modify)", "detail.ts (modify)", "Server-Side Environment Guard", "Zod Input Validation"
  </read_first>
  <action>
    Create `packages/api-client/src/router/_listener-config.ts` exporting `getListenerConfig()` and `parseErrorDetail()` verbatim (move them out of mutations.ts; re-import in mutations.ts to avoid duplication — this is the "extract to shared" option the PATTERNS file flags). Keep behavior identical (call-time env read, never NEXT_PUBLIC_).

    In `mutations.ts` `componentMutationProcedures`, add: `setRole` (input `{componentId: uuid, role: z.enum(["entity","field","unrelated"]).nullable()}` -> PATCH `/v1/components/{id}/role` body `{role}`), `setEntityType` (`{componentId: uuid, entityTypeId: uuid.nullable()}` -> PATCH `/entity-type` body `{entity_type_id}`), `setFieldRelationship` (`{componentId: uuid, parentComponentId: uuid.nullable(), entityTypeFieldId: uuid.nullable()}` -> PATCH `/field-relationship` body `{parent_component_id, entity_type_field_id}`), `autofillFields` (`{entityComponentId: uuid}` -> POST `/autofill-fields` body `{}`), `denyField` (`{componentId: uuid}` -> POST `/deny` body `{}`), and `confirmField` (`{componentId: uuid, correctedFields: z.record(z.unknown()).nullable().optional()}` -> POST `/confirm` body `{corrected_fields}`) IF a confirm proxy is not already present in mutations.ts (check first — Phase 7 may have added `confirmComponent`; reuse it rather than duplicating). All use `getListenerConfig` + `parseErrorDetail`; ids validated as `z.string().uuid()` before interpolation.

    In `detail.ts`, add `role: EmailComponents.role`, `entityTypeId: EmailComponents.entityTypeId`, `entityTypeFieldId: EmailComponents.entityTypeFieldId` to the component `.select({})` (direct column reads — no join change; EntityTypes is already joined via the extraction record).
  </action>
  <verify>
    <automated>cd packages/api-client && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/router/_listener-config.ts` exports `getListenerConfig` + `parseErrorDetail`; mutations.ts imports them (no duplicated definitions).
    - `grep -nE 'setRole|setEntityType|setFieldRelationship|autofillFields|denyField' packages/api-client/src/router/emails/mutations.ts` shows all five new mutations; each carries `"X-API-Key": apiKey`.
    - `grep -nE 'EmailComponents.role|EmailComponents.entityTypeId|EmailComponents.entityTypeFieldId' packages/api-client/src/router/emails/detail.ts` shows the three columns added to the select.
    - No occurrence of `NEXT_PUBLIC_.*API_KEY` anywhere; `EMAIL_LISTENER_API_KEY` appears only inside `_listener-config.ts`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Shared listener config extracted; six component mutations added; detail.ts exposes role/entityTypeId/entityTypeFieldId; key stays server-side.</done>
</task>

<task type="auto">
  <name>Task 2: emails.entitySummary per-email entity rollup (D-23/D-24)</name>
  <read_first>
    - packages/api-client/src/router/emails/detail.ts (the email_components ⟕ extraction_records[status != superseded] ⟕ entity_types join + dedupe-preferring-confirmed — the exact join to lift to a per-email aggregate)
    - packages/api-client/src/router/emails/index.ts (list paging shape; how procedures are spread)
    - 09-CONTEXT.md D-23, D-24 (distinct entity-type labels + counts per email; chips deep-link to /emails/[id] until P10)
    - 09-PATTERNS.md (no per-row client fetch — a single batch query keyed by the visible page of email ids)
  </read_first>
  <action>
    Create `packages/api-client/src/router/emails/entity-summary.ts` exporting `emailEntitySummaryProcedures` with an `entitySummary` query: input `{ emailIds: z.array(z.string().uuid()).max(100) }`. Query `email_components` for those email ids where `role = 'entity'` (preferred path now that 09-01 added the column) OR (fallback) join through `extraction_records[status != superseded]` -> `entity_types` as `detail.ts` does, dedupe-preferring-confirmed, then aggregate per `email_id` to `{ emailId, entities: Array<{ entityTypeId, label, count }> }`. Prefer the role/entityTypeId direct path; if a component has role='entity' + entityTypeId, read its label by joining `entity_types` on `EmailComponents.entityTypeId`. Return one row per requested emailId (emails with no entities -> empty `entities` array). Use parameterized Drizzle `inArray(EmailComponents.emailId, input.emailIds)`. Spread `emailEntitySummaryProcedures` into `emailsRouter` in `index.ts`.
  </action>
  <verify>
    <automated>cd packages/api-client && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/router/emails/entity-summary.ts` exists; `entitySummary` takes `{ emailIds }` and returns `{ emailId, entities: [{ entityTypeId, label, count }] }[]`.
    - The query uses `inArray(EmailComponents.emailId, ...)` (parameterized, batch — no per-row fetch) and joins entity_types for labels.
    - `emailEntitySummaryProcedures` is spread into `emailsRouter` in index.ts.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>emails.entitySummary returns distinct entity-type labels+counts per email in one batch query, ready for the inbox chips.</done>
</task>

<task type="auto">
  <name>Task 3: entity-types write mutations (D-26)</name>
  <read_first>
    - packages/api-client/src/router/entity-types.ts (entityTypesRouter; groupEntityTypeRows; createTRPCRouter merge)
    - packages/api-client/src/router/_listener-config.ts (from Task 1)
    - 09-03 endpoints (/v1/entity-types: POST /, PATCH /{id}, POST /{id}/fields, PATCH /fields/{id}, DELETE /fields/{id}, POST /{id}/fields/reorder)
    - 09-CONTEXT.md D-27 (field_type allowlist + slug + delete-guard at the Zod boundary too)
    - 09-PATTERNS.md sections "entity-types.ts (modify)", "entity-types-write.ts"
  </read_first>
  <action>
    Create `packages/api-client/src/router/entity-types-write.ts` exporting `entityTypesWriteProcedures` (plain object) using the shared `getListenerConfig`/`parseErrorDetail`: `create` (`{slug, label, description?}` -> POST `/v1/entity-types`), `update` (`{entityTypeId: uuid, label?, description?, isActive?}` -> PATCH `/v1/entity-types/{id}` body snake_cased), `createField` (`{entityTypeId: uuid, slug, label, fieldType: z.enum(["string","number","date","array","object"]), isRequired?, sortOrder?, isIdentifier?, description?}` -> POST `/v1/entity-types/{id}/fields`), `updateField` (`{fieldId: uuid, ...all optional, fieldType: same enum optional}` -> PATCH `/v1/entity-types/fields/{id}`), `deleteField` (`{fieldId: uuid}` -> DELETE `/v1/entity-types/fields/{id}`), `reorderFields` (`{entityTypeId: uuid, orderedFieldIds: z.array(z.string().uuid())}` -> POST `/v1/entity-types/{id}/fields/reorder`). Validate ids as `z.string().uuid()` and `fieldType` via the z.enum allowlist (mirrors D-27 server-side). Spread `entityTypesWriteProcedures` into `entityTypesRouter` in `entity-types.ts` (keep the existing `list`).
  </action>
  <verify>
    <automated>cd packages/api-client && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/router/entity-types-write.ts` exports `entityTypesWriteProcedures` with create/update/createField/updateField/deleteField/reorderFields, each using `getListenerConfig` + `"X-API-Key"`.
    - `fieldType` is validated with `z.enum(["string","number","date","array","object"])` in createField/updateField.
    - `entity-types.ts` spreads `entityTypesWriteProcedures` into `entityTypesRouter` and still exports `list`.
    - `npx tsc --noEmit` exits 0; no API key in client-importable code.
  </acceptance_criteria>
  <done>entityTypes router gains create/update type + field CRUD/reorder mutations proxying /v1/entity-types, key server-side only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: vitest coverage for the new mutations + entity-summary rollup</name>
  <behavior>
    - Each new component mutation issues a fetch to the correct FastAPI path with the X-API-Key header and snake_cased body; a non-2xx response throws with the parsed detail.
    - getListenerConfig throws when env vars are unset (no NEXT_PUBLIC_ leakage).
    - entity-summary aggregation collapses multiple entity components of the same type into one {label, count} and returns an empty entities array for an email with none.
    - entity-types-write mutations reject an out-of-allowlist fieldType at the Zod boundary.
  </behavior>
  <read_first>
    - packages/api-client/src/router/__tests__/ (existing mutation test style from Phase 6/7 — fetch-mock + getListenerConfig env stub)
    - packages/api-client/vitest.config.ts (SKIP_ENV_VALIDATION usage)
    - Tasks 1-3 outputs
  </read_first>
  <action>
    Write vitest tests mirroring the Phase 6/7 mutation test style (mock global fetch, stub EMAIL_LISTENER_URL/API_KEY): `component-relationship-mutations.test.ts` (setRole/setEntityType/setFieldRelationship/autofillFields/denyField hit the right path + headers + body; error path throws), `entity-summary.test.ts` (extract the pure aggregation into a small exported helper like `groupEntityTypeRows` and unit-test the rollup without a DB — same testability pattern as the existing entity-types grouping), `entity-types-write.test.ts` (create/field mutations issue correct requests; invalid fieldType is rejected by the input schema). Refactor the entity-summary aggregation into a pure exported helper so it is DB-free testable.
  </action>
  <verify>
    <automated>cd packages/api-client && npx vitest run 2>&1 | tail -25 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The three new test files exist and pass under `vitest run`.
    - A pure entity-summary aggregation helper is exported and unit-tested (same pattern as `groupEntityTypeRows`).
    - Tests assert X-API-Key header presence + correct snake_cased bodies + thrown error on non-2xx.
    - `vitest run` all green and `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>New data-layer procedures are vitest-covered; aggregation helper is pure/testable; tsc + vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js server (tRPC) | Mutations run server-side; the FastAPI key must never reach the client bundle. |
| Next.js server -> FastAPI | X-API-Key crosses here only, read at call time from server env. |
| client input -> URL path interpolation | componentId/entityTypeId/fieldId must be validated before being placed in a URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-30 | Information Disclosure | EMAIL_LISTENER_API_KEY in client bundle | mitigate | Key read only inside `_listener-config.ts` `getListenerConfig()` at call time; never NEXT_PUBLIC_; verified by grep + the env-unset test. |
| T-09-31 | Tampering | path-segment injection via ids | mitigate | All ids validated with `z.string().uuid()` before URL interpolation (07-01 pattern). |
| T-09-32 | Tampering | invalid fieldType / role reaching backend | mitigate | z.enum allowlists for role and fieldType at the tRPC input boundary (defense-in-depth with the Pydantic validators in 09-02/03). |
| T-09-33 | Denial of Service | unbounded entitySummary batch | mitigate | `emailIds` capped at `.max(100)` (matches the inbox page size); single `inArray` query, no per-row fetch. |
| T-09-34 | Information Disclosure | raw FastAPI errors to client | mitigate | parseErrorDetail surfaces only the FastAPI `{detail}` (already generic); friendly toasts at the UI layer (09-06/07/09). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages (zod/drizzle/trpc already present); legitimacy gate N/A. |
</threat_model>

<verification>
- `tsc --noEmit` clean across packages/api-client.
- emails.detail returns role/entityTypeId/entityTypeFieldId; six component mutations + entitySummary + entity-type write mutations exist.
- `EMAIL_LISTENER_API_KEY` appears only in `_listener-config.ts`; no NEXT_PUBLIC_ key.
- vitest run green including the new mutation + aggregation tests.
</verification>

<success_criteria>
- D-15: detail field exposure + component mutations proxy the FastAPI endpoints (key server-side only).
- D-23/D-24: emails.entitySummary per-email entity rollup in one batch query (no new table).
- D-26: entity-type write mutations proxy /v1/entity-types.
- tsc + vitest green; no client-side key.
</success_criteria>

<output>
Create `.planning/phases/09-entity-field-region-relationships-canvas/09-04-SUMMARY.md` when done
</output>

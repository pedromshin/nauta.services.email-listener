---
phase: 09-entity-field-region-relationships-canvas
plan: 07
type: execute
wave: 6
depends_on: ["09-04", "09-05"]
files_modified:
  - apps/web/src/app/entity-types/page.tsx
  - apps/web/src/app/entity-types/_components/entity-type-detail.tsx
  - apps/web/src/app/entity-types/_components/field-row-dialog.tsx
  - apps/web/src/app/entity-types/_components/use-entity-type-admin.ts
  - packages/api-client/src/router/entity-types.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-25: /entity-types is a master/detail page — list all entity types; create/rename/edit-description/activate-deactivate a type; create/edit/delete/reorder its fields (label/slug/field_type/is_required/sort_order/is_identifier)"
    - "D-26: all writes go through the tRPC entity-type write mutations (server-side proxy) with optimistic snapshot/revert + sonner toasts"
    - "D-27: the field dialog constrains field_type to {string,number,date,array,object}; a Delete that would orphan a referenced field surfaces a deactivate (not hard-delete) outcome to the user"
    - "Deactivate uses a non-destructive variant (never `destructive`), mirroring the D-16 reprocess rule"
  artifacts:
    - path: "apps/web/src/app/entity-types/page.tsx"
      provides: "entity-types master/detail route under the app shell"
      contains: "entityTypes"
    - path: "apps/web/src/app/entity-types/_components/field-row-dialog.tsx"
      provides: "create/edit field dialog with field_type Select allowlist"
      contains: "field_type"
  key_links:
    - from: "apps/web/src/app/entity-types/_components/use-entity-type-admin.ts"
      to: "api.entityTypes.create/update/createField/updateField/deleteField/reorderFields"
      via: "tRPC mutations with optimistic snapshot/revert"
      pattern: "useMutation"
    - from: "apps/web/src/app/entity-types/page.tsx"
      to: "api.entityTypes.list"
      via: "useQuery master list"
      pattern: "entityTypes.list"
---

<objective>
Build the entity-type & property management surface at `/entity-types` (D-25/D-26/D-27): a master/detail page under the app shell that lists entity types, creates/renames/edits/deactivates a type, and creates/edits/deletes/reorders its fields — all through the tRPC entity-type write mutations (server-side proxy, D-26) with optimistic snapshot/revert + sonner toasts, the `field_type` allowlist Select (D-27), and a delete flow that surfaces the confirmed-reference deactivate outcome (D-27, never orphaning the D-04 FK).

Purpose: Realizes R2. Depends on the shell (09-05/06 hosts the route) and the entity-type write data layer (09-04).

Output: `entity-types/page.tsx`, `entity-type-detail.tsx`, `field-row-dialog.tsx`, and a `use-entity-type-admin.ts` hook owning the mutations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-CONTEXT.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-PATTERNS.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-UI-SPEC.md

<interfaces>
<!-- Source-of-truth analogs. Read the cited files before coding. -->
From 09-04 entityTypes router (after the write mutations land):
- `api.entityTypes.list.useQuery()` -> Array<{ slug, label, description, fields: [{ key, label, dataType, isRequired }] }> (NOTE: the existing `list` shape exposes `slug`/`key`/`dataType`/`isRequired` but NOT ids; the detail page needs entityTypeId + fieldId for the write mutations. EITHER extend `list` to also return `id`/`isActive`/per-field `id`/`fieldType`/`sortOrder`/`isIdentifier` (preferred — small additive change to entity-types.ts `list`), OR add an `entityTypes.byId` query. Pick one and document it; the write mutations need ids.)
- Write mutations: `api.entityTypes.create`, `.update`, `.createField`, `.updateField`, `.deleteField`, `.reorderFields` (from 09-04).

From apps/web/src/app/emails/[id]/_components/use-region-edit.ts:
- The optimistic snapshot/revert + sonner pattern: `onMutate` cancel+getData+setData; `onError` restore prevData + `toast.error(...)`; `onSuccess` invalidate + `toast.success(...)`. Mirror this for the entity-type mutations (cache key = `api.entityTypes.list`).

From apps/web/src/app/emails/[id]/_components/nest-picker.tsx:
- Controlled Dialog/Popover shape (open/onOpenChange/onSave/onDelete) — the analog for field-row-dialog.tsx.

From @nauta/ui:
- `form`/`input`/`textarea`/`select`/`checkbox`/`dialog`/`alert-dialog`/`table`/`switch`/`button`/`badge`/`skeleton` all present.

From 09-UI-SPEC.md section "R2 — Entity-Type Management":
- Master list (left `w-72 border-r`): row = label + active/inactive Badge; "+ New type" in a frosted header. Detail (right `flex-1`): name/description form (input+textarea) + active Switch; Fields table below (columns Label · Slug · Type · Required · Identifier · ⋯), "+ Add field". Row edit -> dialog (input label/slug, select field_type ∈ {string,number,date,array,object}, checkbox is_required + is_identifier). Reorder via up/down or drag (Discretion). Delete -> AlertDialog; if referenced by a confirmed component's entity_type_field_id, the dialog states it will DEACTIVATE, not hard-delete. Save uses `bg-primary` (non-destructive); deactivate `variant=secondary`, NEVER `destructive`. Loading -> Skeleton rows; errors -> friendly toast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-entity-type-admin hook (optimistic CRUD mutations)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/use-region-edit.ts (optimistic snapshot/revert + sonner toast pattern; api.useUtils + cancel/getData/setData/invalidate)
    - 09-04 entity-types-write.ts (the create/update/createField/updateField/deleteField/reorderFields mutation inputs)
    - 09-UI-SPEC.md section "R2" (friendly error toasts; deactivate vs delete copy)
    - 09-CONTEXT.md D-26, D-27
  </read_first>
  <action>
    Decide + implement the id-exposure fix: extend `entityTypes.list` (in packages/api-client/src/router/entity-types.ts) to additionally return `id` + `isActive` per type and `id`/`fieldType`/`sortOrder`/`isIdentifier` per field (additive to the existing `groupEntityTypeRows` output), OR add an `entityTypes.byId` query — whichever is cleaner; document the choice. (This touches the api-client; coordinate that the change is additive and keeps the Phase 7 picker's existing fields.)

    Create `apps/web/src/app/entity-types/_components/use-entity-type-admin.ts` (`"use client"`): wrap the six write mutations with the use-region-edit optimistic idiom against the `api.entityTypes.list` cache (snapshot `getData`, optimistic `setData`, `onError` restore + `toast.error("... could not be saved.")`, `onSuccess` invalidate + `toast.success(...)`). Expose typed handlers: `createType`, `updateType` (label/description/isActive), `createField`, `updateField`, `deleteField` (returns/surfaces whether the server hard-deleted or soft-deactivated, from the 09-03 delete endpoint response), `reorderFields`. Track in-flight ids for disabling controls (mirror `mutatingIds`).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `entityTypes.list` (or a new `byId`) now exposes type `id`/`isActive` + field `id`/`fieldType`/`sortOrder`/`isIdentifier`; the Phase 7 picker still compiles (additive change).
    - `use-entity-type-admin.ts` wraps all six write mutations with optimistic snapshot/revert against `api.entityTypes.list` + sonner toasts (success + friendly error).
    - `deleteField` surfaces the hard-delete-vs-deactivate outcome to the caller.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A hook owns optimistic entity-type/field CRUD with revert + toasts; the list query exposes the ids the writes need.</done>
</task>

<task type="auto">
  <name>Task 2: field-row-dialog (create/edit field; field_type allowlist; delete-guard copy)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/nest-picker.tsx (controlled dialog/popover shape; onSave/onDelete)
    - apps/web/src/app/emails/[id]/_components/reject-dialog.tsx (AlertDialog confirmation pattern for the delete-guard)
    - the use-entity-type-admin hook from Task 1
    - 09-UI-SPEC.md section "R2" (field dialog inputs; field_type Select allowlist; deactivate-not-delete copy)
    - 09-CONTEXT.md D-27
  </read_first>
  <action>
    Create `apps/web/src/app/entity-types/_components/field-row-dialog.tsx` (`"use client"`): a controlled `Dialog` (open/onOpenChange) for create+edit of a field. Inputs: `label` + `slug` (`Input`), `field_type` (`Select` constrained to exactly `string | number | date | array | object` — D-27), `is_required` + `is_identifier` (`Checkbox`), `description` (optional). Validate with Zod at the boundary before calling `createField`/`updateField` from the hook. Include a Delete affordance gated behind an `AlertDialog`: copy states "Delete this field?" and, when the field is referenced by a confirmed component (the server response or a passed `referenceCount > 0`), the dialog states it will **deactivate** the field instead of hard-deleting (matching the 09-03 guard), with the confirm button as `variant=secondary` (never `destructive`). Save button uses `bg-primary` (default variant — non-destructive).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `field-row-dialog.tsx` has a `field_type` Select whose options are exactly string/number/date/array/object (grep the five literals).
    - Create/edit calls `createField`/`updateField` only after Zod validation.
    - The Delete flow uses an AlertDialog and surfaces a deactivate-not-hard-delete message + a non-`destructive` confirm when the field is referenced.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The field dialog enforces the field_type allowlist and surfaces the delete-guard (deactivate) outcome without a destructive button.</done>
</task>

<task type="auto">
  <name>Task 3: entity-types master/detail page + fields table + reorder</name>
  <read_first>
    - apps/web/src/app/page.tsx (the list/Skeleton/error/empty pattern to adapt for the master list)
    - the use-entity-type-admin hook (Task 1) + field-row-dialog (Task 2)
    - @nauta/ui table/form/input/textarea/switch/badge/skeleton modules
    - 09-PATTERNS.md sections "entity-types/page.tsx", "entity-type-detail.tsx"
    - 09-UI-SPEC.md section "R2 — Entity-Type Management" (master/detail dimensions, fields table columns, frosted header, copy)
  </read_first>
  <action>
    Create `apps/web/src/app/entity-types/page.tsx` (`"use client"`): `api.entityTypes.list.useQuery()`; master list left (`w-72 border-r`, frosted header with "+ New type" button that calls `createType` via the hook), each row = label + active/inactive `Badge`, click selects the type. Skeleton rows while loading; friendly error card; empty state. Right pane renders `<EntityTypeDetail type={selected}/>`.

    Create `apps/web/src/app/entity-types/_components/entity-type-detail.tsx` (`"use client"`): name/description `form` (`Input` + `Textarea`) bound to `updateType` (debounced or save-on-blur), an active `Switch` (deactivate uses `variant=secondary` semantics — never destructive), and a Fields `Table` (columns Label · Slug · Type · Required · Identifier · ⋯) listing the type's fields ordered by sort_order, with a "+ Add field" button (opens `FieldRowDialog` in create mode), a per-row edit (opens the dialog in edit mode), and reorder via up/down buttons (calls `reorderFields` with the new id order — drag is optional Discretion). All writes go through the hook (optimistic + toasts).

    The page renders inside the app-shell `SidebarInset` (09-06) — no extra full-page chrome needed.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15 && npx next build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `/entity-types` master list shows types with active/inactive Badges + "+ New type"; selecting one shows the detail form + Fields table (Label/Slug/Type/Required/Identifier/⋯).
    - Add/edit field opens `FieldRowDialog`; reorder calls `reorderFields`; deactivate uses a non-destructive control.
    - All writes route through `use-entity-type-admin` (optimistic + sonner) — no direct `fetch` to FastAPI from the page.
    - `cd apps/web && npx tsc --noEmit` exits 0 AND `npx next build` succeeds.
  </acceptance_criteria>
  <done>/entity-types is a working master/detail CRUD surface (types + fields + reorder) under the shell; tsc + next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form input -> tRPC -> FastAPI | Entity-type/field schema edits; the FastAPI key stays server-side (09-04 proxy). |
| field delete -> confirmed components | UI must surface the deactivate-not-orphan outcome (server-enforced in 09-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-60 | Tampering | invalid field_type submitted | mitigate | field_type is a `Select` limited to the 5 allowed values + Zod-validated before the mutation; backend re-validates (09-03 Pydantic). |
| T-09-61 | Information Disclosure | API key in client | accept/mitigate | The page calls only tRPC mutations; the key lives server-side in the 09-04 proxy — no fetch-to-FastAPI from this client code (grep confirms). |
| T-09-62 | Data loss | accidental field hard-delete | mitigate | Delete behind an AlertDialog; referenced fields show a deactivate-not-delete message with a non-`destructive` confirm; the server guard (09-03) is the authority. |
| T-09-63 | Information Disclosure | raw server errors in UI | mitigate | Friendly sonner toasts only; technical detail logged to devtools (WR-02 pattern from Phase 5). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages (form/table/dialog/select already in @nauta/ui); legitimacy gate N/A. |
</threat_model>

<verification>
- `/entity-types` lists types, creates/renames/deactivates a type, and CRUDs+reorders fields, all via optimistic tRPC mutations.
- field_type is constrained to the 5-value allowlist; field delete surfaces deactivate-not-orphan.
- `apps/web` tsc clean + `next build` succeeds.
</verification>

<success_criteria>
- D-25: master/detail entity-type + field CRUD with reorder under the shell.
- D-26: all writes via the server-side tRPC proxy (optimistic + toasts); no client key.
- D-27: field_type allowlist enforced; confirmed-reference delete surfaces deactivate (non-destructive).
- tsc + next build green.
</success_criteria>

<output>
Create `.planning/phases/09-entity-field-region-relationships-canvas/09-07-SUMMARY.md` when done
</output>

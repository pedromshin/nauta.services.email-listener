---
phase: 09-entity-field-region-relationships-canvas
plan: 08
type: execute
wave: 6
depends_on: ["09-04"]
files_modified:
  - apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx
  - apps/web/src/app/emails/[id]/_components/overlay-layer.tsx
  - apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx
  - apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx
  - apps/web/src/app/emails/[id]/_components/canvas-shell.tsx
  - apps/web/src/app/emails/[id]/_components/use-canvas-state.ts
  - apps/web/src/app/emails/[id]/_components/use-role-mutations.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-06: a four-zone canvas shell (toolbar + 256px LAYERS slot + flex-1 canvas + 288px INSPECTOR slot) renders the existing PDF/overlay/draw primitives inside the new frame"
    - "D-07: zoom (0.25–4.0, Cmd/Ctrl+scroll zoom-to-cursor, Fit width/page, reset) + pan (Space+drag, two-finger scroll) work in the canvas"
    - "D-08: drag on empty canvas draws a rectangle by default; click a box selects; Shift-click multi-selects; a Select/Draw toolbar toggle exists; Esc cancels"
    - "D-09: moving a box (drag body) or resizing (drag handles) reuses Phase 6 redraw semantics — supersede via a new email_components row, never in-place geometry mutation"
    - "D-10: an active-parent model — selecting an ENTITY box arms next-drawn boxes to become its FIELD children; role-color overlays (entity=violet, field=amber, unrelated=slate, unclassified=primary) render per box"
    - "D-05: UNRELATED regions (role='unrelated') are excluded from the default canvas/layers view (anti-bloat, D-12) — the overlay-layer roleFilter hides them and the toolbar Unrelated toggle defaults off"
    - "use-role-mutations wraps setRole/setEntityType/setFieldRelationship/confirmField/denyField with optimistic snapshot/revert against emails.detail; autofillFields is non-optimistic (invalidate)"
  artifacts:
    - path: "apps/web/src/app/emails/[id]/_components/canvas-shell.tsx"
      provides: "four-zone editor shell"
      contains: "w-64"
    - path: "apps/web/src/app/emails/[id]/_components/canvas-toolbar.tsx"
      provides: "tool mode + nav + zoom + view toggles toolbar"
      contains: "Draw"
    - path: "apps/web/src/app/emails/[id]/_components/use-canvas-state.ts"
      provides: "active-parent + tool-mode + selection state machine"
      contains: "activeParentId"
    - path: "apps/web/src/app/emails/[id]/_components/use-role-mutations.ts"
      provides: "optimistic role/entity-type/field-relationship/confirm/deny mutations + autofillFields"
      contains: "setRole"
  key_links:
    - from: "apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx"
      to: "the role-color palette"
      via: "role prop -> ROLE_BORDER class override + isActiveParent ring"
      pattern: "violet|amber|slate"
    - from: "apps/web/src/app/emails/[id]/_components/use-role-mutations.ts"
      to: "api.emails.setRole/setEntityType/setFieldRelationship/autofillFields/denyField"
      via: "useMutation optimistic snapshot/revert"
      pattern: "utils.emails.detail"
---

<objective>
Build the canvas editor's structural layer (D-06..D-10): extend `region-overlay-box.tsx` with role-color rendering + active-parent ring + a `showConfirmDeny` slot; extend `overlay-layer.tsx` with role filtering (D-12 default-hide rules) + activeParentId; expand `pdf-preview-pane.tsx`'s zoom to 0.25–4.0 as the inner canvas zone; build the `canvas-toolbar.tsx` (tool mode / page nav / zoom / view toggles) and the four-zone `canvas-shell.tsx`; and the two hooks `use-canvas-state.ts` (active-parent + tool-mode + selection state) and `use-role-mutations.ts` (optimistic role/relationship/confirm/deny + non-optimistic autofillFields). Move/resize of a box reuses the existing Phase 6 redraw (supersede-never-mutate, D-09).

Purpose: This is the editor frame + interaction state the panels (09-09) compose into. Splitting the canvas across 08/09 keeps each plan within budget. Depends on 09-04 (the mutations/detail fields).

Output: extended overlay-box/overlay-layer/pdf-preview-pane, new canvas-toolbar + canvas-shell, and the two hooks. (LAYERS/INSPECTOR panels + pickers + page wiring land in 09-09.)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-CONTEXT.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-PATTERNS.md
@.planning/phases/09-entity-field-region-relationships-canvas/09-UI-SPEC.md

<interfaces>
<!-- Source-of-truth analogs. Read the cited files before coding. -->
From apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx:
- Existing `statusClasses` (lines ~60-75): pending -> dashed primary/[0.08]; rejected/superseded -> dashed muted/50 opacity-40; else -> border-primary/80 bg-primary/10. Existing label chip `absolute -top-5 left-0` bg-primary. Has `pointer-events-auto` (06-03 fix). Extend with `role`, `isActiveParent`, `showConfirmDeny` props.

From apps/web/src/app/emails/[id]/_components/overlay-layer.tsx:
- Existing filter: sourceType==='region' && hasPolygon && pageIndex===currentPage-1 && (!rejected/superseded || showHistory). z-10 pointer-events-none. Add `roleFilter` + `activeParentId` props + the D-12 visibility rules.

From apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx:
- react-pdf Document/Page + overlay stack; zoom currently 0.5–3.0 step 0.25 (extend to 0.25–4.0); page nav; OverlayLayer hidden via CSS display:none to preserve sync (05-04). DrawOverlay z-20 cursor-crosshair.

From apps/web/src/app/emails/[id]/_components/draw-overlay.tsx (reused unchanged):
- pointer-capture draw, MIN_DRAW_SIZE=0.01, live dashed preview; emits a normalized rect on completion. z-20.

From apps/web/src/app/emails/[id]/_components/use-region-edit.ts:
- The optimistic mutation idiom (api.useUtils, onMutate cancel+getData+setData map over components, onError restore prevData + toast, onSuccess invalidate + toast); `mutatingComponentIds` derived from isPending + variables. ALSO holds the existing Phase 6 `redraw` mutation (supersede-never-mutate, D-09) — REUSE it for move/resize, do not author a new geometry mutation.

From apps/web/src/app/emails/[id]/_components/use-autofill.ts:
- The phase-machine + non-optimistic invalidate-after-success pattern for autofill (results come back as new candidates on the next invalidation). 6000ms failure toast.

From packages/api-client/src/geometry.ts:
- `polygonToRect(polygon)`, `clientXYToNormalized(...)`, `normalizedRectToPolygon(rect)` — immutable, clamped [0,1]. Bbox containment for "is field box inside entity box?" derives from polygonToRect.

From 09-04: api.emails.setRole / setEntityType / setFieldRelationship / autofillFields / denyField / confirmField (or the Phase 7 confirm). emails.detail now returns role / entityTypeId / entityTypeFieldId per component. The existing api.emails.redraw mutation (Phase 6) is the geometry-supersede path for D-09.

From 09-UI-SPEC.md:
- §Layout Shell (the exact Tailwind class sequence for the 4-zone shell), §Toolbar (tool mode group, nav, zoom group with keybindings, view toggles incl. Regions/History/Unrelated switches), §Color (role-color palette + active-parent ring-4 ring-violet-400/40), §Canvas (z-stack 0/2/10/20/30; role box rendering table; min draw 0.01; "Move/resize semantics (D-09): dragging a box corner/edge creates a new row — the existing redraw"), §Keybindings, §Active-Parent Model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-color region-overlay-box + role-filtering overlay-layer (D-10/D-12 + Color palette)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx (the file being modified — existing statusClasses + label chip)
    - apps/web/src/app/emails/[id]/_components/overlay-layer.tsx (the file being modified — existing filter)
    - 09-PATTERNS.md sections "region-overlay-box.tsx (modify)", "overlay-layer.tsx (modify)", §"Role-Color Palette"
    - 09-UI-SPEC.md §Color (Role-Color Palette table; active-parent ring), §Canvas (Region Box Rendering by Role table; label chip per role)
  </read_first>
  <action>
    `region-overlay-box.tsx`: add props `role?: "entity"|"field"|"unrelated"|null`, `isActiveParent?: boolean`, `showConfirmDeny?: boolean`, plus `onConfirm?`/`onDeny?` callbacks. Add a `ROLE_BORDER` map (entity `border-violet-500/80 bg-violet-500/10`, field `border-amber-500/80 bg-amber-500/10`, unrelated `border-slate-400/40 bg-slate-400/[0.06] opacity-60`); when `role` is set and status is not rejected/superseded, use the role class instead of `statusClasses` (`roleClass ?? statusClasses`). Selected adds `ring-2 ring-{role-color}/50`; `isActiveParent` adds the outer `ring-4 ring-violet-400/40`. Label chip tinted per role (entity violet-500/white, field amber-500/white, unrelated slate-400/white, unclassified existing primary). Render an inline confirm/deny slot at `absolute -top-3 right-0 z-30 pointer-events-auto` ONLY when `showConfirmDeny` is true (the actual `ConfirmDenyControls` component is created in 09-09; here render a minimal inline ✓/✗ button pair per the UI-SPEC, wired to onConfirm/onDeny — 09-09 may extract it).

    `overlay-layer.tsx`: add props `roleFilter?: "entity"|"field"|"unrelated"|null` and `activeParentId?: string|null`. Implement the D-12 visibility rules: by default show ENTITY boxes + unclassified; show a FIELD box only when its `parentComponentId === activeParentId` (i.e. its parent entity is selected) OR when `roleFilter` explicitly requests fields; hide UNRELATED unless `showUnrelated` (a new prop or via roleFilter). Pass `isActiveParent={c.id === activeParentId}` to each box. Keep the existing region/page/history filters.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'role|isActiveParent|showConfirmDeny|violet-500|amber-500|slate-400' apps/web/src/app/emails/[id]/_components/region-overlay-box.tsx` shows the role-color map + active-parent ring + confirm/deny slot.
    - `grep -nE 'roleFilter|activeParentId|parentComponentId' apps/web/src/app/emails/[id]/_components/overlay-layer.tsx` shows the new props + D-12 field-visibility rule (fields shown when parent === activeParentId).
    - Boxes with no role fall back to the existing primary `statusClasses` (unchanged behavior for Phase 6/7 boxes).
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Overlay boxes render role colors + active-parent ring + an inline confirm/deny slot; the overlay layer filters fields to the selected entity (D-12) and marks the active parent.</done>
</task>

<task type="auto">
  <name>Task 2: canvas-toolbar + pdf-preview-pane zoom expansion (D-07/D-08)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx (zoom 0.5–3.0 -> 0.25–4.0; page nav; overlay stack)
    - apps/web/src/app/emails/[id]/_components/action-toolbar.tsx (the Tooltip>TooltipTrigger asChild>Button toolbar idiom; aria-pressed toggle)
    - apps/web/src/app/emails/[id]/_components/draw-mode-bar.tsx (status-bar pattern, reused for active-parent banner in 09-09)
    - 09-UI-SPEC.md §Toolbar (tool mode group classes incl. armed `bg-primary/10 text-primary border border-primary/30`; nav group; zoom group + keybindings; view toggles Regions/History/Unrelated), §Keybindings
    - 09-PATTERNS.md section "canvas-toolbar.tsx"
  </read_first>
  <action>
    `pdf-preview-pane.tsx`: extend the zoom range to 0.25–4.0 (step 0.25); add Cmd/Ctrl+scroll zoom-to-cursor (compute normalized pointer pos before zoom, apply scale, scroll to maintain pointer position) and Space+drag pan (toggle `cursor-grab`/`cursor-grabbing` class; two-finger/trackpad scroll pans without a modifier) on the scroll viewport. Add Fit-width / Fit-page handlers (compute scale from panel width / page size). Keep the overlay stack + the display:none-not-unmount overlay sync (05-04). Expose zoom/page/fit state + setters so the toolbar can drive them (lift to props or a small local state object the shell passes down).

    Create `canvas-toolbar.tsx` (`"use client"`, `role="toolbar" aria-label="Canvas tools"`, `h-11 flex items-center gap-2 border-b px-3 bg-background shrink-0`): the tool-mode group (Select `MousePointer2` / Draw `Pencil`, two-button `role="group"`, armed button gets `bg-primary/10 text-primary border border-primary/30`, inactive ghost; aria-pressed + aria-keyshortcuts "v s" / "d"); the nav group (Prev/Next page `ChevronLeft/Right` + "Page N / M" aria-live=polite); the zoom group (ZoomOut/ZoomIn, "{N}%" reset button, Fit width, Fit page) with the keybindings from the UI-SPEC; view-toggle switches (Regions / History / Unrelated — Unrelated default off, D-12); and a right-group ghost `X` close button. Wire the keybindings (V/S, D, Esc, Cmd/Ctrl +/-/0, Cmd/Ctrl+Shift+W/F) as documented.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '0.25|4|Fit width|Fit page|cursor-grab' apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx` shows the expanded zoom range + fit + pan.
    - `canvas-toolbar.tsx` has the Select/Draw toggle (armed `bg-primary/10 text-primary border border-primary/30`), page nav, zoom group, and the Regions/History/Unrelated switches; Unrelated defaults off.
    - aria-pressed + aria-keyshortcuts present on the tool-mode buttons; Page indicator is aria-live=polite.
    - `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The canvas zooms 0.25–4.0 with zoom-to-cursor + fit + Space-pan, and a toolbar exposes tool mode / nav / zoom / view toggles with the documented keybindings.</done>
</task>

<task type="auto">
  <name>Task 3: use-canvas-state + use-role-mutations + canvas-shell (D-06/D-08/D-09/D-10)</name>
  <read_first>
    - apps/web/src/app/emails/[id]/_components/use-region-edit.ts (the optimistic mutation idiom + mutatingComponentIds; selection/draw state; the existing redraw mutation = D-09 supersede path)
    - apps/web/src/app/emails/[id]/_components/use-autofill.ts (the non-optimistic phase machine for autofillFields)
    - apps/web/src/app/emails/[id]/_components/pdf-preview-pane.tsx + draw-overlay.tsx + overlay-layer.tsx (what the shell composes)
    - 09-04 mutations (setRole/setEntityType/setFieldRelationship/autofillFields/denyField/confirmField)
    - 09-PATTERNS.md sections "use-canvas-state.ts", "use-role-mutations.ts", "use-autofill-fields.ts", "canvas-shell.tsx"
    - 09-UI-SPEC.md §Layout Shell (exact class sequence), §Active-Parent Model, §Drawing Model (D-08, D-09 move/resize=redraw), §API + Mutation Patterns
  </read_first>
  <action>
    `use-canvas-state.ts` (`"use client"`): owns tool mode (`type CanvasMode = "select"|"draw"`), selection (`selectedIds: string[]`, single + shift-multi), and the active-parent model (`activeParentId: string|null`). Selecting an ENTITY-role component sets `activeParentId`; Esc / click-empty / explicit Clear resets it. When `activeParentId` is set, a newly completed draw is tagged "create as field of activeParent" (the create call is wired in 09-09 via createRegion + setFieldRelationship, OR here expose an `onDrawComplete(rect)` that the shell/09-09 routes to the right create path). Move/resize (D-09): expose an `onBoxGeometryChange(componentId, rect)` that routes to the EXISTING Phase 6 `redraw` mutation (supersede-never-mutate) — never an in-place geometry write. Return an immutable `as const` object (never mutate state). Implement the D-08 default: drag on empty canvas draws regardless of select/draw toggle; click a box selects; Shift-click adds to selection.

    `use-role-mutations.ts` (`"use client"`): wrap `setRole`, `setEntityType`, `setFieldRelationship`, `confirmField` (or the existing Phase 7 confirm), `denyField` with the use-region-edit optimistic idiom (cancel + getData + setData map over `prev.components`, onError restore + `toast.error(...)`, onSuccess invalidate + optional toast). `autofillFields` is NON-optimistic: phase machine (idle/extracting/reviewing/failed) + `await utils.emails.detail.invalidate({id: emailId})` on success to pick up new candidate field children; 6000ms failure toast `"AI autofill is unavailable — model access is pending."`. Expose `mutatingComponentIds` for aria-busy. (D-09 redraw stays in use-region-edit — reuse it, do not duplicate.)

    `canvas-shell.tsx` (`"use client"`): the four-zone shell per §Layout Shell — outer `flex flex-col h-full`; toolbar `h-11 shrink-0` (renders `<CanvasToolbar/>`); inner `flex flex-1 min-h-0 overflow-hidden` with a 256px `w-64 shrink-0 border-r` LAYERS slot, a `flex-1 min-w-0 overflow-hidden relative bg-muted/40` CANVAS slot (renders `<PdfPreviewPane/>` + `<OverlayLayer/>` + `<DrawOverlay/>` + the active-parent banner slot), and a 288px `w-72 shrink-0 border-l` INSPECTOR slot. The LAYERS and INSPECTOR contents are passed as props/children (the actual panels are 09-09); the shell wires tool mode/zoom/selection/active-parent from `use-canvas-state` + `use-role-mutations` to the canvas children. It renders inside the app-shell `SidebarInset` (09-06).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -15 && npx next build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `use-canvas-state.ts` exposes `mode`, `selectedIds`, `activeParentId`, `setActiveParentId`/clear, `onBoxGeometryChange` (routes to the existing redraw = D-09), and returns an immutable `as const` object; drag-draws-by-default + click-selects + shift-multiselects are implemented.
    - `use-role-mutations.ts` wraps setRole/setEntityType/setFieldRelationship/confirm/deny optimistically (revert on error) and runs autofillFields non-optimistically with invalidate + the exact 6000ms failure toast copy; exposes `mutatingComponentIds`. No new geometry mutation is authored (D-09 reuses redraw).
    - `canvas-shell.tsx` lays out the 4 zones with the exact `w-64`/`flex-1 min-w-0`/`w-72`/`h-11` classes and composes toolbar + pdf-pane + overlay + draw + active-parent banner slot.
    - `cd apps/web && npx tsc --noEmit` exits 0 AND `npx next build` succeeds.
  </acceptance_criteria>
  <done>The canvas state machine (tool mode + selection + active-parent + redraw-based move/resize, D-09), the optimistic + autofill mutation hook, and the 4-zone shell exist and compile; panels plug in via 09-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas pointer input -> normalized geometry | Client pointer coords converted to normalized [0,1] before any write. |
| mutation hooks -> tRPC -> FastAPI | All writes proxied server-side; the key never reaches the canvas client code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-70 | Information Disclosure | API key in canvas client | accept/mitigate | Hooks call only tRPC mutations (09-04 proxy); no fetch-to-FastAPI, no key in client (grep confirms no EMAIL_LISTENER_API_KEY in _components). |
| T-09-71 | Tampering | out-of-bounds geometry | mitigate | Pointer coords normalized + clamped to [0,1] via geometry.ts (clientXYToNormalized/normalizedRectToPolygon); FastAPI re-validates polygon (4 pairs, [0,1]) at the boundary. |
| T-09-72 | Tampering | stale optimistic state / in-place geometry edit | mitigate | Optimistic snapshot/revert (use-region-edit idiom): onError restores prevData; onSuccess invalidates emails.detail. Move/resize routes to the existing redraw (supersede-never-mutate, D-09) — originals are never mutated. |
| T-09-73 | Denial of Service | autofill spam / model-down | mitigate | autofillFields is non-optimistic with a phase machine + friendly 6000ms degrade toast; controls disabled while extracting (aria-busy via mutatingComponentIds). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages (react-pdf/lucide/@nauta/ui already present); legitimacy gate N/A. |
</threat_model>

<verification>
- Overlay boxes render role colors + active-parent ring; overlay layer filters fields to the selected entity (D-12).
- Canvas zooms 0.25–4.0 with zoom-to-cursor/fit/Space-pan; toolbar exposes tool mode/nav/zoom/view toggles + keybindings.
- 4-zone shell composes toolbar + canvas primitives; hooks own tool/selection/active-parent + optimistic mutations + non-optimistic autofill; move/resize reuses redraw (D-09).
- `apps/web` tsc clean + `next build` succeeds.
</verification>

<success_criteria>
- D-06: four-zone editor shell renders the existing primitives in the new frame.
- D-07: zoom + pan controls (range, zoom-to-cursor, fit, Space-pan) work.
- D-08: drag-draws-by-default + click/shift-select + Select/Draw toggle + Esc.
- D-09: move/resize reuses the Phase 6 redraw supersede (never mutates geometry in place).
- D-10: active-parent model + role-color overlays + active-parent ring.
- Optimistic role/relationship/confirm/deny + non-optimistic autofillFields hooks; tsc + next build green.
</success_criteria>

<output>
Create `.planning/phases/09-entity-field-region-relationships-canvas/09-08-SUMMARY.md` when done
</output>

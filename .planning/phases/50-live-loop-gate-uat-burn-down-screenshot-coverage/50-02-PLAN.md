---
phase: 50-live-loop-gate-uat-burn-down-screenshot-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/uat-39-tool-round.spec.ts
  - apps/web/e2e/uat-41-knowledge-preview.spec.ts
  - apps/web/e2e/helpers/uat-chat-fixtures.ts
  - .planning/milestones/v1.6-phases/39-tool-round-ui-citation-chips/39-HUMAN-UAT.md
  - .planning/milestones/v1.6-phases/41-knowledge-preview-canvas-node/41-HUMAN-UAT.md
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "Every scenario in 39-HUMAN-UAT.md (2) and 41-HUMAN-UAT.md (5) has a recorded disposition — none remain 'pending'"
    - "39.1 (live in-round tool-round activity affordance) is proven against the LOCAL seeded-session stack with a DB-backed assertion"
    - "41.1-41.5 (knowledge-preview node: two-ring layout, tooltip, popover, viewport-center placement, remove-then-reload persistence) are exercised against the LOCAL stack with the remove/reload round-trip DB-verified"
    - "Any scenario that cannot pass locally becomes a tracked-fix todo with repro steps — never silently parked"
  artifacts:
    - path: "apps/web/e2e/uat-39-tool-round.spec.ts"
      provides: "Seeded-session spec covering 39.1 tool-round affordance + 39.2 citation chip render/deep-link"
      contains: "seedAuthenticatedContext"
    - path: "apps/web/e2e/uat-41-knowledge-preview.spec.ts"
      provides: "Seeded-session spec covering 41.1-41.5 knowledge-preview node scenarios"
      contains: "knowledge-preview"
    - path: "apps/web/e2e/helpers/uat-chat-fixtures.ts"
      provides: "Seeds knowledge_nodes/edges + a chat canvas layout with a placed knowledge-preview node"
      exports: ["seedKnowledgeGraphFixture"]
  key_links:
    - from: "apps/web/e2e/uat-41-knowledge-preview.spec.ts"
      to: "chat.saveCanvasLayout (tRPC) → DB"
      via: "remove node → reload → DB-assert the node stays gone"
      pattern: "saveCanvasLayout|canvas_layout"
    - from: "apps/web/e2e/uat-39-tool-round.spec.ts"
      to: "chat_run_events / provenance-link"
      via: "live tool round → ToolInvocationResultRow → ProvenanceLink chip"
      pattern: "chat_run_events|ProvenanceLink"
---

<objective>
Burn down the /chat-surface UAT backlog (LIVE-05, part A): the two Phase-39 scenarios (tool-round activity
affordance + citation chips) and the five Phase-41 scenarios (knowledge-preview canvas node). All are
"visual/live-stack only" pending items auto-approved during v1.6 for lack of playwright — now runnable
against the LOCAL seeded-session stack proven green in 49-03.

Purpose: convert 7 code-verified-but-live-unconfirmed scenarios into DB-verified passes (or explicit
tracked-fix todos), recorded in each phase's HUMAN-UAT.md.
Output: two new e2e specs, a shared chat-graph fixture helper, and updated 39/41 HUMAN-UAT.md files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-CONTEXT.md
@.planning/milestones/v1.6-phases/39-tool-round-ui-citation-chips/39-HUMAN-UAT.md
@.planning/milestones/v1.6-phases/41-knowledge-preview-canvas-node/41-HUMAN-UAT.md

<interfaces>
From apps/web/e2e/helpers/seed-session.ts:
`seedAuthenticatedContext(context): Promise<{ userId, email }>` — real local session, no interactive Google.

Reuse the fixture-seed + DB-assert pattern verbatim from apps/web/e2e/live-loop-green.spec.ts
(dotenv load lines 30-46; pg.Client on POSTGRES_URL_NON_POOLING; fixed-id `ON CONFLICT DO UPDATE` upserts;
`sendChatMessage`/`waitForTurnSettled`/`assertNotLoginUrl` helpers lines 86-146; anchored-regex rail
selection line 294 to avoid cross-project races).

Chat surface components (read to derive stable selectors + expected DOM):
- apps/web/src/app/chat/_components/tool-round-activity-row.tsx (Loader2 spinner + gerund label DURING a round)
- apps/web/src/app/chat/_components/tool-invocation-result-row.tsx (collapsed label + count + up to 5 chips / "+N")
- apps/web/src/components/provenance-link.tsx (Mail/Box/Share2 icon per kind; hrefFor → /emails/[id], /entities/[id], /knowledge?focus={id})
- apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx, knowledge-preview-mini-graph.tsx, knowledge-preview-layout.ts, add-knowledge-preview-popover.tsx
</interfaces>

Local stack: run per docs/RUN-LOCAL.md (preflight; listener on 127.0.0.1:8000 WITHOUT --reload; web on
localhost:3000; verify via DB at 127.0.0.1:54322). Run assertion specs single-project to avoid the shared-stack
race 49-03 documented: `--project=chromium`. npm workspaces (NOT pnpm).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase-39 burn-down — tool-round activity affordance (39.1) + citation chips (39.2)</name>
  <files>apps/web/e2e/uat-39-tool-round.spec.ts, .planning/milestones/v1.6-phases/39-tool-round-ui-citation-chips/39-HUMAN-UAT.md</files>
  <read_first>
    - apps/web/e2e/live-loop-green.spec.ts (fixture-seed + chat-drive + DB-assert pattern to copy)
    - apps/web/src/app/chat/_components/tool-round-activity-row.tsx and tool-invocation-result-row.tsx (selectors + gerund labels)
    - apps/web/src/components/provenance-link.tsx and provenance-link.test.tsx (icon-per-kind + hrefFor routing already unit-proven)
    - .planning/milestones/v1.6-phases/39-tool-round-ui-citation-chips/39-HUMAN-UAT.md (the exact 2 scenarios + expected text)
  </read_first>
  <behavior>
    - 39.1: sending a chat prompt that triggers a real server tool round shows a ToolRoundActivityRow (spinner + gerund label) DURING the round, replaced in-place by a collapsed ToolInvocationResultRow when the round settles; a `chat_run_events` tool_call row exists for the conversation (DB-verified).
    - 39.2: when the tool result carries provenance, a ProvenanceLink chip renders with the correct kind icon and its `href` deep-links to /emails/[id] | /entities/[id] | /knowledge?focus={id}; if no provenance-bearing result is reachable without heavy confirmed-data seeding, the scenario is recorded as a tracked-fix (see action).
  </behavior>
  <action>
    Write `uat-39-tool-round.spec.ts` using the seeded session. Insert a conversation row directly (own random id +
    run-unique title; select it in the rail via anchored regex — the 49-03 anti-race pattern). Drive a prompt that reliably
    triggers a real tool round, and assert: (1) the ToolRoundActivityRow is visible transiently, then (2) the collapsed
    ToolInvocationResultRow appears, backed by (3) a DB assertion that a `chat_run_events` tool_call row exists for that
    conversation. That closes 39.1.
    For 39.2 (citation chips need a provenance-bearing result): first ATTEMPT the honest path — seed the minimum CONFIRMED
    extracted data (an entity_instance / confirmed email region / knowledge_node the search tools read) so `search_emails`
    or `search_knowledge` returns at least one provenance result, then assert a ProvenanceLink chip renders with the correct
    icon and its `href` attribute points at the right deep-link route (href assertion, not an actual new-tab open).
    Decision rule (Claude's discretion per CONTEXT): if seeding confirmed data to force a provenance result exceeds ~30 min,
    STOP seeding, mark 39.2 as `tracked-fix` in 39-HUMAN-UAT.md with concrete repro steps + the seeding blocker, and add a
    todo under `.planning/todos/`. Do NOT fabricate a chip or weaken the assertion to pass.
    Update 39-HUMAN-UAT.md: set each scenario `result:` to `passed — <evidence pointer>` or `tracked-fix — <todo pointer>`,
    update the Summary counts + frontmatter `status`.
  </action>
  <verify>
    <automated>cd apps/web && npm run test:e2e -- uat-39-tool-round.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `uat-39-tool-round.spec.ts` passes on chromium against the live local stack; the tool-round assertion is DB-backed (a real `chat_run_events` tool_call row), never a no-op.
    - 39-HUMAN-UAT.md has zero `result: [pending]` lines; each of the 2 scenarios is `passed` or `tracked-fix` with an evidence/todo pointer.
    - If 39.2 is `tracked-fix`, a `.planning/todos/` entry exists with repro steps (grep the todo filename in 39-HUMAN-UAT.md).
    - Summary block counts (`passed`/`issues`) match the recorded results; frontmatter `status` updated from `partial`.
  </acceptance_criteria>
  <done>Both Phase-39 scenarios have a DB-verified disposition; none pending or silently parked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-41 burn-down — knowledge-preview node scenarios (41.1-41.5)</name>
  <files>apps/web/e2e/uat-41-knowledge-preview.spec.ts, apps/web/e2e/helpers/uat-chat-fixtures.ts, .planning/milestones/v1.6-phases/41-knowledge-preview-canvas-node/41-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.6-phases/41-knowledge-preview-canvas-node/41-HUMAN-UAT.md (the exact 5 scenarios + expected DOM/tier attributes)
    - apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx, knowledge-preview-mini-graph.tsx, knowledge-preview-layout.ts, add-knowledge-preview-popover.tsx (DOM structure: svg line count, stroke-dasharray/opacity tier styling, remove X button, add-popover fields)
    - apps/web/e2e/live-loop-green.spec.ts (seed + DB-assert + reload pattern)
    - .planning/milestones/v1.6-phases/41-knowledge-preview-canvas-node/*SUMMARY*.md if present (how canvas layout persists via chat.saveCanvasLayout)
  </read_first>
  <behavior>
    - 41.1: a placed knowledge-preview node whose focus has 1-hop + 2-hop neighbours renders focus dot centered, inner/outer rings, SVG edges, tier styling (dashed=INFERRED, faint=AMBIGUOUS, solid=EXTRACTED) inside the 280x140 box.
    - 41.2: hovering a mini-graph dot shows a Radix Tooltip (~300ms) with the full label, dismissing on mouse-leave.
    - 41.3: the "Add knowledge preview" toolbar button opens a Popover, the form is usable, closes cleanly on Cancel/Add/outside-click.
    - 41.4: adding a node while the canvas is panned/zoomed places it near the CURRENT viewport center (cascaded off overlaps), not the origin.
    - 41.5: removing a node then a full reload keeps it gone (debounced chat.saveCanvasLayout persisted the removal to the DB).
  </behavior>
  <action>
    Create `uat-chat-fixtures.ts` exporting `seedKnowledgeGraphFixture(userId)` that seeds the minimum knowledge_nodes + edges
    for a focus node with BOTH 1-hop and 2-hop neighbours across tiers (EXTRACTED/INFERRED/AMBIGUOUS) so the mini-graph has real
    content to lay out, plus (if needed) a chat canvas layout row placing one knowledge-preview node. Reuse the fixed-id
    `ON CONFLICT DO UPDATE` discipline; never log secrets.
    Write `uat-41-knowledge-preview.spec.ts` (seeded session, `--project=chromium`) covering all five scenarios:
    41.1 assert the svg line/dot counts + tier stroke attributes on the placed node; 41.2 hover a dot and assert the tooltip
    content appears then dismisses; 41.3 open the add-preview popover, assert it opens/validates/closes on Cancel + outside-click;
    41.4 add a node with the canvas panned and assert its resulting flow-position is within a tolerance of the viewport center
    (read the node's transform / position, not the origin); 41.5 remove a node, reload the page, and DB-assert the canvas layout
    row no longer contains that node id (the round-trip, not just local React Flow state).
    Any scenario that genuinely cannot be exercised deterministically (e.g. React Flow viewport transform not observable) →
    record it `tracked-fix` with repro steps, not pending. Update 41-HUMAN-UAT.md results + Summary + frontmatter `status`.
  </action>
  <verify>
    <automated>cd apps/web && npm run test:e2e -- uat-41-knowledge-preview.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `uat-41-knowledge-preview.spec.ts` passes on chromium; 41.5's persistence check is DB-backed (asserts the layout row after reload, not just DOM).
    - 41-HUMAN-UAT.md has zero `result: [pending]` lines; each of the 5 scenarios is `passed` or `tracked-fix` with an evidence/todo pointer.
    - `seedKnowledgeGraphFixture` is a named export in `uat-chat-fixtures.ts` and seeds at least one EXTRACTED and one INFERRED/AMBIGUOUS edge (so tier styling is actually exercised, not vacuously).
    - Summary counts + frontmatter `status` updated.
  </acceptance_criteria>
  <done>All five Phase-41 scenarios have a DB/DOM-verified disposition; none pending or silently parked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → Supabase admin | seeded session uses service_role via seed-session.ts |
| test-runner → local FastAPI + Bedrock | the live tool round hits real model transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Elevation of Privilege | seeded session in uat specs | mitigate | Specs run only against the local stack (POSTGRES_URL_NON_POOLING → 127.0.0.1); seed-session cookie domain is "localhost"; no hosted target is ever addressed |
| T-50-05 | Information Disclosure | e2e fixtures / evidence pointers | mitigate | Fixtures use synthetic data; no secret/token is written to a UAT file or todo; seed-session never logs secrets |
| T-50-06 | Tampering | npm installs | accept | No new dependencies — pg/dotenv/@playwright/test already present; no install task |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; if one becomes necessary, stop and gate behind a legitimacy checkpoint |
</threat_model>

<verification>
- Both specs pass on chromium against the running local stack.
- 39-HUMAN-UAT.md and 41-HUMAN-UAT.md contain zero `[pending]` results after this plan.
- Any tracked-fix has a `.planning/todos/` entry with repro steps.
</verification>

<success_criteria>
- LIVE-05 (chat-surface slice): all 7 scenarios (39×2 + 41×5) closed as passed or converted to a tracked fix, DB/DOM-verified, recorded in their HUMAN-UAT.md files. Dispositions feed the 50-05 roll-up.
</success_criteria>

<output>
Create `.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-02-SUMMARY.md` when done.
List each scenario's disposition + evidence pointer (50-05 aggregates these into 50-UAT-BURNDOWN.md).
</output>

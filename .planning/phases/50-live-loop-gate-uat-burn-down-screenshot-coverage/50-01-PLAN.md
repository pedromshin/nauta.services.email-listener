---
phase: 50-live-loop-gate-uat-burn-down-screenshot-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/screenshot-review.spec.ts
  - apps/web/e2e/helpers/screenshot-fixtures.ts
autonomous: true
requirements: [LIVE-06]
must_haves:
  truths:
    - "The screenshot harness SURFACES list includes /emails/[id] and the route renders real seeded content (not a not-found/blank)"
    - "Authenticated surfaces (inbox, chat, knowledge, emails) capture real pixels via the 49-03 seeded session — index.md authStatus is 'captured', not 'redirected to /login (no session)'"
    - "The seeded session is used ONLY against a local target; a non-local baseURL/SUPABASE_URL skips seeding rather than sending service_role admin traffic to a hosted project"
    - "Captures land under .planning/ui-reviews/<timestamp>/ per the existing convention"
  artifacts:
    - path: "apps/web/e2e/screenshot-review.spec.ts"
      provides: "Extended capture harness: /emails/[id] surface + seeded-session wiring + local-only guard"
      contains: "seedAuthenticatedContext"
    - path: "apps/web/e2e/helpers/screenshot-fixtures.ts"
      provides: "Seeds a thread+email fixture owned by the seed user's importer, returns the email id for the emails surface path"
      exports: ["seedEmailFixture"]
  key_links:
    - from: "apps/web/e2e/screenshot-review.spec.ts"
      to: "apps/web/e2e/helpers/seed-session.ts"
      via: "seedAuthenticatedContext(context) before the capture loop"
      pattern: "seedAuthenticatedContext"
    - from: "apps/web/e2e/screenshot-review.spec.ts"
      to: ".planning/ui-reviews/<timestamp>/emails-desktop.png"
      via: "captureSurface over the /emails/{id} path"
      pattern: "emails"
---

<objective>
Close todo W-1 (LIVE-06): extend the 47-05 screenshot-review harness so it (a) captures `/emails/[id]`
with a seeded email fixture, and (b) uses the 49-03 seeded session so authenticated surfaces capture
REAL pixels instead of falling back to the "redirected to /login (no session)" textual note.

Purpose: the re-skin phases (51+) and the visual UAT burn-down (50-04) need real authenticated pixel
captures, not the public-only best-effort shots the harness produces today.
Output: an extended `screenshot-review.spec.ts`, a new `screenshot-fixtures.ts` helper, and a fresh
`.planning/ui-reviews/<timestamp>/` run containing authenticated captures for every surface + `/emails/[id]`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-CONTEXT.md

<interfaces>
<!-- Reusable assets — use directly, no codebase exploration needed. -->

From apps/web/e2e/helpers/seed-session.ts:
```typescript
export interface SeedAuthenticatedContextOptions { readonly email?: string }
export interface SeedAuthenticatedContextResult { readonly userId: string; readonly email: string }
// Idempotently ensures the seed user, mints a REAL session via GoTrue admin
// (magiclink + verifyOtp, NEVER interactive Google), injects the @supabase/ssr
// sb-127-auth-token cookie(s) on domain "localhost". Reads SUPABASE_URL /
// SUPABASE_SERVICE_ROLE_KEY / NEXT_PUBLIC_SUPABASE_ANON_KEY from env; never logs a secret.
export async function seedAuthenticatedContext(
  context: BrowserContext, options?: SeedAuthenticatedContextOptions,
): Promise<SeedAuthenticatedContextResult>;
```

Fixture-seed pattern (copy from apps/web/e2e/live-loop-green.spec.ts lines 176-212):
- Look up the seed user's importer: `SELECT id FROM importers WHERE user_id = $1 ORDER BY created_at ASC LIMIT 1`
- `INSERT INTO threads (id, importer_id, subject) ... ON CONFLICT (id) DO UPDATE`
- `INSERT INTO emails (id, importer_id, message_id, received_at, sender_address, sender_name, to_addresses, subject, body_text, parse_status, thread_id) VALUES (...,'parsed',...) ON CONFLICT (id) DO UPDATE`
- DB via `new pg.Client({ connectionString: process.env.POSTGRES_URL_NON_POOLING })`
- Root `.env.local` is loaded with `dotenv` at `path.resolve(process.cwd(), "..", "..", ".env.local")` (Playwright test runner does NOT dotenv-wrap itself).
</interfaces>

Local stack facts (docs/RUN-LOCAL.md): `scripts/preflight-local.ps1` brings it up; Supabase `project_id=polytoken`
on 127.0.0.1:54321 (API) / 54322 (pg); listener on 127.0.0.1:8000 WITHOUT `--reload`; web on localhost:3000;
verify via DB, not the terminal; npm workspaces (NOT pnpm). The harness has its own webServer
(`reuseExistingServer: true`) so it will attach to an already-running `npm run dev`.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add /emails/[id] surface + seeded email fixture, wire the seeded session with a local-only guard</name>
  <files>apps/web/e2e/helpers/screenshot-fixtures.ts, apps/web/e2e/screenshot-review.spec.ts</files>
  <read_first>
    - apps/web/e2e/screenshot-review.spec.ts (the harness being extended — SURFACES, captureSurface, resolveAuthStatus, writeIndex, RUN_DIR anchoring)
    - apps/web/e2e/helpers/seed-session.ts (seedAuthenticatedContext signature + cookie domain "localhost")
    - apps/web/e2e/live-loop-green.spec.ts lines 30-64 and 176-212 (dotenv load + pg fixture-seed pattern + fixed fixture ids)
    - apps/web/playwright.screenshot.config.ts (baseURL http://localhost:3000, workers:1, webServer)
  </read_first>
  <action>
    Create `apps/web/e2e/helpers/screenshot-fixtures.ts` exporting `seedEmailFixture(userId: string): Promise<{ emailId: string }>`.
    It opens a `pg.Client` on `POSTGRES_URL_NON_POOLING` (load root `.env.local` the same way seed-session.ts does),
    looks up the seed user's importer, upserts a fixed-id thread + email (own constant UUIDs, `ON CONFLICT (id) DO UPDATE`,
    `parse_status='parsed'`, a readable subject/body so `/emails/[id]` renders real content), closes the client, and returns
    the fixed `emailId`. Throw a clear error if the user owns no importer (tell the operator to run scripts/preflight-local.ps1).
    Never log secrets; validate the userId argument is a non-empty string at entry.

    In `screenshot-review.spec.ts`:
    1. Add a local-only guard `isLocalTarget(baseURL: string, supabaseUrl: string): boolean` returning true only when BOTH
       hosts are in `{ "localhost", "127.0.0.1" }`. This is the security gate — the seeded session mints a service_role admin
       session and MUST NEVER run against a hosted Supabase/Vercel target (T-50-01).
    2. Before the capture loop: if `isLocalTarget` is true, call `seedAuthenticatedContext(context)` to authenticate, then
       `seedEmailFixture(userId)` to get the fixture email id; if false, skip both (keep today's best-effort public capture,
       and record a header note that authenticated capture was skipped for a non-local target).
    3. Build SURFACES dynamically so it includes `{ name: "emails", path: "/emails/" + emailId }` ONLY when the fixture was
       seeded (local). Keep the existing six surfaces. Do NOT hardcode a bare `/emails/[id]` literal path.
    4. Update the header docstring: the T-47-11 "never fake a session" note is SUPERSEDED for the local case — the seeded
       session is a REAL session minted locally, never usable against hosted (state the guard). Update `writeIndex`'s header
       text so it no longer claims all protected routes redirect; it now reflects authenticated capture on a local target.
    Use named exports only, type everything explicitly, immutable updates (spread), files under 800 lines.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json --incremental false 2>&1 | grep -E "screenshot-review|screenshot-fixtures" || echo "no type errors in changed e2e files (e2e is tsconfig-excluded; scratch-check clean)"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/helpers/screenshot-fixtures.ts` exists and exports `seedEmailFixture` (named export).
    - `grep -n "isLocalTarget" apps/web/e2e/screenshot-review.spec.ts` returns a guard used to gate `seedAuthenticatedContext`.
    - `grep -n "seedAuthenticatedContext" apps/web/e2e/screenshot-review.spec.ts` shows the call is inside the `isLocalTarget` branch (not unconditional).
    - The SURFACES/emails entry is built from the seeded `emailId`, not a literal `[id]` string (`grep -n "/emails/" apps/web/e2e/screenshot-review.spec.ts` shows string concatenation with the id).
    - No secret/token/service-role value is written to any string that reaches the index or a screenshot filename.
  </acceptance_criteria>
  <done>The harness authenticates (local only) and includes an /emails/[id] surface built from a seeded fixture; a non-local target skips seeding.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the harness live against the local stack, capture authenticated surfaces, verify the run</name>
  <files>apps/web/e2e/screenshot-review.spec.ts</files>
  <read_first>
    - docs/RUN-LOCAL.md (cold-start: preflight, listener WITHOUT --reload, dev server, DB verification)
    - apps/web/e2e/helpers/screenshot-fixtures.ts (from Task 1)
    - .planning/ui-reviews/2026-07-10T18-39-30-080Z/index.md (the existing pre-seeded-session run — for the before/after contrast)
  </read_first>
  <action>
    Ensure the local stack is up per docs/RUN-LOCAL.md (`scripts/preflight-local.ps1`; start the listener with
    `uv run uvicorn app.main:app --host 127.0.0.1 --port 8000` WITHOUT `--reload` if not already running; `npm run dev`).
    Then run the harness: `npm run screenshot:review -w @polytoken/web`. This produces a new
    `.planning/ui-reviews/<timestamp>/` directory. Inspect the generated `index.md`: inbox, chat, knowledge, and emails rows
    MUST show authStatus `captured` (not `redirected to /login (no session)`), and an `emails-desktop.png` / `emails-mobile.png`
    pair MUST exist. If any auth surface still shows redirected, the seeded cookie is not being read — fix the wiring
    (check the cookie domain/baseURL host match: cookie domain is "localhost", baseURL must be http://localhost:3000, not
    127.0.0.1) and re-run. Do not weaken the guard to force a pass. Record the produced run directory path in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.planning/ui-reviews';const d=fs.readdirSync(p).filter(x=>fs.statSync(p+'/'+x).isDirectory()).sort();const latest=p+'/'+d[d.length-1];const idx=fs.readFileSync(latest+'/index.md','utf8');const hasEmails=fs.existsSync(latest+'/emails-desktop.png');const redirectedAuth=/(inbox|chat|knowledge|emails)\s*\|\s*(mobile|desktop)\s*\|\s*default\s*\|\s*redirected/i.test(idx);if(!hasEmails)throw new Error('missing emails-desktop.png in '+latest);if(redirectedAuth)throw new Error('an auth surface still redirected to /login in '+latest);console.log('OK: authenticated captures present in '+latest)"</automated>
  </verify>
  <acceptance_criteria>
    - A new `.planning/ui-reviews/<timestamp>/` directory exists with `emails-desktop.png` and `emails-mobile.png` present and non-empty (> 5KB each).
    - `index.md` shows authStatus `captured` for the inbox, chat, knowledge, and emails surfaces (no `redirected to /login` on those rows).
    - `index.md` contains an `emails` surface row (grep `emails`).
    - The `/login` surface still captures (public), proving the harness did not regress the public path.
  </acceptance_criteria>
  <done>A live harness run captured every authenticated surface + /emails/[id] as real pixels under .planning/ui-reviews/<timestamp>/, verified by DB-seeded content and file presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → Supabase admin (service_role) | seed-session mints a REAL session using the service_role key |
| capture harness → filesystem artifact | PNGs + index.md are written to a repo-tracked directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Elevation of Privilege | screenshot-review.spec.ts seeded session | mitigate | `isLocalTarget` guard: seed ONLY when baseURL host AND SUPABASE_URL host are localhost/127.0.0.1; a non-local target skips seeding entirely (never sends service_role admin traffic to hosted) |
| T-50-02 | Information Disclosure | ui-reviews PNGs + index.md | mitigate | Screenshots render only seeded fixture data (the operator's own seed email); no secret/token is ever written to a filename or index string; seed-session already never logs secrets |
| T-50-03 | Tampering | npm installs | accept | No new dependencies — pg/dotenv/@playwright/test/@supabase/ssr already in the tree; no install task in this plan |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs performed; if one becomes necessary, stop and gate behind a legitimacy checkpoint |
</threat_model>

<verification>
- `npm run screenshot:review -w @polytoken/web` completes and produces authenticated captures.
- The verify one-liner (Task 2) confirms `/emails/[id]` PNGs exist and no auth surface redirected.
- Type-check of the two changed e2e files is clean (via scratch tsconfig, since e2e is excluded from the package tsconfig program).
</verification>

<success_criteria>
- LIVE-06 satisfied: harness SURFACES covers `/emails/[id]`; authenticated surfaces capture real pixels via the seeded session; the seeded session is provably local-only.
- W-1 closed.
</success_criteria>

<output>
Create `.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-01-SUMMARY.md` when done.
Record the produced `.planning/ui-reviews/<timestamp>/` path (downstream plan 50-04 references it as visual evidence).
</output>

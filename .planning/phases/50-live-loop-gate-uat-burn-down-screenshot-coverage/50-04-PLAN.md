---
phase: 50-live-loop-gate-uat-burn-down-screenshot-coverage
plan: 04
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - apps/web/e2e/uat-48-token-surfaces.spec.ts
  - .planning/milestones/v1.8-phases/47-brand-foundation-verification-tooling/47-HUMAN-UAT.md
  - .planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "Both Phase-48 scenarios have a DB/CSS-verified disposition and both cite a REAL authenticated pixel capture from the 50-01 harness run — never a textual before/after fallback"
    - "48.1: the ProvenanceLink chip is a true stadium/pill (computed border-radius resolves to the pill token, not a rounded rectangle) and the confirmed-good affordances render the success token green, distinct from destructive red — asserted on /chat and /emails/[id]"
    - "48.2: /knowledge node chrome / filter dots / node-detail badges use the closed graph palette and EXTRACTED vs INFERRED/AMBIGUOUS edges render distinct tier strokes"
    - "47.1 (brand-mark visual fit) has its real login pixels captured (evidence) with the subjective aesthetic sign-off routed to the morning checklist — not pending, not silently parked"
  artifacts:
    - path: "apps/web/e2e/uat-48-token-surfaces.spec.ts"
      provides: "Seeded-session DOM/CSS assertions for 48.1 (chip pill + success green) and 48.2 (graph palette + tier strokes)"
      contains: "getComputedStyle|border-radius|borderRadius"
  key_links:
    - from: "apps/web/e2e/uat-48-token-surfaces.spec.ts"
      to: "apps/web/src/components/provenance-link.tsx"
      via: "computed border-radius == pill token (fully-rounded stadium)"
      pattern: "borderRadius|radius"
    - from: ".planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md"
      to: ".planning/ui-reviews/<50-01-timestamp>/"
      via: "evidence pointer to real authenticated captures"
      pattern: "ui-reviews"
---

<objective>
Burn down the visual/token-surface UAT backlog (LIVE-05, part C): the two Phase-48 token-extension scenarios
(chip/success surfaces on /chat + /emails/[id]; graph/tier surfaces on /knowledge) and the one Phase-47 brand-mark
scenario. Prove the token effects with DOM/CSS assertions against the LOCAL seeded-session stack, and cite the REAL
authenticated pixel captures produced by 50-01 (LIVE-06) as visual evidence — closing out the "textual before/after
fallback" the v1.8 UAT files had to settle for.

Purpose: the last UAT slice, and the one that consumes 50-01's authenticated captures.
Output: one new e2e spec and updated 47/48 HUMAN-UAT.md files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-CONTEXT.md
@.planning/milestones/v1.8-phases/47-brand-foundation-verification-tooling/47-HUMAN-UAT.md
@.planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md

<interfaces>
From apps/web/e2e/helpers/seed-session.ts: `seedAuthenticatedContext(context)` — real local session.
Reuse the seeded email/chat fixture pattern (apps/web/e2e/live-loop-green.spec.ts) so /chat, /emails/[id], /knowledge
render real content.

Token/surface source files (read to derive EXACT expected computed values, not guesses):
- apps/web/src/components/provenance-link.tsx (the chip whose radius must be the pill/stadium token)
- apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx, layers-tree-row.tsx, extraction-summary-panel.tsx (confirmed-good success-green vs destructive-red affordances)
- apps/web/src/app/knowledge/_components/graph-nodes.tsx, graph-legend.tsx, tier-filter-control.tsx, tier-edge-style.ts (closed graph palette + EXTRACTED/INFERRED/AMBIGUOUS tier strokes)
- Token sources: apps/web/src/app/globals.css, packages/tailwind-config/{base.ts,web.ts}, packages/genui/src/theme/tokens.ts (the pill radius / success / graph-palette / tier CSS custom properties + resolved values)

Prior textual artifacts for contrast: .planning/ui-reviews/2026-07-10T20-30-05.134Z/index.md (48-03) and
.planning/ui-reviews/2026-07-10T21-05-50.831Z/index.md (48-04) — the before/after this plan replaces with real pixels.
</interfaces>

DEPENDS ON 50-01: use the authenticated `.planning/ui-reviews/<timestamp>/` run 50-01 produced (chat, emails,
knowledge desktop+mobile PNGs) as the pixel evidence cited in the 48/47 UAT files. Read 50-01-SUMMARY.md for the exact
run-directory path. Local stack per docs/RUN-LOCAL.md; run `--project=chromium`. npm workspaces (NOT pnpm).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase-48 burn-down spec — chip pill + success-green (48.1) and graph palette + tier strokes (48.2)</name>
  <files>apps/web/e2e/uat-48-token-surfaces.spec.ts</files>
  <read_first>
    - .planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md (the exact 2 scenarios + expected effects)
    - apps/web/src/components/provenance-link.tsx (chip radius)
    - apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx, layers-tree-row.tsx, extraction-summary-panel.tsx (success vs destructive)
    - apps/web/src/app/knowledge/_components/{graph-nodes.tsx,graph-legend.tsx,tier-filter-control.tsx,tier-edge-style.ts}
    - apps/web/src/app/globals.css + packages/genui/src/theme/tokens.ts (resolved pill/success/graph/tier values to assert against)
    - apps/web/e2e/live-loop-green.spec.ts (seeded session + fixture + a chat conversation that yields a provenance chip; reuse 50-02's confirmed-data seeding approach if a real chip is needed on /chat)
  </read_first>
  <behavior>
    - 48.1 (/chat + /emails/[id]): a rendered ProvenanceLink chip's computed `border-radius` resolves to the fully-rounded pill token (a stadium, e.g. 9999px), NOT a small rounded-rectangle radius. On /emails/[id], the confirm affordances (layers-tree-row confirm dot, extraction-summary confirmed swatch, CONFIRM button) render the success token color; the DENY/deny controls render the destructive token color; the two are distinct.
    - 48.2 (/knowledge): node chrome / filter-rail dots / node-detail badges use the closed graph-palette custom properties (distinct categories, not old violet/amber/slate); an EXTRACTED edge shows the tier-extracted stroke while an INFERRED/AMBIGUOUS edge shows the dashed/faint tier stroke.
  </behavior>
  <action>
    Write `uat-48-token-surfaces.spec.ts` (seeded session, `--project=chromium`). Seed the fixtures needed for the target
    elements to actually render: an email (for /emails/[id] confirm affordances — reuse the 50-01/50-02 pattern), a knowledge
    graph with at least one EXTRACTED and one INFERRED/AMBIGUOUS edge (for /knowledge tier strokes), and — for the /chat chip —
    a provenance-bearing tool result (reuse 50-02's confirmed-data seeding; if that provenance chip proved to be a tracked-fix
    in 50-02, assert the chip radius on /emails/[id]'s citation surface or the knowledge deep-link chip instead, and note the
    /chat-chip slice inherits 50-02's tracked-fix).
    Assert via `page.evaluate(el => getComputedStyle(el).borderRadius)` (chip = stadium), and compare confirm-vs-deny control
    colors (`getComputedStyle(...).color`/`backgroundColor`) proving success-green ≠ destructive-red. For /knowledge, assert the
    node/dot/badge computed colors match the graph-palette values and that EXTRACTED vs INFERRED edge strokes differ
    (stroke / stroke-dasharray). Resolve expected values from the token sources you read — do not hardcode a guessed hex.
    Keep the spec deterministic (fixed-id fixtures); never log secrets.
  </action>
  <verify>
    <automated>cd apps/web && npm run test:e2e -- uat-48-token-surfaces.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `uat-48-token-surfaces.spec.ts` passes on chromium against the live local stack.
    - The chip-pill assertion reads a computed border-radius equal to the fully-rounded pill token (asserts the resolved value, not a class name).
    - The success-vs-destructive assertion proves the two computed colors differ (confirm ≠ deny), and both are read from live rendered elements.
    - The /knowledge assertion proves EXTRACTED and INFERRED/AMBIGUOUS edge strokes are distinct (different stroke or dasharray).
    - Any slice inheriting a 50-02 tracked-fix is annotated as such (not silently dropped).
  </acceptance_criteria>
  <done>Both Phase-48 token effects are CSS/DOM-verified against the live local stack.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record 48 + 47 dispositions against the real 50-01 captures; route 47.1 aesthetic sign-off to morning</name>
  <files>.planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md, .planning/milestones/v1.8-phases/47-brand-foundation-verification-tooling/47-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-01-SUMMARY.md (the produced ui-reviews run-directory path)
    - the 50-01 run's index.md under .planning/ui-reviews/<timestamp>/ (confirm chat/emails/knowledge/login captures exist)
    - .planning/milestones/v1.8-phases/47-brand-foundation-verification-tooling/47-HUMAN-UAT.md (the brand-mark scenario + its old artifact pointer)
    - .planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md (both scenarios + their old textual index.md pointers)
  </read_first>
  <action>
    Update 48-HUMAN-UAT.md: set 48.1 and 48.2 `result:` to `passed — uat-48-token-surfaces.spec.ts + real pixels
    <path-to-50-01-run>/{chat,emails,knowledge}-desktop.png` (replace the old textual-before/after index.md pointers with the
    REAL authenticated captures from 50-01). Update Summary counts (passed: 2) + frontmatter `status`.
    Update 47-HUMAN-UAT.md: the brand-mark scenario is an inherently subjective aesthetic judgment — set its `result:` to
    `evidence-captured (real login pixels: <path-to-50-01-run>/login-desktop.png) — subjective brand-fit sign-off
    moved-to-morning-checklist`. Update Summary + frontmatter `status`. Do NOT mark it passed (no human has judged it) and do
    NOT mark it pending (evidence exists + a disposition is assigned).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f48=fs.readFileSync('.planning/milestones/v1.8-phases/48-token-system-extensions/48-HUMAN-UAT.md','utf8');const f47=fs.readFileSync('.planning/milestones/v1.8-phases/47-brand-foundation-verification-tooling/47-HUMAN-UAT.md','utf8');if(/result:\s*\[pending\]/.test(f48))throw new Error('48 still has pending');if(/result:\s*\[pending\]/.test(f47))throw new Error('47 still has pending');if(!/ui-reviews/.test(f48))throw new Error('48 missing real-capture pointer');if(!/morning-checklist/i.test(f47))throw new Error('47 missing morning-checklist routing');console.log('OK: 47/48 dispositions recorded with real evidence pointers')"</automated>
  </verify>
  <acceptance_criteria>
    - 48-HUMAN-UAT.md: both scenarios `passed`, each citing a real 50-01 capture path under `.planning/ui-reviews/<timestamp>/`; zero `[pending]`; Summary passed count = 2; frontmatter `status` updated.
    - 47-HUMAN-UAT.md: the brand-mark scenario is `evidence-captured … moved-to-morning-checklist` citing the real `login-desktop.png`; zero `[pending]`; frontmatter `status` updated.
    - No textual-only before/after index.md is the SOLE evidence for any 48 scenario anymore.
  </acceptance_criteria>
  <done>47 and 48 UAT files carry real-pixel-backed dispositions; the subjective brand call is routed to the morning checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → Supabase admin | seeded session uses service_role via seed-session.ts |
| capture evidence → UAT record | authenticated pixel captures are cited as pass evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-10 | Elevation of Privilege | seeded session in uat-48 spec | mitigate | Spec addresses only the local stack (127.0.0.1); relies on 50-01's local-only-guarded captures; no hosted target |
| T-50-11 | Repudiation | UAT pass claims | mitigate | 48 passes are backed by live `getComputedStyle` assertions AND real authenticated captures — not a textual claim; 47.1 is explicitly evidence-captured-but-subjective, not overclaimed as passed |
| T-50-12 | Information Disclosure | cited PNGs | mitigate | Captures come from 50-01 (fixture data only, no secrets); the UAT files reference paths, not embedded secrets |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs |
</threat_model>

<verification>
- `uat-48-token-surfaces.spec.ts` passes on chromium against the local stack.
- 47-HUMAN-UAT.md and 48-HUMAN-UAT.md contain zero `[pending]` and cite real 50-01 captures.
</verification>

<success_criteria>
- LIVE-05 (visual/token slice): all 3 scenarios (48×2 + 47×1) closed as passed (CSS/DOM-verified + real pixels) or evidence-captured→morning (the subjective brand call). Dispositions feed the 50-05 roll-up.
</success_criteria>

<output>
Create `.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-04-SUMMARY.md` when done.
List each scenario's disposition + evidence pointer (50-05 aggregates into 50-UAT-BURNDOWN.md).
</output>

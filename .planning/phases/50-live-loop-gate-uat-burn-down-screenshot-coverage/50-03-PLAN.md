---
phase: 50-live-loop-gate-uat-burn-down-screenshot-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/uat-43-auth.spec.ts
  - apps/web/e2e/uat-45-threads.spec.ts
  - apps/web/e2e/helpers/uat-thread-fixtures.ts
  - .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/43-HUMAN-UAT.md
  - .planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/45-HUMAN-UAT.md
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "Every scenario in 43-HUMAN-UAT.md (4) and 45-HUMAN-UAT.md (7) has a recorded disposition — none remain 'pending'"
    - "43.2 (session persists across refresh/new tab) and 43.3 (sign-out loop → /login, protected route re-redirects) pass against the LOCAL seeded-session stack"
    - "43.4 (signed-out visit to / redirects to /login) passes via the existing auth-redirect.spec.ts"
    - "45.1-45.4 (thread grouping, expand+editor link, singleton rows, minimal styling) pass against seeded thread fixtures; 45.7's UI-visibility slice passes with a seeded verification-code email"
    - "Genuinely deployed/real-Google/real-SES scenarios (43.1, 45.5, 45.6, and 45.7's real-arrival slice) are tagged moved-to-morning-checklist — never faked, never silently parked"
  artifacts:
    - path: "apps/web/e2e/uat-43-auth.spec.ts"
      provides: "Seeded-session spec: 43.2 persistence, 43.3 sign-out loop (+ delegates 43.4 to auth-redirect.spec.ts)"
      contains: "seedAuthenticatedContext"
    - path: "apps/web/e2e/uat-45-threads.spec.ts"
      provides: "Seeded-session spec: 45.1-45.4 thread inbox + 45.7 verification-code visibility"
      contains: "thread"
    - path: "apps/web/e2e/helpers/uat-thread-fixtures.ts"
      provides: "Seeds a multi-message thread (count>1), a singleton, and a verification-code email"
      exports: ["seedThreadFixtures"]
  key_links:
    - from: "apps/web/e2e/uat-43-auth.spec.ts"
      to: "/auth/signout → middleware"
      via: "sign-out POST clears session; revisiting a protected route redirects to /login"
      pattern: "signout|/login"
    - from: "apps/web/e2e/uat-45-threads.spec.ts"
      to: "app/_components/inbox-thread-group.tsx"
      via: "thread entry with count Badge > 1, expand reveals member InboxRows"
      pattern: "thread|Badge"
---

<objective>
Burn down the auth + inbox/threads UAT backlog (LIVE-05, part B): the four Phase-43 auth scenarios and the
seven Phase-45 thread/forwarding scenarios. Run everything runnable against the LOCAL seeded-session stack;
tag the genuinely-deployed/real-Google/real-SES remainder as morning-checklist items (Phase 49's checkpoint
is still pending — there is no live OAuth session on the deployed app yet).

Purpose: close the local-provable half now and make the truly-external half explicit + tracked.
Output: two new e2e specs, a thread-fixture helper, and updated 43/45 HUMAN-UAT.md files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-CONTEXT.md
@.planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/43-HUMAN-UAT.md
@.planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/45-HUMAN-UAT.md

<interfaces>
From apps/web/e2e/helpers/seed-session.ts:
`seedAuthenticatedContext(context): Promise<{ userId, email }>` — real local session, cookie domain "localhost".

Existing spec to reuse for 43.4: apps/web/e2e/auth-redirect.spec.ts already asserts signed-out `/` → `/login`
(playwright.config.ts covers it). 43.4 = run + record it, do NOT rewrite it.

Thread inbox components (derive selectors + expected DOM):
- apps/web/src/app/_components/inbox-thread-group.tsx (thread entry, chevron, aria-expanded, count Badge variant="secondary")
- apps/web/src/app/_components/inbox-row.tsx (member/singleton row; "Open editor →" link → /emails/[id])
- apps/web/src/app/_components/inbox-three-pane.tsx (middle/right pane layout)
Auth plumbing: apps/web/src/lib/supabase/{server,client,middleware}.ts; the sign-out route at /auth/signout.

Fixture-seed + DB-assert pattern: apps/web/e2e/live-loop-green.spec.ts (dotenv load; pg on POSTGRES_URL_NON_POOLING;
fixed-id upserts; threads/emails columns; assertNotLoginUrl).
</interfaces>

Local stack per docs/RUN-LOCAL.md; assertion specs run `--project=chromium`. Phase-45's 45-03 backfill note
records real local thread data (16 emails → 9 threads) — but seed deterministic fixtures rather than depending
on ambient rows. npm workspaces (NOT pnpm).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase-43 auth burn-down — session persistence (43.2), sign-out loop (43.3), redirect smoke (43.4); OAuth (43.1) → morning</name>
  <files>apps/web/e2e/uat-43-auth.spec.ts, .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/43-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/43-auth-google-oauth-sessions-supabase-auth/43-HUMAN-UAT.md (the 4 scenarios)
    - apps/web/e2e/auth-redirect.spec.ts (existing signed-out redirect spec = 43.4)
    - apps/web/src/lib/supabase/middleware.ts (token refresh + protected-route redirect behavior)
    - apps/web/e2e/live-loop-green.spec.ts (seeded-session + assertNotLoginUrl pattern)
  </read_first>
  <behavior>
    - 43.2: after seeding a session, `/` loads without redirect; a full reload AND a fresh browser tab in the same context still load `/` without redirecting to /login (middleware refreshes transparently).
    - 43.3: clicking the sidebar sign-out control POSTs /auth/signout, lands on /login, and a subsequent visit to a protected route (`/`) redirects back to /login.
    - 43.4: the existing auth-redirect.spec.ts passes (signed-out `/` → /login).
    - 43.1: live Google OAuth on the deployed app — NOT runnable locally (real Google + deployed app); disposition = moved-to-morning-checklist, cross-referencing 49-HUMAN-UAT.md §1 (LIVE-03).
  </behavior>
  <action>
    Write `uat-43-auth.spec.ts` (seeded session, `--project=chromium`):
    43.2 — seed session, goto `/`, assertNotLoginUrl; `page.reload()`, assertNotLoginUrl; open a second page in the SAME
    context (`context.newPage()`), goto `/`, assertNotLoginUrl; close it.
    43.3 — from an authenticated `/`, click the sign-out control (read the sidebar component for its accessible name),
    assert landing on /login, then goto `/` again and assert the redirect back to /login (session cleared).
    43.4 — run the existing `auth-redirect.spec.ts` (do not duplicate) and record its pass; you may reference it from the
    new spec's comments but keep it as the canonical 43.4 evidence.
    Update 43-HUMAN-UAT.md: 43.2/43.3 → `passed — <evidence>`; 43.4 → `passed — auth-redirect.spec.ts`; 43.1 →
    `moved-to-morning-checklist — see 49-HUMAN-UAT.md §1 (LIVE-03)`. Update Summary counts + frontmatter `status`.
    Do NOT attempt to fake interactive Google.
  </action>
  <verify>
    <automated>cd apps/web && npm run test:e2e -- uat-43-auth.spec.ts auth-redirect.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `uat-43-auth.spec.ts` AND `auth-redirect.spec.ts` pass on chromium.
    - 43-HUMAN-UAT.md has zero `result: [pending]`; 43.2/43.3/43.4 = `passed`, 43.1 = `moved-to-morning-checklist` with the 49-HUMAN-UAT.md §1 cross-reference.
    - The 43.3 assertion proves session clearing via an actual re-redirect of a protected route (not just the /login landing).
    - Summary counts + frontmatter `status` updated.
  </acceptance_criteria>
  <done>Three Phase-43 scenarios pass locally; the OAuth scenario is explicitly routed to the morning checklist, not parked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-45 threads burn-down — grouping/expand/singleton/styling (45.1-45.4) + code visibility (45.7); forwarding (45.5, 45.6) → morning</name>
  <files>apps/web/e2e/uat-45-threads.spec.ts, apps/web/e2e/helpers/uat-thread-fixtures.ts, .planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/45-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/45-HUMAN-UAT.md (the 7 scenarios + expected tokens/badges)
    - apps/web/src/app/_components/inbox-thread-group.tsx, inbox-row.tsx, inbox-three-pane.tsx (thread entry vs singleton DOM, count Badge, chevron/aria-expanded, "Open editor →")
    - apps/web/e2e/live-loop-green.spec.ts (threads/emails upsert columns; importer lookup; assertNotLoginUrl)
    - .planning/milestones/v1.7-phases/45-email-threads-forwarding-seam/45-UI-SPEC.md if present (45.4 minimal-styling contract: Badge variant="secondary", text-muted-foreground, no new colors)
  </read_first>
  <behavior>
    - 45.1: a seeded multi-message thread shows ONE inbox entry with a count Badge > 1 and the latest snippet/date, not one row per email.
    - 45.2: clicking the entry expands it (chevron rotates, aria-expanded toggles), reveals member InboxRows; selecting a member shows it in the right pane; "Open editor →" reaches /emails/[id] for that exact email.
    - 45.3: a singleton (count 1, incl. a null-thread_id orphan) renders as a flat row — no chevron, no count badge.
    - 45.4: count Badge/snippet/date use only existing tokens (Badge variant="secondary", text-muted-foreground) — DOM-asserted, no new visual language.
    - 45.7: a seeded email containing a verification code is findable via inbox → email detail (UI-visibility), no DB access needed.
    - 45.5 (real Gmail-forward fixture realism) + 45.6 (SES + Gmail forwarding round-trip) → moved-to-morning-checklist.
  </behavior>
  <action>
    Create `uat-thread-fixtures.ts` exporting `seedThreadFixtures(userId)` that seeds: (a) a multi-message thread
    (3 emails sharing a thread_id, subject like the "Fwd: Packing List …" chain, distinct received_at so a "latest" exists);
    (b) a singleton email (its own thread or null thread_id); (c) a verification-code email (body contains a recognizable
    code token, e.g. `code: 483920`). Fixed ids, `ON CONFLICT DO UPDATE`, never log secrets.
    Write `uat-45-threads.spec.ts` (seeded session, `--project=chromium`) covering 45.1-45.4 + 45.7 with DOM assertions
    (count Badge text/variant, chevron aria-expanded toggle, member-row → /emails/[id] href, singleton has no disclosure
    chrome, Badge className uses the existing secondary token, the verification code is visible on the email-detail page).
    45.5 + 45.6 are NOT runnable without a real Gmail forward / live SES — record them `moved-to-morning-checklist`
    (45.6 cross-references 49-HUMAN-UAT.md §2 / LIVE-04; 45.5 is a fixture-realism confirmation the user performs). For 45.7,
    note the UI-visibility slice PASSED locally and that the real-verification-email-arrival slice rides on 45.6/LIVE-04.
    Update 45-HUMAN-UAT.md results + Summary + frontmatter `status`.
  </action>
  <verify>
    <automated>cd apps/web && npm run test:e2e -- uat-45-threads.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `uat-45-threads.spec.ts` passes on chromium; 45.1's grouping and 45.2's editor-link deep-link are DOM-asserted against seeded fixtures.
    - 45-HUMAN-UAT.md has zero `result: [pending]`; 45.1-45.4 + 45.7(UI) = `passed`; 45.5, 45.6 = `moved-to-morning-checklist` with cross-references (45.6 → 49 §2/LIVE-04).
    - `seedThreadFixtures` is a named export seeding a count>1 thread, a singleton, and a verification-code email.
    - Summary counts + frontmatter `status` updated.
  </acceptance_criteria>
  <done>The five local Phase-45 scenarios pass; the two external forwarding scenarios are routed to the morning checklist with cross-references, not parked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → Supabase admin | seeded session uses service_role via seed-session.ts |
| signed-out client → middleware | the auth gate is the security boundary these specs exercise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Elevation of Privilege | seeded session in uat specs | mitigate | Specs address only the local stack (POSTGRES_URL_NON_POOLING → 127.0.0.1; cookie domain "localhost"); interactive Google / hosted target is never faked — routed to the morning checklist |
| T-50-08 | Spoofing | sign-out / session-clear (43.3) | mitigate | 43.3 asserts a REAL protected-route re-redirect after sign-out, proving the session is actually cleared (not just a cosmetic /login landing) |
| T-50-09 | Information Disclosure | verification-code fixture / evidence | mitigate | The seeded verification code is synthetic; no real code or secret is written to a UAT file |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; pg/dotenv/@playwright/test already present |
</threat_model>

<verification>
- `uat-43-auth.spec.ts`, `auth-redirect.spec.ts`, `uat-45-threads.spec.ts` pass on chromium against the local stack.
- 43-HUMAN-UAT.md and 45-HUMAN-UAT.md contain zero `[pending]` results.
- Morning-checklist-bound scenarios carry explicit cross-references (50-05 will append/confirm the checklist entries).
</verification>

<success_criteria>
- LIVE-05 (auth + threads slice): all 11 scenarios (43×4 + 45×7) closed as passed or explicitly moved-to-morning-checklist, DB/DOM-verified where local, recorded in their HUMAN-UAT.md files. Dispositions feed the 50-05 roll-up.
</success_criteria>

<output>
Create `.planning/phases/50-live-loop-gate-uat-burn-down-screenshot-coverage/50-03-SUMMARY.md` when done.
List each scenario's disposition + evidence/morning pointer (50-05 aggregates into 50-UAT-BURNDOWN.md).
</output>

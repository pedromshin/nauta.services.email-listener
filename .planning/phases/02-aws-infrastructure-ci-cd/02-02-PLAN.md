---
phase: 02-aws-infrastructure-ci-cd
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [infrastructure/aws/terraform.tfvars]
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "A GitHub OIDC provider (token.actions.githubusercontent.com) exists in account 271369143207"
    - "terraform.tfvars is filled with github_repository and both API_KEY secret ARNs"
    - "terraform apply provisions ECR, ECS cluster + prod/staging services, ALB, IAM (OIDC deploy role) with no manual edits"
    - "terraform outputs expose github_deploy_role_arn and alb_dns_name"
  artifacts:
    - path: "infrastructure/aws/terraform.tfvars"
      provides: "Resolved input variables (gitignored)"
      contains: "github_repository"
    - path: "(aws) iam role nauta-services-email-listener-github-deploy"
      provides: "OIDC deploy role assumable by repo:pedromshin/nauta.services.email-listener:*"
    - path: "(aws) ALB nauta-services-email-listener"
      provides: "Shared ALB, :80 prod / :8080 staging"
  key_links:
    - from: "iam.tf data.aws_iam_openid_connect_provider.github"
      to: "(aws) OIDC provider token.actions.githubusercontent.com"
      via: "data source lookup (must pre-exist)"
      pattern: "token.actions.githubusercontent.com"
    - from: "ecs.tf task definition secrets[].valueFrom"
      to: "Secrets Manager API_KEY ARNs"
      via: "terraform.tfvars api_key_secret_arn_*"
      pattern: "api_key_secret_arn"
---

<objective>
Ensure the GitHub OIDC provider exists, fill terraform.tfvars with the repo slug and the
secret ARNs from Plan 01, then run terraform init/plan/apply to provision all AWS
infrastructure (INFRA-01). Capture github_deploy_role_arn and alb_dns_name for Plan 03.

Purpose: This is the core infra provisioning. ECS services will come up pointing at ECR
tags (:latest, :staging) that do not yet have images — expect the services to NOT stabilize
until CI pushes the first images (Plan 04). The deployment_circuit_breaker is enabled, so
initial tasks may cycle; this is expected and not a terraform failure.
Output: Live ECR/ECS/ALB/IAM stack; terraform outputs captured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-01-SUMMARY.md
@infrastructure/README.md
@infrastructure/aws/main.tf
@infrastructure/aws/variables.tf
@infrastructure/aws/iam.tf
@infrastructure/aws/locals.tf
@infrastructure/aws/ecs.tf
@infrastructure/aws/outputs.tf
@infrastructure/aws/terraform.tfvars.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify or create the GitHub OIDC provider</name>
  <files></files>
  <read_first>
    infrastructure/aws/iam.tf
    .planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
  </read_first>
  <action>
    iam.tf references the OIDC provider as a DATA source (data.aws_iam_openid_connect_provider.github),
    so it must already exist or terraform apply fails (D-10). Check:
    `aws iam list-open-id-connect-providers --profile default --region us-east-1`.
    If no ARN ending in `token.actions.githubusercontent.com` is present, create it:
    `aws iam create-open-id-connect-provider --url https://token.actions.githubusercontent.com --client-id-list sts.amazonaws.com --thumbprint-list 1c58a3a8518e8759bf075b76b750d4f2df264fcd --profile default`.
    (The thumbprint is no longer validated by AWS for this provider but the arg is required.)
    Do NOT add provider creation to terraform — keep it a data source per existing code.
  </action>
  <verify>
    <automated>aws iam list-open-id-connect-providers --profile default --query "OpenIDConnectProviderList[?contains(Arn, 'token.actions.githubusercontent.com')].Arn" --output text</automated>
  </verify>
  <acceptance_criteria>
    - The list command prints exactly one ARN containing `token.actions.githubusercontent.com`.
  </acceptance_criteria>
  <done>OIDC provider present in account 271369143207.</done>
</task>

<task type="auto">
  <name>Task 2: Fill terraform.tfvars</name>
  <files>infrastructure/aws/terraform.tfvars</files>
  <read_first>
    infrastructure/aws/terraform.tfvars.example
    infrastructure/aws/variables.tf
    .planning/phases/02-aws-infrastructure-ci-cd/02-01-SUMMARY.md
  </read_first>
  <action>
    Copy terraform.tfvars.example to terraform.tfvars (the file is gitignored — confirm it
    is NOT staged by git). Set: aws_region = "us-east-1", aws_profile = "default",
    project = "nauta-services", github_repository = "pedromshin/nauta.services.email-listener".
    Set api_key_secret_arn_prod and api_key_secret_arn_staging to the exact full ARNs
    captured in 02-01-SUMMARY.md (uncomment those lines). Do not invent ARNs — use the
    recorded ones.
  </action>
  <verify>
    <automated>git check-ignore infrastructure/aws/terraform.tfvars && grep -q "pedromshin/nauta.services.email-listener" infrastructure/aws/terraform.tfvars && grep -q "api_key_secret_arn_prod" infrastructure/aws/terraform.tfvars</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore infrastructure/aws/terraform.tfvars` returns the path (i.e. it IS gitignored).
    - File contains github_repository = "pedromshin/nauta.services.email-listener".
    - Both api_key_secret_arn_prod and api_key_secret_arn_staging set to real ARNs from Plan 01.
  </acceptance_criteria>
  <done>terraform.tfvars resolved and gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: terraform init / plan / apply</name>
  <files></files>
  <read_first>
    infrastructure/aws/main.tf
    infrastructure/aws/ecs.tf
    infrastructure/aws/outputs.tf
  </read_first>
  <action>
    From infrastructure/aws: `terraform init`, then `terraform plan -out tfplan` (review for
    unexpected destroys — there should be none on a fresh account), then `terraform apply tfplan`.
    Local state per D-11 (S3 backend stays commented). Apply may take several minutes for the ALB.
    ECS services will register but tasks will fail to pull :latest / :staging images (none pushed
    yet) — terraform apply itself still SUCCEEDS because it does not wait for task health; do not
    treat cycling tasks as an apply failure. After apply, capture outputs:
    `terraform output -raw github_deploy_role_arn` and `terraform output -raw alb_dns_name`.
    Record both in the SUMMARY for Plan 03.
  </action>
  <verify>
    <automated>cd infrastructure/aws && terraform output -raw github_deploy_role_arn && terraform output -raw alb_dns_name && terraform output -raw ecs_cluster_name</automated>
  </verify>
  <acceptance_criteria>
    - `terraform apply` exits 0.
    - `terraform output -raw github_deploy_role_arn` prints an ARN matching `arn:aws:iam::271369143207:role/nauta-services-email-listener-github-deploy`.
    - `terraform output -raw alb_dns_name` prints a non-empty `*.elb.amazonaws.com` hostname.
    - `terraform output -raw ecs_cluster_name` prints `nauta-services-email-listener`.
    - `aws ecs describe-services --cluster nauta-services-email-listener --services nauta-services-email-listener nauta-services-email-listener-staging --region us-east-1 --profile default --query "services[].status" --output text` returns `ACTIVE ACTIVE`.
  </acceptance_criteria>
  <done>All AWS resources provisioned; deploy role ARN and ALB DNS captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → AWS | OIDC role trust restricted to repo:pedromshin/nauta.services.email-listener:* |
| ALB → ECS tasks | public subnets, security group on service_port 8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of privilege | github_deploy IAM role | mitigate | Trust policy StringLike sub = repo:pedromshin/nauta.services.email-listener:* (iam.tf); deploy policy scoped to this cluster ARN |
| T-02-04 | Spoofing | OIDC provider | mitigate | Single provider for token.actions.githubusercontent.com, aud = sts.amazonaws.com |
| T-02-05 | Information disclosure | terraform.tfvars (ARNs) | accept | ARNs are non-secret identifiers; file gitignored; secret values stay in Secrets Manager |
| T-02-SC | Tampering | terraform providers | mitigate | aws provider pinned ~> 5.0 in main.tf; terraform init records lockfile |
</threat_model>

<verification>
- OIDC provider exists; terraform apply clean; outputs captured.
- Both ECS services ACTIVE (tasks may be unhealthy pending first image — expected).
</verification>

<success_criteria>
INFRA-01 satisfied: terraform apply provisions ECR/ECS/ALB/IAM without manual edits;
github_deploy_role_arn and alb_dns_name available as outputs.
</success_criteria>

<output>
Create `.planning/phases/02-aws-infrastructure-ci-cd/02-02-SUMMARY.md`.
Record: github_deploy_role_arn, alb_dns_name, ecr_repository_url, both ECS service names.
</output>

---
phase: 02-aws-infrastructure-ci-cd
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [INFRA-02, INFRA-03]

must_haves:
  truths:
    - "A push to dev triggers the staging deploy workflow which builds, Trivy-scans, pushes :staging, deploys, and the staging /health returns 200 via ALB :8080"
    - "A push to main triggers the production deploy workflow which builds, Trivy-scans, pushes :latest, deploys, and the production /health returns 200 via ALB :80"
    - "Deploys are gated by tests, Trivy CRITICAL/HIGH scan, and a post-deploy /health smoke test"
  artifacts:
    - path: "(ecr) nauta-services-email-listener:staging"
      provides: "First staging image pushed by CI"
    - path: "(ecr) nauta-services-email-listener:latest"
      provides: "First production image pushed by CI"
  key_links:
    - from: "dev branch push"
      to: "ALB :8080/health → 200"
      via: "deploy-email-listener-staging.yml"
      pattern: "STAGING_HEALTH_URL"
    - from: "main branch push"
      to: "ALB :80/health → 200"
      via: "deploy-email-listener.yml"
      pattern: "PRODUCTION_HEALTH_URL"
---

<objective>
Prove both deploy pipelines end-to-end: trigger staging (dev) and production (main),
confirm each workflow passes its test → build → Trivy → ECR push → ECS deploy → /health
smoke test, and verify both ALB endpoints return 200 (INFRA-02, INFRA-03).

Purpose: Until CI pushes the first images, the ECS services from Plan 02 have no runnable
tasks. The staging workflow runs first so any CI fixes (Trivy/uv action versions per
Claude's Discretion) are caught on the lower environment before production. Each service
should stabilize once its first image is pushed and the deployment circuit breaker clears.
Output: Two green pipelines; both /health endpoints returning 200.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-02-SUMMARY.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-03-SUMMARY.md
@.github/workflows/deploy-email-listener.yml
@.github/workflows/deploy-email-listener-staging.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger and verify staging pipeline (dev → :8080)</name>
  <files></files>
  <read_first>
    .github/workflows/deploy-email-listener-staging.yml
    .planning/phases/02-aws-infrastructure-ci-cd/02-03-SUMMARY.md
  </read_first>
  <action>
    Trigger the staging deploy. Prefer an explicit dispatch on dev:
    `gh workflow run deploy-email-listener-staging.yml --repo pedromshin/nauta.services.email-listener --ref dev`.
    If a push trigger is needed (paths filter apps/email-listener/**), make a trivial no-op
    commit on dev under apps/email-listener (e.g. touch a comment) and push. Watch the run:
    `gh run watch --repo pedromshin/nauta.services.email-listener`. If the workflow fails on
    action versions (Trivy / setup-uv) per Claude's Discretion (D-06), fix the workflow file,
    commit, and re-trigger. The run must complete with the deploy job green INCLUDING the
    "Wait for service stability" and "Smoke test" steps. ECS may take a few minutes to
    stabilize on first image — the workflow's `aws ecs wait services-stable` handles this.
  </action>
  <verify>
    <automated>gh run list --repo pedromshin/nauta.services.email-listener --workflow deploy-email-listener-staging.yml --branch dev --limit 1 --json conclusion --jq ".[0].conclusion" | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - Latest staging workflow run conclusion = `success`.
    - `curl -s -o /dev/null -w '%{http_code}' "http://<alb_dns>:8080/health"` returns `200`.
  </acceptance_criteria>
  <done>Staging pipeline green; staging /health 200 via ALB :8080.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger and verify production pipeline (main → :80)</name>
  <files></files>
  <read_first>
    .github/workflows/deploy-email-listener.yml
    .planning/phases/02-aws-infrastructure-ci-cd/02-03-SUMMARY.md
  </read_first>
  <action>
    After staging is green, trigger production:
    `gh workflow run deploy-email-listener.yml --repo pedromshin/nauta.services.email-listener --ref main`
    (or a trivial commit to main under apps/email-listener if the paths-filtered push trigger
    is required). Apply any CI fixes already discovered in Task 1 to the production workflow
    file as well so it does not regress. Watch with `gh run watch`. Run must be green through
    the smoke-test step.
  </action>
  <verify>
    <automated>gh run list --repo pedromshin/nauta.services.email-listener --workflow deploy-email-listener.yml --branch main --limit 1 --json conclusion --jq ".[0].conclusion" | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - Latest production workflow run conclusion = `success`.
    - `curl -s -o /dev/null -w '%{http_code}' "http://<alb_dns>/health"` returns `200`.
  </acceptance_criteria>
  <done>Production pipeline green; production /health 200 via ALB :80.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm both environments live</name>
  <read_first>
    .planning/ROADMAP.md
  </read_first>
  <what-built>Both deploy pipelines proven end-to-end; staging and production serving /health.</what-built>
  <how-to-verify>
    1. Open `http://<alb_dns>:8080/health` → expect HTTP 200 (staging).
    2. Open `http://<alb_dns>/health` → expect HTTP 200 (production).
    3. In GitHub Actions, confirm both latest deploy runs are green with the Trivy scan step
       passing (CRITICAL/HIGH gate) and the smoke-test step passing.
  </how-to-verify>
  <acceptance_criteria>
    - Both URLs return 200.
    - Both workflow runs show test + Trivy + smoke-test steps succeeded.
  </acceptance_criteria>
  <resume-signal>Type "verified" once both endpoints return 200 and both pipelines are green.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → AWS (OIDC) | runner assumes github_deploy role to push ECR + update ECS |
| Internet → ALB | public :80 / :8080 reach /health |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | container image | mitigate | Trivy CRITICAL/HIGH gate (exit-code 1) blocks vulnerable images before ECR push (D-06) |
| T-02-09 | Denial of service | public ALB endpoints | accept | Pre-domain testing surface; /health only; rate limiting deferred to app layer |
| T-02-10 | Repudiation | deploy provenance | mitigate | OIDC short-lived creds; GitHub Actions run logs retained |
| T-02-SC | Tampering | trivy-action / setup-uv versions | mitigate | Versions pinned in workflow; any fix committed and re-run, no [ASSUMED] packages installed locally |
</threat_model>

<verification>
- Staging run green + :8080/health 200.
- Production run green + :80/health 200.
- Trivy + smoke-test steps present and passing in both runs.
</verification>

<success_criteria>
INFRA-02 + INFRA-03 satisfied: dev push deploys staging (200), main push deploys
production (200), both gated by tests + Trivy + post-deploy /health smoke test.
</success_criteria>

<output>
Create `.planning/phases/02-aws-infrastructure-ci-cd/02-04-SUMMARY.md`.
Record: both run URLs, both /health results, any workflow fixes applied.
</output>

---
phase: 02-aws-infrastructure-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INFRA-04]
user_setup:
  - service: github
    why: "Repo creation + branch push require an authenticated gh CLI session"
    dashboard_config:
      - task: "Run `gh auth login` (account pedromshin, scope: repo, workflow)"
        location: "Local terminal — token currently invalid (D-09)"

must_haves:
  truths:
    - "GitHub repo pedromshin/nauta.services.email-listener exists and is public-or-private with main + dev branches pushed"
    - "Secrets Manager holds an API_KEY secret for production and one for staging"
    - "gh CLI is authenticated as pedromshin"
  artifacts:
    - path: "(remote) github.com/pedromshin/nauta.services.email-listener"
      provides: "Remote repo with main + dev branches"
    - path: "(aws) secretsmanager prod/nauta-services/API_KEY"
      provides: "Production API_KEY secret"
    - path: "(aws) secretsmanager staging/nauta-services/API_KEY"
      provides: "Staging API_KEY secret"
  key_links:
    - from: "local git remote origin"
      to: "github.com/pedromshin/nauta.services.email-listener"
      via: "git push -u origin main && git push -u origin dev"
      pattern: "origin.*nauta.services.email-listener"
---

<objective>
Create the GitHub repository, push main + dev branches, and create per-environment
API_KEY secrets in AWS Secrets Manager. This establishes the remote and the secret
ARNs that Terraform (Plan 02) consumes via terraform.tfvars.

Purpose: Repo must exist before OIDC role trust (repo:pedromshin/...:*) and before any
deploy workflow can run. Secret ARNs must exist before terraform apply wires them into
the ECS task definitions and the execution-role read-secrets policy.
Output: Remote repo with two branches; two Secrets Manager secrets; captured ARNs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
@infrastructure/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User authenticates gh CLI</name>
  <read_first>
    .planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
  </read_first>
  <what-built>Nothing automated yet — this gate unblocks repo creation (D-09: token invalid).</what-built>
  <how-to-verify>
    1. In a local terminal run: `gh auth login` and complete the flow for account pedromshin.
       Select scopes including `repo` and `workflow`.
    2. Confirm with: `gh auth status` → shows "Logged in to github.com account pedromshin".
  </how-to-verify>
  <acceptance_criteria>
    - `gh auth status` exits 0 and prints account `pedromshin`.
  </acceptance_criteria>
  <resume-signal>Type "authenticated" once `gh auth status` shows pedromshin.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create remote repo and push main + dev</name>
  <files></files>
  <read_first>
    infrastructure/README.md
    .planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
  </read_first>
  <action>
    Create the GitHub repo under account pedromshin named nauta.services.email-listener
    using gh: `gh repo create pedromshin/nauta.services.email-listener --private --source=. --remote=origin --push` (run from repo root C:\Users\pc\Desktop\nauta.services.email-listener).
    If `--push` only pushes the current branch, explicitly push both: `git push -u origin main`
    and `git push -u origin dev`. If a `dev` branch does not exist locally, create it from main:
    `git branch dev main` then push it. Do NOT force-push. Verify origin URL points at
    github.com/pedromshin/nauta.services.email-listener.
  </action>
  <verify>
    <automated>gh repo view pedromshin/nauta.services.email-listener --json defaultBranchRef,name && git ls-remote --heads origin main dev</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view pedromshin/nauta.services.email-listener` succeeds (repo exists).
    - `git ls-remote --heads origin main dev` lists BOTH refs/heads/main and refs/heads/dev.
    - `git remote get-url origin` contains `pedromshin/nauta.services.email-listener`.
  </acceptance_criteria>
  <done>Repo exists; main and dev branches both present on origin.</done>
</task>

<task type="auto">
  <name>Task 3: Create per-environment API_KEY secrets in Secrets Manager</name>
  <files></files>
  <read_first>
    infrastructure/aws/variables.tf
    infrastructure/aws/iam.tf
    infrastructure/aws/terraform.tfvars.example
  </read_first>
  <action>
    Create two Secrets Manager secrets in account 271369143207, region us-east-1, profile default
    (D-08, D-12). Use naming convention from tfvars.example: `prod/nauta-services/API_KEY` and
    `staging/nauta-services/API_KEY`. Generate a strong random value for each (e.g. 32+ bytes
    base64); never reuse the same value across environments. Commands:
    `aws secretsmanager create-secret --name prod/nauta-services/API_KEY --secret-string <random-prod> --region us-east-1 --profile default`
    and the staging equivalent. Capture the returned full ARNs (including the random suffix) —
    these go into terraform.tfvars in Plan 02 (api_key_secret_arn_prod / api_key_secret_arn_staging).
    Record both ARNs in the SUMMARY. Do NOT print the secret values into the SUMMARY or commit them anywhere.
  </action>
  <verify>
    <automated>aws secretsmanager describe-secret --secret-id prod/nauta-services/API_KEY --region us-east-1 --profile default --query ARN --output text && aws secretsmanager describe-secret --secret-id staging/nauta-services/API_KEY --region us-east-1 --profile default --query ARN --output text</automated>
  </verify>
  <acceptance_criteria>
    - Both `describe-secret` calls return an ARN matching `arn:aws:secretsmanager:us-east-1:271369143207:secret:(prod|staging)/nauta-services/API_KEY-`.
    - Secret values are NOT present in any committed file or the SUMMARY.
  </acceptance_criteria>
  <done>Two API_KEY secrets exist; both ARNs captured for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → GitHub | git push authenticates over gh OAuth token |
| local → AWS API | Secrets Manager writes use profile default credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | API_KEY secret values | mitigate | Generate random per-env values; never write values to SUMMARY/git; only ARNs recorded |
| T-02-02 | Spoofing | gh repo create | accept | gh OAuth token scoped to pedromshin; user-initiated login |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Repo reachable via gh; main + dev pushed.
- Two Secrets Manager ARNs captured (prod + staging), values not leaked.
</verification>

<success_criteria>
GitHub repo pedromshin/nauta.services.email-listener exists with main + dev; two
API_KEY secrets exist in Secrets Manager with ARNs recorded for Terraform.
</success_criteria>

<output>
Create `.planning/phases/02-aws-infrastructure-ci-cd/02-01-SUMMARY.md` when done.
Record: repo URL, the two secret ARNs (NOT values).
</output>

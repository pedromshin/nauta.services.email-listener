---
phase: 02-aws-infrastructure-ci-cd
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: true
requirements: [INFRA-04]

must_haves:
  truths:
    - "GitHub repo secret AWS_DEPLOY_ROLE_ARN is set to the terraform github_deploy_role_arn output"
    - "GitHub repo variables STAGING_HEALTH_URL and PRODUCTION_HEALTH_URL are set from the ALB DNS"
  artifacts:
    - path: "(github) secret AWS_DEPLOY_ROLE_ARN"
      provides: "OIDC role ARN consumed by both deploy workflows"
    - path: "(github) variable PRODUCTION_HEALTH_URL"
      provides: "http://<alb_dns>/health for prod smoke test"
    - path: "(github) variable STAGING_HEALTH_URL"
      provides: "http://<alb_dns>:8080/health for staging smoke test"
  key_links:
    - from: "deploy-email-listener.yml secrets.AWS_DEPLOY_ROLE_ARN"
      to: "(aws) github_deploy role ARN"
      via: "gh secret set"
      pattern: "AWS_DEPLOY_ROLE_ARN"
    - from: "deploy workflows vars.*_HEALTH_URL"
      to: "ALB DNS :80 / :8080"
      via: "gh variable set"
      pattern: "_HEALTH_URL"
---

<objective>
Wire the GitHub repo with the deploy role ARN secret and the two health-URL variables,
using values from Plan 02's terraform outputs. This is the last step before the deploy
workflows can authenticate via OIDC and run their /health smoke tests (INFRA-04).

Purpose: Both deploy workflows reference secrets.AWS_DEPLOY_ROLE_ARN and vars.*_HEALTH_URL.
Without these set, OIDC auth fails and the smoke-test step is skipped. The role + ALB must
exist first (Plan 02), hence this is a separate wave.
Output: Repo secrets/vars configured; ready to trigger pipelines (Plan 04).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-CONTEXT.md
@.planning/phases/02-aws-infrastructure-ci-cd/02-02-SUMMARY.md
@infrastructure/README.md
@.github/workflows/deploy-email-listener.yml
@.github/workflows/deploy-email-listener-staging.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set AWS_DEPLOY_ROLE_ARN secret</name>
  <files></files>
  <read_first>
    .planning/phases/02-aws-infrastructure-ci-cd/02-02-SUMMARY.md
    .github/workflows/deploy-email-listener.yml
  </read_first>
  <action>
    Take the github_deploy_role_arn value from 02-02-SUMMARY.md (or re-read via
    `cd infrastructure/aws && terraform output -raw github_deploy_role_arn`). Set it as a
    repo secret: `gh secret set AWS_DEPLOY_ROLE_ARN --repo pedromshin/nauta.services.email-listener --body "<arn>"`.
  </action>
  <verify>
    <automated>gh secret list --repo pedromshin/nauta.services.email-listener | grep -q AWS_DEPLOY_ROLE_ARN</automated>
  </verify>
  <acceptance_criteria>
    - `gh secret list --repo pedromshin/nauta.services.email-listener` includes `AWS_DEPLOY_ROLE_ARN`.
  </acceptance_criteria>
  <done>Deploy role ARN secret set.</done>
</task>

<task type="auto">
  <name>Task 2: Set STAGING_HEALTH_URL and PRODUCTION_HEALTH_URL variables</name>
  <files></files>
  <read_first>
    .planning/phases/02-aws-infrastructure-ci-cd/02-02-SUMMARY.md
    infrastructure/README.md
  </read_first>
  <action>
    Take alb_dns_name from 02-02-SUMMARY.md. Per README mapping (D-02): production listener
    :80, staging listener :8080. Set repo variables:
    `gh variable set PRODUCTION_HEALTH_URL --repo pedromshin/nauta.services.email-listener --body "http://<alb_dns>/health"`
    and `gh variable set STAGING_HEALTH_URL --repo pedromshin/nauta.services.email-listener --body "http://<alb_dns>:8080/health"`.
    Use plain http (no domain/ACM yet per D-02).
  </action>
  <verify>
    <automated>gh variable list --repo pedromshin/nauta.services.email-listener | grep -E "STAGING_HEALTH_URL|PRODUCTION_HEALTH_URL"</automated>
  </verify>
  <acceptance_criteria>
    - `gh variable list` shows PRODUCTION_HEALTH_URL = `http://<alb_dns>/health`.
    - `gh variable list` shows STAGING_HEALTH_URL = `http://<alb_dns>:8080/health`.
  </acceptance_criteria>
  <done>Both health-URL variables set with correct ports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runtime → repo secrets/vars | workflows read AWS_DEPLOY_ROLE_ARN + health URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information disclosure | AWS_DEPLOY_ROLE_ARN | accept | ARN is non-secret; stored as GitHub encrypted secret regardless |
| T-02-07 | Tampering | health URL vars | accept | Plain http to ALB; no secrets; pre-TLS per D-02 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Secret AWS_DEPLOY_ROLE_ARN present; both health URL vars present with correct ports.
</verification>

<success_criteria>
INFRA-04 wiring complete: repo has the deploy role secret and both health URL variables.
</success_criteria>

<output>
Create `.planning/phases/02-aws-infrastructure-ci-cd/02-03-SUMMARY.md`.
Record: confirmation that secret + both vars are set, and the resolved URL values.
</output>

---
phase: 61-surface-redesign-chat-canvas-mobile-panel-chrome
plan: 06
type: execute
wave: 3
depends_on: ["61-02", "61-05"]
files_modified:
  - apps/web/src/app/chat/_canvas/chat-node.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/chat/_canvas/email-thread-node.tsx
  - apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx
  - apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx
  - apps/web/src/app/chat/_canvas/data-edge.tsx
  - apps/web/src/app/chat/_canvas/__tests__/canvas-node-law.test.tsx
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "A canvas card is a flat sheet on the board — one rule, one radius, no shadow"
    - "A node's KIND is legible without colour; the retired node-type hue is gone from the canvas"
    - "An email thread's subject on a card renders in serif — it is the mail's own words"
    - "Madder appears on this surface only where an action is genuinely irreversible, if at all"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      provides: "The redesigned genui panel chrome (criterion 1's fourth component)"
    - path: "apps/web/src/app/chat/_canvas/data-edge.tsx"
      provides: "The edge on the canvas vocabulary — neutral, because a data wire makes no tier claim"
    - path: "apps/web/src/app/chat/_canvas/__tests__/canvas-node-law.test.tsx"
      provides: "The gate: kind-is-not-hue, no retired family, no madder on a state, serif on the mail's words"
  key_links:
    - from: "apps/web/src/app/chat/_canvas/chat-node.tsx"
      to: "apps/web/src/app/chat/_canvas/canvas-vocabulary.ts"
      via: "CANVAS_NODE_KIND_GEOMETRY lookup instead of a local class map"
      pattern: "CANVAS_NODE_KIND_GEOMETRY"
    - from: "apps/web/src/app/chat/_canvas/data-edge.tsx"
      to: "apps/web/src/app/chat/_canvas/canvas-vocabulary.ts"
      via: "CANVAS_EDGE_TIER.neutral"
      pattern: "CANVAS_EDGE_TIER"
---

<objective>
Put every canvas node and edge on 61-02's vocabulary, redesign the genui panel chrome, and clear
three law violations that are live on this surface right now.

Purpose: this closes criterion 2's "node shells" and criterion 1's "genui panel chrome". The
surface is not merely un-redesigned — it is in breach on three counts a read (not a grep) found:

- `unknown-node-type-placeholder.tsx` frames itself `border-destructive/30` — **madder on a
  STATE.** An unrecognized node type is not an irreversible action. This is the same class of
  violation 60-06 found in `pdf-preview-pane`'s "Preview failed" badge, and it is exactly what
  `role-hue-ban.test.ts` will catch the moment Plan 61-08 adds `chat/` to `SCOPED_DIRS`.
- `email-thread-node.tsx` paints its icon `text-graph-email` — the retired node-type hue, which
  post-59 resolves to `--pencil` anyway. The role coding is already dead on screen.
- Both removable nodes carry `hover:text-destructive` on their `×` — madder TEXT on a control that
  removes a card from a board. That is not irreversible; the thread and the conversation both
  survive. The gate would ban the token; law 1 bans the meaning.

And every shell carries `shadow-elevation-1`/`-2` against the identity's own "flat surfaces,
hairline rules, **zero shadow anywhere**".

Output: five node shells and one edge resolving against one vocabulary, plus a gate over the
rendered result.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-02-SUMMARY.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-05-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

§A — The sketch's card (direction-final.html, CSS 468-494; markup 1047-1091). ONE card language for
     every node kind — the shells below currently disagree with it and with each other:

  .card { position:absolute; z-index:2; background:var(--bright);
          border:1px solid var(--rule); border-radius:var(--r-card);
          display:flex; flex-direction:column; }        <- NO shadow. NO per-kind fill.
  .card:hover{ border-color:var(--rule-hi) }            <- hover = rule, not elevation
  .ch   { display:flex; align-items:center; gap:7px; padding:9px 10px 8px;
          border-bottom:1px solid var(--hair) }
  .ch svg{ width:13px; height:13px; color:var(--faded) }   <- the kind icon is FADED. No hue. Any kind.
  .ch .ct2{ flex:1; min-width:0; font-family:var(--serif); font-size:12.5px; font-weight:600;
            overflow:hidden; text-overflow:ellipsis; white-space:nowrap }   <- SERIF: the card title is
                                                            the mail's/source's own words (law 2)
  .cbody{ padding:10px 12px 11px }
  .cap  { font-size:10.5px; color:var(--pencil); margin-bottom:2px }   <- "From turn 2" = CHROME, sans
  .parts{ font-size:11.5px; color:var(--faded) }                       <- "Rafael Lima, you · 3 messages"
  .csnip{ font-family:var(--serif); font-size:12px; line-height:1.55; color:var(--ink); margin-top:6px }
                                                                       <- SERIF: the thread's own snippet
  .cf   { display:flex; gap:10px; padding:8px 12px 10px; border-top:1px solid var(--hair) }
  .urlline{ font-size:11px; color:var(--pencil); margin-top:5px; font-variant-numeric:tabular-nums }

  Read the numbers as INTENT — reach for Phase 59's named steps (`px-chip-x`/`py-chip-y`,
  `px-row-x`/`py-row-y`, `p-panel`, `rounded-card`, `text-2xs..xl`, `tabular`).

§B — The five shells as shipped. Note how much they already AGREE — that agreement is what
     `CANVAS_NODE_KIND_GEOMETRY` (61-02) makes explicit instead of coincidental:

  ALL FOUR real nodes share, verbatim:
    const SELECTED_RING = "ring-2 ring-primary ring-offset-1";
    ...rounded-lg border border-border/60 bg-background transition-shadow duration-150
       ${selected ? `${SELECTED_RING} shadow-elevation-2` : "shadow-elevation-1"}
    <Handle type="target" position={Position.Left} />  ... <Handle type="source" position={Position.Right} />
    <div className="node-drag-handle flex h-9 shrink-0 cursor-grab items-center ... border-b border-border/60 bg-muted/40 px-3 active:cursor-grabbing">

  chat-node.tsx (157)          `border-l-2 border-l-primary` stripe · `bg-muted/60` header (one step
                               darker than the others' /40) · `MessageSquare` icon `text-primary` ·
                               title `text-sm font-semibold text-foreground` · min-w-[400px] min-h-[320px]
  genui-panel-node.tsx (238)   `bg-muted/40` header · `PanelsTopLeft` icon `text-muted-foreground` ·
                               caption "From turn {n}" · streaming dot `text-primary
                               motion-safe:animate-pulse` aria-label="Streaming" ·
                               min-h-[272px] min-w-[320px] · body is a ScrollArea with `p-4`
  email-thread-node.tsx (234)  h-[220px] w-[320px] · `Mail` icon **`text-graph-email`** (the retired
                               family — its own comment says "NOT text-primary, 54-UI-SPEC") ·
                               `×` button `hover:text-destructive` · Skeleton loading rows
  knowledge-preview-node.tsx   h-[240px] w-[320px] · `Share2` icon `text-primary` ·
                               `×` button `hover:text-destructive` · footer link `h-7 ...
                               hover:bg-accent hover:text-accent-foreground pointer-coarse:h-11`
  unknown-node-type-placeholder.tsx (57)
                               `border-destructive/30 bg-muted/40 p-4` + `AlertTriangle` — **madder
                               framing a STATE.** CANVAS-03's degrade-gracefully placeholder.

§C — data-edge.tsx as shipped (the file Task 2 rewrites):

  <BaseEdge id path={edgePath} markerEnd={markerEnd}
            className="!stroke-primary" style={{ strokeWidth: 2 }} interactionWidth={20} />
  <EdgeLabelRenderer>
    <button type="button" onClick={handleClick}
      className="nodrag nopan absolute rounded-pill border border-border/60 bg-background/80
                 px-2 py-0.5 text-xs text-muted-foreground shadow-sm"
      style={{ transform: `translate(-50%, -50%) translate(${labelX}px, ${labelY}px)`,
               pointerEvents: "all" }}
      aria-label={`Edit connection: ${sourcePath} to ${targetKey}`}>
      {sourcePath} {"→"} {targetKey}
    </button>

  PRESERVE: `getSmoothStepPath`, the ALWAYS-VISIBLE label (23-UI-SPEC: "makes wiring legible without
  opening the picker" — never hide it behind hover), `interactionWidth={20}` (the click target),
  `EdgeLabelClickContext`'s stable callback seam (persisted `edge.data` stays exactly
  `{sourcePath, targetKey}` — never stash a closure in `data`), `animated` staying unset
  (reduced-motion posture), and `nodrag nopan`.
  Sketch counterpart: `.edgelabel` — `--faded` on `--leaf`, micro step, tiny radius, NO shadow.

§D — 61-02's vocabulary (canvas-vocabulary.ts) — the maps this plan wires. Read the SUMMARY for the
     verbatim exports; the shape is:
       CanvasEdgeTier = "neutral" | "confirmed" | "suggested"
       CANVAS_EDGE_TIER: Record<CanvasEdgeTier, { path: string; joint: string }>
         neutral -> the --edge structural wire, NO hue (a data wire makes no tier claim)
       CanvasNodeKind = "chat" | "genui-panel" | "email-thread" | "knowledge-preview" | "unknown"
       canvasNodeKindOf(type: string): CanvasNodeKind        // unknown type -> "unknown", never throws
       CANVAS_NODE_KIND_GEOMETRY: Record<CanvasNodeKind, string>   // structure only, zero colour
       CANVAS_NODE_KIND_LABEL: Record<CanvasNodeKind, string>      // polytoken's words -> SANS, never behind pmark

§E — The law-1/2/3 sentences that decide the judgement calls in this plan:
  Law 1: "Everything else is ink. Buttons, links, nav, selected states, focus rings ... carry NO hue."
         "Madder red | Irreversible — this cannot be undone | Destructive buttons only.
          Never errors, never warnings."
  Law 2: "Content that came from the user's own mail renders in a document serif; all product chrome
          is sans. No exceptions."
  Law 3: "Entity type is shape, never hue."
  Identity summary: "Calm registry rhythm: flat surfaces, hairline rules, zero shadow anywhere."

§F — THE TRAP, from brand-guide §3 (60-05's finding, re-confirmed by 60-06). You WILL meet it in
     Task 1, because a card title is serif and sits beside a sans caption:
  **`pmark`/`chip` IMPLIES `font-serif`** (`font-family: var(--font-serif)`). A container carrying
  it must ALSO carry `font-sans` to cancel the default, with the evidence span re-applying
  `font-serif` explicitly. Skip the cancel and the sans sibling silently inherits the serif — and
  **no className-reading gate can see it**, because it is an inherited property, not a class.
  `font-serif` ⇔ `data-evidence` mutually imply each other; the gates enforce the pair.
  Reach for `.badge`/`.swatch` to state a tier in CHROME; `.chip`/`pmark` only for the document's
  own words.

§G — What Plan 61-08 does to these files afterwards, so you do not collide with it: it appends
     `chat/` to `role-hue-ban.test.ts`'s `SCOPED_DIRS`, at which point every `graph-*` and every
     `text-destructive`/`border-destructive` under `chat/` becomes a hard failure. **This plan is
     what makes that ratchet possible.** Leave none behind, including in comments — that gate reads
     LINES, not prose, so describe a retired token ("the retired node-type hue"), never name it.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted node -> shell | `node.type` and `node.data` come from `chat_canvas_layouts`, a user-writable row reconciled by `validateSavedRow` |
| thread/knowledge content -> card | an email-thread card renders subjects and snippets from attacker-authored mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-17 | Tampering (XSS) | all five shells, `data-edge.tsx` | mitigate | Every class comes from a LOOKUP into `canvas-vocabulary.ts`'s closed maps keyed by a narrowed union — never a string concatenated from `node.data`/`edge.data`/`node.type` (T-61-04's obligation, inherited). Subjects, snippets, `sourcePath` and `targetKey` already render as React text nodes; keep them that way and never interpolate them into a class or a `style`. |
| T-61-18 | Tampering | `unknown-node-type-placeholder.tsx` | mitigate | This placeholder IS the mitigation for an unrecognized persisted `node.type` (CANVAS-03's degrade-gracefully contract, and a versioned-registry hedge). Restyling must not turn it into a throw, must not remove it, and must not make it silently invisible — a node the user saved must still be visibly accounted for. Only its madder framing goes. |
| T-61-19 | Repudiation | the `×` remove control on both removable nodes | mitigate | Removing a card from a board is NOT irreversible — the thread, the conversation and the knowledge node all survive; only the placement is dropped, and it re-adds. So madder is wrong on it by MEANING, not merely by token (law 1: "Never errors, never warnings" — and never merely-undoable actions). Make it ink. If a genuinely irreversible control ever lands on this surface, it gets a madder FILL, never madder text. |
| T-61-20 | Information disclosure | `email-thread-node.tsx` | mitigate | Render exactly the fields it renders today (subject, participants, snippet). Do not add one because the sketch's card has room — the sketch is a mock with invented data, and 60-07 recorded the same trap ("the sketch drew data the product does not have"). |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The card language — one sheet, five kinds, zero shadow</name>

  <read_first>
    - apps/web/src/app/chat/_canvas/chat-node.tsx + genui-panel-node.tsx (the two big shells — full; §B)
    - apps/web/src/app/chat/_canvas/canvas-vocabulary.ts (61-02's maps — the real exports, not this plan's paraphrase)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 468-494 + 1047-1091 (.card/.ch/.cbody/.cf)
    - docs/design/brand-guide.md lines 231-263 ("The provenance chip" — the pmark trap, §F)
  </read_first>

  <files>
    apps/web/src/app/chat/_canvas/chat-node.tsx,
    apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  </files>

  <action>
    Realize the sketch's `.card` (§A) on the two shells that carry the most surface, and route their
    kind differentiation through 61-02's vocabulary instead of three near-identical local strings.

    THE SHELL. `--bright` fill (not `bg-background`, which is `--shelf` — a card sits ABOVE the page,
    and the sketch says so), `--rule` border, `rounded-card`, `--rule-hi` on hover. **Delete
    `shadow-elevation-1`/`-2` and `transition-shadow`** — the identity's own line is "flat surfaces,
    hairline rules, zero shadow anywhere", and hover is a rule change, not a lift. Selection stays
    a ring and the ring stays INK (law 1: "selected states ... carry NO hue"); `ring-primary`
    already resolves to ink, so say ink rather than inheriting it through `primary`'s indirection —
    that indirection is precisely what let a hue live in these files for three milestones.

    KIND VIA THE VOCABULARY. Replace the per-file shell strings with
    `CANVAS_NODE_KIND_GEOMETRY[canvasNodeKindOf(...)]` (§D). ChatNode's `border-l-2
    border-l-primary` stripe is the right IDEA — a left rule saying "this is the conversation" — so
    keep the idea and take it from the map, in ink, explicitly. The three near-identical local
    strings are how this debt accumulated; the map is how it stops.

    THE HEADER. The sketch's `.ch`: a `--hair` bottom rule, a `--faded` icon at 13px, a serif
    title. Today ChatNode's header is `bg-muted/60` while every other node's is `bg-muted/40` — a
    "neutral tonal shift" (26-UI-SPEC FIX-04) invented when the system had nothing else to
    differentiate with. It now has geometry. Prefer the map's rule/weight over two shades of the
    same grey; if you keep a fill distinction, justify it in the summary against `CANVAS_NODE_KIND_
    GEOMETRY` rather than leaving it as inherited.

    ChatNode's icon is `text-primary`; GenuiPanelNode's is `text-muted-foreground`. The sketch says
    **`.ch svg{color:var(--faded)}` for every kind** — the icon is not the differentiator, the
    geometry is (law 3). Make both faded.

    **LAW 2 ON THE CHAT NODE'S TITLE — decide it explicitly.** ChatNode renders
    `conversations.find(...)?.title ?? "Chat"` in `text-sm font-semibold text-foreground`. Ask where
    the words came from: a conversation title is USER-AUTHORED or polytoken-generated — it is not
    the user's mail. The sketch's serif `.ct2` sits on cards whose titles ARE the mail's own words
    (a thread subject, a source title). So the chat node's title is chrome and stays **sans**, and
    the `?? "Chat"` fallback is unambiguously polytoken's word. State this reasoning in the summary —
    Task 2 makes the opposite call on the email-thread card for the same reason, and the pair only
    makes sense together.

    THE GENUI PANEL CHROME (criterion 1's fourth named component). "From turn {n}" is polytoken's
    caption — the sketch's `.cap`, `--pencil` at the micro step, sans. The streaming dot is
    `text-primary` (ink) with `aria-label="Streaming"`; keep the aria, keep the `motion-safe:`
    guard, put it on the identity. The `p-4` body padding becomes the sketch's `.cbody` rhythm via
    a named step.

    PRESERVE EXACTLY:
      - `GenuiPanelNodeBody`'s memo split and the reason for it — React Flow feeds changing position
        props into the node component on every drag tick, defeating its memo; the body's stable
        props are what keep the SpecRenderer tree from re-rendering mid-drag (found live 2026-07-04).
        Do not add an unstable prop (an inline object/lambda) to it while restyling.
      - `useCanvasSpec`/`useCanvasPart`/`usePanelData`/`useDataBindings`/`usePanelOverlay` +
        `resolveActivePanel` + `PanelThemeScope` + `GenuiPartBoundary variant="bare"` — the whole
        D-07/D-09 chain. **`spec-renderer.tsx` stays byte-identical**; it is a locked file.
      - `<PanelActionsToolbar>`'s mount point and its `h-8 ... pointer-coarse:h-11` row. **Plan
        61-08 reuses this component in the transcript** — do not change its props shape.
      - `<GeneratingRing active={generating}>`, `onGeneratingChange`, the `isInteractiveWidget`
        branch, `node-drag-handle` (the h-9 row is the ONLY drag handle — 26-UI-SPEC FIX-04, and
        `chat-canvas.tsx` sets `dragHandle=".node-drag-handle"`), both `<Handle>` elements, the
        min-width/min-height floors (D-07: a node's dimensions never change while its spec streams,
        so the graph never relayouts mid-stream), and ChatNode's `useChatController` throw-on-
        missing-provider posture.
      - `chat-node.tsx`'s `ChatControllerProvider` / `useOptionalChatController` exports — 61-08
        imports the optional accessor.

    The `<Handle>` elements get their appearance from Plan 61-05's `.react-flow__handle` override,
    not from a className here. Do not add one; two owners of one look is how they drift.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/_canvas/ && test $(cat src/app/chat/_canvas/chat-node.tsx src/app/chat/_canvas/genui-panel-node.tsx | grep -v '^\s*\*' | grep -v '^\s*//' | grep -c "shadow-elevation\|text-primary\|border-l-primary") -eq 0 && grep -q "CANVAS_NODE_KIND_GEOMETRY" src/app/chat/_canvas/chat-node.tsx && grep -q "CANVAS_NODE_KIND_GEOMETRY" src/app/chat/_canvas/genui-panel-node.tsx && grep -q "PanelActionsToolbar" src/app/chat/_canvas/genui-panel-node.tsx</automated>
  </verify>

  <done>
    Both shells are the sketch's flat card — `--bright` fill, `--rule` border, `rounded-card`,
    `--rule-hi` hover, zero shadow, ink selection ring stated rather than inherited. Kind comes from
    `CANVAS_NODE_KIND_GEOMETRY`; both icons are faded. The chat title stays sans and the summary says
    why. The panel caption is pencil sans. The memo split, the D-07/D-09 chain, the toolbar mount,
    the drag handle, the dimension floors and every export survive. `spec-renderer.tsx` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: The remaining nodes and the edge — and three live law violations</name>

  <read_first>
    - apps/web/src/app/chat/_canvas/email-thread-node.tsx + knowledge-preview-node.tsx + unknown-node-type-placeholder.tsx (full; §B)
    - apps/web/src/app/chat/_canvas/data-edge.tsx (full; §C)
    - apps/web/src/app/chat/_canvas/canvas-vocabulary.ts (61-02's maps)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 458-467 + 1047-1073 (.e-*/.edgelabel/.card source variants)
    - docs/design/brand-guide.md lines 309-335 ("The madder rule in practice")
  </read_first>

  <files>
    apps/web/src/app/chat/_canvas/email-thread-node.tsx,
    apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx,
    apps/web/src/app/chat/_canvas/unknown-node-type-placeholder.tsx,
    apps/web/src/app/chat/_canvas/data-edge.tsx
  </files>

  <action>
    Bring the remaining three shells onto Task 1's card language, put the edge on the vocabulary, and
    clear the three violations named in this plan's objective. **Verify each violation still exists
    before fixing it** — 60-07 was ordered to fix a defect that had already been fixed, and wasted
    the leg proving it.

    VIOLATION 1 — `unknown-node-type-placeholder.tsx`'s `border-destructive/30`. **Madder framing a
    STATE.** An unrecognized node type is not an irreversible action; it is CANVAS-03's
    degrade-gracefully case, which is closer to "uncertain" than to "danger". Per brand-guide §3:
    an error is ink on a rule, a warning is ink weight, an uncertain read is `--pencil`. Reframe it
    that way. This is the same violation class as 60-06's `<Badge variant="destructive">Preview
    failed</Badge>` — a status talking in the irreversible colour. **Keep the placeholder itself**
    (T-61-18): it must still visibly account for a node the user saved. Only its madder goes.

    VIOLATION 2 — `email-thread-node.tsx`'s `text-graph-email` icon. The retired node-type family,
    which post-59 resolves to `--pencil` anyway, so the "role colour" it encodes is already dead on
    screen. Faded, per the sketch's `.ch svg` (§A) and Task 1's icons. Its comment says "NOT
    `text-primary`, 54-UI-SPEC" — that instruction is superseded by law 3, and **the comment must go
    too**: Plan 61-08's ratchet reads LINES, not prose, so a comment naming the retired token turns
    the gate red on its own documentation (§G, 60-06's trap 1). Describe it, never name it.

    VIOLATION 3 — `hover:text-destructive` on both `×` buttons. Removing a card from a board is NOT
    irreversible (T-61-19): the thread, the conversation and the knowledge node all survive; only
    the placement drops, and it re-adds from the same popover. So madder is wrong here by MEANING,
    not merely by token. Make it ink. Note the gate's own asymmetry while you are here: it bans
    madder TEXT and would ban this even for a genuinely irreversible control — its guidance is that
    such a control should be rebuilt as a madder FILL rather than allowlisted. Neither of these
    qualifies for one.

    THE CARDS. `email-thread-node` and `knowledge-preview-node` take Task 1's card language.
    **LAW 2 LANDS HERE, and this is the pair to Task 1's opposite call:** the sketch's `.ct2` (the
    thread subject) and `.csnip` (the thread snippet) are SERIF, because they are the mail's own
    words — while `.cap`/`.parts` ("Saved from the web", "Rafael Lima, you · 3 messages") are sans,
    because they are polytoken's summary OF the mail. Ask where the words came from, not which
    element holds them. Mark every serif element `data-evidence` — **the pair, always** (§F). And
    watch the trap: a serif title beside a sans caption in one header is precisely where an
    inherited `font-family` slips through, and no className gate can see it.

    Keep `email-thread-node`'s Skeleton loading rows and their `role="status"`/`aria-label`. Render
    exactly the fields rendered today (T-61-20) — the sketch's card shows a URL line and a
    "Saved from the web" caption for a source node that **does not exist in this product yet** (it
    is Phase 63's). Do not build it here; do not invent data to fill the sketch's shape. 60-07
    recorded this exact trap on the inbox row.

    `knowledge-preview-node`'s footer link carries `hover:bg-accent hover:text-accent-foreground`
    and `pointer-coarse:h-11`. It IS interactive (unlike 61-04's tool row), so a hover is honest —
    put it on the sketch's register and keep the touch-target floor.

    THE EDGE (§C). `className="!stroke-primary"` becomes `CANVAS_EDGE_TIER.neutral` from 61-02 (§D).
    **State why neutral in a comment**: a data-carrying edge wires `sourcePath -> targetKey` — it is
    plumbing, not provenance, so it makes no tier claim and therefore earns no hue. That sentence is
    what stops the next person from "improving" it to conf-green because green means connected. The
    `conf`/`sugg` members exist for `/knowledge`'s tier edges (Phase 62) and Phase 63's provenance
    edges; do not use them here.

    The label pill becomes the sketch's `.edgelabel` — `--faded` on `--leaf`, micro step, small
    radius, **no shadow** (it carries `shadow-sm` today). PRESERVE everything §C lists: the
    always-visible label (never hover-gated), `interactionWidth={20}`, `getSmoothStepPath`, the
    `EdgeLabelClickContext` seam and its persisted-`data` purity, `animated` staying unset, and
    `nodrag nopan`.

    If Plan 61-05 registered `--edge` in `@theme` you have a `stroke-edge` utility; if it did not,
    consume `var(--edge)` at the call site. **Read 61-05-SUMMARY.md for the decision rather than
    guessing** — it recorded it precisely so this task would not have to choose twice.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/_canvas/ && test $(cat src/app/chat/_canvas/email-thread-node.tsx src/app/chat/_canvas/knowledge-preview-node.tsx src/app/chat/_canvas/unknown-node-type-placeholder.tsx src/app/chat/_canvas/data-edge.tsx | grep -c "graph-\|text-destructive\|border-destructive\|shadow-elevation\|!stroke-primary") -eq 0 && grep -q "CANVAS_EDGE_TIER" src/app/chat/_canvas/data-edge.tsx && grep -q "data-evidence" src/app/chat/_canvas/email-thread-node.tsx && grep -q "interactionWidth={20}" src/app/chat/_canvas/data-edge.tsx</automated>
  </verify>

  <done>
    All three violations are gone — from code AND comments (the ratchet reads lines). The unknown
    placeholder still accounts for the node, in ink/pencil. The two cards are Task 1's card
    language, with the thread's subject and snippet in serif + `data-evidence` and polytoken's
    captions in sans. No invented source-card fields. The edge is `CANVAS_EDGE_TIER.neutral` with
    the reason stated in a comment, its label is the sketch's pill with no shadow, and every
    preserved behaviour in §C holds. The `--edge` call-site decision was read from 61-05-SUMMARY.md,
    not re-decided.
  </done>
</task>

<task type="auto">
  <name>Task 3: The node/edge law gate — on the rendered shells, not the maps</name>

  <read_first>
    - apps/web/src/app/chat/_canvas/__tests__/canvas-vocabulary.test.ts (61-02's gate — what is ALREADY covered; do not duplicate it)
    - apps/web/src/app/emails/[id]/_components/__tests__/region-overlay-law.test.tsx (the rendered-matrix standard)
    - the five shells + data-edge.tsx (Tasks 1-2)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-06-SUMMARY.md (the madder proxy's blind spot)
  </read_first>

  <files>apps/web/src/app/chat/_canvas/__tests__/canvas-node-law.test.tsx</files>

  <action>
    Gate the RENDERED shells. State the division of labour in the file header so this does not
    duplicate 61-02: **`canvas-vocabulary.test.ts` gates the MAPS; this gates what the components
    actually render with them.** A correct map wired into a component that ignores it is exactly the
    failure neither gate alone can see.

    Mount each shell (`ChatNode`, `GenuiPanelNode`, `EmailThreadNode`, `KnowledgePreviewNode`,
    `UnknownNodeTypePlaceholder`) and `DataEdge` inside the minimal provider tree each needs — copy
    the mount convention from the existing `_canvas/__tests__/` suites rather than inventing one
    (several document a `React` import gotcha under vitest's classic JSX runtime; the shells' own
    headers name it). Assert:

      - NO RETIRED FAMILY: no rendered class on any shell contains the retired node-type family.
        Construct the pattern from parts — do not write the literal, so this gate survives Plan
        61-08 adding `chat/` to `SCOPED_DIRS` (which would otherwise walk this file and match it).
      - NO MADDER ON A STATE: no rendered class contains a madder TEXT or BORDER token. Cover the
        unknown placeholder and both `×` buttons explicitly — those are the three live violations
        Task 2 fixed, and a gate that does not name them lets them back in.
        **Header note, mandatory:** this is a PROXY, not a proof. A `variant="destructive"` control
        passes it and can still violate law 1 — 60-06 found exactly one by READING. Do not let a
        green here read as "law 1 holds on this surface".
      - ZERO SHADOW: no shell's root carries a `shadow-elevation-*` class, selected or unselected.
        The identity's "zero shadow anywhere" made executable for the canvas.
      - SELECTION IS INK: for every shell, the selected-vs-unselected class difference contains no
        tier token — computed as a set difference (60-04's idiom).
      - KIND IS LEGIBLE, RENDERED: the four real shells produce mutually DISTINCT root class
        strings. 61-02 asserts the MAP's values differ; this asserts the components actually use
        them. Wire one shell to the wrong key and 61-02 stays green — this is the gate that catches it.
      - LAW 2, BOTH WAYS: on `EmailThreadNode`, the subject and snippet carry `font-serif` AND
        `data-evidence`; the participants line and any caption carry neither. On `ChatNode`, the
        title carries neither (Task 1's explicit call). Assert the mutual implication over the whole
        rendered tree, not two spot checks.
      - THE EDGE IS NEUTRAL: `DataEdge` renders no `conf`/`sugg` token, and its label is still
        rendered unconditionally (never hover-gated — 23-UI-SPEC).

    NEGATIVE PROOF — required, to 60-04's standard. Run each and paste the RED output in the summary:
      1. Temporarily restore `border-destructive/30` on the unknown placeholder. "No madder on a
         state" MUST go red. Revert.
      2. Temporarily point `GenuiPanelNode` at `CANVAS_NODE_KIND_GEOMETRY.chat`. "Kind is legible,
         rendered" MUST go red — proving this gate catches the mis-wire 61-02's map gate structurally
         cannot. Revert.
      3. Temporarily drop `data-evidence` from `EmailThreadNode`'s subject while keeping
         `font-serif`. "Law 2, both ways" MUST go red. Revert.
      4. Confirm `git diff --stat` against the Task 2 commit is empty after all three reverts.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_canvas/__tests__/canvas-node-law.test.tsx && npx vitest run && npx tsc --noEmit && npm run test:geometry</automated>
  </verify>

  <done>
    `canvas-node-law.test.tsx` is committed and green over all five shells + the edge, asserting no
    retired family, no madder on a state (labelled a proxy in the header), zero shadow, ink
    selection, rendered kind legibility, law 2's mutual implication, and the edge's neutrality. Its
    header states the division of labour against 61-02's map gate. All three negative proofs were
    executed — including the mis-wire proof that shows why both gates are needed — their RED output
    is pasted in the summary, and the tree is restored with an empty diff and a green full suite.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean; `npx vitest run` fully green (Phase 60's gates, the 44
  chat suites, 61-02's map gate, 61-05's stock ban).
- `cd apps/web && npm run test:geometry` green.
- `cd apps/web && npm run build:local` (safe since `7df5ad2`). NEVER `npm run build -w @polytoken/web`.
  NEVER a bare `npx playwright test`.
- `packages/genui/src/renderer/spec-renderer.tsx` byte-identical (`git diff --stat` empty for it) —
  it is a locked file.
- All three negative proofs' RED output recorded in 61-06-SUMMARY.md.
- Zero occurrences of the retired node-type family or a madder text/border token anywhere under
  `chat/_canvas/` — **Plan 61-08's ratchet depends on this being true before it runs.**
</verification>

<success_criteria>
- One card language across every node kind: a flat sheet, one rule, one radius, no shadow.
- Kind is geometry from one map — the three near-identical local strings are gone.
- The mail's own words on a card are serif; polytoken's summary of them is sans; the gate proves both.
- The three live law violations are gone from code and comments, clearing the way for 61-08's ratchet.
</success_criteria>

<output>
Create `.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-06-SUMMARY.md` when
done. Record the chat-title-stays-sans vs thread-subject-is-serif reasoning as a pair — it is the
clearest worked example of law 2's "ask where the words came from" test this project has, and
Phases 62-63 will each need to make the same call. Confirm explicitly that `chat/_canvas/` is now
clear of the retired family and madder-on-a-state; 61-08's ratchet is gated on it.
</output>

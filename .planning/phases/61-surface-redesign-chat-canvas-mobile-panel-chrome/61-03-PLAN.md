---
phase: 61-surface-redesign-chat-canvas-mobile-panel-chrome
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - apps/web/src/app/chat/page.tsx
  - apps/web/src/app/chat/_components/conversation-rail.tsx
  - apps/web/src/app/chat/_components/conversation-row.tsx
  - apps/web/src/app/chat/_components/composer.tsx
  - apps/web/src/app/chat/_components/__tests__/chat-frame-structure.test.tsx
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "The /chat frame reads as a designed registry — rail, column, canvas — not three stacked bars of inherited chrome"
    - "The composer is ink: the send control carries no hue, and its focus is an ink outline"
    - "The rail's selected conversation is stated by fill and weight, never by a hue"
    - "The frame does not scroll its document at 390 or 1440 — proven in a real browser, not asserted"
  artifacts:
    - path: "apps/web/src/app/chat/_components/composer.tsx"
      provides: "The redesigned composer — the sketch's ink send control"
    - path: "apps/web/src/app/chat/_components/conversation-rail.tsx"
      provides: "The redesigned conversation rail, height chain intact"
    - path: "apps/web/src/app/chat/_components/__tests__/chat-frame-structure.test.tsx"
      provides: "The structural gate: the frame restructured, law 1 on the rail and composer"
  key_links:
    - from: "apps/web/src/app/chat/page.tsx"
      to: "apps/web/src/app/chat/_components/conversation-rail.tsx"
      via: "the h-svh height chain the rail hangs from"
      pattern: "h-svh"
---

<objective>
Redesign `/chat`'s frame, conversation rail and composer onto the locked identity — and keep the
height chain that broke tonight provably intact while doing it.

Purpose: this is the half of criterion 1 that is pure chrome. Today the frame is three `h-11` bars
of `border-border/50` stacked on inherited defaults, the rail is a `w-[280px]` list with a
`variant="default"` New-chat button, and the composer is a `max-w-3xl` textarea with a
`hover:bg-primary/90` send button — none of it designed, all of it survived Phase 51's re-token
because a re-token cannot see structure. The sketch designs all three (`.convrail`, `.chatcol`,
`.composer`, `.newchat`, `.citem`) at a specific density, and Phase 59 shipped the density steps to
build it with.

This plan restructures the exact subtree whose height chain collapsed the document to 11,296px
hours ago (`e2a2abf`). That is why it depends on 61-01: the geometry gate must exist BEFORE anyone
touches this frame again.

Output: a designed frame/rail/composer, plus a structural gate proving the restructure happened and
law 1 holds on both controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

§A — The sketch's design target (direction-final.html, CSS lines 393-443; markup 1010-1032).
     This is D-58-01's own rendering of these exact three components:

  .convrail{ width:208px; flex:none; padding:14px 10px; display:flex; flex-direction:column;
             gap:2px; border-right:1px solid var(--hair); }
  .newchat { display:flex; align-items:center; gap:7px; font-size:13px; font-weight:600;
             color:var(--ink); padding:7px 9px; border-radius:6px; margin-bottom:10px;
             text-align:left; border:1px solid var(--rule); background:var(--bright); }
  .newchat:hover{ background:var(--shade); border-color:var(--rule-hi) }
  .citem   { font-size:12.5px; text-align:left; padding:7px 9px; border-radius:6px;
             color:var(--faded); overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
  .citem:hover{ background:var(--ink-05); color:var(--ink) }
  .citem.on{ background:var(--shade); color:var(--ink); font-weight:600 }     <- selection = FILL + WEIGHT, no hue
  .chatcol { width:388px; flex:none; display:flex; flex-direction:column;
             background:var(--bright); border-right:1px solid var(--hair); }
  .composer{ display:flex; gap:8px; padding:14px 16px; border-top:1px solid var(--hair) }
  .composer input{ flex:1; min-width:0; font:400 13px/1.4 var(--sans); color:var(--ink);
             background:var(--leaf); border:1px solid var(--rule); border-radius:var(--r-ctl);
             padding:9px 12px; }
  .composer input::placeholder{ color:var(--pencil) }
  .composer input:focus{ outline:2px solid var(--ink); outline-offset:1px }   <- focus = INK
  .send    { flex:none; width:36px; height:36px; border-radius:var(--r-ctl);
             background:var(--ink); color:var(--on-fill); display:flex; ... }  <- send = INK fill
  .send:hover{ background:var(--fill-hi) }

  Read the numbers as INTENT, not as literals to paste: Phase 59 shipped named steps for exactly
  this (`px-chip-x`/`py-chip-y`, `px-row-x`/`py-row-y`, `p-panel`, `rounded-card`, `text-2xs..xl`).
  Reach for the named step — brand-guide.md §3 "The density steps in practice".

§B — page.tsx as shipped (the frame; the file Task 1 rewrites). The height chain is LOAD-BEARING:

  ChatPage -> <div className="flex h-svh flex-col">
    <div className="flex h-11 shrink-0 items-center gap-2 border-b border-border/50 px-2">
      Button variant="ghost" size="icon" className="size-11"   // rail toggle, aria-label swaps
      <span className="text-base font-semibold text-foreground">Chat</span>
    </div>
    <div className="flex min-h-0 flex-1">
      <ConversationRail selectedId onSelect onDeleted collapsed onCollapsedChange
                        mobileOpen onMobileOpenChange onNewChat creatingConversation />
      <div className="flex min-h-0 min-w-0 flex-1 flex-col">
        {selectedId && selectedConversation ? <ConversationView key={selectedId} .../>
         : selectedId ? "Loading conversation…" : <ChatHomeEmptyState .../>}
      </div>
    </div>

  ConversationView -> <div className="flex h-full min-h-0 flex-col">
    <span className="sr-only" aria-live="polite">{controller.liveAnnouncement}</span>
    <div className="flex h-11 shrink-0 items-center justify-between gap-2 border-b border-border/50 bg-background/95 px-4">
      left:  {!isMobile && <ChatCanvasViewToggle .../>} <ModelPicker .../>
      right: {effectiveViewMode === "canvas" && <SaveStatusIndicator status={canvasSaveStatus} />}
             <ThreadClusterIndicator/> <CostMeter/>
    </div>
    <div className="min-h-0 flex-1">
      {effectiveViewMode === "canvas" ? <ChatCanvasIsland .../>
       : <div className="flex h-full min-h-0 flex-col"><MessageList/><GeneratingIndicator/><Composer/></div>}
    </div>

  DO NOT TOUCH (load-bearing logic, 61-CONTEXT "Specific Ideas"):
    - `const webllm = useWebllmEngine()` at ChatPage level, threaded down (D-08) — ONE instance, so
      switching conversations never re-downloads the engine. Do not move it, do not call it twice.
    - `useConversationController({conversationId, modelId, webllm})` — ONE instance driving BOTH
      the docked body and the canvas island (D-02).
    - `effectiveViewMode = isMobile ? "chat" : viewMode` and the `readStoredViewMode`/
      `writeStoredViewMode` posture (MOBL-01).
    - `key={selectedId}` on ConversationView; `handleOpenConversation`; `handleConversationDeleted`.

§C — conversation-rail.tsx (the file Task 1 also rewrites). §C of 61-01 explains the trap:

  <div className="hidden md:block h-full">
    <Collapsible className="h-full" open={!collapsed} onOpenChange={(open)=>onCollapsedChange(!open)}>
      <div className={cn("h-full shrink-0 overflow-hidden border-r border-border/50 bg-background/95",
                         "t-panel-reveal", collapsed ? "w-0" : "w-[280px]")}>
        <CollapsibleContent forceMount className="h-full w-[280px]">
          {renderRailBody(onSelect, "flex h-full w-[280px] flex-col")}
        </CollapsibleContent>
      </div>
    </Collapsible>
  </div>
  <Sheet open={mobileOpen} onOpenChange={onMobileOpenChange}>
    <SheetContent side="left" className="md:hidden p-0">
      <SheetTitle className="sr-only">Conversations</SheetTitle>
      {renderRailBody(handleMobileSelect, "flex h-full w-full flex-col")}
    </SheetContent>
  </Sheet>
  <DeleteConversationDialog ... />

  renderRailBody(handleSelect, wrapperClassName):
    <div className={wrapperClassName}>
      <div className="shrink-0 p-2"><Button variant="default" size="sm" className="w-full gap-2">
        <Plus className="size-4"/> New chat</Button></div>
      <ScrollArea className="min-h-0 flex-1">
        <div className="space-y-1 p-2 pt-0">
          {isLoading ? <RailSkeleton/> : conversations?.length ? conversations.map(c => <ConversationRow .../>)
           : <p className="px-2 py-4 text-center text-xs text-muted-foreground">No conversations yet.</p>}
        </div>
      </ScrollArea>
    </div>

  **`className="h-full"` on `<Collapsible>` is LOAD-BEARING** — Radix renders it as a bare <div>
  with no class of its own. Its removal is what scrolled the document to 11,296px (`e2a2abf`), and
  it is 61-01's negative proof. If you restructure this subtree, the `h-full` must survive in
  whatever replaces it, and `npm run test:geometry` is how you find out — not the unit suite, which
  passed through the entire bug.

  PRESERVE: the `chat:rail:collapsed` localStorage hydrate + persist effects; the separate
  `mobileOpen` boolean (defaults CLOSED so a phone's first paint never shows the overlay); the
  mobile Sheet's row-select ALSO closing the Sheet; `renamingId`/`deletingConversation` state and
  the single `DeleteConversationDialog` instance; `RailSkeleton`'s `aria-busy`/`aria-label`.

§D — composer.tsx as shipped (the file Task 2 rewrites):

  const MAX_TEXTAREA_HEIGHT_PX = 208;   // max-h-52, 13 lines
  <div className="w-full shrink-0 border-t border-border/60 bg-background shadow-elevation-2">
    <div className="mx-auto flex w-full max-w-3xl items-end gap-2 px-4 py-4">
      <Textarea ref value onChange onKeyDown disabled={isStreaming}
                placeholder="Ask the agent anything…" rows={1}
                className="max-h-52 min-h-[44px] resize-none overflow-y-auto scrollbar-token" />
      <Button variant={isStreaming ? "secondary" : "default"} size="icon"
              className={cn("size-11 shrink-0 focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1",
                            isStreaming ? "hover:bg-secondary/80" : "hover:bg-primary/90")}
              aria-label={isStreaming ? "Stop generating" : "Send message"}
              onClick={isStreaming ? onStop : submit}
              disabled={!isStreaming && value.trim().length === 0}>
        {isStreaming ? <Square className="size-4"/> : <Send className="size-4"/>}
      </Button>
  PRESERVE EXACTLY: `resizeTextarea` (auto-grow to 208 then internal scroll), Enter submits /
  Shift+Enter newline, the disabled-while-streaming posture, focus staying on the composer after
  submit (`requestAnimationFrame` refocus), and **the one-button morph** — Send and Stop are the
  SAME element (one tab stop across the morph, 22-UI-SPEC Accessibility). Do not branch it into two.

§E — The Phase-59 system + the post-59 resolutions that matter here (globals.css):
    --primary: var(--ink)   --ring: var(--ink)   --accent: var(--shade)   --muted: var(--shade)
    --border: var(--rule)   --background: var(--shelf)   --card: var(--leaf)   --popover: var(--bright)
  So `bg-primary`/`ring-ring` ALREADY resolve to ink — there is no hue to remove here, and that is
  the trap: the surface is law-1-compliant BY ACCIDENT of an indirection, not by design. Where the
  sketch says ink, say ink. `hover:bg-primary/90` on a send button is not "the ink hover" the
  sketch designed (`--fill-hi`); it is stock shadcn's opacity trick wearing ink's clothes.
  DECLARED but NOT `@theme`-registered (no utility exists — use `var()` at the call site or leave
  them to Plan 61-05, which owns registration): `--edge` `--grid` `--rule-hi` `--ink-05`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation title -> rail row | `conversation.title` is user-authored free text rendered in the rail and in the delete dialog |
| composer draft -> submit | user input crossing into the turn pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Tampering (XSS) | `conversation-row.tsx` | mitigate | `conversation.title` already renders as a React text node — keep it that way. Never interpolate it into a class string, a `style`, or `dangerouslySetInnerHTML`. Restyling must not turn a text node into markup. |
| T-61-08 | Denial of Service (client) | `composer.tsx` | mitigate | Keep `MAX_TEXTAREA_HEIGHT_PX`'s clamp exactly as it is: `el.style.height = Math.min(el.scrollHeight, 208)`. It is what stops a pasted novel from growing the composer past the viewport and pushing the transcript off-screen. A redesign that "lets the composer breathe" by removing the clamp reintroduces a layout DoS that `npm run test:geometry` would then catch — do not make it find this one. |
| T-61-09 | Repudiation | `composer.tsx` | accept | The Send/Stop morph shares one element and one tab stop; a mis-styled morph could make "stop" look like "send". Accepted: the `aria-label` swaps with the handler in the same expression, so the accessible name can never disagree with the action. Do not split them into two elements. |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. Every primitive it needs (`Button`, `Textarea`, `ScrollArea`, `Collapsible`, `Sheet`) is already vendored in `@polytoken/ui`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The frame and the rail — a registry, not three stacked bars</name>

  <read_first>
    - apps/web/src/app/chat/page.tsx (the file you rewrite — full; §B)
    - apps/web/src/app/chat/_components/conversation-rail.tsx (the file you rewrite — full; §C)
    - apps/web/src/app/chat/_components/conversation-row.tsx (the row; its rename/delete affordances)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 393-412 + 1010-1016 (.convrail/.newchat/.citem)
    - docs/design/brand-guide.md lines 336-345 ("The density steps in practice")
  </read_first>

  <files>
    apps/web/src/app/chat/page.tsx,
    apps/web/src/app/chat/_components/conversation-rail.tsx,
    apps/web/src/app/chat/_components/conversation-row.tsx
  </files>

  <action>
    Redesign the frame and the rail to the sketch's registry rhythm (§A). This is a RESTRUCTURE, not
    a re-token — Phase 51 already did the re-token and the user's verdict was still "ugly/
    experimental". If the diff is only class swaps, this task has failed its own criterion.

    THE FRAME (`page.tsx`). Today two separate `h-11` bars stack: the page's own ("Chat" +  rail
    toggle) and `ConversationView`'s (view toggle, model picker, save status, cost meter). At 900px
    that is 22px of chrome height spent saying "Chat" — a word the nav rail already says. Collapse
    them into ONE header row per the sketch's frame, which has no page-title bar at all: the rail
    toggle and the view/model controls belong on the same rule. Keep the rail toggle reachable when
    the rail is 0px wide (that is why it lives outside the rail — D-11), and keep the `sr-only`
    `aria-live` announcement region.

    **THE HEIGHT CHAIN IS THE RISK.** `h-svh` at the root is what makes `/chat` never scroll its
    document; every descendant hangs off it via `min-h-0 flex-1` / `h-full`. Merging two bars means
    re-deriving that chain. After every structural edit run `npm run test:geometry` (61-01) — the
    unit suite passed through the entire 11,296px bug and will pass through the next one too.

    THE RAIL. Realize `.convrail` (§A): a narrower column (the sketch's 208px against today's
    280px — the rail lists titles, not previews), `p-panel`-derived padding, a `--hair` right rule
    rather than `border-border/50`, and `gap-2`-scale row rhythm.

    The New-chat button is `variant="default"` today, i.e. a FILLED ink block — the loudest control
    on the surface, for the least destructive action on it. The sketch's `.newchat` is an outlined
    control (`--bright` fill, `--rule` border, ink text, `--shade`/`--rule-hi` on hover). Make it
    outlined. This is not a colour change; it is a hierarchy correction, and it is the kind of thing
    a re-token structurally cannot make.

    THE ROW (`conversation-row.tsx`). `.citem`/`.citem.on`: an unselected row is `--faded`;
    selected is `--shade` fill + ink text + `font-semibold`; hover is `--ink-05`. **Selection is
    fill and weight — never a hue** (law 1: "selected states ... carry NO hue"). `--ink-05` has no
    registered utility (§E), so either use `bg-ink/5` if that resolves under this token shape or
    reach for `bg-shade` at a lower emphasis — decide, and say which and why in the summary; do not
    invent a new token for it here (Plan 61-05 owns `@theme` registration).

    KEEP the rail's own `h-full` chain intact (§C) — including `className="h-full"` on
    `<Collapsible>`, whose absence is 61-01's negative proof. If your restructure removes the
    `Collapsible` entirely, that is allowed, but then the geometry gate is the only thing standing
    between you and the same bug in a new shape: run it.

    PRESERVE everything §B and §C list as load-bearing: the single `useWebllmEngine()` (D-08), the
    single `useConversationController` (D-02), `effectiveViewMode`'s mobile forcing, the
    `chat:rail:collapsed` hydrate/persist pair, the separate default-CLOSED `mobileOpen` boolean,
    the mobile Sheet's select-then-close, the rename/delete state and its single dialog instance,
    and `RailSkeleton`'s aria attributes. This plan is a restyle of a working surface; a
    "simplification" of any of these is a regression wearing a redesign's clothes.

    Reach for the named density steps (`px-row-x`/`py-row-y`, `px-chip-x`/`py-chip-y`, `p-panel`,
    `rounded-card`, `text-2xs..xl`) rather than fresh numbers — that is what Phase 59 shipped them
    for, and it is how these surfaces end up sharing a rhythm instead of each inventing one.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/ && npm run test:geometry && test $(grep -v '^\s*\*' src/app/chat/page.tsx | grep -v '^\s*//' | grep -c "h-svh") -ge 1</automated>
  </verify>

  <done>
    The frame is one header rule, not two stacked bars. The rail is the sketch's narrower registry
    column with an outlined New-chat control and fill+weight selection carrying no hue. Every
    load-bearing behaviour in §B/§C survives. `npm run test:geometry` is green at both viewports —
    the document does not scroll. `npx vitest run src/app/chat/` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: The composer — an ink control, designed rather than defaulted</name>

  <read_first>
    - apps/web/src/app/chat/_components/composer.tsx (the file you rewrite — full; §D)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 431-443 (.composer/.send)
    - apps/web/src/app/globals.css (the @utility block + the :root ladder — read the real values, not the guide's summary)
  </read_first>

  <files>apps/web/src/app/chat/_components/composer.tsx</files>

  <action>
    Realize the sketch's `.composer` + `.send` (§A/§D).

    The send control: `--ink` fill, `--on-fill` glyph, `--fill-hi` on hover, `rounded` at the
    control radius. It resolves to ink TODAY via `variant="default"` -> `bg-primary` -> `--ink`,
    which looks like compliance and is not design — `hover:bg-primary/90` is stock shadcn's
    opacity trick, not the ink hover the identity defines. Say ink where the sketch says ink.

    The field: `--leaf` fill, `--rule` border, `--pencil` placeholder, and **focus as a 2px ink
    outline with 1px offset** — the sketch's `outline:2px solid var(--ink); outline-offset:1px`.
    Today it is `focus-visible:ring-2 ring-ring ring-offset-1` on the BUTTON and nothing explicit on
    the field. Focus is ink under law 1 ("focus rings ... carry NO hue"); `--ring` already resolves
    to `--ink`, so this is again about saying it rather than inheriting it.

    The dock: the sketch's composer sits on a `--hair` top rule with `14px 16px` padding and **no
    shadow** — the identity's own note is "flat surfaces, hairline rules, zero shadow anywhere".
    Today it carries `shadow-elevation-2`. Remove the elevation; a hairline rule is the separation.
    Reconsider `max-w-3xl` against the sketch's `.chatcol` (a 388px column with the canvas beside
    it): the reading column's width is a design decision, so make it deliberately and say what you
    chose and why. Do not leave it at 3xl merely because it is already 3xl.

    PRESERVE EXACTLY (§D): `resizeTextarea` and `MAX_TEXTAREA_HEIGHT_PX`'s clamp (T-61-08 — it is
    the layout-DoS guard, not a cosmetic cap), Enter/Shift+Enter, the disabled-while-streaming
    posture, the post-submit refocus, and **the one-button Send/Stop morph** (T-61-09 — one element,
    one tab stop, `aria-label` swapping in the same expression as the handler). Do not split the
    morph into two elements to make styling easier.

    Keep the `.touch-target`/`size-11` floor on the send control (D-48-07, WCAG 2.5.8) — the sketch
    draws 36px, which is BELOW the 44px floor this project already committed to. Where the sketch
    and an accessibility floor disagree, the floor wins; note the deviation in the summary the way
    brand-guide §3 records Phase 60's ("Documented deviations from the sketch — and why").
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/_components/ && test $(grep -v '^\s*\*' src/app/chat/_components/composer.tsx | grep -v '^\s*//' | grep -c "shadow-elevation") -eq 0 && grep -q "MAX_TEXTAREA_HEIGHT_PX" src/app/chat/_components/composer.tsx</automated>
  </verify>

  <done>
    The composer is the sketch's: ink send fill, ink focus outline, `--leaf` field, `--pencil`
    placeholder, hairline top rule, zero shadow. The reading-column width is a stated decision. The
    auto-grow clamp, the keyboard contract, and the one-element Send/Stop morph all survive
    unchanged. The 44px-vs-36px deviation from the sketch is recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: The frame gate — prove the restructure, and that law 1 holds on both controls</name>

  <read_first>
    - apps/web/src/app/_components/__tests__/inbox-structure.test.tsx (Phase 60's structural-gate standard)
    - apps/web/src/app/chat/_components/__tests__/ (the existing chat mount conventions — pick the closest analog)
    - apps/web/src/app/chat/page.tsx + conversation-rail.tsx + composer.tsx (Tasks 1-2)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-02-SUMMARY.md ("the anti-re-token gate, proven able to fail")
  </read_first>

  <files>apps/web/src/app/chat/_components/__tests__/chat-frame-structure.test.tsx</files>

  <action>
    Gate the three components this plan redesigned.

    A NOTE ON SHAPE so nobody later harmonizes it with 61-02's: that gate is a semantic matrix over
    pure maps; this one mounts components. Neither substitutes for `npm run test:geometry`, which is
    the ONLY thing here that can see layout — jsdom computes none. Say that in the file header, or
    the next reader will assume a green suite means the frame renders.

    Mount `ConversationRail` (both the desktop tree and the mobile Sheet) and `Composer`, and assert:

      - SELECTION IS NOT A HUE: a selected `ConversationRow`'s class string differs from an
        unselected one, and the DIFFERENCE contains no `conf`/`sugg`/retired-node-type token — it is
        fill and weight only (law 1). Compute it as a set difference, mirroring 60-04's
        "role is not colour" idiom.
      - SELECTION IS LEGIBLE: selected and unselected produce DISTINCT class strings. Re-encoding
        selection is the goal; deleting it is not.
      - THE COMPOSER IS ONE BUTTON: exactly one control carries the send/stop role across both
        `isStreaming` values, and its `aria-label` is "Send message" when idle and "Stop
        generating" while streaming (T-61-09 — the accessible name can never disagree with the
        action).
      - THE CLAMP SURVIVES: `MAX_TEXTAREA_HEIGHT_PX` is still exported/applied and the textarea
        still carries an internal-overflow class (T-61-08).
      - NO ELEVATION ON THE DOCK: the composer's root carries no `shadow-elevation-*` class — the
        identity's "zero shadow anywhere" made executable for this surface.
      - THE RAIL'S HEIGHT CHAIN IS DECLARED: whatever element the rail body hangs from carries an
        explicit height class. jsdom cannot prove it RESOLVES — only `test:geometry` can — but it
        can prove nobody deleted the declaration, which is exactly what happened in `e2a2abf`.

    NEGATIVE PROOF — required, to 60-02's standard ("proven able to fail"). Run each and paste the
    RED output in the summary:
      1. Temporarily give the selected `ConversationRow` a tier token. "Selection is not a hue"
         MUST go red. Revert.
      2. Temporarily make the selected and unselected row classes identical. "Selection is legible"
         MUST go red. Revert.
      3. Temporarily re-add `shadow-elevation-2` to the composer dock. "No elevation" MUST go red.
         Revert.
      4. Confirm `git diff --stat` against the Task 2 commit is empty after all three reverts.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/chat-frame-structure.test.tsx && npx vitest run && npx tsc --noEmit && npm run test:geometry</automated>
  </verify>

  <done>
    `chat-frame-structure.test.tsx` is committed and green, asserting selection-is-not-a-hue,
    selection-is-legible, the one-button morph with its aria contract, the auto-grow clamp, no
    elevation on the dock, and a declared rail height chain. Its header states plainly that jsdom
    sees no layout and names `test:geometry` as the gate that does. All three negative proofs were
    executed, their RED output is pasted in the summary, and the tree is restored with an empty
    diff and a green full suite.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npx vitest run` — every suite green, including Phase 60's gates and the 44 chat
  suites, unmodified.
- `cd apps/web && npm run test:geometry` green — `/chat`'s document does not scroll at 390 or 1440.
  **This is the gate that matters for this plan; the unit suite passed through the entire 11,296px
  bug.**
- `cd apps/web && npm run build:local` (safe since `7df5ad2` — targets `.next-verify`). NEVER
  `npm run build -w @polytoken/web` (fails on missing env, pre-existing). NEVER a bare
  `npx playwright test`.
- All three negative proofs' RED output recorded in 61-03-SUMMARY.md.
</verification>

<success_criteria>
- The frame is designed: one header rule, a registry rail, a deliberate reading column — visibly
  distinct from the pre-59 three-stacked-bars version in STRUCTURE, not only colour.
- Selection and focus are ink, stated rather than inherited through `primary`'s indirection.
- The composer's send is an ink control on a hairline rule with no shadow.
- The height chain that broke tonight is provably intact in a real browser.
</success_criteria>

<output>
Create `.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-03-SUMMARY.md` when
done. Record the reading-column width decision, the `--ink-05` call-site decision, and the
44px-vs-36px sketch deviation — Plan 61-04 restyles the stream inside this column and 61-07 mounts
panels in it.
</output>

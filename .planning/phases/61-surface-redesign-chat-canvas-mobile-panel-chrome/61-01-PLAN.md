---
phase: 61-surface-redesign-chat-canvas-mobile-panel-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/playwright.geometry.config.ts
  - apps/web/e2e/surface-geometry.spec.ts
  - apps/web/e2e/screenshot-review.spec.ts
  - apps/web/package.json
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "A surface whose height chain is broken fails a committed gate instead of shipping green"
    - "The rail and the transcript scroll inside themselves; the document does not scroll"
    - "A capture records both themes — dark mode is photographed for the first time"
    - "A capture waits for async data to land, so a chip that exists is in the picture"
  artifacts:
    - path: "apps/web/playwright.geometry.config.ts"
      provides: "A config with NO webServer block — cannot spawn a second next dev"
      contains: "testMatch"
    - path: "apps/web/e2e/surface-geometry.spec.ts"
      provides: "The rendered-geometry gate: no unexpected document scroll + scroll containment"
    - path: "apps/web/e2e/screenshot-review.spec.ts"
      provides: "A theme axis (999.23) and a settle step (999.24)"
  key_links:
    - from: "apps/web/package.json"
      to: "apps/web/playwright.geometry.config.ts"
      via: "the test:geometry script"
      pattern: "playwright.geometry.config"
    - from: "apps/web/e2e/surface-geometry.spec.ts"
      to: "http://localhost:3000/chat"
      via: "a real browser against the ALREADY-RUNNING dev server"
      pattern: "/chat"
---

<objective>
Build the two instruments this phase's own verification depends on: a rendered-geometry gate that
catches the bug class four separate green suites missed tonight, and a screenshot harness that can
actually see dark mode and settled data.

Purpose: **jsdom does no layout.** `/chat`'s conversation rail broke its height chain and the
document scrolled to **11,296px at a 900px viewport** — the main pane read as empty — while all 44
chat suites (363 tests) passed before AND after the fix (`e2a2abf`). The sidebar shipped at half
width through Phase 55's 4/4 verification and 730 green tests. Both were found by a human opening
the app. Phases 61-63 are about to restructure the two most layout-dense surfaces in the product;
going in with only class-string gates repeats that exactly. This plan runs FIRST and everything
after it inherits both instruments.

Output: `playwright.geometry.config.ts` (no `webServer` — structurally unable to corrupt the dev
server), `e2e/surface-geometry.spec.ts` with a proven-red negative proof, a `test:geometry` script,
and a screenshot harness with a theme axis (999.23) and a settle step (999.24).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-CONTEXT.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-07-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

§A — The two existing Playwright configs and why a THIRD is required:

  playwright.config.ts        (npm run test:e2e)
    testDir "./e2e", testMatch /.*\.spec\.ts/, testIgnore /screenshot-review\.spec\.ts$/
    projects: chromium + firefox
    webServer: { command: "npm run dev", url: "http://localhost:3000",
                 reuseExistingServer: true, timeout: 120_000 }

  playwright.screenshot.config.ts   (npm run screenshot:review)
    testMatch /screenshot-review\.spec\.ts$/, workers 1, fullyParallel false
    webServer: same shape, reuseExistingServer: true

  BOTH declare a webServer. A dev server is ALREADY live on port 3000 and `next build`/`next dev`
  share `apps/web/.next` — 60-07 root-caused a whole lost verification leg to exactly this
  collision (999.22). The new config declares **NO webServer at all**: it cannot spawn a second
  compiler even if the port check misbehaves. That is the point, and it is why this is a third
  config rather than a testMatch added to an existing one.

  package.json scripts today:
    "dev": "dotenv -e ../../.env.local -- next dev"
    "test:e2e": "playwright test"
    "screenshot:review": "playwright test --config playwright.screenshot.config.ts"

§B — The auth seam every /chat capture needs (both existing specs use it):

  // e2e/helpers/seed-session.ts
  export async function seedAuthenticatedContext(context: BrowserContext):
    Promise<{ userId: string; ... }>
  // Mints a REAL local GoTrue admin session (magiclink + verifyOtp). NEVER interactive Google,
  // NEVER a faked cookie.

  The local-only gate (T-50-01) — `token-render.spec.ts` REIMPLEMENTS this locally rather than
  importing it from screenshot-review.spec.ts, so importing the module never also registers that
  file's capture `test.describe` against this run. Do the same here:

    const LOCAL_HOSTS = new Set(["localhost", "127.0.0.1"]);
    function isLocalTarget(baseURL, supabaseUrl): boolean   // both hosts local, fail closed on throw

  token-render.spec.ts also loads root .env.local itself (Playwright's runner does not):
    loadDotenv({ path: path.resolve(process.cwd(), "..", "..", ".env.local"), override: false });
  cwd is apps/web under npm workspaces, so .env.local is two levels up.

§C — The exact bug this gate exists to catch (`e2a2abf`, fixed hours ago):

  // apps/web/src/app/chat/_components/conversation-rail.tsx (as shipped, post-fix)
  <div className="hidden md:block h-full">
    <Collapsible className="h-full" open={!collapsed} onOpenChange={...}>   // <- `h-full` LOAD-BEARING
      <div className={cn("h-full shrink-0 overflow-hidden border-r ...", collapsed ? "w-0" : "w-[280px]")}>
        <CollapsibleContent forceMount className="h-full w-[280px]">
          {renderRailBody(onSelect, "flex h-full w-[280px] flex-col")}
        </CollapsibleContent>
      </div>
    </Collapsible>
  </div>

  Radix renders `<Collapsible>` as a bare `<div>` with NO class of its own. Given no className it
  grew to content; `CollapsibleContent`'s `h-full` then resolved against THAT instead of the 856px
  wrapper; the document scrolled to 11,296px. Removing `className="h-full"` from the `Collapsible`
  reproduces it exactly — that is Task 1's negative proof.

§D — The height chain the gate asserts against (apps/web/src/app/chat/page.tsx):

  ChatPage        -> <div className="flex h-svh flex-col">
    top bar       -> <div className="flex h-11 shrink-0 ...">
    body          -> <div className="flex min-h-0 flex-1">
      ConversationRail (above)
      main col    -> <div className="flex min-h-0 min-w-0 flex-1 flex-col">
        ConversationView -> <div className="flex h-full min-h-0 flex-col">
          top bar -> h-11 shrink-0
          <div className="min-h-0 flex-1">  -> ChatCanvasIsland | (MessageList + GeneratingIndicator + Composer)

  `h-svh` at the root means a correct /chat NEVER scrolls its document. That single invariant is
  what makes the gate cheap and total.

§E — The internally-scrolling viewports (the containment half):

  message-list.tsx's MessageList wraps its turns in `<ScrollArea>`; the real scroller is Radix's
  internal `[data-radix-scroll-area-viewport]` (the wrapper only forwards a ref to the
  non-scrolling Root). conversation-rail.tsx's `renderRailBody` uses the same `<ScrollArea
  className="min-h-0 flex-1">`. Both therefore expose `[data-radix-scroll-area-viewport]` — the
  attribute is set internally by @radix-ui/react-scroll-area and is the stable selector both the
  product code and this gate already key on.

§F — screenshot-review.spec.ts as shipped (the file Task 2 edits):

  BASE_SURFACES = [login /, inbox /, chat /chat, knowledge /knowledge, studio /studio,
                   forwarding /settings/forwarding]   (+ emails/{id} when authSeeded)
  VIEWPORTS     = [{mobile,390,844}, {desktop,1440,900}]
  RUN_DIR       = <repo-root>/.planning/ui-reviews/{ISO-timestamp}/     (gitignored)

  async function captureSurface(page, surface, viewport, records): Promise<AuthStatus>
    await page.setViewportSize(...)
    await page.goto(surface.path, { waitUntil: "load" })
    await page.waitForTimeout(400)                 // <- 999.24 lives here
    const authStatus = resolveAuthStatus(page.url(), surface.path)
    const filename = buildFilename(surface.name, viewport.name, "default")
    await page.screenshot({ path: path.join(RUN_DIR, filename), fullPage: true })
    records.push({ surface, viewport, pack: "default", authStatus, filename })

  function buildFilename(surfaceName, viewportName, packId): string
    const suffix = packId === "default" ? "" : `-${packId}`;
    return `${surfaceName}-${viewportName}${suffix}.png`;

  interface CaptureRecord { surface; viewport; pack; authStatus; filename }
  writeIndex(records, authSeeded) -> a markdown table
    | Surface | Viewport | Pack | Auth Status | File |

  The nested capture loop:
    for (const surface of surfaces) for (const viewport of VIEWPORTS) {
      const authStatus = await captureSurface(page, surface, viewport, records);
      await captureAlternatePackIfPresent(page, surface, viewport, authStatus, records);
    }

§G — The theme mechanism (apps/web/src/app/layout.tsx:65):

  <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>

  next-themes with `attribute="class"` toggles `.dark` on <html>; its default storage key is
  `theme`. The probe-proven approach (999.23): `page.emulateMedia({ colorScheme })` +
  `localStorage.theme` + confirming `.dark` on <html>. `defaultTheme="system"` + `enableSystem`
  means emulateMedia alone drives it when no stored preference exists — but the storage write makes
  it deterministic rather than dependent on the absence of prior state. Do both, then ASSERT the
  resulting `<html>` class rather than trusting either.

§H — Known harness defects (61-CONTEXT.md; do not re-derive):
  999.23 no theme axis — dark has NEVER been captured.  999.24 shoots before data lands — chips
  missing from every capture.  999.25 fixture seeds zero entities/extractions, so `--sugg` has
  never rendered (NOT this plan's to fix — do not block on it).  The "N" badge over "Sign out" is
  the Next.js dev indicator, not a bug.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> Supabase admin (GoTrue service_role) | `seedAuthenticatedContext` mints a real admin session; a non-local target must never receive it |
| capture artifacts -> repo | PNGs and index.md may contain a signed-in session's rendered secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Elevation of Privilege | `e2e/surface-geometry.spec.ts` | mitigate | Reimplement `isLocalTarget` locally (§B) and gate `seedAuthenticatedContext` behind it. BOTH the app baseURL host and the Supabase URL host must be localhost/127.0.0.1; any unparseable URL fails CLOSED (returns false, seeding skipped). Never import the gate from `screenshot-review.spec.ts` — that would register its capture describe into this run. |
| T-61-02 | Information disclosure | `.planning/ui-reviews/**` | mitigate | The RUN_DIR is gitignored (`*.png`). Task 2 adds a theme axis, which doubles the PNG count but changes nothing about their handling: never `git add -f`, never commit a PNG, never paste a rendered forwarding address / access token / signed URL into a summary. Assert `git check-ignore` before claiming the artifact is safe. |
| T-61-03 | Tampering | `apps/web/.next` | mitigate | The new config declares NO `webServer`. This is the mitigation, not a convenience: a second `next dev` sharing `.next` with the live server corrupts it silently (999.22, cost 60-07 a whole leg). Never add a `webServer` block to `playwright.geometry.config.ts`; never run a bare `npx playwright test` (that resolves the DEFAULT config). |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. `@playwright/test`, `dotenv` and `pg` are already dependencies (used by `token-render.spec.ts`). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The rendered-geometry gate — the thing 363 green tests could not see</name>

  <read_first>
    - apps/web/e2e/token-render.spec.ts lines 1-120 (the assertion-spec + local-auth precedent this mirrors)
    - apps/web/e2e/helpers/seed-session.ts (seedAuthenticatedContext's signature + return shape)
    - apps/web/playwright.screenshot.config.ts (the config shape to mirror MINUS its webServer)
    - apps/web/src/app/chat/page.tsx (the h-svh height chain, §D)
    - apps/web/src/app/chat/_components/conversation-rail.tsx lines 180-210 (the Collapsible h-full, §C)
  </read_first>

  <files>
    apps/web/playwright.geometry.config.ts,
    apps/web/e2e/surface-geometry.spec.ts,
    apps/web/package.json
  </files>

  <action>
    Build the gate that would have caught tonight's rail bug, and make it structurally incapable of
    corrupting the dev server.

    CREATE `apps/web/playwright.geometry.config.ts`. Mirror `playwright.screenshot.config.ts`'s
    shape — `testDir: "./e2e"`, `testMatch: /surface-geometry\.spec\.ts$/`, `fullyParallel: false`,
    `workers: 1`, `retries: 0`, `reporter: "list"`, `use: { baseURL: "http://localhost:3000", trace:
    "off" }`, a single chromium project — with ONE deliberate difference: **no `webServer` block at
    all** (T-61-03). Say why in the file header, naming 999.22: both other configs declare one, a
    dev server is already live on 3000, and `next dev`/`next build` share `apps/web/.next`. A config
    that cannot spawn a compiler cannot corrupt the build dir. This spec asserts against whatever is
    already serving 3000; if nothing is, it fails fast with a connection error, which is the honest
    outcome — do not add a webServer to "fix" that.

    ADD to `apps/web/package.json` scripts: `"test:geometry": "playwright test --config
    playwright.geometry.config.ts"`. Mirror `screenshot:review`'s exact shape.

    ALSO update `playwright.config.ts`'s `testIgnore` so `npm run test:e2e` never picks this spec
    up — it currently ignores only `screenshot-review.spec.ts`, and its `testMatch` is
    `/.*\.spec\.ts/`, so a new e2e spec rides along by default. Widen the ignore to cover BOTH
    (e.g. an alternation over the two filenames). Getting this wrong means `test:e2e` runs the
    geometry spec under the config that DOES spawn a webServer — the precise hazard this plan
    exists to remove.

    CREATE `apps/web/e2e/surface-geometry.spec.ts`.

    Load root `.env.local` exactly as `token-render.spec.ts` does (§B — Playwright's runner does not
    do it for you). Reimplement `isLocalTarget` LOCALLY (T-61-01, §B) — never import it from
    `screenshot-review.spec.ts`. Seed a session via `seedAuthenticatedContext` only when
    `isLocalTarget(baseURL, process.env.SUPABASE_URL ?? "")`; when it is false, `test.skip()` with a
    clear message rather than asserting geometry on a `/login` redirect (a redirect's geometry is
    not the geometry under test, and a gate that silently measures the wrong page is worse than no
    gate — that is 60-07's whole lesson).

    THE PRIMARY ASSERTION — no surface scrolls its document unexpectedly. For `/chat` at BOTH
    viewports (390x844 and 1440x900), after `goto` + a settle (see below), evaluate in the page:

      { scrollHeight: document.documentElement.scrollHeight, innerHeight: window.innerHeight }

    and assert `scrollHeight <= innerHeight + EPSILON` with `EPSILON = 2` (sub-pixel rounding;
    define it as a named constant with that reason in a comment). At 900px viewport height the
    broken rail measured 11,296px — the failure mode is orders of magnitude, not a pixel, so a tight
    epsilon costs nothing and a loose one would have missed nothing either. Put the measured numbers
    in the failure message (`expected <= 902, got 11296`) — a gate whose red output does not name
    the number is a gate someone will re-run instead of read.

    THE CONTAINMENT ASSERTION — the rail and the transcript scroll INSIDE themselves. At the desktop
    viewport, assert at least one `[data-radix-scroll-area-viewport]` exists inside the rail's
    subtree and one inside the message list's subtree (§E), and that each has
    `clientHeight > 0` and a bounded `getBoundingClientRect().height <= window.innerHeight + EPSILON`.
    This is the positive half of the same invariant: the document not scrolling is only correct if
    the content is scrolling somewhere else. Without it, a surface that simply clips its overflow
    would pass.

    SETTLING — `waitUntil: "networkidle"` then an explicit wait for a real element (not a fixed
    sleep): wait for the composer's textarea (`Ask the agent anything…` placeholder) or the rail's
    "New chat" button to be visible. Next dev's HMR websocket keeps a connection open, so
    `networkidle` can be slow — set a generous per-assertion timeout and `test.setTimeout(120_000)`
    for the spec. If `networkidle` proves flaky against the HMR socket, fall back to
    `waitUntil: "load"` + `expect(locator).toBeVisible()`, and record which you used and why.

    HYDRATION CHECK — before asserting anything, prove the app actually hydrated (60-07 reviewed 14
    plausible PNGs of a crashed app). Assert the composer textarea is visible AND enabled. A
    skeleton-only render fails this, loudly, instead of reporting a geometrically-perfect empty page
    as green.

    NEGATIVE PROOF — required, and the whole justification for this task. Run it and paste the RED
    output in the summary:
      1. Temporarily remove `className="h-full"` from the `<Collapsible>` in
         `conversation-rail.tsx` (§C). The document-scroll assertion MUST go RED, and the failure
         message must name a scrollHeight in the thousands. Revert.
      2. Confirm `git diff --stat` is empty after the revert, so no proof edit leaked into the
         committed state.
    If step 1 does NOT go red, the gate is measuring the wrong thing — fix the gate, do not weaken
    the proof.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run test:geometry && node -e "const c=require('./package.json');if(!c.scripts['test:geometry'])process.exit(1)" && grep -c "webServer" playwright.geometry.config.ts | grep -qx 0 && grep -q "surface-geometry" playwright.config.ts</automated>
  </verify>

  <done>
    `playwright.geometry.config.ts` exists with zero `webServer` occurrences. `test:geometry` is a
    script. `playwright.config.ts` ignores the new spec so `test:e2e` cannot run it under a
    webServer config. `surface-geometry.spec.ts` asserts no-document-scroll at 390 and 1440 plus
    scroll-containment for rail and transcript, gates seeding behind a locally-reimplemented
    `isLocalTarget`, and proves hydration before measuring. The negative proof was executed, its RED
    output (with the scrollHeight in the thousands) is pasted in the summary, and the tree is
    restored with an empty diff.
  </done>
</task>

<task type="auto">
  <name>Task 2: The harness grows a theme axis and learns to wait (999.23, 999.24)</name>

  <read_first>
    - apps/web/e2e/screenshot-review.spec.ts (the file you edit — full; §F)
    - apps/web/src/app/layout.tsx lines 60-94 (the next-themes ThemeProvider config, §G)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-07-SUMMARY.md ("Defects filed")
  </read_first>

  <files>apps/web/e2e/screenshot-review.spec.ts</files>

  <action>
    Give the capture harness the two senses it has never had. Phases 62 and 63 inherit both, so the
    shape matters more than the diff size.

    THEME AXIS (999.23). The user's locked pick explicitly requires light AND dark ("we will want
    light and dark theme"), `globals.css` has shipped a full `.dark` block since Phase 59, and **dark
    mode has never once been captured.** Add a third axis alongside surface × viewport:

      const THEMES = ["light", "dark"] as const;

    Apply a theme with all three levers, then VERIFY rather than trust (§G):
      - `await page.emulateMedia({ colorScheme: theme })` — drives `defaultTheme="system"`.
      - Seed `localStorage.theme = theme` via `context.addInitScript` so it is set BEFORE
        next-themes' pre-mount script runs on first paint. Setting it after `goto` needs a reload to
        take effect and is a race — `addInitScript` is the deterministic lever.
      - After navigation, ASSERT the resulting state: `document.documentElement.classList.contains("dark")`
        must equal `theme === "dark"`. If it does not, fail the capture loudly. A harness that
        silently photographs light mode twice and labels one of them "dark" is worse than one with
        no theme axis, because it reads as evidence — that is 999.23's own lesson restated.

    Thread `theme` through `buildFilename` (append a `-{theme}` suffix — keep `light` explicit
    rather than implicit, so no reader has to know which one the bare name meant), through
    `CaptureRecord` as a new field, and through `writeIndex`'s table as a new `| Theme |` column.
    Keep `captureAlternatePackIfPresent`'s existing pack suffix composable with it.

    Note the cost honestly in the file header: this doubles the run (14 -> 28 PNGs, plus studio's
    alternates). That is the correct trade — half of this identity has never been looked at.

    SETTLE BEFORE THE SHUTTER (999.24). `captureSurface` currently does `goto({ waitUntil: "load" })`
    then `waitForTimeout(400)`, which fires **before async data lands**: entity chips are missing
    from every capture ever taken, which nearly produced a false "the redesign has no tier chips"
    verdict and made `inbox-mobile.png` look like three grey skeleton rows when the real mobile feed
    is correct. Replace the fixed 400ms with a real settle: `waitUntil: "networkidle"` where it is
    reliable, plus a bounded wait for content to stop being a skeleton (e.g. wait for
    `[aria-busy="true"]` to detach, with a timeout that DEGRADES to capturing anyway rather than
    failing the run — this is a camera, and a picture of a still-loading surface is still
    information, whereas a hard failure loses the other 27 frames).

    The existing header comment says `networkidle` is "deliberately avoided" because Next dev's HMR
    websocket keeps its connection open indefinitely. **Verify that claim against the current stack
    before you rely on either choice** — if `networkidle` does hang, keep `load` and use an explicit
    content wait instead, and rewrite the comment to say what you actually observed. Do not leave a
    comment asserting a constraint you did not re-check; the whole reason 999.24 survived this long
    is that the 400ms looked deliberate.

    Record in the capture record whether the settle succeeded or timed out, and surface it in
    `index.md`, so a reviewer can tell "this surface has no chips" from "this surface's chips had
    not arrived yet". That distinction is the entire defect.

    DO NOT fix 999.25 (the fixture seeds zero entities/extractions, so pencil-amber `--sugg` has
    never rendered). It is real and it is out of this plan's scope — if the settle work makes it
    trivially visible, note it in the summary for Phase 62, do not expand into it.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run screenshot:review && node -e "const fs=require('fs'),p=require('path');const r=p.resolve('../../.planning/ui-reviews');const d=fs.readdirSync(r).filter(x=>fs.statSync(p.join(r,x)).isDirectory()).sort().pop();const f=fs.readdirSync(p.join(r,d));const dark=f.filter(x=>x.includes('dark')&&x.endsWith('.png'));console.log(d,'dark frames:',dark.length);if(dark.length===0)process.exit(1)"</automated>
  </verify>

  <done>
    `screenshot:review` captures surface × viewport × theme. Dark frames exist on disk for the first
    time in this project's history and the run's `index.md` has a Theme column. The theme assertion
    fails loudly on a mislabelled capture rather than photographing light twice. The fixed 400ms is
    replaced by a real settle whose success/timeout is recorded per capture. The `networkidle`
    claim in the header was re-checked against the live stack and the comment states what was
    actually observed. No PNG is committed (`git check-ignore` verified). 999.25 untouched.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npm run test:geometry` green against the live dev server on 3000.
- `cd apps/web && npm run screenshot:review` produces both light AND dark frames.
- `cd apps/web && npx vitest run` — unchanged, still green (this plan touches no src/).
- `cd apps/web && npm run build:local` (safe since `7df5ad2` — targets `.next-verify`).
- Task 1's negative-proof RED output recorded verbatim in 61-01-SUMMARY.md.
- NEVER run a bare `npx playwright test` (T-61-03).
</verification>

<success_criteria>
- A broken height chain on `/chat` turns a committed gate RED — proven, not asserted.
- The gate cannot spawn a second `next dev`, by construction rather than by discipline.
- Dark mode has been photographed. Both themes are on disk and labelled correctly.
- A capture that shot too early is distinguishable in `index.md` from a surface that has no chips.
</success_criteria>

<output>
Create `.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-01-SUMMARY.md` when
done. Record the geometry spec's exported constants and the exact `test:geometry` invocation
verbatim — Plans 61-03 through 61-07 all run it, and Phases 62-63 inherit it.

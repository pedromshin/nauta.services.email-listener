---
phase: 61-surface-redesign-chat-canvas-mobile-panel-chrome
plan: 04
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - apps/web/src/app/chat/_components/message-turn.tsx
  - apps/web/src/app/chat/_components/message-list.tsx
  - apps/web/src/app/chat/_components/tool-round-activity-row.tsx
  - apps/web/src/app/chat/_components/tool-invocation-result-row.tsx
  - apps/web/src/components/provenance-link.tsx
  - apps/web/src/app/chat/_components/__tests__/message-stream-law.test.tsx
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "A citation chip carrying an email's real subject renders in serif; one carrying polytoken's own fallback label does not"
    - "A tool round reads as quiet machine bookkeeping — pencil, subordinate to the answer it produced"
    - "The turn stream reads as a conversation, not as a stack of bordered cards"
    - "Nothing polytoken says in its own voice wears the serif — including the assistant's answer"
  artifacts:
    - path: "apps/web/src/components/provenance-link.tsx"
      provides: "The citation chip with law-2 provenance discrimination (the regionLabelFor pattern)"
      exports: ["ProvenanceLink", "hrefFor", "fallbackLabel"]
    - path: "apps/web/src/app/chat/_components/message-turn.tsx"
      provides: "The redesigned turn — the sketch's .uturn/.aturn"
    - path: "apps/web/src/app/chat/_components/__tests__/message-stream-law.test.tsx"
      provides: "The gate: law 2's serif/data-evidence pair on the stream, law 1 on the tool rows"
  key_links:
    - from: "apps/web/src/components/provenance-link.tsx"
      to: "font-serif + data-evidence"
      via: "provenance discrimination — a real label is evidence, a fallback label is chrome"
      pattern: "data-evidence"
---

<objective>
Redesign the message stream, the tool-round rows and the citation chip — and land law 2 on the one
surface where the distinction is hardest and most valuable: an agent's answer, which mixes
polytoken's own voice with the user's own material in a single paragraph.

Purpose: this is the rest of criterion 1. The stream today is a `space-y-8` stack in which every
assistant turn wears a `border-l-2` rail (v1.4's "assistant role rail"), tool rounds wear
`hover:bg-accent` like they were clickable, and the citation chip — the ONE place an email's real
subject enters a chat answer — renders it in sans beside a `text-muted-foreground` id. Law 2 says
content from the user's own mail renders serif, **no exceptions**; the chip is where that rule
either becomes real on this surface or quietly does not.

Output: a redesigned stream on the sketch's rhythm, quiet tool rounds, a citation chip that
discriminates evidence from chrome by PROVENANCE, and a gate over the serif/`data-evidence` pair.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-CONTEXT.md
@.planning/phases/58-visual-identity-sketch-pick-human-gate/58-IDENTITY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-05-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

§A — The sketch's design target (direction-final.html, CSS 413-430; markup 1018-1027):

  .turns  { flex:1; overflow-y:auto; padding:20px 18px; display:flex; flex-direction:column; gap:16px }
  .uturn  { align-self:flex-end; max-width:85%; background:var(--shade);
            border-radius:12px; padding:9px 13px; font-size:13.5px; line-height:1.55 }
  .aturn  { font-size:13.5px; line-height:1.65 }        <- NO left rail. NO card. NO border.
  .aturn p + p{ margin-top:8px }
  .tool   { display:flex; align-items:center; gap:7px; font-size:12px; color:var(--pencil);
            margin-bottom:7px }                          <- pencil. NOT interactive-looking.
  .tool svg{ width:13px; height:13px }
  .srcchip{ display:inline-flex; align-items:center; gap:6px; max-width:100%;
            font-size:12px; padding:5px 9px; border-radius:4px; margin-bottom:9px;
            background:var(--leaf); border:1px solid var(--rule); color:var(--ink); cursor:pointer }
  .srcchip:hover{ background:var(--shade); border-color:var(--rule-hi) }
  .srcchip svg{ width:13px; height:13px; color:var(--faded) }
  .srcchip .st{ font-family:var(--serif); ... }          <- THE SUBJECT IS SERIF. This is law 2's
                                                            whole appearance on this surface.

  And the answer paragraph itself (markup 1023) — note precisely what is and is not serif:
    <div class="tool">Searched your mail — 3 results</div>              sans, pencil  (chrome)
    <a class="srcchip"><span class="st">Cotação frete SP → POA</span></a>  SERIF       (the mail's own words)
    <p>Acme Freight came back at <strong class="pm c num">R$ 4.820,00</strong> — pickup Friday…</p>
       ^ the prose is SANS (polytoken speaking); the cited VALUE wears the provenance mark.

§B — message-turn.tsx as shipped (the file Task 1 rewrites). Its part switch is load-bearing:

  <div className={isUser ? "flex justify-end" : "flex justify-start"}>
    <div className={isUser ? "max-w-[85%] rounded-lg bg-muted px-4 py-2"
                           : "w-full max-w-full border-l-2 border-l-border/60 pl-3"}>
      {isCostCapBlocked ? <CostCapBlockedCard/> : isFailed ? <InlineErrorCard onRetry={...}/> : (
        <div className="space-y-2">{parts.map((part, index) => { ... })}</div>
      )}
      {showStatusBadge && <div className="mt-1"><TurnStatusBadge status={status}/></div>}
      {showActionRow && <TurnActionRow parts onRegenerate regenerateDisabled siblings
                                       activeSiblingIndex onNavigateSibling/>}

  The part switch, in emission order — PRESERVE EVERY BRANCH AND ITS ORDER:
    "genui_spec"                  -> <GenuiPartBoundary specJson={JSON.stringify(part.spec)} isStreaming={false}/>
    "genui_spec_streaming"        -> <GeneratingRing active><GenuiPartBoundary specJson={part.partialJson} isStreaming/></GeneratingRing>
    "interactive_widget"          -> <InteractiveWidgetBoundary part displayState submittedValue errorMessage onSubmitResult/>
    "interaction_result"          -> <CompactInteractionEntry widgetKind summary/>
    "interactive_widget_streaming"-> <GeneratingRing active><GenuiPartBoundary specJson={part.partialJson} isStreaming/></GeneratingRing>
    "tool_invocation_streaming"   -> <ToolRoundActivityRow toolName/>
    "tool_invocation"             -> null      (the paired result row already narrates the round — 39-UI-SPEC DO-NOT 7)
    "tool_invocation_result"      -> <ToolInvocationResultRow toolName content isError/>
    default (text)                -> <MarkdownRenderer content={part.text}/> + the streaming tail caret

  Derived flags to preserve verbatim: `isFailed` (assistant + status "failed" — content is FULLY
  replaced by the retry card, D-19), `isCostCapBlocked` (status "cost_capped_pre_turn", D-21),
  `showActionRow`, `showStatusBadge` (D-15/D-21 mutually exclusive).

  **Plan 61-07 also edits this file** (it adds overlay resolution to the `genui_spec` branch). It is
  in a LATER wave and depends on this plan. Leave that branch's shape clean and obvious — do not
  bury the `genui_spec` case inside a new abstraction that 61-07 then has to unpick.

§C — message-list.tsx as shipped (the file Task 1 also touches):

  <div className="relative min-h-0 flex-1">
    <ScrollArea ref={scrollAreaRootRef} className="h-full">
      <div className="mx-auto max-w-3xl space-y-8 px-4 py-6">     <- space-y-8 = 32px. Sketch: 16px.
        {turns.map((turn) => <MessageTurn key={turn.id} role parts isStreamingTurn={turn.id === streamingTurnId}
                                          status siblings activeSiblingIndex regenerateDisabled
                                          onRegenerate onNavigateSibling widgets/>)}
    {!isPinnedToBottom && <JumpToBottomButton onClick={scrollToBottom}/>}

  export interface MessageListItem { id; role; parts; status?; siblings?; activeSiblingIndex?;
                                     regenerateTargetId? }
  PRESERVE: the `[data-radix-scroll-area-viewport]` query + scroll listener, the
  NEAR_BOTTOM_THRESHOLD_PX=80 pin logic, the stick-to-bottom effect, `JumpToBottomButton`.
  **The viewport this queries is what `npm run test:geometry` asserts scroll-containment on** — a
  restructure that moves the ScrollArea must keep an internally-scrolling viewport, or the gate goes
  red and it will be right.

  `GeneratingIndicator` also lives in this file (Loader2 + "Generating…" on a `border-t` above the
  composer).

§D — provenance-link.tsx as shipped (apps/web/src/components/, the file Task 2 rewrites):

  export type ProvenanceKind = ...            // email | entity | knowledge (+ per-kind icon)
  export interface ProvenanceLinkProps { kind; id; label?: string; ... }
  export function hrefFor(kind: ProvenanceKind, id: string): string
  export function fallbackLabel(kind: ProvenanceKind, id: string): string
    // "{Capitalized kind} · {first 8 of id}" — shown when the caller passes NO label. This is the
    // EXPECTED case for Phase 36/37's ToolCitation(kind, id, route), which carries no label field.
  export function ProvenanceLink({ kind, id, label }) {
    <Link className={CHIP_CLASS_NAME}>
      <Icon className="size-3 shrink-0" aria-hidden/>
      <span className="min-w-0 truncate">{label ?? fallbackLabel(kind, id)}</span>

  ONE shared primitive, consumed by the chat's citation chips AND the knowledge-preview node (39-04).
  Its header calls it "a small neutral-palette chip".

§E — THE PATTERN TO REUSE, not re-derive (brand-guide.md §3 + 60-04):

  `regionLabelFor` exists because the pre-60 code collapsed three different PROVENANCES into one
  string (`entityTypeLabel ?? contentSnippet(...) ?? status`) and therefore could not obey law 2. It
  discriminates instead:
      { kind: "type",   text }   polytoken's own word for a category   -> chrome, sans
      { kind: "text",   text }   the document's own words              -> evidence, SERIF + data-evidence
      { kind: "status", text }   no extraction happened yet            -> chrome, sans

  `ProvenanceLink` has the IDENTICAL shape one level up: `label ?? fallbackLabel(kind, id)` collapses
  "the email's real subject" and "polytoken's `Email · 1a2b3c4d` placeholder" into one string.

  THE TWO TRAPS (60-05's finding, re-confirmed by 60-06 — do not rediscover them):
    1. **`pmark` IMPLIES serif** (`font-family: var(--font-serif)`). A container carrying `pmark`
       must also carry `font-sans` to CANCEL that default, with the evidence span re-applying
       `font-serif` explicitly. Skip the cancel and the sans text silently inherits the serif —
       and **no className-reading gate can see it**, because it is an inherited property, not a class.
    2. **`font-serif` ⇔ `data-evidence` mutually imply each other.** The gates enforce the PAIR;
       marking one without the other is a test failure, not a style nit.

§F — tool rows as shipped (the files Task 2 also rewrites):

  ToolRoundActivityRow (tool-round-activity-row.tsx) — in-flight round:
    <div role="status" className="flex items-center gap-2 rounded-md py-1 text-sm text-muted-foreground
         transition-colors hover:bg-accent hover:text-accent-foreground focus-visible:outline-none
         focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1
         motion-safe:animate-in motion-safe:fade-in motion-safe:duration-200">
      <Loader2 className="size-4 shrink-0 motion-safe:animate-spin"/><span>{label}</span>
    LABEL_BY_TOOL_NAME = { lookup_entity:"Looking up an entity…", search_emails:"Searching emails…",
                           search_knowledge:"Searching knowledge…", web_search:"Searching the web…" }
    FALLBACK_LABEL = "Running a lookup…"
    A `role="status"` div with `hover:bg-accent` + a focus ring — it is NOT interactive and NOT
    focusable. It has been dressed as a button for two milestones. The sketch's `.tool` is a
    pencil status line.

  ToolInvocationResultRow (tool-invocation-result-row.tsx) — the settled round + its citation chips.

§G — The Phase-59 system available here (globals.css):
  font-serif · tabular · pmark / pmark-confirmed / pmark-suggested · text-2xs..xl (text-lg = 15.5/1.7,
  the reading-pane serif step) · text-ink text-faded text-pencil · bg-shade bg-leaf bg-bright ·
  border-rule border-hair · px-chip-x/py-chip-y · rounded-card
  Post-59 resolutions: --muted: var(--shade) · --accent: var(--shade) · --muted-foreground: var(--faded)
  --primary/--ring: var(--ink)
  DECLARED but NOT registered (no utility): --edge --grid --rule-hi --ink-05 (Plan 61-05 owns
  registration — do not depend on them here).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> transcript | `part.text` is LLM-generated from a prompt containing attacker-authored mail; rendered as markdown |
| tool result -> transcript | `ToolInvocationResultRow`'s `content` and every `ProvenanceLink` label originate in quarantined tool envelopes over attacker-influenced data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-10 | Tampering (XSS) | `markdown-renderer.tsx` consumers | mitigate | Do NOT change the markdown renderer's sanitization posture while restyling. It renders LLM output derived from attacker-authored mail. Restyling means classes on the wrapper and its `components` map — never `dangerouslySetInnerHTML`, never a new raw-HTML plugin, never `skipHtml`/`rehype-raw` "to make a chip render inline". If a design needs inline markup the model does not emit, the answer is no. |
| T-61-11 | Tampering (XSS) | `provenance-link.tsx` | mitigate | `label` is tool-envelope-derived (an email subject from a hostile document). It already renders as a React text node inside a `<span>` — keep it that way. Never interpolate it into a class string or `style`. Select the serif-vs-sans treatment by the DISCRIMINATED branch (a closed union), never by inspecting the label's content. |
| T-61-12 | Spoofing | `provenance-link.tsx` | mitigate | Making a real subject serif and a fallback sans is a PROVENANCE claim rendered as typography. `hrefFor(kind, id)` must keep building the href from the narrowed `kind` + `id`, never from `label` — otherwise a crafted subject could aim the chip somewhere else while wearing the evidence serif that says "this came from your mail". |
| T-61-13 | Information disclosure | `tool-invocation-result-row.tsx` | mitigate | Tier-filtered envelopes are enforced upstream (FOUND-6 `tool_envelope_gate`, Phase 38's three belts). Restyling must not widen what is rendered — do not add a field to the row "because there is room now". Render exactly the fields rendered today. |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. In particular: do not reach for a markdown/sanitizer plugin (see T-61-10). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The turn — a conversation, not a stack of bordered cards</name>

  <read_first>
    - apps/web/src/app/chat/_components/message-turn.tsx (the file you rewrite — full; §B)
    - apps/web/src/app/chat/_components/message-list.tsx (the stream container; §C)
    - .planning/phases/58-visual-identity-sketch-pick-human-gate/sketches/direction-final.html lines 413-419 + 1018-1027 (.turns/.uturn/.aturn)
    - apps/web/src/app/chat/_components/markdown-renderer.tsx (read ONLY — do not change its sanitization posture, T-61-10)
  </read_first>

  <files>
    apps/web/src/app/chat/_components/message-turn.tsx,
    apps/web/src/app/chat/_components/message-list.tsx
  </files>

  <action>
    Realize the sketch's `.turns`/`.uturn`/`.aturn` (§A).

    THE ASSISTANT'S LEFT RAIL GOES. Today every assistant turn wears `border-l-2 border-l-border/60
    pl-3` — v1.4's "assistant role rail", added when the two roles needed telling apart and the
    system had nothing else to say it with. The sketch's `.aturn` has no rail, no card, no border:
    the user's turn is a right-aligned `--shade` bubble and the assistant's is simply the page. That
    IS the differentiation, and it is stronger — the answer becomes the surface rather than a thing
    on it. Removing it is a hierarchy decision, not a colour swap, and it is exactly the class of
    change a re-token cannot make.

    THE RHYTHM. `space-y-8` (32px) between turns against the sketch's `gap:16px`; the sketch's
    `.turns` padding is `20px 18px`. The current stream is spaced like a document; the sketch spaces
    it like a conversation. Reach for the named steps (§G) rather than fresh numbers.

    THE USER BUBBLE. Already close (`max-w-[85%] rounded-lg bg-muted` — `bg-muted` resolves to
    `--shade`). Say `--shade` where the sketch says `--shade` rather than routing through `muted`'s
    indirection, and check the radius against the sketch's 12px — `rounded-card` is 10px,
    `rounded-frame` is 12px. Pick one deliberately and say which in the summary.

    LAW 2 ON THE PROSE — GET THIS RIGHT AND STATE IT. The assistant's answer is **polytoken
    speaking**, so it is SANS. So is the user's typed message. The serif is reserved for the user's
    own MATERIAL — mail, saved sources, values pulled out of them — and 58-IDENTITY audited this
    exact temptation: "Nothing polytoken says in its own voice wears the serif — not even a
    manifesto." The one violation found in the whole sketch was its own manifesto lede, set back to
    sans. **Do not make the assistant's answer serif to make it feel bookish.** If you are unsure
    which side a string is on, ask where the WORDS came from, not which element holds them.

    PRESERVE EVERY BRANCH of the part switch and its emission order (§B), including
    `tool_invocation` -> `null` (39-UI-SPEC DO-NOT 7) and the `isFailed`/`isCostCapBlocked` full-
    content replacement (D-19/D-21). Preserve `MessageList`'s viewport query, pin threshold,
    stick-to-bottom effect and `JumpToBottomButton` (§C) — and note that the
    `[data-radix-scroll-area-viewport]` this queries is what `test:geometry` asserts containment on.

    LEAVE THE `genui_spec` BRANCH CLEAN. Plan 61-07 adds overlay resolution there and depends on
    this plan. Do not wrap it in a new abstraction it then has to unpick.

    Do not touch `markdown-renderer.tsx` in this task beyond reading it (T-61-10).
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/ && test $(grep -v '^\s*\*' src/app/chat/_components/message-turn.tsx | grep -v '^\s*//' | grep -c "border-l-2") -eq 0 && test $(grep -c "tool_invocation_result\|interaction_result\|interactive_widget_streaming\|genui_spec_streaming" src/app/chat/_components/message-turn.tsx) -ge 4</automated>
  </verify>

  <done>
    The assistant's left rail is gone; the user's bubble is the only bordered thing in the stream.
    The turn rhythm is the sketch's, built from named steps. Every part-switch branch and its order
    survive, `tool_invocation` still renders null, and the failed/cost-capped replacements still
    replace. The prose is sans on both roles, and the summary says why. `src/app/chat/` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: The citation chip learns provenance; the tool round stops pretending to be a button</name>

  <read_first>
    - apps/web/src/components/provenance-link.tsx + provenance-link.test.tsx (the file you rewrite + its gate; §D)
    - apps/web/src/app/chat/_components/tool-round-activity-row.tsx (§F)
    - apps/web/src/app/chat/_components/tool-invocation-result-row.tsx (the settled round + its chips)
    - apps/web/src/app/emails/[id]/_components/region-vocabulary.ts lines 190-211 (`regionLabelFor` — the pattern you are reusing, §E)
    - docs/design/brand-guide.md lines 231-263 + 295-308 ("The provenance chip", "Law 2 in practice")
  </read_first>

  <files>
    apps/web/src/components/provenance-link.tsx,
    apps/web/src/app/chat/_components/tool-round-activity-row.tsx,
    apps/web/src/app/chat/_components/tool-invocation-result-row.tsx
  </files>

  <action>
    THE CHIP (§D/§E). `ProvenanceLink` renders `label ?? fallbackLabel(kind, id)` — one string
    collapsing two completely different provenances: an email's REAL SUBJECT (the user's own
    material -> evidence -> serif) and polytoken's own `Email · 1a2b3c4d` placeholder (chrome ->
    sans). That collapse is structurally the same defect `regionLabelFor` was written to fix one
    surface over, and the fix is the same shape. Do not invent a new one.

    Discriminate by PROVENANCE, e.g. a small pure helper returning
    `{ kind: "evidence", text }` when the caller passed a real `label`, and
    `{ kind: "chrome", text }` when it fell back — then:
      - evidence -> `font-serif` AND `data-evidence` (**the pair, always** — the gates enforce it,
        §E trap 2), plus `tabular` if the label can carry numbers.
      - chrome   -> sans, no `data-evidence`.
    Preserve `fallbackLabel`'s and `hrefFor`'s exact behaviour — `hrefFor` keeps building from the
    narrowed `kind` + `id` and NEVER from `label` (T-61-12: typography is making a provenance claim
    here, so the href must not be steerable by the thing wearing the claim).

    Realize the sketch's `.srcchip` for the shape (§A): `--leaf` fill, `--rule` border, `--faded`
    icon, `--shade`/`--rule-hi` hover, chip-scale padding. It is the same mark family as the inbox's
    chip — one mark language everywhere.

    **THE `pmark` TRAP, decided explicitly and recorded** (§E trap 1): `pmark`/`chip` IMPLIES
    `font-serif`. This chip is neutral-palette (no tier), so `pmark-confirmed`/`pmark-suggested` are
    wrong for it — a citation chip makes no tier claim. Decide whether the bare `pmark` utility
    applies (it would force serif onto the whole chip INCLUDING the icon and any sans sibling) or
    whether this chip composes `border`/`bg` directly and applies `font-serif` only to the evidence
    span. **State the decision and the reason in the summary.** Getting this wrong smuggles serif
    onto chrome where no className-reading gate can see it, because it is an inherited property.

    `ProvenanceLink` is ONE shared primitive — the knowledge-preview node consumes it too (39-04).
    Check its other call sites before changing the props shape; a new REQUIRED prop breaks them.
    Prefer deriving the discrimination from the props that already exist.

    THE IN-FLIGHT TOOL ROW (§F). `ToolRoundActivityRow` is a `role="status"` div — not interactive,
    not focusable — wearing `hover:bg-accent hover:text-accent-foreground` and a
    `focus-visible:ring-2` focus ring. It has been dressed as a button for two milestones and cannot
    be clicked or focused. Strip the hover and the focus ring: they are affordance lies. Make it the
    sketch's `.tool` — a `--pencil` status line at the small step, subordinate to the answer it
    produced. Keep `role="status"`, keep `LABEL_BY_TOOL_NAME` + `FALLBACK_LABEL` verbatim (they are
    39-UI-SPEC copy), keep `motion-safe:` guards, keep the spinner.

    **`--pencil` is legal on `--shelf`/`--leaf`/`--bright` but NEVER on `--shade`** (4.23:1 light /
    4.02:1 dark — below the AA floor). The gate catches the token PAIR it can see; this usage rule
    is on you (brand-guide §3 "Enforcement"). The stream sits on `--bright` per the sketch's
    `.chatcol`, so pencil is legal there — but if a tool row ever lands inside the user's `--shade`
    bubble, it is not. Check where it actually renders before reaching for pencil.

    THE SETTLED TOOL ROW (`tool-invocation-result-row.tsx`). Same register: quiet, collapsed,
    subordinate — the round is bookkeeping, the citation chips are the payload. Render exactly the
    fields it renders today (T-61-13 — do not add one because there is room now).

    LAW 1 CHECK WHILE YOU ARE HERE — read, do not grep (the gate is a proxy, not a proof, and 60-06
    found a `variant="destructive"` status badge that PASSED it): `isError` on a tool result is a
    STATE, not an irreversible action. If it speaks in madder anywhere in these two files, it is a
    law-1 violation regardless of what the gate says. An error is ink on a rule; an uncertain read
    is `--pencil`. Report what you found either way — "checked, none present" is a finding.
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/components/provenance-link.test.tsx src/app/chat/ && test $(grep -v '^\s*\*' src/app/chat/_components/tool-round-activity-row.tsx | grep -v '^\s*//' | grep -c "hover:bg-accent\|focus-visible:ring") -eq 0 && grep -q "data-evidence" src/components/provenance-link.tsx && grep -q "font-serif" src/components/provenance-link.tsx</automated>
  </verify>

  <done>
    `ProvenanceLink` discriminates evidence from chrome by provenance: a real subject renders
    `font-serif` + `data-evidence`, a fallback label renders sans with neither. `hrefFor` still
    builds from `kind`+`id` only. The `pmark`-vs-compose decision is recorded with its reason. The
    in-flight tool row is a pencil status line with no hover and no focus ring on a non-focusable
    element. The settled row renders exactly the fields it did before. The madder read was performed
    and reported. Every existing `ProvenanceLink` call site still compiles.
  </done>
</task>

<task type="auto">
  <name>Task 3: The stream gate — law 2's pair, and law 1 on the rounds</name>

  <read_first>
    - apps/web/src/app/emails/[id]/_components/__tests__/region-overlay-law.test.tsx (the law-2 discrimination gate this mirrors)
    - apps/web/src/app/chat/_components/message-turn.tsx + provenance-link.tsx + tool-round-activity-row.tsx (Tasks 1-2)
    - .planning/phases/60-surface-redesign-inbox-email-detail/60-05-SUMMARY.md ("extraction shape gate"; the pmark-implies-serif finding)
  </read_first>

  <files>apps/web/src/app/chat/_components/__tests__/message-stream-law.test.tsx</files>

  <action>
    Gate law 2 on the stream — the surface where it is hardest, because one paragraph mixes
    polytoken's voice with the user's material.

    Mount `MessageTurn` across the part types and `ProvenanceLink` across its two provenance
    branches, and assert:

      - THE PAIR HOLDS BOTH WAYS: every element carrying `font-serif` also carries `data-evidence`,
        and every element carrying `data-evidence` also carries `font-serif`. Assert it as a
        mutual implication over the whole rendered tree, not as two separate spot checks — that is
        what makes it a gate rather than two examples (brand-guide §3: "the gates enforce the pair").
      - EVIDENCE IS DISCRIMINATED: `<ProvenanceLink label="Cotação frete SP → POA — Lote 88" .../>`
        renders serif + `data-evidence`; `<ProvenanceLink kind="email" id="1a2b..." />` with NO
        label renders neither. Same component, same props shape, opposite treatment — decided by
        where the words came from.
      - POLYTOKEN'S VOICE IS SANS: an assistant text turn's prose carries neither `font-serif` nor
        `data-evidence`. So does a user turn's. This is the assertion that catches the tempting
        wrong move (§A/Task 1) — and 58-IDENTITY says one exception makes the rule unlearnable.
      - THE TOOL ROW IS NOT A BUTTON: `ToolRoundActivityRow` renders no hover-background class and
        no focus-ring class, and is not focusable. An affordance lie is a design bug with a
        checkable signature.
      - LAW 1 ON THE ROUNDS: no rendered class in either tool row contains a madder TEXT or BORDER
        token (`isError` is a state — an error is ink on a rule). **Note in the file header that
        this is a PROXY, not a proof**: a `variant="destructive"` status badge passes it and still
        violates law 1 — 60-06 found exactly one by reading. Do not let this assertion read as
        having settled the question.
      - NO SERIF SMUGGLING: no element carries `pmark`/`chip` without either `data-evidence` or an
        explicit sans cancel. This is the ONE law-2 hole a className-reading gate can partially
        cover, and it is the exact trap 60-05 hit and 60-06 re-hit.

    NEGATIVE PROOF — required, to 60-05's standard. Run each and paste the RED output in the summary:
      1. Temporarily make `MarkdownRenderer`'s wrapper `font-serif` in `message-turn.tsx`.
         "Polytoken's voice is sans" MUST go red. Revert.
      2. Temporarily drop `data-evidence` from `ProvenanceLink`'s evidence branch while keeping
         `font-serif`. "The pair holds both ways" MUST go red. Revert.
      3. Temporarily re-add `hover:bg-accent` to `ToolRoundActivityRow`. "The tool row is not a
         button" MUST go red. Revert.
      4. Confirm `git diff --stat` against the Task 2 commit is empty after all three reverts.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run src/app/chat/_components/__tests__/message-stream-law.test.tsx && npx vitest run && npx tsc --noEmit && npm run test:geometry</automated>
  </verify>

  <done>
    `message-stream-law.test.tsx` is committed and green, asserting the serif/`data-evidence` mutual
    implication, evidence-vs-chrome discrimination on one component, sans on both roles' prose, the
    tool row's non-button-ness, law 1 on the rounds (labelled a proxy in the header), and no serif
    smuggling via `pmark`. All three negative proofs were executed, their RED output is pasted in
    the summary, and the tree is restored with an empty diff and a green full suite.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean.
- `cd apps/web && npx vitest run` — every suite green, including Phase 60's gates and the 44 chat
  suites.
- `cd apps/web && npm run test:geometry` green — `MessageList`'s ScrollArea viewport still contains
  its own scroll after the restructure.
- `cd apps/web && npm run build:local` (safe since `7df5ad2` — targets `.next-verify`). NEVER
  `npm run build -w @polytoken/web`. NEVER a bare `npx playwright test`.
- All three negative proofs' RED output recorded in 61-04-SUMMARY.md.
</verification>

<success_criteria>
- An email's real subject reaches a chat answer in serif; polytoken's own fallback label does not.
  Same component, decided by provenance.
- The stream reads as a conversation — one bubble, no rails, the sketch's rhythm.
- Tool rounds are quiet bookkeeping and no longer pretend to be clickable.
- Law 2's hardest case is executable, and the gate detects all three ways to break it.
</success_criteria>

<output>
Create `.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-04-SUMMARY.md` when
done. Record the `pmark`-vs-compose decision for the citation chip and the user-bubble radius choice
verbatim — Plan 61-07 renders panels into this same stream and must not contradict either.
</output>

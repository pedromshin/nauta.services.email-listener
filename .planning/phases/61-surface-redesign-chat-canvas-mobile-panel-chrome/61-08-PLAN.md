---
phase: 61-surface-redesign-chat-canvas-mobile-panel-chrome
plan: 08
type: execute
wave: 4
depends_on: ["61-06", "61-07"]
files_modified:
  - apps/web/src/app/chat/_canvas/transcript-panel-host.tsx
  - apps/web/src/app/chat/_components/message-turn.tsx
  - apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx
  - apps/web/src/app/__tests__/role-hue-ban.test.ts
  - apps/web/src/app/chat/_components/__tests__/transcript-panel-toolbar.test.tsx
  - .planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/artifacts/61-SCREENSHOT-REVIEW.md
autonomous: false
requirements: [SURF-07, SURF-02]

must_haves:
  truths:
    - "On a 390px viewport a user can switch a panel's pack, edit its params, regenerate it and re-theme it"
    - "The canvas's own ChatNode transcript does NOT sprout a second toolbar beside the real panel's"
    - "chat/ is inside the role-hue ban from now on — the ratchet only ever grows"
    - "Both themes of the redesigned chat and canvas have been looked at by a human, on real pixels"
  artifacts:
    - path: "apps/web/src/app/chat/_components/__tests__/transcript-panel-toolbar.test.tsx"
      provides: "Criterion 3's gate — all four controls reachable in the docked transcript, absent on canvas"
    - path: "apps/web/src/app/__tests__/role-hue-ban.test.ts"
      provides: "The ratchet, advanced: SCOPED_DIRS now covers this phase's surface roots"
    - path: ".planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/artifacts/61-SCREENSHOT-REVIEW.md"
      provides: "The look — reviewed in BOTH themes for the first time in this project's history"
  key_links:
    - from: "apps/web/src/app/chat/_components/message-turn.tsx"
      to: "apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx"
      via: "the docked-host marker — mounts the existing toolbar, zero new controls"
      pattern: "PanelActionsToolbar"
---

<objective>
Close SURF-07's write half — the editable-panel toolbar becomes reachable on mobile — advance the
role-hue ratchet over `chat/`, and actually look at what this phase built, in both themes.

Purpose: criterion 3. `PANL-01..04` shipped four real editing controls (pack switch, param edit,
regenerate, re-theme) and mounted every one of them inside `GenuiPanelNode` — a React Flow node. And
`effectiveViewMode = isMobile ? "chat" : viewMode` means **the canvas never mounts below `md`**, so
on a phone those four controls do not exist. That is 999.17's write half, and Plan 61-07 just built
the seam that closes it: the docked transcript now hosts the same store and the same persistence.
This plan mounts the EXISTING toolbar into it. Zero new controls.

Then the ratchet: `role-hue-ban.test.ts`'s `SCOPED_DIRS` is a one-way gate that each of Phases 61-63
advances as it sweeps. 61-06 cleared `chat/_canvas/`'s violations; this plan makes them unrepeatable.

Then the part no gate can do: read the pixels. In both themes, which 61-01 made possible and which
this product has never once done.

Output: criterion 3 closed, the ratchet advanced, and an honest screenshot review.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-CONTEXT.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-06-SUMMARY.md
@.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-07-SUMMARY.md
@.planning/phases/60-surface-redesign-inbox-email-detail/60-07-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

§A — The toolbar you are MOUNTING, not building (panel-actions-toolbar.tsx — read the real file;
     61-06 may have restyled it):

  export interface PanelActionsToolbarProps {
    readonly panelId: string;
    readonly provenance: Provenance;
    readonly activeSpecJson: string;
    readonly resolvedPackId: StylePackId;
    readonly isStreaming?: boolean;           // force-locks every control
    readonly onGeneratingChange?: (on: boolean) => void;
  }
  <div role="toolbar" aria-label="Panel actions"
       className="flex h-8 shrink-0 items-center justify-between gap-1 border-b border-border/60
                  bg-background px-2 pointer-coarse:h-11">          <- the coarse-pointer floor EXISTS
    <PackSwitcher panelId resolvedPackId isLocked onBusyChange/>
    <TooltipProvider delayDuration={300}>
      {["edit","regenerate","retheme","history"].map(id => <Control .../>)}   // ACTION_CONTROLS map
  It owns the per-panel mutual-exclusion lock (`busyAction`): while any one action is pending every
  OTHER control is locked, while the in-flight one keeps its own busy affordance. `isStreaming`
  force-locks all five.

  Its controls write through `usePanelOverlay` — **the THROWING one** — which is correct: a write
  with no persistence wired IS a wiring bug. Inside `TranscriptPanelHost` the providers exist, so
  the same controls work unmodified. **Do not swap them to the optional read.**

§B — Where it mounts (`GenuiPanelNode`, the canonical caller — mirror its prop derivation exactly):

  const { overlay } = usePanelOverlay(panelId);
  const resolved = resolveActivePanel(overlay, specJson, isStreaming);
  {!isInteractiveWidget && (
    <PanelActionsToolbar panelId={panelId} provenance={provenance}
                         activeSpecJson={resolved.specJson} resolvedPackId={resolved.packId}
                         isStreaming={isStreaming} onGeneratingChange={onGeneratingChange}/>
  )}
  Note `!isInteractiveWidget` — an interactive_widget panel has NO toolbar, by design and out of
  PANL's scope. The transcript must honour the same exclusion.

§C — `provenance` is derivable in the transcript — no new data, no migration. From
     use-canvas-persistence.ts:126-130, the canvas builds its node as:
       id: genuiPanelNodeId(row.id, partIndex),
       data: { provenance: { messageId: row.id, partIndex, runId: null }, turnIndex: row.turnIndex }
     `row.id` is the chat_messages id — the same `messageId` Plan 61-07 threaded into `MessageTurn`,
     and the same `partIndex` the map already has. So the transcript constructs the IDENTICAL
     provenance the canvas node carries. Use that shape verbatim (`runId: null` included).

§D — **THE THREE-TREE PROBLEM, WRITE EDITION** (61-07 §D). `MessageTurn` renders in three trees:
  1. DOCKED, inside `TranscriptPanelHost`      -> the toolbar SHOULD mount. This is criterion 3.
  2. ON CANVAS, inside `ChatNode` inside `chat-canvas.tsx`'s providers -> the toolbar must NOT
     mount: the real `GenuiPanelNode` beside it already has one, and a second toolbar editing the
     same overlay from inside a chat node on the same board is a bug, not a feature.
  3. BARE (tests)                               -> no providers, no toolbar, no throw.

  Tree 2 is the trap: 61-07's `useOptionalPanelOverlay` resolves happily there (the canvas's
  providers ARE present), so a naive `if (overlay !== undefined)` mounts a toolbar on the canvas.
  **Presence of the store does not distinguish tree 1 from tree 2 — both have it.**

  THE DISCRIMINATOR: `TranscriptPanelHost` is mounted in tree 1 and ONLY tree 1 (61-07 wraps the
  docked branch alone; the branches are mutually exclusive). So the host should expose a marker its
  descendants can read — "you are in the docked transcript" — and the toolbar mounts on that marker,
  never on store presence and never on a viewport check.

  **Do NOT gate on `useIsMobileViewport()`.** Criterion 3 says the user "can reach" the controls on
  a mobile viewport — it does not say "only on mobile". The marker satisfies mobile AND gives the
  desktop docked view the same editing, which falls out of the same seam at zero extra cost. A
  viewport gate would additionally re-introduce a JS mount decision where CSS suffices, against
  53-UI-SPEC's "only 2 consumers this phase" rule for that hook.

§E — THE RATCHET (`apps/web/src/app/__tests__/role-hue-ban.test.ts`), verbatim:

  /** THE RATCHET. Roots swept clean of role-as-hue, relative to `apps/web/src/app`.
   *   - `_components`  — the inbox surface        (Phase 60, swept)
   *   - `emails/[id]`  — the email-detail surface (Phase 60, swept)
   * PHASES 61-63: append your root here as you sweep it. Do not remove one. */
  export const SCOPED_DIRS: readonly string[] = ["_components", "emails/[id]"];

  Its own pinning tests, which you must EXTEND rather than replace:
    it("covers BOTH Phase-60 roots — the inbox and the email-detail view", ...)
       expect(SCOPED_DIRS).toContain("_components"); expect(SCOPED_DIRS).toContain("emails/[id]");
    it("every scoped root exists and actually yields files — the gate cannot be made vacuous", ...)
       // a root that walks to zero files would police nothing, and a gate that inspects nothing
       // passes everything
  Two live traps in this file, both cost a rework in 60-06:
    1. **It reads LINES, not prose** — a comment citing a banned literal turns the gate red on its
       own documentation. Describe retired tokens; never name them.
    2. **The pattern requires the colour-utility PREFIX, not the bare family** — the walk covers
       `__tests__` dirs inside the scoped roots, where sibling gates legitimately assert on the
       banned family by name. Widen it and the gate executes its own siblings.
  `ALLOWLIST` is EMPTY and must stay empty: an entry is an amendment to D-58-01 (LOCKED), which
  requires a demonstrated usability failure of the monochrome treatment — not a preference.

§F — The harness, as 61-01 left it: `cd apps/web && npm run screenshot:review` — surface × viewport
     × **theme**, settling before the shutter, into a gitignored `.planning/ui-reviews/{timestamp}/`.
     `npm run test:geometry` is the rendered-geometry gate. **NEVER a bare `npx playwright test`.**

§G — 60-07's hard-won lessons about this exact leg. Do not re-learn them:
  - **The harness photographs a crash and still reports `1 passed`.** It is a camera, not a gate.
    **Prove the stylesheet loaded and the app hydrated before reviewing a single frame** — 60-07
    reviewed 14 plausible-looking PNGs of an app that had never executed client JS, and a reviewer
    skimming them would have reported a catastrophic regression that did not exist.
  - Tell-tale of a corrupted `.next`: production artifacts (`BUILD_ID`, `prerender-manifest.json`,
    `server/pages/_document.js` — a Pages-Router file in an App-Router app) beside
    `.next/static/development`. Recovery: stop the server, `rm -rf apps/web/.next`, restart
    `npm run web:dev`. (`build:local` is safe since `7df5ad2` — it targets `.next-verify`.)
  - Byte-identical PNGs across a warm re-run is a DIAGNOSTIC, not a curiosity: a cold-compile race
    jitters, a broken build is deterministic.
  - **999.25 is live: the fixture seeds zero entities/extractions, so pencil-amber `--sugg` has
    never rendered anywhere.** If `/chat` shows no suggested tier, that is the fixture, not the
    redesign. Do not report it as a design gap and do not fix it here.
  - The "N" badge over "Sign out" is the Next.js dev indicator. Not a bug.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript toolbar -> `chat.saveCanvasLayout` | a new UI entry point onto the write path 61-07 opened |
| capture artifacts -> repo | PNGs of a signed-in session, now doubled by the theme axis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-25 | Denial of Service (data loss) | the transcript toolbar | mitigate | Every control writes through `usePanelOverlay` -> `scheduleSave`, i.e. straight into T-61-21's blast radius. This plan adds the first REAL user-facing caller of that path. Re-run 61-07's layout round-trip regression test as part of this plan's gate — do not assume it still holds because it held last wave. |
| T-61-26 | Elevation of Privilege | `RethemeControl` from the transcript | accept | Re-theme calls Bedrock through the existing `RethemeResolutionSchema` web-boundary gate (GEN-03/D-08 — the authoritative allowlist). Mounting the same control in a different tree adds a caller, not a capability, and crosses no new boundary. No server change. |
| T-61-27 | Information disclosure | `.planning/ui-reviews/**` | mitigate | The theme axis doubles the frames; handling is unchanged. Never `git add -f`, never commit a PNG, never paste a rendered forwarding address / access token / signed URL into the review. `git check-ignore` before claiming the artifact is safe. |
| T-61-28 | Repudiation | `61-SCREENSHOT-REVIEW.md` | mitigate | 60-07's standard, and it is this project's most valuable precedent: split the verdict at exactly the line the evidence supports. Never describe a capture that did not run; never substitute a hand-made image; name an unproven thing UNPROVEN rather than waving it through. A camera reporting `1 passed` is not evidence. |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: The toolbar reaches the phone — criterion 3</name>

  <read_first>
    - apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx (the component you MOUNT — full; §A)
    - apps/web/src/app/chat/_canvas/genui-panel-node.tsx lines 115-170 (the canonical prop derivation; §B)
    - apps/web/src/app/chat/_canvas/transcript-panel-host.tsx + message-turn.tsx (61-07's seam + its per-part child)
    - .planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-07-SUMMARY.md (TranscriptPanelHost's exact props + the T-61-21 mechanism)
    - apps/web/src/app/chat/_canvas/node-data-schemas.ts (the `Provenance` type)
  </read_first>

  <files>
    apps/web/src/app/chat/_canvas/transcript-panel-host.tsx,
    apps/web/src/app/chat/_components/message-turn.tsx,
    apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx
  </files>

  <action>
    Mount the EXISTING toolbar into the docked transcript. **Build no new control.** All four already
    exist, are tested, and carry a mutual-exclusion lock; the only thing that has ever been wrong is
    where they live.

    THE DISCRIMINATOR (§D) — this is the whole design decision. Store presence does NOT separate the
    docked transcript from the canvas's own ChatNode transcript: both have the providers. Have
    `TranscriptPanelHost` expose a marker its descendants can read ("you are the docked transcript
    host"), and mount the toolbar on that marker. Tree 2 (a ChatNode on the canvas) has no marker,
    so it renders no toolbar — the real `GenuiPanelNode` beside it already has one, and two toolbars
    editing one overlay on one board is a bug. Tree 3 (bare) has no marker either.

    **Do NOT gate on `useIsMobileViewport()`** (§D). Criterion 3 says the user "can reach" the
    controls on a mobile viewport, not "only on mobile"; the marker satisfies mobile AND hands the
    desktop docked view the same editing at zero cost. It also keeps a JS mount decision out of a
    place CSS already handles.

    Derive the props exactly as `GenuiPanelNode` does (§B/§C): `panelId` from
    `genuiPanelNodeId(messageId, partIndex)` (61-07 already computes it), `provenance` as
    `{ messageId, partIndex, runId: null }` — the identical shape `reconcileNodesFromHistory` puts
    in the node's data, so both surfaces address the same panel by construction — `activeSpecJson`
    and `resolvedPackId` from the `resolveActivePanel` result 61-07 already has, and the real
    `isStreaming`. **Honour `!isInteractiveWidget`** (§B): an interactive_widget panel has no
    toolbar on the canvas and gets none here.

    The controls keep writing through the THROWING `usePanelOverlay` (§A) — inside the host the
    providers exist, so they work unmodified. Do not swap them to 61-07's optional read; that read
    is for the non-throwing display path, and a write with no persistence wired IS a wiring bug that
    should throw.

    TOUCH TARGETS. The toolbar already carries `pointer-coarse:h-11` (§A) — the 44px floor
    (D-48-07, WCAG 2.5.8) — but it was written for a toolbar that had never rendered on a phone. Now
    it will. Check the four icon buttons AND the pack `Select` against the floor at 390px, and check
    that `PackSwitcher`'s dropdown and `EditParamsControl`'s dialog are actually usable there rather
    than merely present. **Reachable means operable.** Criterion 3 is a mobile-reachability claim,
    not a media query — 61-CONTEXT says so explicitly. If a control is technically mounted but
    unusable at 390px, criterion 3 is not met and the honest move is to say so.

    Restyle only what mounting demands. 61-06 already put the toolbar on the identity; if it did not
    reach it, bring it onto the card language now — but do not redesign the controls.

    PRESERVE: the `busyAction` mutual-exclusion lock and the `isStreaming` force-lock, every
    control's props shape, `role="toolbar"` + `aria-label="Panel actions"`, and the
    `onGeneratingChange` -> `GeneratingRing` signal (52-UI-SPEC Judgment Call #5 — the ring is never
    the SOLE signal; each busy control keeps its own aria-label/spinner as the independent
    accessible one).
  </action>

  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vitest run src/app/chat/ && grep -q "PanelActionsToolbar" src/app/chat/_components/message-turn.tsx && test $(grep -v '^\s*\*' src/app/chat/_components/message-turn.tsx | grep -v '^\s*//' | grep -c "useIsMobileViewport") -eq 0 && grep -q "isInteractiveWidget\|interactive_widget" src/app/chat/_components/message-turn.tsx</automated>
  </verify>

  <done>
    The four existing controls mount in the docked transcript, gated on `TranscriptPanelHost`'s
    marker — not on store presence, not on a viewport check. The canvas's ChatNode transcript renders
    no toolbar. interactive_widget panels render none either. Props are derived identically to
    `GenuiPanelNode`, including `runId: null`. Touch targets and the pack dropdown / params dialog
    were checked as OPERABLE at 390px and the finding is recorded. The lock, the aria contract and
    the generating signal all survive. Zero new controls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Advance the ratchet — chat/ joins the ban, permanently</name>

  <read_first>
    - apps/web/src/app/__tests__/role-hue-ban.test.ts (full — the header, the ratchet, both traps; §E)
    - .planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-06-SUMMARY.md (its confirmation that chat/_canvas/ is clear)
    - docs/design/brand-guide.md lines 309-335 ("The madder rule in practice" — the proxy's blind spot)
  </read_first>

  <files>apps/web/src/app/__tests__/role-hue-ban.test.ts</files>

  <action>
    Advance `SCOPED_DIRS` over this phase's surface roots (§E). This is the mechanism that makes
    61-06's sweep permanent instead of temporary: without it, the next phase to touch these files
    reintroduces a role hue in an afternoon and nothing objects.

    ADD `chat` (covering `_components/` AND `_canvas/`) and `_vocabulary` (Plan 61-02's new shared
    module) to `SCOPED_DIRS`. **Append. Never remove a root** — the scope only ever grows, and
    `SCOPED_DIRS` is exported and pinned precisely so narrowing it breaks a test instead of passing
    as a one-word diff nobody reviews.

    EXTEND the two pinning tests rather than replacing them:
      - the "covers BOTH Phase-60 roots" test keeps asserting `_components` and `emails/[id]` and
        now also asserts this phase's roots. Rename it honestly (it is no longer about Phase 60).
      - the vacuity test ("every scoped root exists and actually yields files") already loops
        `SCOPED_DIRS`, so it covers the new roots for free — **verify that it does** rather than
        assuming. A root that walks to zero files polices nothing, and a gate that inspects nothing
        passes everything.

    UPDATE THE HEADER PROSE. It currently says the banned tokens "are still legitimately in use on
    the canvas and knowledge surfaces (`knowledge/`, `entities/`, `chat/`), which Phases 61-63 own
    and have not swept yet." **`chat/` has now been swept (61-06) and that sentence becomes false the
    moment you edit the array.** A gate whose own documentation lies about its scope is how the next
    reader concludes the ban does not apply to them. Leave `knowledge/` and `entities/` named as
    unswept — they are Phase 62's, and the ban being scoped rather than global is the whole reason it
    is not red on arrival.

    **TRAP 1 (§E): this gate reads LINES, not prose.** You are ADDING `chat` to the walk, so every
    file under `chat/` — including its `__tests__/` — is now scanned. Two consequences:
      - Any comment under `chat/` naming a retired token turns the gate red on its own
        documentation. 61-06 was told to strip those; **verify it rather than trusting it** (60-07
        was ordered to fix an already-fixed defect and burned a leg proving it).
      - Plans 61-02/61-06's sibling gates legitimately assert on the banned family BY NAME. They
        survive only because `ROLE_HUE_PATTERN` requires a colour-utility PREFIX (trap 2). **Confirm
        those suites still pass after the scope widens** — if one goes red, the fix is in the sibling
        (construct the pattern from parts), never in widening the allowlist.

    `ALLOWLIST` STAYS EMPTY. An entry is a claim that a role reads better as a hue than as a shape,
    or that a state has earned the irreversible colour — both are amendments to D-58-01 (LOCKED),
    which requires a demonstrated usability failure of the monochrome treatment, not a preference.
    Take it to the identity record, not to this array.

    **THEN DO WHAT THE GATE CANNOT: READ.** The madder rule's gate is a PROXY — `variant="destructive"`
    passes it by design, so a status badge can wear the irreversible colour and still be green. 60-06
    found exactly one (`<Badge variant="destructive">Preview failed</Badge>` in `pdf-preview-pane`)
    by reading, not by grep. Grep `chat/` for `variant="destructive"` and `bg-destructive` and read
    each hit: is it an IRREVERSIBLE ACTION, or a state talking? Report every occurrence and your
    judgement — **"checked, all N are genuine destructive controls" is a finding**, and it is the
    only form of evidence that exists for the half of law 1 no test can see.
  </action>

  <verify>
    <automated>cd apps/web && npx vitest run src/app/__tests__/role-hue-ban.test.ts && npx vitest run && node -e "const s=require('fs').readFileSync('src/app/__tests__/role-hue-ban.test.ts','utf8');if(!/SCOPED_DIRS[^=]*=[^;]*\"chat\"/s.test(s)){console.error('chat not in SCOPED_DIRS');process.exit(1)}if(!/\"_components\"/.test(s)||!/\"emails\/\[id\]\"/.test(s)){console.error('a Phase-60 root was removed — the ratchet only grows');process.exit(1)}"</automated>
  </verify>

  <done>
    `SCOPED_DIRS` contains `_components`, `emails/[id]`, `chat` and `_vocabulary`; no root was
    removed. The pinning test asserts all four and is honestly renamed; the vacuity test was verified
    to cover the new roots. The header no longer claims `chat/` is unswept, and still names
    `knowledge/`/`entities/` as Phase 62's. Every sibling gate under the widened scope still passes
    with no allowlist entry. `ALLOWLIST` is still empty. The madder read over `chat/` was performed
    and every `variant="destructive"`/`bg-destructive` occurrence is reported with a judgement.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Look at it — both themes, real pixels, honest verdict</name>

  <what-built>
    Plans 61-01..61-08 redesigned `/chat` and its canvas on D-58-01 and closed 999.17:
    - a rendered-geometry gate + a screenshot harness that finally has a theme axis (61-01)
    - one tier/kind vocabulary shared across three surfaces (61-02)
    - the frame, rail and composer (61-03); the message stream, tool rounds and citation chip (61-04)
    - the React Flow chrome incl. the never-styled handles, + a gate that parses the library's own
      stylesheet (61-05); every node shell and the edge (61-06)
    - the transcript panel host + overlays crossing to the transcript (61-07)
    - the editing toolbar on mobile + the role-hue ratchet over `chat/` (61-08 Tasks 1-2)

    Before the checkpoint, run the capture and write the review yourself:

    1. **Prove the app is alive before believing a single frame** (§G — this is 60-07's whole
       lesson, and it cost a full verification leg). Check that `apps/web/.next` holds no production
       artifacts beside `static/development`; `curl` the `layout.css` the page actually links and
       confirm a 200 with real content (a 404 renders browser defaults that look like a
       catastrophic regression that is not there). If it is corrupted: stop the dev server,
       `rm -rf apps/web/.next`, restart `npm run web:dev`.
    2. `cd apps/web && npm run test:geometry` — green before you photograph anything. A frame of a
       collapsed height chain is a picture of a bug.
    3. `cd apps/web && npm run screenshot:review` — surface × viewport × **theme** (61-01).
       **NEVER a bare `npx playwright test`** (§F).
    4. Confirm hydration in the frames: skeletons everywhere means the app never mounted and the
       run is worthless. Byte-identical PNGs across a warm re-run is a DIAGNOSTIC of a deterministic
       broken state, not a curiosity (§G).
    5. Write `artifacts/61-SCREENSHOT-REVIEW.md` to **60-07's standard** (T-61-28): split the verdict
       at exactly the line the evidence supports. PROVEN / UNPROVEN per claim. Never describe a
       capture that did not run. An unproven thing is named UNPROVEN, not waved through.

    Judge against the four criteria and the three laws:
      - **Criterion 1** — composer, message stream, tool-round rows, genui panel chrome: is the
        layout and hierarchy visibly distinct from the pre-59 version, or only recoloured? Phase 51
        already re-tokened this surface and the user's verdict was still "ugly/experimental" — that
        is this phase's failure mode, and it looks like green tests.
      - **Criterion 2** — canvas chrome end-to-end, zero stock React Flow. The gate proves the
        stylesheet is overridden; only your eyes prove it looks like polytoken. **Look at a handle.**
      - **Law 1** — is there hue anywhere on chrome? **Law 2** — does anything polytoken says wear
        the serif? Does an email subject NOT wear it? **Law 3** — is any node kind separated by hue?
      - **Dark mode** — being seen for the first time ever. The identity promises hue and chroma hold
        across themes with only lightness moving. Did that survive contact with a real surface?
      - **999.25 is live** (§G): the fixture seeds zero entities/extractions, so pencil-amber
        `--sugg` has never rendered anywhere. If `/chat` shows no suggested tier, that is the
        fixture, not the redesign. Do not report it as a design gap; do not fix it here.
  </what-built>

  <how-to-verify>
    1. Open http://localhost:3000/chat on a desktop viewport. Does it look designed, or does it
       look like shadcn with different colours?
    2. Toggle to Canvas. Look at the board (dot grid), the controls, and **a node's connection
       handles** — those were stock React Flow navy dots until this phase.
    3. Switch the app to dark mode. Is long text comfortable on the graphite ground? Does anything
       that was legible in light mode disappear?
    4. Narrow the window to ~390px (or use device emulation). On a genui panel in the transcript:
       **can you switch its pack, edit its params, regenerate it, and re-theme it?** That is
       criterion 3, and "reachable" means you can actually operate it with a thumb — not that the
       button exists.
    5. Re-theme a panel on the Canvas, then switch to Chat. **Does the transcript show the new
       theme?** That is criterion 4 / 999.17, unfixed since Phase 52.
    6. Read `artifacts/61-SCREENSHOT-REVIEW.md` against the frames in
       `.planning/ui-reviews/{timestamp}/`. Does its verdict match what you see?
  </how-to-verify>

  <resume-signal>
    Type "approved" to close Phase 61, or describe what looks wrong — layout, hierarchy, density, a
    law violation, or a control you could not operate on the phone. Specific beats polite: a
    "looks fine" here is what produced 999.18 three milestones running.
  </resume-signal>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` clean; `npx vitest run` fully green — every gate this phase
  built plus Phase 60's, plus the 44 pre-existing chat suites.
- `cd apps/web && npm run test:geometry` green at 390 and 1440.
- **Re-run 61-07's layout round-trip regression test** (T-61-25) — this plan adds the first real
  user-facing caller of the write path it protects. Do not assume it still holds.
- `cd apps/web && npm run screenshot:review` — both themes captured, hydration confirmed BEFORE any
  frame is reviewed (§G). NEVER a bare `npx playwright test`.
- `cd apps/web && npm run build:local` (safe since `7df5ad2`). NEVER `npm run build -w @polytoken/web`.
- `git check-ignore` confirms no PNG is committed (T-61-27).
- No server change: `git diff --name-only` touches nothing under `packages/api-client`.
</verification>

<success_criteria>
- On a 390px viewport a user can switch a panel's pack, edit its params, regenerate it and re-theme
  it — operably, not just structurally. 999.17's write half, closed.
- The canvas's own ChatNode transcript sprouts no second toolbar.
- `chat/` is inside the role-hue ban permanently, the ratchet only grew, the allowlist is still
  empty, and the half of law 1 no gate can see was checked by reading.
- Both themes of the redesigned surface have been looked at by a human, and the review says PROVEN
  or UNPROVEN per claim rather than a single comfortable word.
</success_criteria>

<output>
Create `.planning/phases/61-surface-redesign-chat-canvas-mobile-panel-chrome/61-08-SUMMARY.md` when
done, plus `artifacts/61-SCREENSHOT-REVIEW.md`.

Then update the inherited docs the way 60-07 did — Phases 62-63 read these, not these plans:
- `docs/design/brand-guide.md` §3 "Realized surface patterns" gains Phase 61's: the canvas card
  language, tier-on-edges (and why a data wire is neutral), the chat-title-sans vs
  thread-subject-serif pair as law 2's clearest worked example, and the `TranscriptPanelHost` seam.
- `.claude/skills/polytoken-design-system/SKILL.md`: the harness now HAS a theme axis (its current
  text says dark mode has never been captured — that becomes false here), `test:geometry` exists and
  what it catches, and the ratchet now covers `chat/`.
Record any deviation from this plan the way 60-07 did — as a numbered finding with its rule.
</output>

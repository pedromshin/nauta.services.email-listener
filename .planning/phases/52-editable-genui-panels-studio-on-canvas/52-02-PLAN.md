---
phase: 52-editable-genui-panels-studio-on-canvas
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx
  - apps/web/src/app/chat/_canvas/controls/pack-switcher.tsx
  - apps/web/src/app/chat/_canvas/controls/edit-params-control.tsx
  - apps/web/src/app/chat/_canvas/controls/regenerate-control.tsx
  - apps/web/src/app/chat/_canvas/controls/retheme-control.tsx
  - apps/web/src/app/chat/_canvas/controls/version-history-control.tsx
  - apps/web/src/app/chat/_canvas/genui-panel-node.tsx
  - apps/web/src/app/chat/_canvas/__tests__/pack-switcher.test.tsx
  - apps/web/src/app/chat/_canvas/__tests__/genui-panel-node-toolbar.test.tsx
autonomous: true
requirements: [PANL-01]
must_haves:
  truths:
    - "A genui panel shows a second, non-drag toolbar row: a pack-switcher Select on the left and four action icon-buttons (edit, regenerate, re-theme, history) on the right, each with the UI-SPEC tooltip copy"
    - "Switching the pack in the Select applies immediately (optimistic), persists, and re-themes the rendered spec in place; a failed persist reverts the Select and shows an error toast"
    - "The pack choice rehydrates on reload (the panel renders in the last-chosen pack)"
    - "The panel's rendered content is resolved from its overlay (active version if any, else the base spec) and themed by the resolved pack"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx"
      provides: "PanelActionsToolbar — the h-8 toolbar row composing pack-switcher + 4 controls, owning the mutual-exclusion lock + generating signal"
      contains: "role=\"toolbar\""
    - path: "apps/web/src/app/chat/_canvas/controls/pack-switcher.tsx"
      provides: "PackSwitcher — PANL-01 Select with optimistic apply / revert-on-error / persist"
      contains: "onValueChange"
    - path: "apps/web/src/app/chat/_canvas/genui-panel-node.tsx"
      provides: "Panel node extended with toolbar mount + PanelThemeScope wrap + overlay-resolved active spec + min-h-[272px] + GeneratingRing shell"
      contains: "PanelActionsToolbar"
  key_links:
    - from: "controls/pack-switcher.tsx"
      to: "usePanelOverlay(panelId).writeOverlay + setPack"
      via: "onValueChange → optimistic local set → writeOverlay(setPack(overlay, id)); revert on scheduleSave failure surrogate"
      pattern: "setPack\\("
    - from: "genui-panel-node.tsx"
      to: "resolveActivePanel + PanelThemeScope"
      via: "resolve active spec/pack from overlay, wrap ScrollArea body in PanelThemeScope"
      pattern: "resolveActivePanel"
---

<objective>
Add the **panel toolbar chrome** (52-UI-SPEC Component 1) to `GenuiPanelNode` and deliver **PANL-01
end-to-end**: a per-panel style-pack switcher that applies immediately, persists, re-themes in place,
and rehydrates on reload. Create the four action-control component files as **interface-first
skeletons** (correct icon + tooltip + disabled) so Plans 03/04/06 can each implement one control
without ever re-touching the toolbar or the panel node.

Purpose: the toolbar is the single mount point every PANL-01..04 entry point shares; wiring it once
(with all plumbing: mutual-exclusion lock, generating signal, theme resolution) unblocks the feature
plans to run in parallel against a stable control-prop contract.
Output: `panel-actions-toolbar.tsx`, `controls/*.tsx` (pack-switcher implemented; edit/regenerate/
retheme/history skeletons), extended `genui-panel-node.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-editable-genui-panels-studio-on-canvas/52-CONTEXT.md
@.planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md
@apps/web/src/app/chat/_canvas/genui-panel-node.tsx
@apps/web/src/app/chat/_canvas/panel-overlay.ts
@apps/web/src/app/chat/_canvas/panel-overlay-context.tsx
@apps/web/src/app/chat/_canvas/panel-theme-scope.tsx

<interfaces>
Plan 52-01 exports (import verbatim):
- panel-overlay.ts: `resolveActivePanel(overlay, baseSpecJson, baseIsStreaming) → ResolvedPanel`,
  `setPack(overlay, packId)`, `PanelOverlay`.
- panel-overlay-context.tsx: `usePanelOverlay(panelId) → { overlay, writeOverlay }`,
  `useCanvasPersistenceContext() → { scheduleSave, conversationId }`, type `PanelActionControlProps`
  `{ panelId, provenance, activeSpecJson, resolvedPackId, isLocked, onBusyChange, onGeneratingChange }`,
  type `PanelActionId`.
- panel-theme-scope.tsx: `PanelThemeScope({ packId, tokenOverrides?, children })`.

Style pack registry: `STYLE_PACKS[id].label`, `STYLE_PACK_IDS`, `DEFAULT_PACK_ID` from `@polytoken/genui/theme`.

UI primitives (all exist in @polytoken/ui): `select` (Select/SelectTrigger/SelectContent/SelectItem/SelectValue),
`tooltip` (Tooltip/TooltipTrigger/TooltipContent/TooltipProvider), `button`, `scroll-area`. Icons:
`SlidersHorizontal, RotateCw, Wand2, History, PanelsTopLeft` from lucide-react. Toast: `import { toast } from "sonner"`.
`GeneratingRing` from `~/components/generating-ring` (props `{ active, className, children }`, needs own rounding).

52-UI-SPEC BINDING classes/copy: Component 1 toolbar row + shared icon-button class (lines 116-152);
tooltip copy table (§Copywriting); Interactive-State row for "Switch pack" (optimistic / revert +
`toast.error("Couldn't switch style — try again.", { action: { label: "Retry", onClick } })` / disabled+aria-busy).
26-UI-SPEC FIX-04 invariant: the existing `h-9 .node-drag-handle` row is the ONLY drag handle — the new
`h-8` toolbar row is NOT part of it. Judgment Call #2: outer shell `min-h-[240px]` → `min-h-[272px]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolbar shell + control-prop contract + four interface-first control skeletons</name>
  <read_first>
    - .planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md (Component 1 markup lines 116-152; §Copywriting tooltip rows; §Interactive-State "Empty/disabled edge case")
    - apps/web/src/app/chat/_canvas/knowledge-preview-node.tsx (existing size-6 icon-button + remove-button recipe to mirror)
    - apps/web/src/app/chat/_canvas/add-knowledge-preview-popover.tsx (Tooltip+Popover composition precedent)
    - apps/web/src/app/chat/_canvas/panel-overlay-context.tsx (PanelActionControlProps, PanelActionId)
  </read_first>
  <action>
    Create `panel-actions-toolbar.tsx` ("use client"): render the exact `role="toolbar"
    aria-label="Panel actions"` `h-8` row from the UI-SPEC (border-b border-border/60 bg-background
    px-2). Left slot = `<PackSwitcher {...} />`. Right slot = one shared `TooltipProvider delayDuration={300}`
    wrapping the four controls `<EditParamsControl/> <RegenerateControl/> <RethemeControl/> <VersionHistoryControl/>`.
    Own the mutual-exclusion state here: `const [busyAction, setBusyAction] = useState<PanelActionId|null>(null)`
    and `const [generating, setGenerating] = useState(false)`; pass each control
    `isLocked={busyAction !== null && busyAction !== thisId}`,
    `onBusyChange={(b) => setBusyAction(b ? thisId : null)}`,
    `onGeneratingChange={setGenerating}`. Expose `generating` upward via an
    `onGeneratingChange?: (on: boolean) => void` prop the panel node passes in (so the node can drive
    its GeneratingRing shell). PackSwitcher gets `isLocked` (disabled while any other action pending)
    + `onBusyChange`.
    Create `controls/edit-params-control.tsx`, `controls/regenerate-control.tsx`,
    `controls/retheme-control.tsx`, `controls/version-history-control.tsx` as INTERFACE-FIRST
    skeletons: each is a named export implementing `PanelActionControlProps`, rendering the shared
    size-6 icon-button (its lucide icon: SlidersHorizontal / RotateCw / Wand2 / History) wrapped in
    Tooltip with the exact UI-SPEC tooltip copy ("Edit parameters" / "Regenerate" / "Re-theme" /
    "Version history"), `disabled` (skeleton is inert), `aria-label` matching the tooltip. Add a
    header comment on each: "Interface-first skeleton — full implementation lands in Plan 52-03 / 52-04
    / 52-06 (this phase). Do not defer." These skeletons compile and render but perform no mutation yet.
  </action>
  <verify>
    <automated>npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "role=\"toolbar\"" apps/web/src/app/chat/_canvas/panel-actions-toolbar.tsx` = 1.
    - All four `controls/*.tsx` export a component implementing `PanelActionControlProps` and render
      a Tooltip with the exact copy string (grep each file for its tooltip literal).
    - `npm run typecheck -w @polytoken/web` clean.
    - `palette-ban.test.ts` green (only token classes used).
  </acceptance_criteria>
  <done>Toolbar composes pack-switcher + four skeleton controls; prop contract is stable for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PackSwitcher — PANL-01 optimistic apply / revert-on-error / persist (TDD)</name>
  <read_first>
    - .planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md (Component 1 pack-switcher markup + "Pack-switcher persistence" + Interactive-State "Switch pack" row)
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (plain Select + STYLE_PACKS[id].label precedent — Judgment Call #3)
    - apps/web/src/app/chat/_canvas/panel-overlay.ts (setPack)
    - apps/web/src/app/chat/_canvas/panel-overlay-context.tsx (usePanelOverlay)
  </read_first>
  <behavior>
    - PackSwitcher shows the resolved current pack (resolvedPackId prop) as the Select value.
    - On change: optimistically set a local pending value, call writeOverlay(setPack(overlay, id))
      (which persists via scheduleSave). No confirmation step.
    - On persist failure (scheduleSave surfaced an error): revert the local value to the prior pack and
      fire `toast.error("Couldn't switch style — try again.", { action: { label: "Retry", onClick } })`.
      (Persistence is fire-and-forget/debounced; model the failure path via an injected/spyable persist
      result so it is unit-testable without the live save.)
    - Disabled + aria-busy while isLocked or its own switch is pending.
  </behavior>
  <action>
    Implement `controls/pack-switcher.tsx` per the UI-SPEC compact Select recipe (h-6 w-28 border-none
    bg-transparent … hover:bg-accent, focus-visible ring). Map `STYLE_PACK_IDS` → `SelectItem`
    value=id label=`STYLE_PACKS[id].label`. Value = local optimistic state seeded from `resolvedPackId`.
    onValueChange: set local, `writeOverlay(setPack(overlay ?? { activeVersionId: null, versions: [] }, id))`,
    `onBusyChange(true)`, and on the persist error path revert + toast + `onBusyChange(false)`. To make
    the error path testable, expose persistence result via the context's `scheduleSave` (make it return
    void but allow the test to simulate revert by re-rendering with the prior resolvedPackId, asserting
    the Select reverts and the toast fired). Use `sonner` toast with the exact copy + Retry action shape
    from `apps/web/src/app/emails/[id]/_components/confirm-deny-controls.tsx`.
  </action>
  <verify>
    <automated>npm run test -w @polytoken/web -- pack-switcher.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/pack-switcher.test.tsx` (jsdom) asserts: selecting a new pack calls `writeOverlay` with
      an overlay whose `stylePackId` equals the chosen id AND schedules a persist; the visible Select
      value updates optimistically; simulating a persist failure reverts the value to the prior pack and
      calls `toast.error` with the exact copy + a `Retry` action; the Select is disabled when `isLocked`.
    - Green; typecheck clean.
  </acceptance_criteria>
  <done>Pack switch is optimistic, persistent, reverts on error, and matches the UI-SPEC copy verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Mount toolbar + theme wrap + overlay-resolved spec in GenuiPanelNode</name>
  <read_first>
    - apps/web/src/app/chat/_canvas/genui-panel-node.tsx (GenuiPanelNodeBody + shell — full file)
    - apps/web/src/app/chat/_canvas/panel-overlay.ts (resolveActivePanel)
    - apps/web/src/app/chat/_canvas/panel-theme-scope.tsx (PanelThemeScope)
    - apps/web/src/app/chat/_canvas/panel-overlay-context.tsx (usePanelOverlay)
    - .planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md (Judgment Calls #1/#2/#5; toolbar placement; GeneratingRing shell wrap)
  </read_first>
  <action>
    Edit `GenuiPanelNodeBody`: after the existing `useCanvasSpec(provenance)` (base spec), call
    `usePanelOverlay(panelId)` and `const resolved = resolveActivePanel(overlay, specJson, isStreaming)`.
    Feed `resolved.specJson` (NOT the raw base) to `GenuiPartBoundary` for the genui_spec branch. Wrap
    ONLY the `ScrollArea` body content in `<PanelThemeScope packId={resolved.packId} tokenOverrides={resolved.tokenOverrides}>`.
    Insert `<PanelActionsToolbar panelId={panelId} provenance={provenance} activeSpecJson={resolved.specJson}
    resolvedPackId={resolved.packId} onGeneratingChange={setGenerating}/>` as a NEW row BETWEEN the
    existing `.node-drag-handle` h-9 row and the `ScrollArea` — ONLY when `!isInteractiveWidget` (the
    interactive_widget branch keeps its current chrome, no toolbar). Do NOT modify the h-9 drag-handle
    row (26-UI-SPEC FIX-04). While `isStreaming`, the toolbar renders but PackSwitcher/actions are
    disabled (pass an `isStreaming` disable down, or rely on resolved base + a `disabled` flag).
    In `GenuiPanelNode` shell: change `min-h-[240px]` → `min-h-[272px]`; hold
    `const [generating, setGenerating] = useState(false)` and wrap the shell's inner content in
    `<GeneratingRing active={generating} className="rounded-lg">` (per Judgment Call #5; the ring is
    never the sole signal — the busy control's own aria-label/spinner is the independent signal).
    Keep the memo-split (GenuiPanelNodeBody) intact.
  </action>
  <verify>
    <automated>npm run test -w @polytoken/web -- genui-panel-node-toolbar.test.tsx --run && npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/genui-panel-node-toolbar.test.tsx` mounts a GenuiPanelNode (with CanvasStoreProvider +
      CanvasSpecProvider + CanvasPersistenceProvider stubs) and asserts: the `role="toolbar"` row renders
      for a genui_spec panel; the rendered content sits inside a `.nauta`-style themed wrapper whose
      inline `--primary` equals the pack of an overlay seeded with `stylePackId` (proves rehydrated pack
      themes the panel); an `interactive_widget` panel renders NO toolbar row.
    - `grep -n "min-h-\\[272px\\]" apps/web/src/app/chat/_canvas/genui-panel-node.tsx` matches; the
      `.node-drag-handle` h-9 row is byte-unchanged (diff shows no edit inside it).
    - `npm run typecheck -w @polytoken/web` clean; `palette-ban.test.ts` green.
  </acceptance_criteria>
  <done>Panels render the toolbar, resolve their active spec from the overlay, and theme by the resolved pack; PANL-01 works end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → pack Select → overlay write | a pack id chosen in the UI is written to the overlay + persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02-01 | Tampering | pack id value | mitigate | Select options come only from `STYLE_PACK_IDS`; `setPack`/`PanelOverlaySchema` constrain `stylePackId` to `z.enum(STYLE_PACK_IDS)`; `getStylePack` falls back safely on any unknown id (T-17-04) |
| T-52-02-02 | Denial of Service | rapid pack toggling | accept | writes coalesce through the existing ~800ms debounced `scheduleSave`; no per-keystroke server call |
| T-52-SC | Tampering | npm installs | mitigate | ZERO new dependencies (Select/Tooltip/Button/GeneratingRing/sonner all vendored) — no install, nothing to vet |
</threat_model>

<verification>
- `npm run test -w @polytoken/web -- pack-switcher.test.tsx genui-panel-node-toolbar.test.tsx --run` green.
- `npm run typecheck -w @polytoken/web` clean; committed palette/contrast/registration gates green.
- Playwright canvas render of the toolbar + live pack switch is AUTHORED-BUT-NOT-RUN (Docker down) → queue to MORNING-CHECKLIST §G.
</verification>

<success_criteria>
- Toolbar row renders with pack-switcher + four labelled action buttons; drag-handle invariant preserved.
- PANL-01: switch pack → immediate + persistent + re-themed in place + rehydrates on reload; failure reverts + toasts.
- Four control skeletons present with correct icons/tooltips/prop-contract for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/52-editable-genui-panels-studio-on-canvas/52-02-SUMMARY.md` when done.
</output>

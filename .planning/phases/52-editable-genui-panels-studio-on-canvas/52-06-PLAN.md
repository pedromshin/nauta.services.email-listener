---
phase: 52-editable-genui-panels-studio-on-canvas
plan: 06
type: execute
wave: 3
depends_on: [52-02, 52-05]
files_modified:
  - apps/web/src/app/chat/_canvas/controls/retheme-control.tsx
  - apps/web/src/app/chat/_canvas/__tests__/retheme-control.test.tsx
  - apps/web/src/app/chat/_canvas/__tests__/retheme-apply-integration.test.tsx
  - .planning/MORNING-CHECKLIST.md
autonomous: true
requirements: [PANL-04]
must_haves:
  truths:
    - "A user can type a natural-language look instruction (≤280 chars, with a live counter) on a panel and apply it"
    - "Applying calls the resolveRetheme boundary and, on success, appends a 'retheme' version carrying the resolved pack + token overrides (content unchanged), re-themes the panel in place, and shows the 'Panel re-themed' toast"
    - "An invalid/failed resolution shows the inline error banner with the instruction preserved (form stays open) — never a partial or silent apply"
    - "The resolved pack + token overrides visibly re-theme the panel through PanelThemeScope"
  artifacts:
    - path: "apps/web/src/app/chat/_canvas/controls/retheme-control.tsx"
      provides: "NL Re-theme Popover (UI-SPEC Component 5)"
      contains: "Describe a new look"
  key_links:
    - from: "controls/retheme-control.tsx"
      to: "api.genui.resolveRetheme → appendVersion(retheme)"
      via: "apply → resolveRetheme → writeOverlay(appendVersion(overlay, { generatedBy:'retheme', stylePackId, tokenOverrides, specJson: activeSpecJson, instruction }))"
      pattern: "resolveRetheme"
    - from: "resolveActivePanel(retheme version) → PanelThemeScope"
      to: "themed panel"
      via: "version.stylePackId + version.tokenOverrides flow to PanelThemeScope's inline CSS vars"
      pattern: "tokenOverrides"
---

<objective>
Deliver the **client side of PANL-04**: the NL Re-theme Popover (52-UI-SPEC Component 5) that turns a
natural-language instruction into an applied theme. It calls the `genui.resolveRetheme` boundary from
Plan 52-05, and on success appends a `retheme` version (resolved pack + token overrides, panel content
unchanged) via the Plan 52-01 overlay path — re-theming the panel in place through `PanelThemeScope`.
One-shot, no repair loop; invalid resolution shows an inline banner with the instruction preserved.

Purpose: close PANL-04 end-to-end on the client and prove the resolved pack/overrides actually re-theme
a rendered panel.
Output: `retheme-control.tsx` (replacing Plan 02's skeleton) + a phase-integration test, and the phase's
live-canvas confirmation queued to MORNING-CHECKLIST §G.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-editable-genui-panels-studio-on-canvas/52-CONTEXT.md
@.planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md
@apps/web/src/app/chat/_canvas/panel-overlay.ts
@apps/web/src/app/chat/_canvas/panel-theme-scope.tsx

<interfaces>
Plan 52-05: `api.genui.resolveRetheme.useMutation()` (or `.useQuery(..., {enabled:false})` + refetch)
→ `{ ok: true, stylePackId, tokenOverrides } | { ok: false, reason? }`. Input
`{ instruction: string(1..280), currentStylePackId?: StylePackId }`. FastAPI is DOWN this session — the
real call is not exercised tonight (mock the procedure); the real end-to-end goes to MORNING §G.

Plan 52-01/52-02: `appendVersion(overlay, { generatedBy:"retheme", specJson, stylePackId, tokenOverrides,
instruction, parentVersionId })`, `resolveActivePanel`, `usePanelOverlay(panelId) → { overlay, writeOverlay }`,
`PanelThemeScope`, `PanelActionControlProps` (`{ panelId, provenance, activeSpecJson, resolvedPackId,
isLocked, onBusyChange, onGeneratingChange }`).

UI primitives: `popover`, `textarea`, `button`; `GeneratingRing`; icons `Wand2, Loader2` (lucide);
`toast` (sonner). 52-UI-SPEC BINDING: Component 5 markup (lines 285-306) — popover `w-72 space-y-3`,
heading "Describe a new look", placeholder `e.g. "Make it feel more playful and colorful"`, 280-char
cap + `{n}/280` counter, CTA "Apply look"/"Re-theming…" + secondary "Discard", inline error copy
"Couldn't apply that look — try describing it differently.", success toast "Panel re-themed",
Interactive-State "Apply re-theme" row (Textarea readOnly + GeneratingRing while pending).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NL Re-theme Popover (retheme-control.tsx)</name>
  <read_first>
    - apps/web/src/app/chat/_canvas/controls/retheme-control.tsx (Plan 02 skeleton to replace)
    - .planning/phases/52-editable-genui-panels-studio-on-canvas/52-UI-SPEC.md (Component 5 markup lines 285-306; Judgment Call #6 280-cap; Copywriting re-theme rows; Interactive-State "Apply re-theme")
    - apps/web/src/app/chat/_canvas/panel-overlay.ts (appendVersion)
    - apps/web/src/app/studio/_components/generation-sandbox-island.tsx (query refetch idiom, GeneratingRing usage precedent)
  </read_first>
  <behavior>
    - Popover trigger = Wand2 icon-button (tooltip "Re-theme"), align="end". Content per UI-SPEC
      Component 5: heading "Describe a new look"; Textarea rows={3} maxLength={280} + right-aligned
      `{instruction.length}/280` counter; "Apply look" primary (disabled when empty/pending) + "Discard"
      ghost.
    - Apply: set busy (onBusyChange(true) + onGeneratingChange(true)); Textarea readOnly; button shows
      Loader2 + "Re-theming…". Call `api.genui.resolveRetheme` with `{ instruction, currentStylePackId:
      resolvedPackId }`.
    - On `{ ok:true }`: `writeOverlay(appendVersion(overlay, { generatedBy:"retheme", specJson:
      activeSpecJson, stylePackId: res.stylePackId, tokenOverrides: res.tokenOverrides, instruction,
      parentVersionId: overlay?.activeVersionId ?? null }))`; close popover; `toast.success("Panel
      re-themed")`; clear busy/generating.
    - On `{ ok:false }` (or mutation error): render the inline error banner (copy "Couldn't apply that
      look — try describing it differently."); KEEP the popover open with the instruction text intact
      (no partial/silent apply); clear busy/generating.
    - Disabled while `isLocked`.
  </behavior>
  <action>
    Replace the skeleton with the full control per <behavior> and the UI-SPEC Component 5 markup/classes.
    ZERO raw palette classes / hex — destructive banner uses the `border-destructive/30 bg-destructive/5`
    token recipe. The retheme version stores the CURRENT content spec (activeSpecJson) unchanged; only
    stylePackId + tokenOverrides differ (re-theme changes look, not content).
  </action>
  <verify>
    <automated>npm run test -w @polytoken/web -- retheme-control.test.tsx --run && npm run typecheck -w @polytoken/web</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/retheme-control.test.tsx` (jsdom, mock `api.genui.resolveRetheme`): typing >280 is
      capped and the counter reflects length; Apply with a mocked `{ ok:true, stylePackId:"playful-rounded",
      tokenOverrides:{ primary:"262 83% 58%" } }` calls `writeOverlay` with a `retheme` version carrying
      that pack + overrides + the unchanged specJson AND fires `toast.success("Panel re-themed")`; a mocked
      `{ ok:false }` renders the exact inline banner, does NOT call writeOverlay, and preserves the typed
      instruction.
    - `palette-ban.test.ts` green; typecheck clean.
  </acceptance_criteria>
  <done>PANL-04 client applies a resolved theme as a retheme version with correct one-shot / no-partial-apply behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme→theme integration proof + queue live-canvas UAT to MORNING §G</name>
  <read_first>
    - apps/web/src/app/chat/_canvas/panel-theme-scope.tsx (inline CSS-var application)
    - apps/web/src/app/chat/_canvas/panel-overlay.ts (resolveActivePanel with version stylePackId/tokenOverrides)
    - apps/web/src/app/chat/_canvas/__tests__/genui-panel-node-toolbar.test.tsx (panel-mount harness from Plan 02)
    - .planning/MORNING-CHECKLIST.md (existing §G structure — append, do not rewrite)
  </read_first>
  <action>
    Write `retheme-apply-integration.test.tsx`: seed a canvas store overlay with a `retheme` version
    (stylePackId + a `primary` token override), mount the GenuiPanelNode harness, and assert the rendered
    content's themed wrapper carries the overridden `--primary` inline CSS var (proving
    resolveActivePanel → PanelThemeScope applies pack + override). Then append ONE consolidated entry to
    `.planning/MORNING-CHECKLIST.md` §G queuing the Phase-52 live-canvas confirmation (Docker/FastAPI down
    tonight): bring up the stack, open a chat canvas with a genui panel, and manually verify all five
    actions on a REAL panel — pack switch persists across reload; param edit saves + re-renders;
    regenerate swaps content (real FastAPI); version history + restore; NL re-theme (real Bedrock) —
    plus screenshot-diff against the Phase-51 baseline. List the authored-but-unrun Playwright specs (if
    any) from this phase.
  </action>
  <verify>
    <automated>npm run test -w @polytoken/web -- retheme-apply-integration.test.tsx --run && grep -n "Phase-52" .planning/MORNING-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `retheme-apply-integration.test.tsx` green: a retheme overlay themes the panel (asserted inline
      `--primary` equals the override value, not the base pack value).
    - `.planning/MORNING-CHECKLIST.md` §G contains a Phase-52 live-canvas confirmation entry naming all
      five PANL actions + the screenshot-diff step.
  </acceptance_criteria>
  <done>The full PANL-04 client path (resolve → version → theme) is proven at the unit level; the live confirmation is honestly queued, not faked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolveRetheme result → overlay version → render | the resolved pack/overrides applied to a panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Tampering | tokenOverrides applied to CSS vars | mitigate | overrides arrive ONLY from `resolveRetheme`, already gated by `RethemeResolutionSchema` (Plan 52-05); PanelThemeScope sets `--{key}` via the React `style` object (no dangerouslySetInnerHTML), so even a hypothetical bad value is an inert CSS var, never executable |
| T-52-06-02 | Tampering | retheme version specJson | mitigate | specJson = the current activeSpecJson (already SpecRootSchema-valid); the render path re-runs safeParse (FOUND-6) regardless |
| T-52-06-03 | Spoofing | "silent"/partial apply on failure | mitigate | `{ ok:false }` never writes an overlay; the popover stays open with the instruction preserved (locked no-partial-apply) |
| T-52-SC | Tampering | npm installs | mitigate | ZERO new dependencies — no install, nothing to vet |
</threat_model>

<verification>
- `npm run test -w @polytoken/web -- retheme-control.test.tsx retheme-apply-integration.test.tsx --run` green.
- `npm run typecheck -w @polytoken/web` clean; `palette-ban.test.ts`/`token-contrast`/`token-registration` green.
- Live NL re-theme (real Bedrock + real re-theme render) is DEFERRED to MORNING-CHECKLIST §G (Docker/FastAPI down).
</verification>

<success_criteria>
- NL re-theme popover: ≤280 instruction + counter → resolveRetheme → retheme version + toast; invalid → inline banner, instruction preserved, no partial apply.
- Integration test proves the resolved pack + overrides re-theme a rendered panel.
- Phase-52 live-canvas confirmation queued to MORNING §G (all five PANL actions + screenshot-diff).
</success_criteria>

<output>
Create `.planning/phases/52-editable-genui-panels-studio-on-canvas/52-06-SUMMARY.md` when done.
</output>

---
phase: 04-email-intelligence
plan: 09
type: execute
wave: 6
depends_on: ["04-04", "04-05", "04-06"]
files_modified:
  - apps/email-listener/tests/corpus/__init__.py
  - apps/email-listener/tests/corpus/README.md
  - apps/email-listener/tests/corpus/manifest.json
  - apps/email-listener/tests/corpus/scan_noise/.gitkeep
  - apps/email-listener/tests/corpus/logistics_vocab/.gitkeep
  - apps/email-listener/tests/corpus/hard_cases/.gitkeep
  - apps/email-listener/tests/corpus/ground_truth.json
  - apps/email-listener/tests/test_corpus_pipeline.py
  - apps/email-listener/tests/corpus/forwarding_harness.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A layered corpus exists: real scan noise, logistics vocabulary, and hand-assembled hard cases with controlled ground truth (D-17)"
    - "Hard cases include multi-invoice-in-one-PDF, nested entities on one page, junk/corrupt PDF, and photo-of-screen (D-17)"
    - "The pipeline is tested against authentic messiness — corpus PDFs run through parse → propose-regions and are asserted against ground truth (D-17)"
    - "A forwarding harness can feed a corpus PDF through the decompose entry point as if forwarded (D-17)"
  artifacts:
    - path: "apps/email-listener/tests/corpus/manifest.json"
      provides: "catalog of corpus files with layer + provenance + expected entity types"
      contains: "hard_cases"
    - path: "apps/email-listener/tests/corpus/ground_truth.json"
      provides: "controlled ground truth for hard cases"
      contains: "expected"
    - path: "apps/email-listener/tests/test_corpus_pipeline.py"
      provides: "end-to-end corpus assertions over parse + propose-regions"
      contains: "ground_truth"
  key_links:
    - from: "test_corpus_pipeline"
      to: "PdfParser + ProposeRegionsUseCase"
      via: "run corpus PDFs through the real pipeline"
      pattern: "parse"
    - from: "forwarding_harness"
      to: "DecomposeEmailUseCase"
      via: "feed corpus PDF as a forwarded email"
      pattern: "decompose"
---

<objective>
Assemble the layered test corpus as a Phase 4 deliverable (D-17): (a) real scan noise (RVL-CDIP invoice subset + DocLayNet pages), (b) logistics vocabulary (LogisticQA + 1–2 format templates per type: BL, commercial invoice, packing list, booking), (c) hand-assembled hard cases with controlled ground truth (multi-invoice-in-one-PDF, nested entities on one page, junk/corrupt PDF, photo-of-screen). Build a forwarding harness and an end-to-end test that runs corpus PDFs through the real parse → propose-regions pipeline and asserts against ground truth.

Purpose: Prove the pipeline against authentic messiness before the phase is "done" — the corpus is the evidence that the depth-first PDF work actually clears the bar.
Output: corpus tree + manifest + ground truth + forwarding harness + end-to-end corpus test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-email-intelligence/04-RESEARCH.md
@.planning/phases/04-email-intelligence/04-PATTERNS.md
@.planning/phases/04-email-intelligence/04-CONTEXT.md
@.planning/phases/04-email-intelligence/04-04-SUMMARY.md
@.planning/phases/04-email-intelligence/04-05-SUMMARY.md
@.planning/phases/04-email-intelligence/04-06-SUMMARY.md

<interfaces>
PdfParser (04-04): async parse(*, file_bytes, content_type, attachment_id) -> list[Component]; handles clean/scanned/corrupt.
ProposeRegionsUseCase (04-05): execute(*, email_id, importer_id) -> list[Component]; multi/overlapping/nested/junk.
DecomposeEmailUseCase (04-06): execute(*, s3_key, meta) — the forwarding entry point (harness can substitute a local-file storage fake).
Test patterns — 04-PATTERNS.md "Adapter unit tests" (_HAS_<dep> gating, fixtures) + conftest cache-clear.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corpus tree, manifest, ground truth, forwarding harness</name>
  <files>apps/email-listener/tests/corpus/__init__.py, apps/email-listener/tests/corpus/README.md, apps/email-listener/tests/corpus/manifest.json, apps/email-listener/tests/corpus/scan_noise/.gitkeep, apps/email-listener/tests/corpus/logistics_vocab/.gitkeep, apps/email-listener/tests/corpus/hard_cases/.gitkeep, apps/email-listener/tests/corpus/ground_truth.json, apps/email-listener/tests/corpus/forwarding_harness.py</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-CONTEXT.md D-17 (the three layers + the four named hard cases) and the "chaotic single page" specific
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §3.3 (entity types the corpus should exercise) + §10 (seed entity types for expected labels)
    - .planning/phases/04-email-intelligence/04-06-SUMMARY.md (DecomposeEmailUseCase entry-point signature the harness wraps)
  </read_first>
  <action>Create tests/corpus/ with three layer subdirs: scan_noise/, logistics_vocab/, hard_cases/. Author manifest.json: an array of corpus entries each `{file, layer, provenance, content_type, expected_entity_types, notes}` where `file` is a path RELATIVE to tests/corpus/ that points at a real committed file. For scan_noise: reference RVL-CDIP invoice subset + DocLayNet pages (record download/provenance instructions in README; commit a small representative subset of GENUINE document images/PDFs, not the full datasets). For logistics_vocab: commit 1–2 hand-authored format templates per type for bill_of_lading, invoice (commercial), packing_list, booking (generate as real PDFs in the corpus, with realistic logistics vocabulary/identifiers — container numbers like MSCU1234567, BL/booking/PO numbers). For hard_cases: commit the four named cases as GENUINE files (D-17 — no empty placeholders/stubs): multi-invoice-in-one-PDF (multiple invoice entities in one file), nested-entities-on-one-page (a screenshot-of-invoice + inline BL + container-status table on one page), junk-corrupt.pdf (a real truncated/garbage PDF, non-empty), photo-of-screen.pdf (an actual photographed-screen image wrapped as an image-only PDF). Author ground_truth.json keyed by hard-case filename with `expected` = the entity types + key identifier field values that MUST be recovered (controlled ground truth). Write tests/corpus/forwarding_harness.py: `async def forward_corpus_file(path, *, importer_id, decompose_use_case, content_type) -> None` that reads the file bytes, wraps them as a forwarded-email meta, and drives DecomposeEmailUseCase using a local-file EmailStorage fake (so no real S3). Write README.md documenting the three layers, dataset provenance + licensing, how to regenerate templates, and the ground-truth schema.</action>
  <verify>
    <automated>cd apps/email-listener && python -c "import json,os; C='tests/corpus'; m=json.load(open(C+'/manifest.json')); g=json.load(open(C+'/ground_truth.json')); layers={e['layer'] for e in m}; assert {'scan_noise','logistics_vocab','hard_cases'} <= layers, layers; hc={e['file'] for e in m if e['layer']=='hard_cases'}; assert any('multi' in f for f in hc) and any('nested' in f for f in hc) and any('junk' in f or 'corrupt' in f for f in hc) and any('photo' in f for f in hc), hc; assert g, 'ground truth empty'; missing=[e['file'] for e in m if not os.path.isfile(os.path.join(C, e['file']))]; assert not missing, ('manifest files missing on disk: %s'%missing); empties=[e['file'] for e in m if os.path.getsize(os.path.join(C, e['file']))==0]; assert not empties, ('empty (stub) corpus files: %s'%empties); by_layer={}; [by_layer.setdefault(e['layer'], []).append(e['file']) for e in m]; [ (lambda L: (lambda real: (real or (_ for _ in ()).throw(AssertionError('no real file for layer %s'%L))))([f for f in by_layer.get(L, []) if os.path.getsize(os.path.join(C, f))>0]))(L) for L in ('scan_noise','logistics_vocab','hard_cases')]; print('CORPUS_MANIFEST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json contains entries for all three layers; hard_cases include multi-invoice, nested-entities, junk/corrupt, and photo-of-screen files.
    - Every `file` entry in manifest.json EXISTS on disk under tests/corpus/ (the verify fails on any missing path — an executor cannot pass with metadata only).
    - At least one real (non-zero-byte) file exists in EACH of scan_noise/, logistics_vocab/, hard_cases/; no committed corpus file is an empty placeholder (depth-first/no-stubs, D-17 — scanned/photo-of-screen hard-case PDFs are genuine, asserted non-zero size).
    - ground_truth.json provides expected entity types + key identifier values for each hard case.
    - forwarding_harness.py drives DecomposeEmailUseCase from a local file via a storage fake (no real S3).
    - README documents layers, provenance/licensing, and the ground-truth schema.
    - CORPUS_MANIFEST_OK prints.
  </acceptance_criteria>
  <done>The three-layer corpus (with genuine, non-empty files asserted on disk), manifest, controlled ground truth, and forwarding harness exist.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end corpus pipeline test against ground truth</name>
  <files>apps/email-listener/tests/test_corpus_pipeline.py</files>
  <read_first>
    - tests/corpus/manifest.json + ground_truth.json (Task 1)
    - app/infrastructure/pdf/pdf_parser.py (04-04) + app/application/use_cases/propose_regions.py (04-05) — the real pipeline under test
    - .planning/phases/04-email-intelligence/04-PATTERNS.md "Adapter unit tests" (_HAS_<dep> gating; mark LLM/OCR-dependent cases integration + skipif when keys/deps absent)
  </read_first>
  <action>Write tests/test_corpus_pipeline.py that loads manifest.json and, per corpus file, runs PdfParser.parse over the file bytes and asserts the parser-level expectations: clean text-layer files yield text-layer Components, scanned/photo files route through OCR (mark these `@pytest.mark.integration` + skipif when Textract creds absent), and junk/corrupt files yield a parse-error Component without raising. For the hard_cases, additionally run ProposeRegionsUseCase (with a fake or mocked segmenter for the deterministic unit run, and a real-LLM `@pytest.mark.integration` variant gated on ANTHROPIC_API_KEY) and assert against ground_truth.json: multi-invoice yields ≥2 invoice-typed proposed regions; nested-entities yields regions with parent_component_id linkage; junk yields zero or only 'unknown' regions. Keep the always-run portion deterministic (parser + fake segmenter) so the suite passes offline; gate anything needing live OCR/LLM behind integration markers. Ensure the deterministic portion keeps overall coverage ≥ 80%.</action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_corpus_pipeline.py -q -m "not integration" && uv run pytest -q --cov=app --cov-fail-under=80 && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - The deterministic (non-integration) corpus test runs the real PdfParser over corpus files and asserts clean/scanned/corrupt behavior per manifest.
    - Hard-case assertions against ground_truth.json hold for multi-invoice (≥2 invoice regions), nested-entities (parent linkage), and junk (zero/unknown regions) using a fake segmenter.
    - Integration-marked variants exist for live OCR/LLM but are excluded from the gating run.
    - Full suite passes with coverage ≥ 80%; import-linter clean.
  </acceptance_criteria>
  <done>The corpus runs through the real parse → propose-regions pipeline and is asserted against controlled ground truth (D-17); offline-deterministic, with integration variants gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus files → pipeline | Deliberately adversarial fixtures (junk/corrupt/photo-of-hostile-screen) exercise the parser's failure handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-31 | Denial of Service | corrupt/zip-bomb corpus fixture hangs the test run | mitigate | junk/corrupt fixtures are small and bounded; the parser (04-04) already caps pages/raster size and degrades gracefully — the corpus test asserts no-raise rather than processing unbounded input. |
| T-04-32 | Information Disclosure | committing third-party datasets with restrictive licenses | mitigate | README records provenance + licensing; only small representative subsets are committed; full datasets referenced by download instruction, not vendored. |
| T-04-33 | Tampering | a corpus photo-of-screen carries embedded injection text | mitigate | The pipeline's structural defense (D-14, 04-05/04-07) already isolates content to the user turn; the corpus validates this case rather than introducing new surface. |
</threat_model>

<verification>
- Both task verify-commands pass; deterministic corpus test green offline; coverage ≥ 80%.
- All four named hard cases are present (as genuine files on disk) and asserted (D-17 success criterion #5).
</verification>

<success_criteria>
- Layered corpus (scan noise + logistics vocab + hard cases) with controlled ground truth proves the pipeline against authentic messiness (D-17); forwarding harness exercises the real decompose entry point.
</success_criteria>

<output>
Create `.planning/phases/04-email-intelligence/04-09-SUMMARY.md` recording the corpus contents, dataset provenance/licensing, the ground-truth schema, and which cases require integration markers.
</output>
</output>

---
phase: 04-email-intelligence
plan: 08
type: execute
wave: 6
depends_on: ["04-03", "04-07"]
files_modified:
  - packages/db/src/schema/knowledge-nodes.ts
  - packages/db/src/schema/entity-instances.ts
  - packages/db/src/schema/sender-profiles.ts
  - packages/db/src/schema/component-links.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/
  - apps/email-listener/app/domain/ports/embedding_protocol.py
  - apps/email-listener/app/domain/ports/retrieval_port.py
  - apps/email-listener/app/infrastructure/llm/embedding_adapter.py
  - apps/email-listener/app/infrastructure/supabase/retrieval_repository.py
  - apps/email-listener/app/application/use_cases/confirm_region.py
  - apps/email-listener/app/application/use_cases/autofill.py
  - apps/email-listener/app/presentation/api/v1/components.py
  - apps/email-listener/app/container.py
  - apps/email-listener/tests/test_embedding_adapter.py
  - apps/email-listener/tests/test_retrieval_repository.py
  - apps/email-listener/tests/test_confirm_region.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "On human confirmation, the region + confirmed field values are embedded (halfvec) and indexed as a few-shot child (D-15)"
    - "Future autofill for a same-type region runs S4–S6 hybrid retrieval (vector + trigram identifier match, RRF k=60) over confirmed regions (D-15, RESEARCH §4)"
    - "Retrieved top-N confirmed regions are injected as few-shot examples into the autofill prompt (D-15, RESEARCH §4.3)"
    - "Child = the registered region; retrieval runs on the small child, synthesis assembles parent context (D-11, S4–S6)"
    - "Confirming never overwrites a prior confirmed value; corrections are recorded on the extraction record (D-16)"
  artifacts:
    - path: "packages/db/src/schema/knowledge-nodes.ts"
      provides: "knowledge_nodes Drizzle table with halfvec(1536) embedding + knowledge_node_scope enum"
      contains: "knowledgeNodes"
    - path: "apps/email-listener/app/infrastructure/supabase/retrieval_repository.py"
      provides: "hybrid vector + trigram retrieval with RRF merge"
      contains: "60"
    - path: "apps/email-listener/app/application/use_cases/confirm_region.py"
      provides: "ConfirmRegionUseCase: embed + index confirmed region"
      contains: "class ConfirmRegionUseCase"
  key_links:
    - from: "ConfirmRegionUseCase"
      to: "ComponentRepository.update_embedding"
      via: "embed confirmed region"
      pattern: "update_embedding"
    - from: "AutofillUseCase"
      to: "RetrievalPort.find_similar_confirmed"
      via: "few-shot retrieval when prior examples exist"
      pattern: "find_similar_confirmed"
---

<objective>
Close the learning flywheel (D-15): on human confirmation, embed the region + confirmed field values and index them; then make autofill run S4–S6 hybrid retrieval (vector cosine + pg_trgm identifier match, merged with RRF k=60 per RESEARCH §4) over confirmed regions, injecting the top-N as few-shot examples into the prompt-cache extension. This lands the retrieval schema (knowledge_nodes, entity_instances, sender_profiles, link tables) deferred from 04-01, as Drizzle TS schema + a custom SQL migration.

Purpose: Confirmed corrections become retrievable context that improves future same-type autofill — the moat. Child = region (small, for search), parent = page/attachment/email (large, for synthesis) per D-11.
Output: retrieval schema (Drizzle TS + custom SQL migration, applied to live staging), embedding adapter, retrieval repository with RRF, ConfirmRegionUseCase, autofill upgraded to use retrieval, confirm API, wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-email-intelligence/04-RESEARCH.md
@.planning/phases/04-email-intelligence/04-PATTERNS.md
@.planning/phases/04-email-intelligence/04-01-SUMMARY.md
@.planning/phases/04-email-intelligence/04-03-SUMMARY.md
@.planning/phases/04-email-intelligence/04-07-SUMMARY.md

<interfaces>
ExtractionRepository (04-03): save, find_by_component_id, supersede_active.
ComponentRepository (04-03): find_by_id, update_embedding(component_id, embedding: tuple[float,...]).
AutofillProtocol.autofill(*, region_text, entity_type, knowledge_base_text, examples) (04-07) — examples is the few-shot slot to fill from retrieval.
RRF — RESEARCH §4.1: rrf_score(rank,k=60)=1/(k+rank); merge 4 ranked lists; k=60 (S7,S8). Vector via pgvector `<=>`; identifiers via pg_trgm on identifiers::text.
Embedding adapter analog — examples/acme-boards-main/src/acme_boards/embedders/gemini.py (embed→halfvec list). RESEARCH §12 Q2 hosting options.
Drizzle schema source — RESEARCH §3.6 knowledge_nodes, §3.8 entity_instances, §3.9 link tables, §3.10 sender_profiles. Mirror the 04-01 Drizzle conventions: one file per entity under packages/db/src/schema/, re-export via the barrel src/schema/index.ts, reuse the halfvec customType from src/schema/_halfvec.ts and the extension/RLS/HNSW custom-SQL-migration patterns (see 04-01-SUMMARY for the generated/custom migration layout). migrate.ts already ensures vector/uuid-ossp/pg_trgm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieval schema — Drizzle TS (knowledge_nodes, entity_instances, sender_profiles, link tables) + custom SQL migration (RLS, HNSW, pg_trgm GIN, moddatetime)</name>
  <files>packages/db/src/schema/knowledge-nodes.ts, packages/db/src/schema/entity-instances.ts, packages/db/src/schema/sender-profiles.ts, packages/db/src/schema/component-links.ts, packages/db/src/schema/index.ts, packages/db/migrations/</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §3.6, §3.8, §3.9, §3.10 (the canonical schema — map columns 1:1 to drizzle pg-core) + §3.4 HNSW note
    - .planning/phases/04-email-intelligence/04-PATTERNS.md "Migration Patterns" (RLS deny-all, moddatetime, HNSW, statement-breakpoint)
    - .planning/phases/04-email-intelligence/04-01-SUMMARY.md (the halfvec customType helper, the barrel layout, the generated + custom migration filenames/numbering established in 04-01 — extend, do not duplicate)
    - packages/db/src/schema/_halfvec.ts (the halfvec customType to reuse for embedding columns)
    - packages/db/src/schema/components.ts (04-01 table conventions to mirror: importerId FK, halfvec embedding, $inferSelect/$inferInsert exports)
    - examples/acme-os-dev/packages/db/migrations/0079_board_deck_chunks.sql (HNSW + RLS RESTRICTIVE deny-all + DO $$ FK guard + statement-breakpoint)
  </read_first>
  <action>Create the four Drizzle schema files mirroring 04-01 conventions, each importing the halfvec customType from ./_halfvec where an embedding column is specified and re-exporting $inferSelect/$inferInsert types: knowledge-nodes.ts (`knowledgeNodes` table per §3.6, with a pgEnum `knowledgeNodeScopeEnum` "knowledge_node_scope": entity_type, entity_instance, sender, importer_global; importer_id FK; halfvec("embedding",{dimensions:1536})); entity-instances.ts (`entityInstances` per §3.8, importer_id FK, identifiers jsonb, halfvec embedding); sender-profiles.ts (`senderProfiles` per §3.10, importer_id FK); component-links.ts (`componentKnowledgeNodeLinks` + `componentEntityCandidateLinks` per §3.9, FKs to email_components/knowledge_nodes/entity_instances). Add the §3.x inline btree/gin indexes via drizzle `index(...)` where drizzle-kit can express them; the HNSW + pg_trgm-on-expression indexes go in the custom SQL migration (drizzle-kit cannot emit halfvec_cosine_ops or `gin ((identifiers::text) gin_trgm_ops)`). Update src/schema/index.ts to `export *` the four new modules. Then from packages/db run `npm run migration:generate` for the new tables/enum, and `npm run migration:generate:custom --name=retrieval_rls_indexes` for the custom DDL: ENABLE ROW LEVEL SECURITY + RESTRICTIVE deny-all (anon+authenticated) on all five tables; HNSW halfvec_cosine_ops indexes (m=16, ef_construction=64) on knowledge_nodes.embedding and entity_instances.embedding; a GIN pg_trgm index on entity_instances using `gin ((identifiers::text) gin_trgm_ops)`; and a moddatetime BEFORE UPDATE trigger on knowledge_nodes.updated_at. Use `--> statement-breakpoint` and DO $$ guards consistent with 04-01's custom migrations.</action>
  <verify>
    <automated>cd packages/db && npm run typecheck && npm run check && python -c "import glob,sys; files=glob.glob('migrations/**/*.sql',recursive=True); blob=' '.join(open(f,encoding='utf-8').read().lower().replace('\"','') for f in files); tables=['knowledge_nodes','entity_instances','sender_profiles','component_knowledge_node_links','component_entity_candidate_links']; missing=[t for t in tables if ('alter table %s enable row level security'%t) not in blob and ('alter table public.%s enable row level security'%t) not in blob]; assert not missing, ('RLS missing for: %s'%missing); assert 'gin_trgm_ops' in blob and 'using hnsw' in blob and 'halfvec_cosine_ops' in blob, 'index check failed'; print('RETRIEVAL_SCHEMA_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Four Drizzle schema files define all five tables + the knowledge_node_scope enum, re-exported via the barrel; `cd packages/db && npm run typecheck` passes.
    - `npm run check` (drizzle-kit check) passes with no drift after generate.
    - The custom SQL migration defines, BY TABLE NAME, RESTRICTIVE deny-all RLS on each of knowledge_nodes, entity_instances, sender_profiles, component_knowledge_node_links, component_entity_candidate_links (a 5-but-wrong-tables drift is caught — the verify fails if any named table lacks its own ENABLE ROW LEVEL SECURITY), HNSW halfvec_cosine_ops on the two embedding columns, a pg_trgm GIN index on entity_instances identifiers, and the moddatetime trigger on knowledge_nodes.
    - RETRIEVAL_SCHEMA_OK prints.
  </acceptance_criteria>
  <done>Retrieval schema exists as Drizzle TS + a custom SQL migration with per-table RLS (asserted by name), HNSW halfvec, and trigram indexes; typecheck + drizzle-kit check pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embedding adapter + hybrid retrieval repository (RRF k=60)</name>
  <files>apps/email-listener/app/domain/ports/embedding_protocol.py, apps/email-listener/app/domain/ports/retrieval_port.py, apps/email-listener/app/infrastructure/llm/embedding_adapter.py, apps/email-listener/app/infrastructure/supabase/retrieval_repository.py, apps/email-listener/tests/test_embedding_adapter.py, apps/email-listener/tests/test_retrieval_repository.py</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §4.1 (four-way search + rrf_score formula k=60) + §4.3 (few-shot extension) + §12 Q2 (embedding hosting)
    - .planning/phases/04-email-intelligence/04-PATTERNS.md "embedding_adapter.py" entry + "No Analog → retrieval_adapter.py" note
    - examples/acme-boards-main/src/acme_boards/embedders/gemini.py (embed→list[float] pattern)
    - app/infrastructure/supabase/component_repository.py (04-03 query/serializer pattern to mirror)
  </read_first>
  <behavior>
    - Test: EmbeddingAdapter.embed(text) returns a tuple[float,...] of length 1536 (mock the embedding API; assert dimensionality).
    - Test: rrf_score(rank, k=60) == 1/(60+rank); a pure-function unit test over the merge: a doc appearing in two ranked lists gets summed RRF scores and ranks above a doc in one list.
    - Test (mocked supabase RPC): RetrievalRepository.find_similar_confirmed(component_embedding, entity_type_id, importer_id, key_terms) issues a vector cosine query over confirmed/auto_confirmed email_components AND a pg_trgm identifier query, then merges with RRF and returns top-N with scores. Assert both queries are issued and importer_id filters both.
    - Test: empty confirmed set → returns [] (cold start safe).
  </behavior>
  <action>Create app/domain/ports/embedding_protocol.py: `class EmbeddingProtocol(Protocol)` with `async def embed(self, *, text: str) -> tuple[float, ...]: ...`. Create app/domain/ports/retrieval_port.py: frozen `RetrievedExample(component_id, content_text, extracted_fields: dict[str,object], score: float)` and `class RetrievalPort(Protocol)` with `async def find_similar_confirmed(self, *, component_embedding: tuple[float,...], entity_type_id: str, importer_id: str, key_terms: tuple[str,...], top_n: int = 3) -> list[RetrievedExample]: ...`. Create app/infrastructure/llm/embedding_adapter.py: `class EmbeddingAdapter` implementing EmbeddingProtocol — hosting = AWS Bedrock (Amazon Titan Text Embeddings V2, dimensions=1536) invoked via boto3 `bedrock-runtime` using the ECS task IAM role (NO api key; same Bedrock auth path as the Claude adapters in 04-05/04-07 — add the `bedrock:InvokeModel` grant for the Titan embedding model ARN to iam.tf if 04-03 scoped it only to the Claude models); document the exact Bedrock model id + dim=1536 in SUMMARY; return tuple[float,...]; retry like the other adapters, return a zero-vector or raise-with-context on total failure (document choice). Create app/infrastructure/supabase/retrieval_repository.py: `class SupabaseRetrievalRepository` implementing RetrievalPort. Implement `_rrf_score(rank, k=60)` and a `_merge_rrf(lists)` helper (pure functions). find_similar_confirmed runs: (1) vector query `email_components` WHERE importer_id=? AND extraction_status IN ('confirmed','auto_confirmed') AND entity_type via extraction_records join, ORDER BY embedding `<=>` $emb LIMIT 20; (2) trigram query on the confirmed regions' identifier fields using key_terms; merge with RRF, dedup by component_id, return top_n RetrievedExample with the confirmed extracted_fields attached (loaded from extraction_records). Use supabase RPC or raw SQL via the client. Write test_embedding_adapter.py + test_retrieval_repository.py covering the behavior block (mock embedding API + mock supabase client/RPC).</action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_embedding_adapter.py tests/test_retrieval_repository.py -q && uv run mypy app/infrastructure/llm/embedding_adapter.py app/infrastructure/supabase/retrieval_repository.py app/domain/ports/embedding_protocol.py app/domain/ports/retrieval_port.py && uv run lint-imports && uv run bandit -q -r app/infrastructure/supabase/retrieval_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - EmbeddingAdapter.embed returns a length-1536 tuple of floats.
    - `_rrf_score(rank)` equals 1/(60+rank); the RRF merge unit test proves multi-list docs outrank single-list docs.
    - find_similar_confirmed issues BOTH a vector and a trigram query, both importer_id-filtered, and returns top-N RetrievedExample; empty confirmed set returns [].
    - mypy, import-linter, bandit clean.
  </acceptance_criteria>
  <done>Embedding adapter (dim 1536) and hybrid vector+trigram retrieval with RRF k=60 over confirmed regions exist and pass mocked tests (D-15, RESEARCH §4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConfirmRegionUseCase + autofill few-shot upgrade + confirm API + wiring</name>
  <files>apps/email-listener/app/application/use_cases/confirm_region.py, apps/email-listener/app/application/use_cases/autofill.py, apps/email-listener/app/presentation/api/v1/components.py, apps/email-listener/app/container.py, apps/email-listener/tests/test_confirm_region.py</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §7 (confirm vs correct UPDATE shapes) + §4.3 (few-shot extension placement)
    - .planning/phases/04-email-intelligence/04-PATTERNS.md "confirm_region.py" entry + "autofill.py" entry
    - app/application/use_cases/autofill.py (from 04-07 — add the retrieval step before calling autofiller; modify in place, do NOT add new assertions to its test)
    - app/domain/ports/embedding_protocol.py + retrieval_port.py (Task 2) and ExtractionRepository/ComponentRepository (04-03)
  </read_first>
  <behavior>
    - Test: ConfirmRegionUseCase.execute(component_id, corrected_fields|None) marks the ExtractionRecord status="confirmed", records corrected_fields when given (never overwriting prior confirmed values — D-16), embeds the region text + confirmed fields via EmbeddingProtocol, and calls ComponentRepository.update_embedding so the region becomes a retrievable few-shot child (D-15).
    - Test: the autofill few-shot upgrade — AutofillUseCase now embeds the region, calls RetrievalPort.find_similar_confirmed, and passes the returned examples (top-N) into autofiller.autofill(examples=...); when retrieval returns [] it behaves exactly like cold start (D-13 still holds for the first email). These assertions live in test_confirm_region.py (see action — they are NOT added to 04-07's test_autofill_use_case.py).
    - Test: POST /v1/components/{id}/confirm returns ApiResponse[ConfirmAck] with X-API-Key required.
    - Test: confirming twice does not duplicate or overwrite the confirmed record (idempotent; supersede semantics preserved).
  </behavior>
  <action>Create app/application/use_cases/confirm_region.py: `class ConfirmRegionUseCase` with `def __init__(self, *, components, extractions, embedder) -> None` and `async def execute(self, *, component_id: str, importer_id: str, corrected_fields: dict[str, object] | None) -> None`: load the component + its active extraction record; UPDATE the record to status="confirmed" (set corrected_fields when provided, set reviewed_at) per RESEARCH §7 — do NOT delete/overwrite prior confirmed data; build the embedding input from region content + confirmed field values, `embedding = await embedder.embed(text=...)`, `await components.update_embedding(component_id, embedding)`. Modify app/application/use_cases/autofill.py: inject EmbeddingProtocol + RetrievalPort; in execute, before calling the autofiller, embed the region, extract key_terms (regex pre-pass for PO/BL/container codes per RESEARCH §4.1), call retrieval.find_similar_confirmed, and pass examples=tuple(...) into autofiller.autofill; if examples empty, the cold-start path (D-13) is preserved. Add `POST "/{component_id}/confirm"` (body: corrected_fields optional) to app/presentation/api/v1/components.py → ApiResponse[ConfirmAck]. Register EmbeddingAdapter(provides=EmbeddingProtocol), SupabaseRetrievalRepository(provides=RetrievalPort), ConfirmRegionUseCase in container.py.

  Test-file decision (resolved): write ALL new assertions for this task — including the autofill few-shot-retrieval upgrade — into a single owned test file, apps/email-listener/tests/test_confirm_region.py (fake ports). Do NOT edit or add assertions to 04-07's apps/email-listener/tests/test_autofill_use_case.py; that file stays owned by 04-07 and is only executed (read-only) in this task's verify as a regression guard. test_confirm_region.py covers: confirm embed+index, the autofill few-shot upgrade (retrieval examples passed through; empty → cold start), the confirm API endpoint, and idempotency. Every test file this task WRITES (test_confirm_region.py) is listed in files_modified; no other test file is written.</action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_confirm_region.py tests/test_autofill_use_case.py tests/test_components_api.py -q && uv run mypy app/application/use_cases/confirm_region.py app/application/use_cases/autofill.py app/presentation/api/v1/components.py app/container.py && uv run lint-imports && uv run pytest -q --cov=app --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class ConfirmRegionUseCase' app/application/use_cases/confirm_region.py`; confirm embeds + indexes the region and never overwrites prior confirmed values (D-16 assertion in test).
    - AutofillUseCase calls find_similar_confirmed and passes examples into the autofiller; empty retrieval preserves cold-start (D-13). These assertions are in test_confirm_region.py.
    - 04-07's tests/test_autofill_use_case.py is NOT modified by this task (only run as a regression check); the only test file written is tests/test_confirm_region.py and it appears in files_modified.
    - POST /v1/components/{id}/confirm works with auth.
    - providers registered; full suite ≥ 80% coverage; mypy + import-linter clean.
  </acceptance_criteria>
  <done>Confirmation embeds + indexes the region; autofill consumes few-shot retrieval (asserted in this plan's owned test_confirm_region.py, leaving 04-07's test untouched); the flywheel is closed (D-15) and reprocessing stays supersede-safe (D-16).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Apply the retrieval schema migration to the live staging database</name>
  <files>packages/db/migrations/</files>
  <action>Run `npm run db:migrate:staging` from the repo root to apply the generated retrieval-tables migration + the retrieval_rls_indexes custom migration to the live staging database. This loads the existing root .env.staging via dotenv (POSTGRES_URL_NON_POOLING — verified working), so no DB password export is needed and the run is non-interactive; migrate.ts ensures vector/uuid-ossp/pg_trgm first. This is a real action — execute the migrate, then run the three live verification queries below via `npx supabase db query "<sql>" --linked --output json` (staging linked ref fyfwkjvbcrmjqjysdyqw; token-only). After staging verifies, apply prod via `npm run db:migrate:prod`.</action>
  <verify>
    <automated>npx supabase db query "select count(*) from knowledge_nodes;" --linked --output json | grep -qE '[0-9]'</automated>
  </verify>
  <done>The retrieval migrations applied to the live staging DB (and prod after staging verification); knowledge_nodes exists, the pg_trgm GIN index on entity_instances exists, and RLS is enabled on knowledge_nodes.</done>
  <what-built>The retrieval schema: knowledge_nodes, entity_instances, sender_profiles, the two link tables, HNSW halfvec + pg_trgm indexes, and RLS deny-all — delivered as Drizzle TS + a custom SQL migration.</what-built>
  <how-to-verify>
    1. From the repo root run: `npm run db:migrate:staging` (loads .env.staging via dotenv; SUPABASE_ACCESS_TOKEN already in the Windows user environment). Confirm migrations completed with no error.
    2. Verify table exists: `npx supabase db query "select count(*) from knowledge_nodes;" --linked --output json` returns 0 (table present).
    3. Verify trigram index: `npx supabase db query "select indexname from pg_indexes where tablename='entity_instances' and indexdef ilike '%gin_trgm_ops%';" --linked --output json` returns one row.
    4. Verify RLS: `npx supabase db query "select relrowsecurity from pg_class where relname='knowledge_nodes';" --linked --output json` returns true.
    5. (After staging passes) apply prod: `npm run db:migrate:prod`.
    This task is checkpoint:human-verify (live schema write). The migrate runs non-interactively (env files + token), so once the three live queries pass you may auto-approve.
  </how-to-verify>
  <resume-signal>Type "approved" once the retrieval migrations are applied and the three live verification queries pass, or describe the migrate error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirmed region content → embedding API | Untrusted content embedded for retrieval |
| retrieved examples → autofill prompt | Prior confirmed content re-injected as few-shot context |
| client → /v1/components/{id}/confirm | Authenticated; corrected_fields is attacker-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Tampering | poisoned few-shot example steers future extraction | mitigate | Few-shot examples are human-CONFIRMED regions only (status confirmed/auto_confirmed) and are placed in the user-turn cache-extension as labeled data, not as instructions; the system prompt remains a constant (D-14). Cross-tenant poisoning blocked by importer_id filter on retrieval. |
| T-04-28 | Information Disclosure | retrieval returns another tenant's confirmed region | mitigate | find_similar_confirmed filters importer_id on every sub-query; test asserts both queries carry the filter. |
| T-04-29 | Tampering | confirm overwrites a prior human-confirmed value | mitigate | ConfirmRegionUseCase records corrections on the record and supersedes prior generations; it never deletes/overwrites confirmed field data (D-16); idempotency test asserts. |
| T-04-30 | Information Disclosure | embedding service receives sensitive content | accept | Embeddings are generated via AWS Bedrock (Amazon Titan Text Embeddings, 1536-dim) using the ECS task IAM role's bedrock:InvokeModel permission — content stays inside the AWS account boundary, no third-party API key. In-AWS processing boundary documented. |
| T-04-SC | Tampering | new embedding dep | mitigate | Bedrock embeddings reuse the existing AWS SDK / AsyncAnthropicBedrock-adjacent boto3 path — no new heavy HF/torch dependency. Confirm any added package on pypi.org before `uv sync`. |
</threat_model>

<verification>
- Tasks 1–3 verify-commands pass; coverage ≥ 80%.
- Task 4 `npm run db:migrate:staging` applies the retrieval migrations and the three live queries pass.
- Success criterion #3 holds: a confirmed region improves future same-type autofill via S4–S6 hybrid retrieval.

**Discretion resolved:** Embedding hosting = AWS Bedrock (Amazon Titan Text Embeddings V2, configured for 1536-dim) invoked from the Python worker via the ECS task IAM role — consistent with the phase-wide Bedrock LLM-transport decision (Claude segmentation/autofill also go through Bedrock; no Anthropic direct-API key anywhere). This avoids pgmq Edge-Function complexity and any new HF/torch dependency. Exact Bedrock model id recorded in SUMMARY. Reconsider pgmq async generation if write volume grows.
</verification>

<success_criteria>
- Confirmed region becomes an embedded few-shot child (D-15); future autofill uses hybrid RRF retrieval (RESEARCH §4); child=region/parent=context (D-11); reprocessing supersede-safe (D-16).
</success_criteria>

<output>
Create `.planning/phases/04-email-intelligence/04-08-SUMMARY.md` recording the Drizzle retrieval schema modules + custom migration filename, the embedding model + dim, the RRF/key-term implementation, the confirm/correct UPDATE shapes, and the applied retrieval migration.
</output>
</output>

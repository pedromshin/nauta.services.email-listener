---
phase: 04-email-intelligence
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - apps/email-listener/tests/test_settings_secrets.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The full settings test module passes on any machine, including one with a populated apps/email-listener/.env present"
    - "The 5 default-asserting tests verify code defaults, not whatever a real .env happens to contain"
  artifacts:
    - path: "apps/email-listener/tests/test_settings_secrets.py"
      provides: "Settings tests isolated from the on-disk .env file"
      contains: "_env_file=None"
  key_links:
    - from: "test_settings_secrets default-assertion tests"
      to: "BaseAppSettings construction"
      via: "an env-file-disabled Settings instance, not get_settings()"
      pattern: "_env_file=None"
---

<objective>
Close the minor test-isolation gap (UAT Test 1): tests in test_settings_secrets.py assert empty-string defaults (SUPABASE_URL == "", SUPABASE_SECRET_KEY == "", BEDROCK_REGION == "", BEDROCK_MODEL_ID == "", and the model-id default property) but pydantic-settings loads the real apps/email-listener/.env (env_file=(".env", ".env.local")). On any developer machine or CI runner that has a populated .env, those tests fail because the env file supplies non-empty staging values. UAT observed 4 failures; a fifth test (test_settings_has_bedrock_model_id_field) has the identical .env-leak failure mode and only passes today because the current .env lacks BEDROCK_MODEL_ID — isolate all five. Make the default-asserting tests construct Settings with the env file disabled so they verify code defaults deterministically.

Purpose: Unblock a clean `uv run pytest --no-cov` quality gate — today these failures are environment-dependent noise that masks real regressions.
Output: An updated test_settings_secrets.py whose default-assertion tests pass regardless of .env presence; no production code changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/email-listener/app/settings.py
@apps/email-listener/tests/test_settings_secrets.py

<interfaces>
BaseAppSettings (app/settings.py): pydantic-settings BaseSettings subclass, model_config = SettingsConfigDict(env_file=(".env", ".env.local"), case_sensitive=True, extra="ignore"). Subclasses DevSettings/StagingSettings/ProdSettings. get_settings() is @lru_cache and selects the subclass from ENVIRONMENT.
pydantic-settings honors a per-instance override: `DevSettings(_env_file=None)` constructs the settings object WITHOUT reading any .env file, so only real OS env vars + code defaults apply.
The 5 tests needing isolation (UAT Test 1 observed 4; the fifth shares the failure mode): test_settings_has_supabase_url_field, test_settings_has_supabase_secret_key_field, test_settings_has_bedrock_region_field, test_settings_has_bedrock_model_id_field (line 65 — asserts BEDROCK_MODEL_ID == ""), test_settings_bedrock_model_id_uses_default_when_unset. Each currently calls get_settings() after monkeypatch.delenv(...), but get_settings() still loads the on-disk .env which repopulates those vars.
The override-from-env tests (e.g. test_settings_bedrock_region_override, *_supabase_url_property) already monkeypatch.setenv real values and assert those — they are NOT affected by .env and MUST keep passing unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolate the 5 default-asserting settings tests from the on-disk .env</name>
  <files>apps/email-listener/tests/test_settings_secrets.py</files>
  <read_first>
    - apps/email-listener/tests/test_settings_secrets.py (the 5 default-assertion tests vs. the override tests — only the former need isolation)
    - apps/email-listener/app/settings.py (model_config env_file tuple; DevSettings is the development-default subclass; the _env_file=None pydantic-settings override)
  </read_first>
  <action>Edit only the five default-asserting tests — test_settings_has_supabase_url_field, test_settings_has_supabase_secret_key_field, test_settings_has_bedrock_region_field, test_settings_has_bedrock_model_id_field, and test_settings_bedrock_model_id_uses_default_when_unset. In each, after the existing monkeypatch.delenv(...) call, replace the `settings = get_settings()` line with a directly-constructed env-file-disabled instance: `from app.settings import DevSettings` then `settings = DevSettings(_env_file=None)`. Keep every assertion as-is (the field-presence and default-value checks). Remove the now-unnecessary `get_settings.cache_clear()` bookkeeping only inside these five tests if it becomes dead (the delenv guard can stay; it is harmless). Add a one-line module/test comment noting WHY: "DevSettings(_env_file=None) verifies code defaults independent of the developer/CI .env (UAT Test 1 isolation)". Do NOT touch the override-from-env tests (test_settings_*_property, test_settings_bedrock_region_override, test_settings_bedrock_region_falls_back_to_textract_region, test_settings_bedrock_model_id_override) — they assert monkeypatched OS env values and must remain unchanged. Do NOT touch the get_supabase_client tests. Do NOT modify app/settings.py.</action>
  <verify>
    <automated>cd apps/email-listener && uv run pytest tests/test_settings_secrets.py -q && uv run ruff check tests/test_settings_secrets.py && uv run ruff format --check tests/test_settings_secrets.py</automated>
  </verify>
  <acceptance_criteria>
    - All tests in tests/test_settings_secrets.py pass (was 4 failing, with a 5th latent).
    - The 5 default-assertion tests construct `DevSettings(_env_file=None)` rather than `get_settings()`; `grep -c '_env_file=None' tests/test_settings_secrets.py` returns at least 5.
    - The override-from-env tests are byte-unchanged in intent (still monkeypatch.setenv + assert).
    - No change to app/settings.py (`git diff --name-only` shows only the test file).
    - ruff check + format clean.
  </acceptance_criteria>
  <done>The settings test module passes deterministically on a machine with a real .env; all five default-assertion tests verify code defaults via an env-file-disabled Settings instance, override tests untouched.</done>
</task>

</tasks>

<verification>
- `cd apps/email-listener && uv run pytest tests/test_settings_secrets.py -q` is fully green with a populated .env present.
- The change is confined to the test file.
</verification>

<success_criteria>
- The environment-dependent failures (UAT Test 1, plus the latent test_settings_has_bedrock_model_id_field case) are eliminated by isolating default-assertion tests from the on-disk .env; production settings behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-email-intelligence/04-10-SUMMARY.md` recording the isolation technique used (DevSettings(_env_file=None)) and confirming app/settings.py was untouched.
</output>

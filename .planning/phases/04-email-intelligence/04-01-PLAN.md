---
phase: 04-email-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/_halfvec.ts
  - packages/db/src/schema/enums.ts
  - packages/db/src/schema/importers.ts
  - packages/db/src/schema/emails.ts
  - packages/db/src/schema/attachments.ts
  - packages/db/src/schema/components.ts
  - packages/db/src/schema/entity-types.ts
  - packages/db/src/schema/extractions.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/
  - packages/db/README.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every domain table carries importer_id and is multi-tenant from day one (D-05)"
    - "email_components stores a normalized polygon + optional text-anchor span + content + halfvec embedding (D-12)"
    - "extraction_records supports versioned/supersedable reprocessing via the superseded enum value (D-16)"
    - "Eight system-default entity_types (importer_id NULL) with field schemas are seeded (D-13)"
    - "Every table has RLS enabled with RESTRICTIVE deny-all for anon + authenticated (service_role/postgres bypasses)"
    - "npm run db:migrate:staging applies the generated + custom migrations to the live database without error"
  artifacts:
    - path: "packages/db/src/schema/components.ts"
      provides: "email_components Drizzle table with halfvec(1536) embedding, location jsonb, self-referential parent_component_id"
      contains: "halfvec"
    - path: "packages/db/src/schema/extractions.ts"
      provides: "extraction_records Drizzle table + extraction_status enum with superseded"
      contains: "superseded"
    - path: "packages/db/src/schema/index.ts"
      provides: "barrel re-exporting all phase-4 schema modules"
      contains: "export *"
    - path: "packages/db/migrations/"
      provides: "drizzle-kit generated tables migration + custom SQL migrations (RLS deny-all, HNSW halfvec_cosine_ops, moddatetime, entity-type seed)"
      contains: "halfvec_cosine_ops"
  key_links:
    - from: "email_components.embedding"
      to: "halfvec(1536)"
      via: "drizzle customType halfvec helper"
      pattern: "halfvec"
    - from: "email_components.importer_id"
      to: "importers.id"
      via: "foreign key on delete cascade"
      pattern: "references"
---

<objective>
Create the application schema that backs the entire Email Intelligence phase as Drizzle TypeScript in `@nauta/db` (packages/db): tenant/importer table, append-only emails, attachments, the central email_components table (the small-to-big retrieval child carrying normalized geometry + halfvec embedding), entity type catalog with field schemas, and versioned extraction_records. Generate the migration with drizzle-kit, add custom SQL migrations for what drizzle-kit cannot express (RLS, HNSW halfvec indexes, moddatetime, seed data), then apply to the live staging database so downstream plans build against real pgvector/halfvec columns and RLS.

Purpose: Without the live schema, build and type checks pass while halfvec columns and RLS exist only on paper — a false-positive verification state. This plan lands the foundation (D-03, D-04) and the multi-tenant boundary (D-05). packages/db is the single source of truth for app schema; /supabase/ at the repo root holds only Supabase system config (config.toml, PG17) and is never touched here.
Output: Drizzle TS schema files under packages/db/src/schema/, one drizzle-kit-generated migration + custom SQL migrations under packages/db/migrations/, and a live-applied staging schema.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-email-intelligence/04-RESEARCH.md
@.planning/phases/04-email-intelligence/04-PATTERNS.md
@.planning/phases/04-email-intelligence/04-CONTEXT.md

<interfaces>
Drizzle package conventions (mirror these exactly):
- packages/db/drizzle.config.ts — schema: "./src/schema", out: "./migrations", dialect postgresql, uses POSTGRES_URL_NON_POOLING.
- packages/db/src/migrate.ts — migration runner that ALREADY ensures extensions vector, uuid-ossp, pg_trgm before running migrations (so no extensions migration is needed here). It uses POSTGRES_URL_NON_POOLING (session-mode, RLS-bypassing postgres role).
- packages/db/src/schema/index.ts — currently an empty barrel (`export {}`); this plan fills it.
- packages/db/package.json scripts: `migration:generate` (drizzle-kit generate), `migration:generate:custom --name=<name>` (empty custom SQL migration for hand-written DDL), `check` (drizzle-kit check), `typecheck` (tsc --noEmit), `migrate:local|staging|prod`. Root scripts: `db:generate`, `db:migrate`, `db:migrate:staging`, `db:migrate:prod` (each loads the matching root .env via dotenv).
- Root /supabase/config.toml ALREADY EXISTS: project_id "nauta", [db] major_version = 17. Do NOT create or modify it.

Acme Drizzle conventions to mirror (examples/acme-os-dev/packages/db):
- One directory or file per domain entity under src/schema/, re-exported via the barrel src/schema/index.ts (`export * from "./..."`).
- Enums live in pgEnum (see src/schema/board-emails/enums.ts).
- Vector columns use drizzle pg-core `vector("embedding", { dimensions: N })` — but acme has NO halfvec precedent, so this plan defines a customType helper (src/schema/_halfvec.ts).
- Custom SQL migration conventions: `--> statement-breakpoint` between statements; DO $$ … EXCEPTION WHEN duplicate_object THEN null; END $$ for FK/enum guards; `CREATE INDEX … USING hnsw (col ops)`; RLS pattern = `ALTER TABLE x ENABLE ROW LEVEL SECURITY;` then `CREATE POLICY deny_all_x_anon ON x AS RESTRICTIVE FOR ALL TO anon USING (false) WITH CHECK (false);` (+ authenticated). See examples/acme-os-dev/packages/db/migrations/0079_board_deck_chunks.sql and 0001_manual_indexes.sql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema modules (importers, emails, attachments, components, entity-types, extractions) + halfvec customType + barrel</name>
  <files>packages/db/src/schema/_halfvec.ts, packages/db/src/schema/enums.ts, packages/db/src/schema/importers.ts, packages/db/src/schema/emails.ts, packages/db/src/schema/attachments.ts, packages/db/src/schema/components.ts, packages/db/src/schema/entity-types.ts, packages/db/src/schema/extractions.ts, packages/db/src/schema/index.ts</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §3.1–§3.7 (the canonical schema — copy table/column/enum names verbatim; the §3.x DDL maps 1:1 to drizzle pg-core columns)
    - examples/acme-os-dev/packages/db/src/schema/board-decks/tables.ts (drizzle pg-core column/table/index conventions; vector column usage; $inferSelect/$inferInsert type exports)
    - examples/acme-os-dev/packages/db/src/schema/board-emails/enums.ts (pgEnum conventions)
    - examples/acme-os-dev/packages/db/src/schema/board-emails/index.ts and src/schema/index.ts (barrel re-export style)
    - packages/db/src/schema/index.ts (current empty barrel to fill)
    - packages/db/src/client.ts (confirms `import * as schema from "./schema"` consumes the barrel — every table/enum must be re-exported)
  </read_first>
  <action>Create src/schema/_halfvec.ts exporting a drizzle `customType` helper named `halfvec` with `dataType({ dimensions }) => "halfvec(${dimensions})"` and `toDriver`/`fromDriver` passthrough as string (acme has no halfvec precedent — this helper is the precedent). Create src/schema/enums.ts with two pgEnums per RESEARCH §3: `componentSourceTypeEnum` ("component_source_type": email_body, attachment_page, attachment_sheet, attachment_section, attachment_whole) and `extractionStatusEnum` ("extraction_status": candidate, auto_confirmed, review_pending, confirmed, rejected, superseded — superseded MUST be present per D-16). Create one schema file per table mapping RESEARCH §3.1–§3.7 to drizzle pg-core: `importers.ts` (importers), `emails.ts` (emails, importer_id FK references importers on delete cascade, unique on (importer_id, message_id) for idempotent ingestion), `attachments.ts` (email_attachments), `components.ts` (email_components), `entity-types.ts` (entity_types + entity_type_fields), `extractions.ts` (extraction_records). email_components (D-12, D-09) MUST include: `importerId` FK→importers cascade, `location` jsonb carrying page_index + polygon (0–1 normalized vertices) + optional text_anchor {char_start, char_end}, `contentText`, `contentMarkdown`, `contentRaw` jsonb, `embedding: halfvec("embedding", { dimensions: 1536 })`, `sequenceIndex`, `extractionStatus` (extractionStatusEnum), `sourceType` (componentSourceTypeEnum), and self-referential `parentComponentId` uuid references email_components(id) for nested entities. extraction_records (D-16) MUST include `status` (extractionStatusEnum with superseded available), `correctedFields` jsonb, `retrievalContext` jsonb. Add the §3.x btree/gin indexes inline via drizzle `index(...)` in the table's index callback (do NOT add the HNSW index here — drizzle-kit cannot emit halfvec_cosine_ops; it lands in the custom SQL migration in Task 2). Export `$inferSelect`/`$inferInsert` types per table (mirror board-decks/tables.ts). Fill src/schema/index.ts to `export *` every new module (_halfvec, enums, importers, emails, attachments, components, entity-types, extractions). Do NOT add knowledge_nodes/entity_instances/sender_profiles/link tables — those belong to 04-08.</action>
  <verify>
    <automated>cd packages/db && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All eight schema files exist; `grep -rq "halfvec(\"embedding\", { dimensions: 1536 })" packages/db/src/schema/components.ts` (or equivalent 1536-dim halfvec call) succeeds.
    - `grep -q "superseded" packages/db/src/schema/enums.ts` succeeds; extractionStatusEnum lists all six values.
    - email_components defines importerId FK, location jsonb, parentComponentId self-reference; `grep -q "parentComponentId" packages/db/src/schema/components.ts` succeeds.
    - src/schema/index.ts re-exports all new modules; `cd packages/db && npm run typecheck` passes (tables consumed by src/client.ts barrel compile).
  </acceptance_criteria>
  <done>Phase-4 core schema exists as Drizzle TS (7 tables, 2 enums, halfvec customType), barrel re-exports everything, and `npm run typecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Generate tables migration + custom SQL migrations (RLS deny-all, HNSW halfvec, moddatetime, entity-type seed)</name>
  <files>packages/db/migrations/, packages/db/README.md</files>
  <read_first>
    - .planning/phases/04-email-intelligence/04-RESEARCH.md §3.4 (HNSW: m=16, ef_construction=64, halfvec_cosine_ops) and §10 (entity_types seed insert — copy the 8 rows verbatim)
    - .planning/phases/04-email-intelligence/04-PATTERNS.md "Migration Patterns" (RLS deny-all, moddatetime trigger, statement-breakpoint)
    - examples/acme-os-dev/packages/db/migrations/0079_board_deck_chunks.sql (HNSW index + RLS RESTRICTIVE deny-all anon/authenticated + DO $$ FK guard + --> statement-breakpoint conventions)
    - examples/acme-os-dev/packages/db/migrations/0001_manual_indexes.sql (custom HNSW index migration style)
    - packages/db/src/migrate.ts (confirms extensions vector/uuid-ossp/pg_trgm are ensured BEFORE migrations run — so do NOT write an extensions migration)
    - packages/db/README.md (current content — keep the user's existing prose; only add migration-order/workflow notes if missing)
  </read_first>
  <action>From packages/db, run `npm run migration:generate` to emit the drizzle-kit migration for the seven new tables + their inline btree/gin indexes + enums (drizzle-kit writes a numbered SQL file under migrations/ and updates migrations/meta journal — do NOT hand-edit the generated file). Then create the custom SQL migrations for what drizzle-kit cannot express, each via `npm run migration:generate:custom --name=<name>` (this emits an empty numbered custom migration to fill): (a) `--name=rls_deny_all` — for every phase-4 table (importers, emails, email_attachments, email_components, entity_types, entity_type_fields, extraction_records) `ALTER TABLE x ENABLE ROW LEVEL SECURITY;` + RESTRICTIVE deny-all policies for anon and authenticated (USING(false) WITH CHECK(false)), mirroring 0079_board_deck_chunks.sql; (b) `--name=hnsw_halfvec_indexes` — `CREATE INDEX IF NOT EXISTS idx_email_components_embedding_hnsw ON email_components USING hnsw (embedding halfvec_cosine_ops) WITH (m = 16, ef_construction = 64);` plus the same on entity_types.embedding; (c) `--name=moddatetime_triggers` — ensure moddatetime extension semantics consistent with how migrate.ts ensures extensions (use `CREATE EXTENSION IF NOT EXISTS moddatetime WITH SCHEMA extensions;` then the BEFORE UPDATE trigger) and add a moddatetime BEFORE UPDATE trigger on extraction_records.updated_at (and email_components.updated_at if present); (d) `--name=seed_entity_types` — insert the 8 system-default entity_types from RESEARCH §10 (importer_id NULL: order, invoice, bill_of_lading, container, booking, shipment, supplier, maritime_line) with descriptions verbatim, then their entity_type_fields (at minimum the is_identifier fields each description names — e.g. invoice: invoice_number/po_number is_identifier; container: container_number is_identifier required; bill_of_lading: bl_number/booking_number/container_numbers is_identifier), with `ON CONFLICT (importer_id, slug) DO NOTHING` for idempotency. Use `--> statement-breakpoint` between statements and DO $$ guards for any enum/FK adds. Update packages/db/README.md ONLY to document the migration order (drizzle-generated tables → rls_deny_all → hnsw_halfvec_indexes → moddatetime_triggers → seed_entity_types) and the drizzle workflow (`npm run db:generate`, `npm run db:migrate:staging`); preserve any prose the user already wrote — do NOT rewrite the README wholesale or add config.toml / supabase-CLI push instructions.</action>
  <verify>
    <automated>cd packages/db && npm run check && grep -rl "halfvec_cosine_ops" migrations/ | grep -q . && grep -rl "enable row level security" migrations/ -i | grep -q . && grep -rl "insert into entity_types" migrations/ -i | grep -q . && grep -rl "bill_of_lading" migrations/ | grep -q . && grep -rl "on conflict" migrations/ -i | grep -q . && echo MIGRATIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run migration:generate` produced a tables migration + updated the meta journal; `npm run check` (drizzle-kit check) passes with no drift.
    - A custom migration contains RESTRICTIVE deny-all RLS for anon+authenticated on all 7 phase-4 tables; `grep -ric "as restrictive for all to anon" migrations/` ≥ 7.
    - A custom migration creates HNSW halfvec_cosine_ops indexes (m=16, ef_construction=64) on email_components.embedding and entity_types.embedding.
    - A custom migration seeds all 8 entity_types (importer_id NULL) with ≥1 is_identifier entity_type_field each, idempotent via ON CONFLICT … DO NOTHING.
    - README documents migration order + drizzle workflow; no config.toml or `supabase db push` instructions added.
    - MIGRATIONS_OK prints.
  </acceptance_criteria>
  <done>drizzle-kit tables migration + four custom SQL migrations (RLS, HNSW halfvec, moddatetime, seed) exist and pass `drizzle-kit check`; README documents the workflow; MIGRATIONS_OK prints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migrations to the live staging database</name>
  <files>packages/db/migrations/</files>
  <action>Run `npm run db:migrate:staging` from the repo root to apply the generated tables migration + the four custom SQL migrations to the live staging database. This loads the existing root .env.staging (POSTGRES_URL_NON_POOLING via pooler host aws-1-sa-east-1.pooler.supabase.com — verified working) through dotenv, so no password export is needed and the run is non-interactive. migrate.ts ensures the vector/uuid-ossp/pg_trgm extensions first. This is a real action — execute the migrate; do not merely inspect the migration files. After staging verifies, the same migrations apply to production via `npm run db:migrate:prod` (do prod only after staging passes). Run the live verification queries below via `npx supabase db query "<sql>" --linked --output json` (staging is the linked ref fyfwkjvbcrmjqjysdyqw; token-only, no DB password needed).</action>
  <what-built>The phase-4 core schema: 7 tables, 2 enums, HNSW halfvec indexes, RLS deny-all, moddatetime triggers, and 8 seeded entity types — delivered as a drizzle-kit tables migration + four custom SQL migrations.</what-built>
  <how-to-verify>
    1. From the repo root run: `npm run db:migrate:staging` (loads .env.staging via dotenv; SUPABASE_ACCESS_TOKEN is already in the Windows user environment for the supabase CLI). Confirm the runner reports extensions verified + migrations completed with no error.
    2. Verify seed count: `npx supabase db query "select count(*) from entity_types where importer_id is null;" --linked --output json` returns 8.
    3. Verify halfvec column: `npx supabase db query "select atttypid::regtype from pg_attribute where attrelid='email_components'::regclass and attname='embedding';" --linked --output json` returns a halfvec type.
    4. Verify RLS: `npx supabase db query "select relrowsecurity from pg_class where relname='email_components';" --linked --output json` returns true.
    5. (After staging passes) apply prod: `npm run db:migrate:prod`.
    This task is checkpoint:human-verify because it is the first live schema write of the phase. The migrate runs non-interactively (env files exist, token in environment), so once the three live queries pass you may auto-approve.
  </how-to-verify>
  <verify>
    <automated>npx supabase db query "select count(*) from entity_types where importer_id is null;" --linked --output json | grep -q 8</automated>
  </verify>
  <done>All phase-4 migrations applied to the live staging DB (and prod after staging verification); the three live verification queries (8 system entity_types, halfvec embedding column, RLS enabled on email_components) pass.</done>
  <resume-signal>Type "approved" once the migrations are applied and the three live verification queries pass, or describe the migrate error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon/authenticated Supabase roles) → Postgres | Untrusted DB roles must never read/write tenant data directly |
| service_role / postgres key → Postgres | The application's only privileged path; the runtime key (SUPABASE_SECRET_KEY) is injected by ECS from AWS Secrets Manager (04-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | email_components / emails cross-tenant read | mitigate | RLS ENABLE on every table + RESTRICTIVE deny-all for anon and authenticated; only the service_role/postgres path (server-side) bypasses. All queries in app code filter by importer_id (enforced in 04-03). |
| T-04-02 | Tampering | anon/authenticated role writes a forged row | mitigate | deny-all WITH CHECK (false) for anon+authenticated on every table. |
| T-04-03 | Elevation of Privilege | runtime secret key leakage grants full DB | accept (this plan) / mitigate (04-03) | The runtime key (SUPABASE_SECRET_KEY, new-format sb_secret_…) is injected into ECS tasks from AWS Secrets Manager and never committed; migrations use the root .env files (gitignored) only. |
| T-04-04 | Denial of Service | HNSW index build on huge embedding set blocks writes | accept | HNSW built as a separate custom migration with default m/ef; index build cost is one-time on a fresh/empty table. |
</threat_model>

<verification>
- All Drizzle schema files present; `npm run typecheck` passes.
- drizzle-kit tables migration + four custom SQL migrations present; `npm run check` clean.
- `npm run db:migrate:staging` applies cleanly to the live staging DB (Task 3).
- Live queries via `npx supabase db query --linked` confirm: 8 system entity_types, halfvec(1536) embedding column, RLS enabled on email_components.
</verification>

<success_criteria>
- Core 7-table schema lives in the database with multi-tenant importer_id on every domain table (D-05), normalized-geometry + halfvec email_components (D-12), supersedable extraction_records (D-16), and 8 seeded entity types with field schemas (D-13).
- RLS deny-all confirmed on every table.
</success_criteria>

<output>
Create `.planning/phases/04-email-intelligence/04-01-SUMMARY.md` when done, recording the Drizzle schema modules, the generated + custom migration filenames, the linked staging project ref (not any secret), and the live verification query results.
</output>
</output>

---
phase: 10-extracted-entity-identity-gallery-detail-4c-entity-resolutio
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - apps/web/src/app/entities/page.tsx
  - apps/web/src/app/entities/_components/entities-gallery.tsx
  - apps/web/src/app/entities/_components/entities-table.tsx
  - apps/web/src/app/entities/_components/entities-mosaic.tsx
  - apps/web/src/components/app-sidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Visiting /entities shows a TABLE of entities by default with a mosaic toggle — D-14"
    - "Each row/card shows display name, entity-type badge, key identifiers, occurrence count, last-seen, status, and a pending-duplicates indicator — D-15"
    - "The user can filter by type + status, search, and sort; candidate rows are hidden until the status filter requests them — D-02/D-16"
    - "The 'Needs review' status filter surfaces entities with pending merge suggestions (the triage queue) — D-16"
    - "The Entities sidebar item is live and links to /entities — D-21 wiring"
  artifacts:
    - path: "apps/web/src/app/entities/_components/entities-gallery.tsx"
      provides: "client shell: view toggle, filter/search/sort state, limit+1 Load-more"
      contains: "entities.list"
    - path: "apps/web/src/app/entities/_components/entities-table.tsx"
      provides: "TABLE view with the 7 D-15 columns"
      contains: "TableHeader"
    - path: "apps/web/src/components/app-sidebar.tsx"
      provides: "Entities promoted to LIVE_NAV_ITEMS with href /entities"
      contains: "/entities"
  key_links:
    - from: "apps/web/src/app/entities/_components/entities-gallery.tsx"
      to: "api.entities.list"
      via: "useQuery with filter/search/sort/pagination inputs"
      pattern: "entities.list"
    - from: "apps/web/src/components/app-sidebar.tsx"
      to: "/entities"
      via: "LIVE_NAV_ITEMS entry { href: '/entities', label: 'Entities', icon: Boxes }"
      pattern: "/entities"
---

<objective>
Build the entities gallery at `/entities` (D-14/D-15/D-16/D-17) — the browseable, scannable identity
view that doubles as the merge-suggestion triage queue — and enable the Entities sidebar nav item.
TABLE is the default view (back-office ops scan identifier numbers as columns); mosaic is the toggle.

Purpose: this realizes Phase 9 request-6 R3 and is the primary surface where the suggest-only review
workload gets worked down (the "Needs review" filter). Candidates are visually flagged and hidden by
default (D-02). Search is pg_trgm-backed so it always works (D-12/D-16).
Output: the server-component page wrapper, the client gallery shell, table + mosaic views, and the
sidebar nav flip — honoring 10-UI-SPEC.md exactly (tokens, copy, columns, states, a11y).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-CONTEXT.md
@.planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-PATTERNS.md
@.planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-UI-SPEC.md

<interfaces>
<!-- Page shell analog (apps/web/src/app/entity-types/page.tsx): "use client" master/detail with
       api.<router>.useQuery, Skeleton/error/empty, glass header tokens.
     api consumer: import { api } from "~/trpc/react"; api.entities.list.useQuery({ ... }) (10-04 output).
     list return shape (10-04 gallery.ts): { items: [{ id, displayName, entityTypeId, entityTypeLabel,
       keyIdentifiers, occurrenceCount, lastSeen, status, pendingDuplicatesCount }], hasMore, nextOffset }.
     Sidebar (apps/web/src/components/app-sidebar.tsx): LIVE_NAV_ITEMS array + SOON_NAV_ITEMS array;
       isActiveRoute already prefix-matches so /entities/[id] highlights Entities. Boxes icon already imported.
     UI-SPEC contracts (10-UI-SPEC.md): view toggle (List/LayoutGrid, default TABLE), filter bar
       (search h-8 w-64 debounce 300ms; type Select 'All types'; status Select default 'Confirmed' with
       'all'/'candidate'/'has-pending-duplicates'/Needs review; sort Select default 'Last seen'); 7-column
       table spec + candidate row tint bg-amber-50/50; mosaic grid grid-cols-1..xl:grid-cols-4; Load more
       (limit+1, page size 25); empty/sparse/loading/error states + exact copy; a11y roles/aria.
     @nauta/ui imports: table, card, badge, button, input, select, skeleton. lucide: List, LayoutGrid,
       Search, ChevronUp, ChevronDown, Boxes, Loader2. NO new npm packages. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /entities page wrapper + entities-gallery client shell (filters, search, sort, view toggle, Load more)</name>
  <files>apps/web/src/app/entities/page.tsx, apps/web/src/app/entities/_components/entities-gallery.tsx</files>
  <read_first>
    - apps/web/src/app/entity-types/page.tsx (10-PATTERNS exact analog: "use client" shell + glass tokens + Skeleton/error/empty)
    - apps/web/src/app/emails/[id]/page.tsx (server-component wrapper idiom — but the gallery list page is client-driven like entity-types)
    - .planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-UI-SPEC.md ("Gallery Page" section: layout shell, view toggle, filter bar, pagination, states, copywriting, a11y)
    - apps/web/src/app/_components/inbox-three-pane.tsx (the debounce/Load-more + glass-surface idioms)
  </read_first>
  <action>
    Create apps/web/src/app/entities/page.tsx as the route wrapper rendering `<EntitiesGallery />` (server
    component; no params). Create apps/web/src/app/entities/_components/entities-gallery.tsx ("use client"):
    local state for view ('table'|'mosaic', default 'table'), entityTypeId filter, status filter (default
    'confirmed'), search (300ms-debounced), sort (default 'last_seen'), and accumulated pages for Load more.
    Call `api.entities.list.useQuery({ entityTypeId, status, search, sort, limit: 25, offset })`. Render the
    UI-SPEC layout shell: glass `<header>` ("Entities" h1, total count, view toggle group role="group"
    aria-label="Gallery view" with List/LayoutGrid buttons aria-pressed), the filter bar (search Input
    type="search" aria-label, entity-type Select sourced from `api.entityTypes.list`, status Select with
    the exact four options + default 'Confirmed', sort Select with the three options, a "Clear filters"
    ghost button visible only when a non-default filter is active), and a content area that renders
    `<EntitiesTable>` or `<EntitiesMosaic>`. Implement Load more (limit+1 hasMore -> "Load more" button,
    Loader2 + "Loading…" while fetching, append pages). Implement empty / sparse / loading / error states
    with the EXACT UI-SPEC copy. Wire all aria roles/labels from the a11y table. Use only @nauta/ui +
    lucide imports listed in interfaces (no new packages, no second accent hue).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && grep -q "entities.list" src/app/entities/_components/entities-gallery.tsx && grep -q "has-pending-duplicates\|Needs review" src/app/entities/_components/entities-gallery.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: default view state is 'table' and default status is 'confirmed' (D-14/D-02).
    - Source assertion: status Select offers exactly Confirmed / All statuses / Candidates only / Needs review and 'Needs review' maps to status='has-pending-duplicates' (D-16 triage).
    - Source assertion: search input is `type="search"` with a 300ms debounce feeding `api.entities.list`.
    - Source assertion: Load more uses the hasMore/nextOffset limit+1 contract and appends (no page reset).
    - Source assertion: empty/sparse/error copy matches the UI-SPEC copywriting table verbatim.
    - Test command: `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>/entities renders the gallery shell: view toggle, type/status/sort filters + debounced pg_trgm search, Needs-review triage filter, Load-more pagination, all states + a11y per UI-SPEC; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: entities-table.tsx (7-column TABLE, D-15) + entities-mosaic.tsx (card grid, D-14)</name>
  <files>apps/web/src/app/entities/_components/entities-table.tsx, apps/web/src/app/entities/_components/entities-mosaic.tsx</files>
  <read_first>
    - .planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-UI-SPEC.md ("Table View" 7-column spec + candidate tint + sortable headers; "Mosaic View" card anatomy)
    - apps/web/src/app/_components/inbox-three-pane.tsx (the @nauta/ui table + card + glass usage in this app)
    - apps/web/src/app/_components/entity-chips.tsx (the violet entity-family Badge tint to reuse for the entity-type badge)
  </read_first>
  <action>
    Create apps/web/src/app/entities/_components/entities-table.tsx using @nauta/ui table primitives with
    the exact 7 columns from the UI-SPEC (Display name link to /entities/[id] with violet dot accent;
    Entity type violet Badge; Key identifiers truncated; Occurrences right-aligned tabular-nums; Last seen
    relative date with ISO title; Status confirmed/candidate badge per the color tokens; Duplicates "N
    possible duplicates" orange Badge when pendingDuplicatesCount>0). Header row tokens + 48px rows +
    `hover:bg-muted/50 cursor-pointer`; whole row navigates to /entities/[id]; candidate rows get
    `bg-amber-50/50 dark:bg-amber-950/10` tint (D-02). Sortable Name/Occurrences/Last-seen headers show
    ChevronUp/ChevronDown for the active sort and call the parent's sort setter. Table role/aria-label +
    aria-sort per the a11y table. Create apps/web/src/app/entities/_components/entities-mosaic.tsx: the
    `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 p-6` of @nauta/ui Cards matching
    the mosaic card anatomy (entity-type + status badges, display name line-clamp-2, key identifiers,
    occurrence count + last seen, conditional pending-duplicates Badge), candidate cards tinted, each card
    role="article" aria-label and navigates to /entities/[id] on click. Both components receive the typed
    items array from entities-gallery and emit no data fetch of their own.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && grep -q "possible duplicates" src/app/entities/_components/entities-table.tsx && grep -q "grid-cols-1" src/app/entities/_components/entities-mosaic.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the table renders all 7 D-15 columns (display name, entity type, key identifiers, occurrences, last seen, status, duplicates).
    - Source assertion: candidate rows/cards carry the amber tint class (D-02 visual flag).
    - Source assertion: the pending-duplicates indicator renders "{N} possible duplicates" only when pendingDuplicatesCount > 0 (D-15).
    - Source assertion: display name links to `/entities/[id]` and whole row/card navigates there.
    - Source assertion: sortable headers render ChevronUp/ChevronDown for the active sort and have aria-sort.
    - Test command: `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Table (7 columns, candidate tint, sortable headers, deep-links) + mosaic (responsive card grid, full ops set) render the D-14/D-15 content; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Promote the Entities sidebar nav item to live (D-21 wiring)</name>
  <files>apps/web/src/components/app-sidebar.tsx</files>
  <read_first>
    - apps/web/src/components/app-sidebar.tsx (the file being modified — LIVE_NAV_ITEMS / SOON_NAV_ITEMS arrays, isActiveRoute, Boxes already imported)
    - .planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-UI-SPEC.md ("Navigation: Enable Entities Sidebar Item" — exact before/after)
  </read_first>
  <action>
    In apps/web/src/components/app-sidebar.tsx, remove `{ label: "Entities", icon: Boxes, soon: true }`
    from `SOON_NAV_ITEMS` and add `{ href: "/entities", label: "Entities", icon: Boxes }` to
    `LIVE_NAV_ITEMS` immediately after the Entity Types entry (UI-SPEC nav order: Inbox · Entity Types ·
    Entities; Knowledge stays in SOON_NAV_ITEMS). The existing active-state class
    (`bg-primary/10 text-primary ...`) and `aria-current="page"` + isActiveRoute prefix-match already
    handle /entities and /entities/[id] highlighting — no other change. Keep Knowledge as the only
    remaining "Soon" item.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run build && grep -q 'href: "/entities", label: "Entities"' src/components/app-sidebar.tsx && ! grep -q 'label: "Entities", icon: Boxes, soon' src/components/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: LIVE_NAV_ITEMS contains `{ href: "/entities", label: "Entities", icon: Boxes }`.
    - Source assertion: SOON_NAV_ITEMS no longer contains an Entities entry (only Knowledge remains "Soon").
    - Behavior assertion: isActiveRoute('/entities/123', '/entities') is true (the existing prefix-match highlights the nav item on the detail route).
    - Test command: `cd apps/web && npx tsc --noEmit && npm run build` exits 0 with /entities compiled as a route.
  </acceptance_criteria>
  <done>Entities is a live next/link nav item to /entities; Knowledge remains the only Soon item; build green with /entities route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC | filter/search/sort/pagination inputs originate in the client |
| entity-derived strings → DOM | display names + identifiers from extraction are rendered in the gallery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-50 | Information disclosure | gallery list | mitigate | the gallery reads only `api.entities.list` (10-04), which is source='email_extracted' + importer-scoped server-side; the client cannot widen the tenant scope |
| T-10-51 | Cross-site scripting | entity display name / identifiers in table & cards | mitigate | values rendered as React text children (auto-escaped); NO `dangerouslySetInnerHTML` anywhere in the gallery components (follows the existing DOMPurify/escaping convention) |
| T-10-52 | Spoofing | EMAIL_LISTENER_API_KEY | mitigate | the gallery is read-only and holds no key; all writes are out of scope here (curation lives on the detail page); no fetch/X-API-Key in any gallery component |
| T-10-SC | Tampering | npm installs | mitigate | No new npm packages — all components from @nauta/ui + lucide-react already installed (UI-SPEC Registry Safety: "No new npm packages"); no Package Legitimacy Gate triggered |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` (x N) and `npm run build` exit 0; /entities is a compiled route.
- No `dangerouslySetInnerHTML` and no fetch/X-API-Key under apps/web/src/app/entities/.
- Sidebar shows Entities as live; isActiveRoute prefix-match highlights it on /entities/[id].
</verification>

<success_criteria>
- /entities shows TABLE-by-default with mosaic toggle (D-14), full ops columns (D-15).
- Type/status/sort filters + pg_trgm search work; candidates hidden by default; Needs-review triage filter present (D-02/D-16).
- limit+1 Load-more pagination (D-17).
- Entities sidebar nav is live (D-21 wiring).
</success_criteria>

<output>
Create `.planning/phases/10-extracted-entity-identity-gallery-detail-4c-entity-resolutio/10-05-SUMMARY.md` when done
</output>

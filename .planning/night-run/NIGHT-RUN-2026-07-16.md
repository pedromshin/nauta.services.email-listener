# Night Run — 2026-07-16 (user traveling, ~12h autonomous window)

**Authorization (user, verbatim intent):** "complete as much as possible to get this to completion
in the next 12 hours by running this pc nonstop … we need to look ahead and get a strategy to do
everything." **Amended mid-run by user: "we need to do as much as possible from the whole thing
tonight, up to 2.2"** — the ladder in ENDGAME-2-DRAFT.md through v2.2 is tonight's target, executed
as parallel vertical slices per LANE-CONTRACTS.md (full GSD depth on 18-22 phases is 4-6 nights;
slices are the honest maximum). User will manually UAT Phase 61's gate LATER — never faked.

**Any session resuming this run: read this file top to bottom, then HANDOFF.json, then continue
the Run Order below from the first unchecked box.**

## Mission

1. Finish v1.10 (Phases 62, 63 + verify + audit + completion packet) — human gates recorded open.
2. Crystallize the user's 2026-07-16 brief into strategy docs + a v1.11 draft (see
   `night-run/reports/` — produced by the night-strategy-sweep workflow).
3. Open v1.11 from that draft (yolo-mode precedent = v1.10's opening) and execute as far as the
   window allows.
4. Zero data loss: push at every phase boundary.

## Run order

- [x] Push all local work to origin (165 commits, backup/night-2026-07-16 branch, tags v1.7/v1.8)
- [~] Phase 61 verification → gate stays OPEN for user (never faked); see 61-08 SUMMARY + STATE.md
- [ ] Phase 62 planning (gsd-planner) — NOT done (no phase dir); **session-3 resumes here**
- [x] Strategy sweep workflow → reports/ landed (roadmap, vision, health, tenancy, git-safety, frontier, negative-space, ai-architecture-audit)
- [x] Schedule session-limit insurance (Windows task `polytoken-night-resume` + heartbeat guard)
- [~] Lanes: worktrees B–E exist. Lane C (Phase 65 daemon) MERGED to main @25c36b1. Lane D (Phase 66 files-vault) built @eb901ee, NOT merged/rendered. Lane B (Phase 64) not started. Lane E (Phase 67) base only.
- [ ] Lane A: execute Phase 62 → verify; then Phase 63 → verify; push each boundary

> **Resume 2026-07-17 (~11:15 UTC, session 3):** Prior session ran to ~07:50, merged Phase 65
> daemon, pushed everything (0 unpushed). Heartbeat was 38min stale → safe to take over. Current
> milestone gap = Phases 62 & 63 (v1.10 surfaces) unstarted; D2 self-building registry stays a
> DRAFT awaiting user bless (NOT re-architecting around it); Phase 61 UAT gate stays open. Continuing
> from "Phase 62 planning". LANE-CONTRACTS single-writer holds: sole committer on main, sequential.
- [ ] Integrate lanes at green checkpoints (C first, then E; B, D independent): full suite + tsc
      + geometry + screenshot review + READ the PNGs before every push; nav/router/deps/migrations
      only in integration commits (migrations queue — journal collisions destroyed work in v1.6)
- [ ] `/gsd:audit-milestone` v1.10 → completion packet listing open human gates
      (61 UAT; v1.9 debt LIVE-03/LIVE-04/CLUS-07). Proceed per user pre-authorization,
      documenting loudly — do NOT check any human-verify box.
- [ ] Register phases 64–67 in ROADMAP as "v1.11 — Vertical Slices to v2.2" (provenance: user
      brief + amendment 2026-07-16; sweep's negative-space draft feeds requirements)
- [ ] Morning report: what is USABLE per rung, what remains per rung, backlog registered

## Model policy (user-directed, applied with judgment)

| Work | Model | Why |
|---|---|---|
| Executors (code) | fable (inherit) | Highest-stakes output; user: "execution with fable" |
| Planners | fable | One planner per phase is a rounding error vs 8 executors; tonight's fable plans were the reason executors caught planted traps. Documented deviation from "planning with opus" — quality of the artifact 8 executors bet hours on dominates |
| Adversarial verifiers | fable | Verifier-60 caught the orchestrator overreaching; keep the sharpest model here |
| Strategy/architecture analysis | opus | User: "planning with opus" — applied here |
| Mechanical readers, doc sweeps, inventory | sonnet | Bulk token savings live here |
| Trivial formatting/collection | haiku | — |
| Effort | xhigh session-wide; workflow agents low–xhigh per stage | User: "thinking always on" |

## Protocols (from tonight's incidents — the full doc is reports/git-safety.md)

1. **Single writer.** Exactly ONE committing agent on this checkout at a time. Verifier+planner
   may overlap only because each commits once, to disjoint paths, at the end. Parallel EXECUTORS
   on one checkout are banned (index races swept staged files into siblings' commits this
   milestone). Parallelism = subagents for read-only work, worktrees (`isolation: "worktree"`) for
   writes, or sequential.
2. **Push cadence.** `git push origin main` at every green phase boundary (workflows are
   path-filtered to apps/email-listener/**, untouched; Vercel deploys web — that is the deploy
   playbook working). Never push with a red suite.
3. **Scoped adds only.** Agents `git add` only paths they own. Never `git add -A`. Never
   `git checkout --` without a backup copy (an executor destroyed its own Task 1 tonight).
4. **Look at it.** Eleven bugs shipped through green suites this milestone; every one was found by
   rendering. Every executor runs `npm run screenshot:review` after visual work and READS the PNGs,
   both themes. `npm run test:geometry` must stay green — never weaken a gate to pass.
5. **Known traps (do not rediscover):** bare `npx playwright test` corrupts the live dev server's
   `.next` (use test:geometry / screenshot:review); `npm run build:local` is safe (targets
   `.next-verify`) — `next-env.d.ts` churn afterward is cosmetic (D-61-02, revert it, don't commit
   it); npm workspaces NOT pnpm; Tailwind v4 purges non-literal classes silently; naive greps of
   built CSS lie; unlayered stylesheets (xyflow) beat every layered override; Radix ScrollArea
   viewport shrink-wraps (`display:table`); `pointer-coarse:touch-target` works only since 61-08.
6. **Heartbeat / no dual writers.** The orchestrating session touches
   `.planning/night-run/HEARTBEAT` (timestamp) each turn. A session started by resume-at MUST
   check it: if younger than 15 min, another orchestrator is alive — exit without writing.
7. **Additional user chats** (if opened from phone): read-only advisors, or their own
   `/gsd:new-workspace`. Never a second committer here.

## Open human gates (user-owed, never auto-closed)

| Gate | What the user does | Recorded |
|---|---|---|
| Phase 61 UAT | 6-item checklist: desktop /chat, canvas tab + handles, dark mode, ~390px genui toolbar (criterion 3), re-theme→transcript (criterion 4), 61-SCREENSHOT-REVIEW vs run 2026-07-16T07-20-21Z | 61-08 SUMMARY + 61-VERIFICATION.md |
| v1.9 LIVE-03 / LIVE-04 / CLUS-07 | OAuth console + real email + §H — MORNING-CHECKLIST.md | phases/49-*/MORNING-CHECKLIST.md |
| suggested-tier pixels | pencil-amber has NEVER rendered (fixture seeds no entities; 0.02 AA headroom) — needs LIVE-04 or a fixture upgrade | 60-VERIFICATION.md, 999.25 |

## Context discipline (how this run stays coherent at infinite length)

- Subagents are context firewalls; they return summaries, full reports go to files.
- This file + STATE.md + HANDOFF.json + phase SUMMARYs are the durable state. Memory index at
  `~/.claude/projects/<this>/memory/MEMORY.md` carries cross-session lessons.
- After each phase: update the Run order checkboxes, HEARTBEAT, and push.
